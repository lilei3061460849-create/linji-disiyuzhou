# 功能实现状态总览

> 本文档回答"现在完全实现了什么、还剩什么没做"，是给DM/项目负责人看的**审计结果**，
> 不是世界观正文（那是`README.md`）、不是API参考（那是`engine/README.md`）、
> 也不是设计取舍记录（那是`AI_EXPERIENCE.md`）——三份文档的具体条目仍以各自文件为准，
> 本文件只做汇总索引 + 记录**本次盘点新发现、尚未处理**的问题。
>
> 最近核对：2026-08-12。核心判断方法：不是"看代码里有没有这个名字"，而是逐条确认
> "计算出来的效果数值/状态，有没有被下游代码真正读取并产生游戏后果"——很多此前被认为
> "已实现"的道纹，实际只是`calculate_xxx()`算出了一个字典，从未被任何地方消费。
>
> **测试默认**：用户说「测试」时，由 AI 通过 `GameEngine` 逐步点选手操并按推演格式写战报。
> 禁止默认跑 `TacticalAI` / `sim/pick_best_report.py` / `test_with_ai.py` / sim 批量通关。
> `pytest` 与 `tests/test_engine.py` 只作回归锁。`战报.md` 只保留最新一次轮回记录。

## 一、已完整实现并有自动化测试覆盖的系统

以下系统均有专门的`tests/test_*.py`（pytest）或`tests/test_engine.py`（旧式套件）覆盖。
当前验证：`.venv/bin/python tests/test_engine.py` → 24/24；`.venv/bin/python -m pytest tests -q` → **443 passed**（另有 `test_build_learner.py::test_random_seed_mode_varies_samples` 与本次无关的既有抖动）。

| 系统 | 测试文件 | 状态 |
|------|---------|------|
| 死之传承（命零中断→审核→写入`死者之书.md`遗言节） | test_death_book.py | ✅ |
| 癌变（任一角色：轮回者/同伴直接命零；怪物吸收进书） | test_death_book.py + test_engine.py | ✅ |
| 回音长廊打碎镜子（自选一页，缺选择则拒绝且不改文件） | test_death_book.py | ✅ |
| 核心战斗循环（回始/回终/攻击/闪避/道纹/法术/飞行/裂变/降服/雕塑/癌变/还债） | test_engine.py | ✅ |
| 随机数自动结算（`DiceEngine.auto_roll`，可传`rng_seed`复现） | test_rng_engine.py | ✅ |
| 出怪系统（数量=战斗场数-3，最低1，允许重复） | test_monster_draw.py | ✅ |
| 出手预算校验（`action_count`按轮回者/微光者分流公式） | test_action_budget.py | ✅ |
| 撤退机制（[朋友]/[员工]濒死时触发，`has_retreated`战终重置） | test_retreat.py | ✅ |
| 员工经济系统（派遣/工资pay-or-refuse/黑名单/解雇） | test_employee_economy.py | ✅ |
| 雇佣转化道纹发现 + "追求者"事件 | test_hire_daowen_and_events.py | ✅ |
| 听指令行动（[朋友]/[员工]代为攻击/发动道纹） | test_ally_commands.py | ✅ |
| 员工叛变（镇压/让利/谈判三分支） | test_rebellion.py | ✅ |
| 遗物池13件（血誓戒/买路财/同魂笔/回锋刀/折速法印/三相残韵盘/鲜血契约/避风铃/守夜灯/钱袋/卖身契/无所求/忘忧香） | test_relic_fixes.py + test_engine.py | ✅ |
| 龙心谷"炼心"系统（龙心消耗品，抵消衰老/疲惫/流血代价） | test_dragon_heart.py | ✅ |
| 最终的冠冕 / 第8场最终死斗（封存候选人JSON持久化、双方带队、交替出手；对手轮回者可发动道纹、回始双方加法力、回终双方清法力、一方出手尽后余手连动） | test_final_duel.py | ✅ |
| 封印仅移出怪物；雕塑对任何非轮回者；轮回者开局攻面板 0×0 | test_seal_and_sculpture_identity.py | ✅ |
| **三副本终音法器（10件）** | **test_terminal_artifacts.py（新增，13条）** | ✅ |
| **初拥之夜（9选1）** | **test_first_embrace.py（新增，20条）** | ✅ |
| **真龙之心（龙性经济+8件龙族遗物）** | **test_dragon_traits.py（新增，17条）** | ✅ |

粗体三行是本轮新完成的内容，细节见下节；其余为此前各轮里程碑，设计取舍记录见
`AI_EXPERIENCE.md`对应章节。

## 二、本轮新增：三副本终音法器 / 初拥之夜 / 真龙之心

死斗胜利后，按`current_region`领取1件终音法器（`choose_terminal_artifact`）：

- **扭曲都市**（4选1）：体外心脏（战始血限/生命翻倍）、羔羊之泪（战始全员减半生命）、
  红头绳（解锁局外"献祭"：衰老3换精力+2）、猩红尖牙（强制触发初拥之夜后才封存）。
- **罪孽都市**（3选1）：黑金名片（战始敌方血限减半，等量碎片，允许负债≤50）、
  罪业金库（回始X碎片换2X格挡，X≤2%碎片）、教父左轮（耐久6/6，伤害随本场使用次数递增）。
- **龙心谷**（3选1）：共心环（选定龙心类型后[朋友]/[员工]也能共享抵消代价）、
  负岳碑（预声明保护，某队友即将撤退时改为玩家流血20顶替）、真龙之心（见下）。

**初拥之夜**（`choose_first_embrace`，9选1，1~8限选1次，9号"封存血脉"可重复保留触发权）：
血族血脉（造成伤害则回终回复等量否则流血20）、不朽之躯（血限减半但免疫衰老/无法增长）、
鲜血之翼（流血5X换飞行X）、血族尖牙（衰老20把弱于自己的目标转化为赤族仆从）、
真理眼（**中断交DM裁定**，不做自动判定，冷却2场）、寒冰法力（对某目标累计施加10法力
使其出手-1，含对自己施法也算数）、血影（流血10取消一次非必中判定）、
血食（命零一名赤族换取等量回复）。

**真龙之心**：解锁后进入独立的"龙性"资源经济（6衰老=2枯竭=1萎缩=12龙性），
每花12龙性解锁1种独有遗物（`unlock_dragon_trait`），8种全部实现：
龙族血脉（对怪物秒杀/对非怪物伤害翻倍）、龙威（无需代码——怪物阶段本就恒定只打玩家本人，
详见下方"顺带确认的既有行为"）、龙族利爪（初始3次攻击1点攻击力，每完成一次行动+1次+2点）、
龙息（敌方行动前受10×当前回合数必中伤害）、震岳龙躯（花6X龙性换X回合15点伤害上限）、
吞骸龙胃（吞噬已死怪物回复12+可选龙心+6耐久）、断尾求生（预声明命零时移除其他遗物抵消）、
烬翼（花3X龙性换飞行X）。

**测试过程中发现并修复的3个真实bug**（详见`AI_EXPERIENCE.md`对应章节）：
1. 震岳龙躯的持续回合数从未递减过，一旦激活就是永久护体。
2. 黑金名片的"负债≤50"校验在已经把敌方血限改掉之后才做，校验失败也已经生效。
3. 龙族利爪的"每完成一次行动+1次攻击"挂在动作执行**之前**，导致【攻击】自己都满足
   不了自己刚提出的目标数量要求。

**已知的设计局限（非bug，已在代码与`AI_EXPERIENCE.md`中disclose）：**
封存的对手快照里`artifacts_owned`/`first_embrace_traits`/`dragon_traits`只做记录保真，
死斗中不会让对手一侧真正触发这些遗物的被动效果（全代码库的trait判定目前只认
"当前操作方"，未做成双方对称生效，需要更大重构，本轮不做）。

## 三、本次盘点顺带发现的"死数据"道纹（✅ 2026-08-11 已全部修复，见更新）

> **更新（2026-08-11）**：本节所列 12 条死数据/部分失效道纹已全部实装：
> 龙心谷4（逆鳞/嫁祸/背负/伤痕）见 `tests/test_exclusive_longji.py`；
> 罪孽7（洗劫/逼债/抵扣/清算/赌命/消灾/假钞）+ 扭曲2（爆裂/退化）见
> `tests/test_exclusive_zui_twisted.py`（26 条，正常14+边界9+错误3）；该修复当轮全库 394 passed。
> 以下保留原始审计结论供追溯（标✅的为当时已生效项，❌/⚠️ 项现已修复）。

在为本文档核对"效果有没有真的被消费"的过程中，发现三副本各自专属道纹链条里，
有相当一部分**只算出了一个数字/状态，从未被任何下游代码真正读取执行**——这与
最初审计"没有实际作用的功能"是同一类问题，但不属于本轮"终音法器/初拥之夜/真龙之心"
的范围，是排查这次任务时顺带翻出来的，**还没有动手修，需要用户确认范围后再排期**：

### 龙心谷专属道纹链（加害⇄龙鳞⇄逆鳞⇄活血⇄裂变⇄嫁祸⇄背负⇄伤痕⇄加害）
- ✅ 加害、龙鳞、活血、裂变：确认有真实的伤害计算/回复钩子。
- ❌ **逆鳞**：只挂了一个空status，"每掉1HP积1层、下次伤害+层数后清空"的核心机制完全没写。
- ❌ **嫁祸**："自身下X次受伤由目标承担"——计算出`redirect_count`后再无下文，没有任何
  伤害重定向逻辑。
- ❌ **背负**：同上，`absorb_count`算出来后同样没有下文。
- ❌ **伤痕**："每次掉血后血限-X"——虽然靠通用兜底逻辑挂上了一个叫"伤痕"的status，
  但没有代码读取它来真正扣血限。

### 罪孽都市专属道纹链（洗劫⇄逼债⇄抵扣⇄清算⇄赎金⇄假钞⇄赌命⇄消灾⇄洗劫）
- ✅ 赎金（夺碎片/扣速度）、假钞（获得假碎片，虽与真碎片混在同一个池子里，见下条）：
  基本效果生效。
- ❌ **洗劫**："造成伤害时夺取目标等量碎片，持续X"——只加了一个空status，从未在造成
  伤害的地方触发夺取碎片。
- ⚠️ **逼债**："回始使目标失去X碎片，否则失去2X血限"——现有代码只在发动瞬间做过一次性
  的通用`shard_drain`处理，不是文档要求的"每回始重复判定"。
- ❌ **抵扣**："封印目标一件遗物"——计算出`relic_seal`后，代码里搜不到任何消费这个key、
  或者实际"封印遗物"的逻辑。
- ⚠️ **清算**：与逼债类似，"回始扣格挡"目前只有发动瞬间的一次性处理，不是逐回合重复生效。
- ❌ **赌命**："回始按存活角色投随机数，随机一个目标失去30%血限当前生命"——现有代码
  把它当成对施法时选定目标的一次性效果，既没有"每回始"的循环，也没有"随机挑一个存活
  角色"的逻辑；另外其代价文档写的是"消耗X**假碎片**"，但代码里`cost_type`用的是通用的
  "消耗"（=法力），会被当成扣法力而不是扣假碎片，是资源类型对不上的bug。
- ⚠️ **消灾**："重置随机数X次以改变结果"——只扣了碎片，`rerolls`这个key算出来后没有
  接入任何真正"重投骰子"的入口；另外文档给了"50X假碎片 / 5X碎片"两种付费方式，代码
  只实现了后一种。
- ⚠️ **假钞**："获得10X假碎片（战斗中失去碎片时优先失去假碎片）"——目前假碎片和真碎片
  混用同一个`state.shards`整数，没有区分"优先失去假碎片"这条规则，等于假碎片和真碎片
  完全等价，起不到文档说的缓冲作用。

### 扭曲都市专属道纹链（变形⇄定型⇄畸变⇄僵化⇄超频⇄坏死⇄爆裂⇄退化⇄变形）
- ✅ 变形、定型、畸变、僵化、坏死、超频：确认有真实机制。
- ❌ **爆裂**："受到伤害后攻击者失去等量生命，持续X"——只挂了状态，从未在承受伤害后
  反向扣攻击者生命。
- ⚠️ **退化**："使目标每次发动道纹时数值-X"——现有代码只是在"怪物战斗难度提示"里把
  它列为一个"叙事性标记"，从未真正让目标发动道纹时的X值被扣减。

**核对方法说明**：以上结论基于静态代码走查（确认`calculate_xxx()`产出的字典key，
在`engine/combat.py`/`engine/api.py`里有没有被真正读取消费），标✅的是已确认有真实
运行时效果的；标❌的是确认完全没有下游消费、纯粹的死数据；标⚠️的是"有一部分效果生效，
但和文档描述的触发时机/资源类型对不上"。这些结论尚未逐条补充运行时脚本复现，
按本项目的一贯要求，动手修复前应先用`python3 -c`临时脚本实际复现问题、再补齐修复与
对应的`tests/test_*.py`，而不是直接改代码——这部分工作量不小（12种道纹，横跨三个副本），
建议作为独立的下一个里程碑来做，需要用户先确认是否要修、以及优先级。

## 四、当前未实现范围

1. 二阶及以上副本仍是规则草案：乱葬岗、永夜庭、沉沦海、荒疫古城、巴别塔不会进入当前运行时。
2. 草案中的预算、专属道纹、事件、物品与法器只参与文档完整性检查，不代表引擎已经实现。
3. 当前实现与待平衡项以 `功能文档.md` 为准；历史审计结论保留在上一节，仅供追溯，不再作为待办清单。
