# AI经验库

> 用户提到的“AI 知识库”指仓库根目录的 `AI_EXPERIENCE.md`。
> 本文件只保存现行工程约束、当前有效经验和尚未解决事项；提交历史由 Git 保存，不在这里保留废案流水账。

## 第四宇宙开发与维护规则（项目最高优先级）

> **第四宇宙不是为了追求代码架构完美而存在的。架构服务于游戏规则。**
> **任何重构、优化、抽象、统一，都不得以牺牲已经确定的游戏规则为代价。**
> **遇到不确定的问题，宁可保留现状并报告，也不要自作主张改变玩法。**

本节优先级高于本文件其余全部条目；与后文任何工程准则冲突时，以本节为准。

### 1. 游戏规则优先于架构统一

「代码结构更统一」与「现有游戏规则保持不变」冲突时，**始终优先保持游戏规则**。
宁可保留一个特殊分支，也不要为了统一代码而改变已有规则。

### 2. 不能把代码现状当成游戏规则，也不得自造规则

必须区分「当前代码是什么」与「游戏设计规则是什么」。看到旧代码不得直接推断"规则就是这样"；
看到 `current_hp`/`is_alive` 等核心字段的直接修改**不能自动定性为 Bug**——必须先判断这是
正常机制、模型层原语、故意绕过还是历史遗留。规则未明确时**不要自行发明隐藏规则**，
列出当前行为与可能的两种解释、等待设计裁定；遇到不确定的问题，宁可保留现状并报告，
也不要自作主张改变玩法。（合并原 R02/R11/R19/R22。）

### 3. 特殊机制允许绕过统一入口，但必须说明原因

尸爆、死斗结算、局外事件、剧情特殊死亡、特殊自毁等机制**可能故意不走普通战斗流程**。
特殊死亡可以拥有独立流程。看到 `is_alive = False` 时必须先判断属于：正常战斗死亡 /
特殊战斗死亡 / 战斗结束后的结算死亡 / 模型层状态操作 / 离场操作。
绕过必须满足三要件：① 明确为什么绕过；② 明确哪些普通机制不会触发；③ 加注释防止未来
Agent "顺手统一"。**尤其禁止把「代码不统一」和「代码错误」混为一谈。**（合并原 R03/R07/R08。）

### 4. EffectContext 使用规则

`EffectContext` 的核心目的是**追踪战斗因果来源**，不是要求所有函数都必须有 ctx。
需要：伤害、生命损失、治疗、血限变化、速度变化、代价、死亡、离场等能形成战斗因果链的效果；
不需要：初始化、纯 UI 数据、局外静态数据、纯剧情文本。
判断标准：**这个变化以后是否需要回答"为什么会发生、是谁造成的、来自哪条效果链"？**

### 5. CombatEvent 与 EffectContext 必须保持职责分离

```text
CombatEvent    = 发生了什么
EffectContext  = 为什么发生 / 来自哪里
```

不要让两个系统互相替代；不要因为已经有 EffectContext 就删除 CombatEvent。

### 6. 核心战斗效果必须优先使用统一入口

| 效果 | 统一入口 |
| --- | --- |
| 伤害 | `CombatEngine._apply_hostile_damage` |
| 生命损失 | `CombatEngine._raw_hp_loss` |
| 血限变化 | `CombatEngine._apply_blood_limit_change` |
| 死亡判断 | `CombatEngine._check_hp_zero_death` |
| 死亡通知 | `CombatEngine._on_entity_death` |
| 治疗 | `GameState.apply_heal` |
| 代价 | `CombatEngine.pay_numeric_cost` |
| 离场 | `CombatEngine._remove_from_combat` → `Entity.depart_battle` |
| 失速/归零 | `CombatEngine._lose_current_speed` |

禁止为了一个新遗物、新道纹、新事件重新实现一套扣血、死亡、血限变化、治疗逻辑。

### 7. Hook 顺序属于游戏规则

只要不同顺序会产生不同战斗结果，**这个顺序就是游戏规则的一部分**。修改 priority、注册顺序、
Hook 分组、分发流程之前，必须确认不改变相对顺序。顺序敏感的既有约定：
`倍率 → incoming adjust → 重定向 → before damage → mitigation → 实际扣血 → after damage`；`加害 → 龙鳞`。
未经明确规则裁定，禁止改变相对顺序。

### 8. 一个效果只能有一个正式执行入口

避免「旧 combat 逻辑 + Hook 同一效果执行两次」。新增效果必须确认正式执行入口；
仅用于测试/兼容/扩展的 Hook 必须在代码中明确标注。长期维护优先保护四件事：
效果知道自己为什么发生；连锁效果知道谁触发了谁；正常死亡不能绕过死亡管线；同一效果不能多入口重复触发。（合并原 R10/R20。）

### 9. 测试失败时禁止直接修改测试答案

```text
测试失败 → 确认实际实现 → 确认设计规则 → 判断"实现错"还是"测试错" → 只修改错误的一方
```

### 10. 核心战斗代码修改后必须进行语义验证

高风险文件：`engine/combat.py`、`engine/combat_hooks.py`、`engine/effect_context.py`、
`engine/combat_events.py`、`engine/models.py`。改后不能只跑 pytest，应做旧/新语义差分
（`git worktree add --detach <旧commit>` + 同一份场景探针两边比对），差异必须定位到
具体场景/效果/调用链/Context/Hook/执行顺序，不能看到差异就直接改测试。

### 11. 修改后必须保护"为什么这样写"

特殊规则、反直觉逻辑、绕过统一入口、历史兼容逻辑，都必须保留说明原因的注释。
**不要删除解释特殊规则的注释**——它是防止未来 Agent 再次把代码改坏的防线。

### 12. 最小必要修改；不因"大文件"主动拆分

用户要求修一个效果时，不要顺便拆 API、重构、统一无关旧代码；`api.py`/`combat.py` 很长、
某函数几百行均不构成重构理由。大规模拆分必须作为独立任务执行。（合并原 R15/R16。）

### 13. 每次新增游戏机制都必须先判断它属于哪一层

层次：战斗事实 / 战斗效果 / Hook / 道纹 / 遗物 / 角色能力 / 事件 / 副本规则 / 局外成长 / 剧情特殊结算。
不要随意跨层实现。

### 14. 修改已有机制时必须保留旧行为证据

涉及伤害、死亡、治疗、血限、Hook、遗物、道纹、状态的修改，优先找到相关测试、相关模拟、
旧版本行为，用证据判断修改是否改变规则；不能只依据"新的写法逻辑上看起来更合理"。

### 15. 提交修改后的标准汇报

1. **改了什么**——精确到文件、函数、行为。
2. **为什么改**——Bug / 规则要求 / 架构要求 / 兼容要求。
3. **哪些地方故意没改**——尤其是特殊规则、兼容逻辑、局外事件、战斗结束结算。
4. **测试结果**——至少包含 pytest 结果、新增测试、失败测试。
5. **是否存在行为变化**——有就明确列出；没有就明确写"除明确裁定的规则变化外，没有发现新的战斗行为变化"。

## 软件工程实现与验证准则

以下准则适用于本仓库中的需求分析、实现、验证、文档和交付；目标是交付可验证的实现，而不是给出听起来合理的解释。

> **最高要求**：真正做不到的情况下必须直接告诉用户并解释为什么，禁止胡编乱造和为了偷懒故意说做不到。

1. **不得编造。** 不得捏造文件内容、函数、测试结果、命令输出、性能数据、接口或实现进度。真正做不到的情况下必须直接告诉用户并解释为什么，禁止胡编乱造和为了偷懒故意说做不到。
2. **不可见或不可运行时必须明示。** 无法直接看到仓库、文件或运行结果时，必须明确写“我无法验证/无法运行”，并说明原因。
3. **完成声明必须有证据。** 任何“我已经实现/已完成/已修复/已测试通过”的表述，都必须附带可核查证据，包括相关 `git diff`、文件路径、关键代码片段、测试用例或断言，以及复现方法。
4. **有歧义先停止并提问。** 只要存在不确定、歧义或规则口径可能分叉，必须停止实现并向用户提问，得到裁定后才能继续；禁止按常见做法猜测。
5. **禁止用简化实现冒充完成。** 未经用户明确同意，不得用简化版、临时版或写死少量例子的方式声称完成完整需求。
6. **输出使用固定结构。** 每次需求处理的输出必须按以下顺序组织；若存在必问问题，则写完第 (2) 项后停止实现，待用户答复后再继续：
   - (1) 需求复述
   - (2) 不确定点清单 + 必问问题
   - (3) 设计方案
   - (4) 实现计划
   - (5) 交付
   - (6) 测试
   - (7) 进度与限制
7. **新增功能必须有三类测试。** 至少包含 1 个正常路径测试、1 个边界条件测试、1 个错误输入/非法配置测试；错误测试必须验证校验器会拒绝非法输入。
8. **结论必须由验证支撑。** 禁止使用“我认为没问题”或“应该可以”作为结论；必须提供断言、测试结果或可检查输出。
9. **可自定义能力必须由数据驱动。** 需求包含“可自定义/可配置/可扩展”时，必须做到不改代码、只改配置或数据即可新增至少 2 个不同实例；自定义维度必须覆盖需求指定的触发条件、效果等要点，并提供两个配置示例。
10. **硬编码不等于自定义系统。** 如果只实现少数硬编码例子，必须明确写“这不是自定义系统，只是固定实现”，并停止声称已经完成自定义系统。
11. **发现规则或架构冲突时停止。** 必须给出两到三个可选方案及各自取舍，等待用户选择后再实现，不得自行裁定。
12. **及时维护知识库。** 新裁定、当前限制和可复用经验应及时写入；过期方案、失效数据以及已经解决的修复记录必须从本文件删除，不得以“仅供追溯”为由继续保留。

## 第四宇宙规则正文

> 自 README 迁入（2026-09-12）：README 只保留上手流程，规则条文集中在本节。
> 本节是引擎的规则事实源（`engine/events.py` 解析通用事件池、`engine/rule_sync.py` 解析道纹体系）。

### 基础定义

[战始]：战斗开始时
[战终]：战斗结束时
[回始]：完整回合开始时
[回终]：完整回合结束时
[敌回始]：敌对角色回合开始时（对轮回者而言指怪物回合开始时，对怪物而言指轮回者回合开始时）
[敌回终]：敌对角色回合结束时（对轮回者而言指怪物回合结束时，对怪物而言指轮回者回合结束时）
[目标]：一个目标 / 一名目标 / 一个角色 / 任意目标。凡要求[目标]的行动都必须显式选定合法目标；未选定目标时该行动失效，不得默认改为自身或任意其它角色。凡是带有 [目标] 道纹（如【寄生】、【加害】等），目标在被选定时均可消耗 1 点当前速度进行闪避！若成功闪避，该道纹的判定与结算完全失效

平分规则：当一个数值同时作用于多个目标时，若要求平分，则将该效果的总数值在所有目标之间平均分配；无法整除时，余数按随机数分配。除非效果另有说明，多目标不会复制或增加效果的总数值。
[碎片]：灵魂碎片 / 碎片
[血限]（生命上限）：当前生命无法超过[血限]，受到伤害扣除等量生命。当[命零]且无回复手段时当场死亡
 [法限]（法力上限）：法力是**一场战斗一池**的资源（DM裁定 2026-09-09）：[战始]直接获得等同当前[法限]的法力，[回始]不再回填、[回终]与[敌回终]都不清空，整场战斗按剩余量支配，[战终]复原到[法限]（与当前速度同口径）。遗物额外获得（折速法印、守夜灯等）叠在这一池之上，因此当前法力**允许**超过[法限]（DM裁定 2026-08-30：51/34 这类面板是合法的，不是假账）；仅遗物【不朽之躯】将获得的法力压回[法限]。
[速限]（速度上限）：出手次数不再由速度换算（DM裁定 2026-09-10）——**轮回者固定 2 次**，[朋友]/[员工] = [攻击次数] / 3，怪物 = [速限] / 3；【疯狂】+X、【无力】-X 的修正照旧。被选定为非必中判定（攻击、道纹、法术）的目标后，可选择消耗 1 点当前速度（速度-1）完全闪避本次判定与结算。成功闪避后本局速度-1，[战终]复原。
[员工]：受雇微光者 / 受雇者
[朋友]：非受雇队友 / 非受雇微光者 ，［临时朋友］［战终］离去
[命零]：当前生命/血限为0 
一轮攻击（仅限怪物与微光者）：连续发动 N 次攻击（N=自身攻击次数）。每次攻击独立选定目标、独立判定闪避（消耗 1 点速度闪避 1 次）、独立造成“攻击力”点伤害
发现：从随机抽取的3个未持有选项中选择1个
消耗品（X/Y）：可使用X次，耐久上限 Y，每次使用后 X-1，X 归零后消耗。使用时不消耗出手。同名消耗品叠加合并。当完全相同的【消耗品】时，直接将其当前耐久与【当前耐久上限】自动合并叠加（例如：手头拥有2/3的 A，再次搜获1把3/3的 A 后，自动合并扩容为5/6的 A）。同一时点连续使用多个消耗品时，必须逐件完整结算前一件消耗品的目标、效果、耐久、伤害、死亡与状态变化后，才能使用下一件。
数值型【代价】：以数值支付的代价，包括流血、衰老、枯竭、萎缩、疲惫与异变。当某项效果允许数值型【代价】由[朋友]/[员工]共同承担时：可指定一名存活的[朋友]或[员工]，将代价平分（按平分规则，余数随机分配）；双方都必须能够完整支付各自承担的部分。抵消代价的效果先结算，再拆分剩余后果。法力【消耗】、失忆、冷却与唯一不属于可共同承担的数值型【代价】。
[回复X]：使[目标]恢复X点当前生命
格挡：抵消等量伤害，**不再[敌回终]清空**（DM裁定 2026-09-10：原口径下 4 法力换 8 点格挡、一回合就被抹掉等于没有价值），保留到被打掉或[战终]统一清除。代价绝对无法被格挡护盾吸收/抵消；格挡仅能抵消外部【伤害】。格挡的持有量**没有**上限（DM裁定 2026-08-30）：正文只规定它「抵消等量伤害」，不设血限压帽，叠到超过[血限]的格挡是合法面板
持续X：效果持续 X 个目标自己的回合，在目标回合结束时 X-1）。同名效果直接合并：数值相加，例如兴奋4+兴奋4=兴奋8；触发次数相加；飞行等状态不增强效果，只记录层数

效果作用域：数值增加/减少的方向与效果何时清除是两件独立的事。战斗中产生的面板变化若未明确写为「永久」、不是【代价】后果、不是伤害造成的当前生命变化、也不是资源获得或支出，一律属于局内增益/减益。持续期间直接覆盖或修正面板的效果在对应「持续X」到期时逆向清除；畸变、伤痕等已经逐次产生的累计局内后果保留至[战终]再逆向清除。局外产生的面板变化与[战终]发放的奖励默认保留到本次轮回结束；明确写为「永久」的效果跨轮回保留。【代价】后果不因清除局内增益/减益而复原。格挡跨回合保留（DM裁定 2026-09-10，原[回终]清空废止）；当前法力一场战斗一池（[战始]给满、[回始]不回填、[敌回终]不清空），与当前速度、局内状态及局内追踪计数一样在[战终]复原或清空（DM裁定 2026-09-09）。
共计X：该道纹在结算时，将X作为效果系数，例如共计5=杀伐5/再生5/波及5，并豁免该次发动的全部消耗与代价
转化道纹：以自身已持有的一种道纹为起点，沿其所在闭环选择一条相邻的变化路径（转换／反转／曲解），该路径指向的道纹
声明：部分道纹与遗物要求作出【声明】。声明须在其要求的时点公开作出，内容一经作出不可更改，并在其指定的判定时点结算正误。一次道纹要求声明X项内容时，视为X次声明，各自独立判定正误。声明被判定为错误时，无论其内容实际正确与否，一律视为错误

### 核心规则

自由规则：只要没有明确禁止的任何操作都是允许的

随机数规则：需要随机时，先统计当前池中符合条件的选项总数，由游戏引擎在该范围内自动生成随机数并直接结算对应选项，过程记录随机种子与结果序号以便追溯与复现，玩家与DM不再需要手动提供数字。已触发、重复或不符合条件的选项不计入池中，范围随之缩减。
当前事件池由所有未遇到的通用事件，以及当前区域中符合条件且未遇到的专属事件共同组成；通用事件排列在前，专属事件排列在后。已触发或条件不满足的事件不进入当前池。

整数规则：所有计算都向上取整

自由控X规则：施法者在发动道纹或法术时，可根据当前剩余法力与战术需求，自由指定/控制公式中的代数 X 数值（1 ≤ X ≤ 当前可用法力/代价上限），无需强制一次性清空全部法力。X=0 为「拒绝发动」（DM裁定 2026-08-30）：轮到发动却不想发动该道纹时，可显式提交 X=0，该次发动视为无效——不支付代价、不消耗出手、不产生任何结算，也不判定为一次失败的尝试（轮回者有权拒绝发动不想发动的道纹）。X=0 只表示“放弃这一次机会”，与「轮空／让行」同义。

积木规则、循环规则、中断规则、法术阶级规则的完整正文见
[法术索引](法术索引.md#二三大法则与法术阶级规则)。

属性与状态规则：开局分配 25 点初始属性点，计价 **1属性点 = 6[血限]，2属性点 = 1[速限] = 1[法限]**。
局外【修行】与开局初始分配同口径、不作区分：修行所得属性点同样可以兑[血限]、[速限]与[法限]。
属性点可以存储、随时兑换：速限/法限按 2 点一档（点数须为偶数），血限按 1 点一档，余点留在池内，兑换仅限战前。
[攻击次数]与[攻击力]不是可购买的独立面板（仅限轮回者）：**[攻击次数] = 当前速度、[攻击力] = 当前法力**，战斗越久两者越低、输出随之衰减；[朋友]/[员工]与怪物读自身面板。
轮回者的普攻伤害 = [攻击次数]×[攻击力]，走与怪物相同的两阶段攻击接口，占用出手次数；攻次/攻力归 0 同样触发【雕塑】。

轮回者：［血限］/［法限］/［速限］
 
微光者：攻击次数×攻击力/[血限]

在各个意外事件遇到，可以与其同行，作为[朋友]加入时，开局即在战场，同轮回者一同行动，不收取工资，不触发待命。微光者会根据情况对敌方出手（出手次数=当攻击次数/3），可能是发动一轮攻击或发动一次自身道纹效果

怪物：攻击次数×攻击力/[血限]

未觉醒者/微光者因各种原因所变，怪物会根据情况对轮回者出手，每回合固定发动一次攻击和发动一种道纹

### 残韵作用

1.残韵未生效则不消耗
2.残韵作用于任意角色拥有的道纹时，将其永久变为变化后的道纹，施法者同时永久获得变化后的道纹。
3.通过残韵获得的道纹，X值按施法者自由控X规则自定义，不固定为原道纹数值
4.残韵在道纹结算前生效前，原道纹的消耗或代价、目标要求、效果与后续流程全部改为新道纹的原版公式，不保留原公式的未结算部分。已经完成的结算不追溯撤销；新公式的消耗或目标条件无法满足时，后续流程中断。
5.角色拥有的道纹始终保持唯一，同名道纹不会重复存在

### 局外系统

行动选项如下，（）中的数字代表所消耗的[碎片]
休整（按轮回者[血限]的20%/40%/60%产生恢复量，向上取整；碎片代价 0/10/25；恢复量可自由分配给自己或队友）
修行（获得1点/2点（15）/3点（35）/4点（65）/5点（100）/6点（150）属性点）
学习（选择学会一种/两种（10）/三种（25）法术/自创一种法术/习得一种/两种（10）转化道纹）
（自创法术须完全由创建时已拥有的道纹按三大法则组装，[战终]可以进行修订；完整设计原则、三大法则与自创法术标准语法见 [法术索引](法术索引.md)）
学习门槛：当前副本的专属道纹须先通过残韵从本副本怪物处转化获得至少一种，此后才可学习该副本的其它专属道纹；其它副本专属道纹不可学习。怪物转化道纹只能从自身已持有道纹经残韵变化获得，原始怪物道纹无法由人类学习。
残韵获取途径：开局必选 1 种；遗物【三相残韵盘】；乱葬岗事件「贴符加固」；遗忘书屋事件《自我剖析》；以及事件文案中「曲解／反转／转换 + 残韵」的通用解析。
共鸣（精力再次-1，发现/自选（15）一件遗物）
探索（发现 1 次 / 2次（30）未遇到事件）

### 怪物准则
1.免疫一切现实伤害（指现实中的物理，化学等等）
2.只通过灵魂锁定位置
3.陷入困境时强制选择逃跑/进化，每场战斗限一次，且逃跑与进化二者只能选择其一。
4.困境是指怪物的主要优势被针对、原有取胜路线被稳定限制，继续依靠现有属性与道纹将明显陷入被动，但尚未达到必败。核心道纹被变化或失效、连续无法有效攻击、对方建立稳定压制循环、被特殊手段破解主要优势时，均应立即进行困境检查。困境检查以当前胜率为依据，而非当前生命
5. 怪物不持有法力，也没有[法限]。面板中的“可分配属性点”仅为设计阶段的度量衡，道纹配额另按副本阶级给，不是战场上的可用资源。怪物发动自身面板上的道纹不支付法力，只消耗出手；面板未列出的道纹无法发动。怪物发动代价类道纹时必须照常支付对应代价：
   - 发动【冷却X】道纹（如固执、束缚、迟滞）后该道纹立即进入冷却，在本场战斗剩余回合及后续X场内无法再次发动，严禁单场内重复复读发动；
   - 发动【流血X】道纹（如血债）必须真实扣除怪物自身X点生命；生命不足X时无法发动；
   - 发动【衰老X】扣除自身X点血限，发动【疲惫X】扣除自身X点当前速度，发动【异变5X】累加自身异变层数。
6.拥有智慧且没有道德底线，同样会向"某人"祈求许愿，判定标准与轮回者相同。
7.怪物与轮回者共享基础规则，但怪物可额外拥有符合自身物种、来源与副本机制的怪物准则；该类准则不得无视已有规则、代价与因果。
8.白板开局：怪物在[战始]的第一回合，仅具备基础攻击次数、攻击力和生命值，（例如具有【飞行】或【庇护】的怪物，在第1回合绝非飞行状态也无初始格挡，须在其出手轮主动发动后方可生效）
9.道纹递增：怪物可以在不同回合重复发动同一道纹（每回合每道纹至多一次；【冷却X】道纹仍受冷却约束）。每当怪物实际发动一次某道纹，该道纹的X在本场内累加+2×副本阶级（一阶+2、二阶+4，以此类推；例如一阶怪物发动再生5后，下次发动即为再生7）。无法支付代价、发动被打断或被控制跳过的回合不计。递增同步放大效果与真实代价（流血/衰老/异变等照新X支付；怪物自愈的回复量照常计入其癌变累计——怪物不是特殊角色）。

### 代价

附带代价的道纹不消耗法力，代价直接由施法者支付。
唯一：使用后，本次轮回中无法再次使用
冷却X：使用后该道纹记为【X（0）/Y】。[战终]后已完成战斗场数+1，达到Y时才能再次使用
流血X：失去X点生命。
衰老X：失去X点［血限］
枯竭X：失去X点［法限］
萎缩X：失去X点［速限］
疲惫X：失去X点当前速度
失忆X：本次轮回永久失去自身拥有的X种道纹。
异变X：获得X层【异变】。

### 道纹体系

杀伐X（起点/终点） ⇄ （反转） 再生X ⇄ （曲解） 庇护X ⇄ （曲解） 固执X ⇄ （反转） 血债X ⇄ （转换） 波及X ⇄ （反转） 增殖X ⇄ （曲解） 透支X ⇄ （转换） 贯穿X ⇄ （曲解） 束缚X ⇄ （曲解） 封印X ⇄ （反转） 杀伐X（闭环合拢）

杀伐X（起点）：消耗X。对[目标]造成 5X 点伤害。
再生X：消耗X。为[目标][回复] 4X 点生命。
庇护X：消耗X。使[目标]获得 2X 点格挡（可抵消等量伤害，保留到被打掉或[战终]）。
固执X：代价：冷却X。自身单次失去生命最高为 1，持续X。
血债X：代价：流血X。选择[目标] X 次，每次对其造成 1 点伤害。
波及X：消耗3X。选择X个[目标]建立/解除波及效果，持续∞。你发动的道纹同时作用于所有拥有波及效果目标；数值平分（按平分规则）。（发动时X按当前合法目标数自适应降X：合法目标不足面板X时按目标数结算，目标数为0时不可发动。）

增殖X：消耗X。［目标］［血限］+X
束缚X：代价：冷却2X。使[目标]无法行动，持续X。
透支X：代价：流血3X。你获得 X 点法力。
贯穿X：消耗5X。你造成的[伤害]无视格挡，持续X。
封印X：代价：异变8X。使 X 个[目标]怪物移出本场战斗（被移出的怪物不提供任何[碎片]收益）。

### 原始怪物道纹与转化道纹

狂暴X（代价：异变5X：[回始]发动一轮额外攻击，持续X）→（转换）愤怒X（消耗5X：使[目标]法力消耗减半，持续X）
狂暴X→（反转）自残X（消耗10X：使[目标]对其自身打出X次攻击）
狂暴X→（曲解）无神X（消耗20X：使[目标]选择目标时强制改为自身，持续X）

强化X（代价：异变5X：使[目标]攻击力+X，持续∞）→（转换）借力X（消耗10X：使[目标]造成伤害+10X%，持续∞）
强化X→（反转）弱化X（消耗3X：使[目标]攻击力-X，持续∞）
强化X→（曲解）自食X（消耗X：将自身X点攻击力转化为等量[回复]）

疯狂X（代价：异变5X：使[目标]出手次数+X，持续∞）→（转换）兴奋X（消耗5X，使[目标]每次出手后速度+1，持续X）
疯狂X→（反转）无力X（消耗10X，［回始］使[目标]出手次数-X，持续∞）
疯狂X→（曲解）迟滞X（代价：冷却 X：使[目标]攻击次数固定为1，持续 X）

净化X：消耗5X。使[目标]【异变】-X 层（荒疫古城专属；计算已实现，不可经局外学习直接获得）

减速X（代价：异变5X：使[目标]速度减半，持续X）→（转换）急速X（消耗20X：使[目标]每闪避两次速度+1，持续X）
减速X→（反转）加速X（消耗20X：使[目标]获得的速度翻倍，持续X）
减速X→（曲解）眩晕X（消耗20X：使[目标]无法出手，持续X，［目标］失去生命后立刻苏醒）

必中X（代价：异变5X：自身下X次选择［目标］时，其无法闪避）→（转换）洞察X（代价：疲惫X：使[目标]每次闪避后下回合法力+10，持续X）
必中X→（反转）蒙蔽X（消耗5X：使[目标]下X次造成的伤害无效）

自愈X（代价：异变5X：[回始]获得自身[血限]10X%的[回复]，持续∞）→（转换）滋养X（消耗5X：使[目标]获得[血限]10X%的[回复]）
自愈X→（反转）衰败X（消耗15X：使[目标]［回始］失去10X%当前生命，持续∞；发动时不立即触发）
自愈X→（曲解）寄生X（消耗10X：使[目标]受到的伤害20X%转化为施法者的[回复]，持续∞）

飞行X（代价：异变5X：无法被非飞行角色选为目标，持续X）→（转换）滑翔X（消耗5X：获得【飞行】，持续X）
飞行X→（反转）坠落X（消耗X：所有飞行角色无法飞行且造成伤害减半，持续X）

### 特殊事件

全局触发规则：不参与事件池，不消耗精力，仅在满足条件后自动触发。
死之传承（轮回者[命零]时触发）
该轮回者可在《死者之书》中新增一页〖遗言〗：一句话，最多20字，除此之外没有其他限制。

凡庸（任一角色连续五回合未出手/五回合未能使敌对角色生命减少时触发）
多个角色触发凡庸时，非轮回者优先触发；按优先序逐个结算，一旦先炸裂的角色已经清空某一方战场（战斗胜负已定），立即中断剩余凡庸结算，幸存一方尚未结算的待爆者不再炸裂（其计数随战斗结束清零）。
该角色的无能让某人感到无趣，于是其凭空全身炸裂，如果是轮回者直接死亡，如果是怪物则命零死亡，轮回者获得残骸（1/1）：局内使用恢复20生命并获得异变10

癌变（任一角色在本场战斗内累计受到回复=2×其[血限]时触发。累计回复属于局内减益追踪，[战终]清零；局外【休整】不计入该累计）
轮回者、[朋友]、[员工]及其他非怪物角色的身体无法承受这种强度的细胞分裂，直接判定[命零]死亡。怪物触发癌变时不按普通击杀结算，改为被吸收进《死者之书》，不提供[碎片]奖励；每吸收一只癌变怪物，局外【休整】产生的恢复量永久+8，可叠加、作用于全部休整档位并跨轮回保留。

还债（怪物的负债[碎片]达到20时触发；主要见于罪孽都市）
怪物的[碎片]类代价允许透支成负债：余额不足时不拒绝发动，真碎片直接扣成负数；【逼债X】的无力支付部分同样记为负债。玩家/[朋友]/[员工]的碎片类代价仍维持"余额不足拒绝发动"。
触发后将其视为[员工]立即参战，并进入独立债务轨道：不结算工资、不计入失信黑名单；轮回者一次付清其全部负债[碎片]后该[员工]离队。

雕塑（任意角色的攻击次数或攻击力之一变为0时触发）
该角色化为对应雕塑消耗品（X/Y，Y=其[血限]5%）；每消耗1点耐久，可对1个[目标]造成15点伤害，或使自身获得20点格挡。

员工叛变（[战终]检查，或被效果强制触发）
将所有[员工]的“攻击次数×攻击力”相加。若[员工]攻击总值≥轮回者当前生命+所有[朋友]攻击总值，所有[员工]共同叛变夺取《死者之书》；被效果强制触发时，跳过该数值检查，所有[员工]直接叛变。
-镇压：与所有叛变[员工]开启战斗！若战斗失败或选择【撤退】，失去所有[碎片]，随后所有叛变[员工]携财逃跑；若战斗胜利，肃清叛徒并保留财产。
-让利：本次轮回所有[员工]每场工资+5，叛变平息。
-谈判：给出合理的谈判方案破解叛乱。

许愿（轮回者向"某人"祈求时触发）
轮回者许下一个愿望，消耗一次出手。愿望本身没有固定的可行范围，也不存在"无法实现"的愿望；
"某人"会以能够实现愿望、但最符合其扭曲本质的方式实现愿望；愿望的代价与扭曲方式由愿望本身决定，不预先公开。
引擎只提交现场状态，实现方式与代价由DM裁定。

逃跑与追击（一方选择逃跑时触发）
战斗无缝继续：逃跑方触发逃跑后，战斗与回合正常流转。
逃跑方行阻截：逃跑方必须消耗自身出手，企图拖延时间逃跑。
追击方破局拦截：追击方在正常回合内，一边抵御其余敌对角色攻击，一边阻截逃跑方；阻截成功后逃跑方逃跑失败继续战斗。否则逃脱成功

进化（怪物选择进化时触发）
原初X：代价：异变5X。选择一种当前轮回者已持有、且自身未持有的道纹，[战终]前视为持有该道纹（其数值固定为本次X），借用的道纹发动时照常支付其自身代价。

崩解（任一角色【异变】达到50层时触发）
该角色直接判定[命零]死亡，尸体变为怪物

撤退（任意[朋友]/［员工］即将受到足以使当前[命零]的攻击时触发）
该角色在本次致死攻击结算前自动撤退；撤退后本次攻击失去目标，伤害消失，该[朋友]/［员工］保留当前生命，无法再次加入本场战斗。持续减益、范围伤害或代价致死不触发撤退（直接判定[命零]）。

“最终的冠冕”（轮回者完成第7场战斗后触发）：若当前没有本阶级封存候选，则将该轮回者及其队友、生命、属性、碎片、道纹、残韵、遗物、法术与其它状态完整封存进本阶级封存槽，直到下一名同阶级轮回者完成第7场战斗。若已经存在同阶级封存候选，则新到达者立即成为挑战者，与该阶级队首候选进入作为第8场战斗的最终死斗。
死斗先手与流程：根据[速限]→[法限]→[血限]→当前生命的优先级决定先后出手；四项完全平局时随机决定首回合首手，之后每回合交换首手方。每轮双方交替消耗出手次数；一方出手次数耗尽后，另一方余下出手继续。残韵可任意时刻插队使用。死斗无法逃跑，只有胜者能够进入下一阶级副本。胜者进入进阶封存，不得再与原阶级角色重复死斗。
死斗命零归因（DM裁定 2026-08-30）：主将命零即判该方失守（守擂主将命零→挑战者胜；挑战者命零→守擂卫冕），此判定不因死因改变。但**归因必须如实**：若主将是自己支付【代价】（如【血债X】的流血X）流死的，判定文写明「自付代价命零，非对手击杀」——挑战者仍获胜，但不得白捡一个击杀记功。

救赎（怪物当前生命≤其[血限]10%，且没有七种原始怪物道纹时触发）
转化道纹与副本专属道纹不计入该判定。满血白板不会立刻救赎。
随着最后一缕恶意消散，怪物的身躯开始融化，原地只剩下一个昏迷的微光者
1.接纳 ta，获得[员工]（待命：须耗1出手派遣才参战；参战者战终付工资、全体计入叛变），其面板=原怪物面板/2（满血），名字由你自定义
2.无视，无事发生

“初拥之夜”（得到猩红尖牙时触发；拥有猩红尖牙时，每击杀一名血族后触发。除9外每项限选1次，每次选择后[回复]30%生命）：
1.接纳血脉：获得遗物[血族血脉](物品索引.md#血族血脉)
2.淬炼身体：获得遗物[不朽之躯](物品索引.md#不朽之躯)
3.长出双翼：获得遗物[鲜血之翼](物品索引.md#鲜血之翼)
4.淬炼尖牙：获得遗物[血族尖牙](物品索引.md#血族尖牙)
5.淬炼双眼：获得遗物[真理眼](物品索引.md#真理眼)
6.淬炼法力：获得遗物[寒冰法力](物品索引.md#寒冰法力)
7.化为血影：获得遗物[血影](物品索引.md#血影)
8.举行血食：获得遗物[血食](物品索引.md#血食)
9.封存血脉：保留触发权，随时再次触发初拥之夜。

### 通用事件池

“无名冢”：一片插满残破兵器的荒地，每一把兵器下都埋葬着一个失败的轮回者。风吹过，兵器发出呜咽，似乎在诉说着它们生前的荣光与诅咒。
1.拔出兵器：失去10[碎片]，随机获得一件遗物
2.为你而战：销毁一件当前遗物（包括遗物池中），设计一种新的遗物加入遗物池，获得15[碎片]。
3.拒绝：无事发生。

遗忘书屋：在一处坍塌的街角，一间散发着霉味与干涸血迹的旧书店静静伫立。无光的柜台后，一个没有五官的店员指了指桌上三本由皮革缝制的古籍。
1.阅读《战争残卷》：流血15，选择学会两种法术。
2.阅读《禁忌法典》：失忆1，自选一件遗物与20[碎片]。
3.阅读《自我剖析》：枯竭1，自选残韵×1
4.拒绝：无事发生

祭坛：你来到了一座扭曲的黑石祭坛前，祭坛中央有一柄干瘪的石刃，冰冷的声音在你脑海中响起：“以你之重，换吾之轻。”
1.献祭血肉：衰老8，获得1点[速限]
2.献祭神智：枯竭5，获得2点[速限]
3.拒绝：无事发生

"过路商人"
一个拖着骡车的独眼商人在岔路口支起了摊子，车上的货物用破布盖着，隐约能看到金属反光和跳动的微光。
1.限制选择权：失去 8 [碎片]，限制下一场战斗怪物的选择权（例如：使怪物陷入困境时只能选择【逃跑】或者使其每回合最多只能发动一次道纹）。
2.以物易物：失去一件当前遗物，随机获得一件新遗物
3.拒绝：无事发生

“猩红暴雨”
粘稠如血的暴雨从铅灰色天空倾泻而下，地面的积水散发着令人作呕的铁锈味。带有腐蚀性的雨水拍打在你的皮肤上，发出滋滋的声响。
1.硬扛：流血20
2.用法力撑起屏障：枯竭3
3.躲入漏雨的废墟：失去1次精力

“无名碑林”：在一片长满青苔的石碑群中，碑上刻满了扭曲的符文，记录着无数死者的执念。当你靠近时，石碑产生了共鸣。
1.触摸：流血15。获得【残韵：曲解×1】与15[碎片]。
2.拒绝：无事发生

“回音长廊”：一条两侧挂满破碎镜子的长廊，镜子里映出的是你前几次轮回中惨死的画面。走廊尽头放着一个生锈的八音盒。
1.聆听安魂曲：在《死者之书》中留下一条错误遗言，获得10[碎片]。
2.打碎镜子：流血5，清除《死者之书》中一条遗言
3.捂住耳朵：无事发生。

回忆当铺：一家没有门的当铺，柜台后坐着一团由无数眼球和脑髓组成的阴影。它用沙哑的声音说：“过去的碎片，在这里能称出重量。你要典当，还是赎回？”
1.典当：获得10[碎片]，本次轮回无法再获得前世记忆
2.赎回：消耗10[碎片]，获得一段前世记忆
3.拒绝：无事发生。

手术：一个废弃的地下手术室，旁边的培养皿里有着一颗跳动的、长满触手的心脏正散发着微光。
1.强制移植：使一名微光者队友被植入一种随机的怪物道纹（该道纹每场战斗三回合后若保持原样，则仍变为怪物）
2.抽取灵魂：失去一名微光者队友，获得其[血限]50%的[碎片]
3.拒绝：无事发生

“无魂泥潭”：一处散发着胶皮烧焦气味的沥青深坑。黑色的淤泥在缓缓蠕动，这种物质能彻底隔绝灵魂的波动，但采集它需要付出肉体被腐蚀的代价。
1.采集“绝息淤泥”：流血10。获得“绝息淤泥”（可随时使用，使用后屏蔽自身灵魂位置，可使本次[战终]，立刻逃脱）。
2.拒绝：无事发生。

## 第四宇宙设计原则（自 README 迁入 2026-09-12）

> README 只保留上手流程与规则速查；以下十一条设计原则面向设计与实现，不面向玩家。
> 复杂度原则、道纹与残韵设计原则等在新增任何内容前必须逐条通过（见《AI协作规范》第3条）。

【二、复杂度原则】

新增设计时必须遵守以下顺序：
1. 能否解释为已有道纹？
2. 能否解释为已有法术？
3. 能否设计成事件？
4. 能否设计成遗物？
5. 能否设计成行动？
6. 只有以上全部无法实现时，才允许新增规则。

任何设计都优先归纳，而不是扩张。

【三、道纹与残韵设计原则】

道纹只有两字，不是技能，而是概念，是世界的最小规则单元，相当于语言中的词语。法术相当于语句，由多个已有道纹组合而成。

新增道纹必须代表一个单一、纯粹的基础规则概念，而不是一个完整技能。严禁出现一个道纹无脑完爆其他道纹，或缝合多种独立道纹效果。

凡是与已有道纹组合而成的法术效果类似的道纹，均没有任何存在的意义。

道纹的 X 要么与效果强度挂钩，要么与持续挂钩，禁止两者都挂钩

残韵（转换、反转、曲解）不仅是局外掌握道纹的桥梁，更是战斗中进行实时插队、招架、攻防逆转与概念篡改的核心战术陷阱：
1. 反转（极性对冲）：逆转因果极性，将增益逆转为减益，将伤害逆转为治愈，将控制逆转为爆发。
2. 转换（平向支流）：平移法则维度，将单体转化为范围，将伤害转化为控制，将控制转化为放逐。
3. 曲解（概念腐化）：扭曲代数逻辑，将直接效果腐化为条件触发，将确定效果腐化为阈值判定。

【四、法术设计原则】

法术设计原则、三大法则（积木/循环/中断）与法术阶级规则的完整正文见
[法术索引](法术索引.md#一法术设计原则)。

【五、事件设计原则】

事件负责未知、探索、选择和成长。每个事件必须包含场景描述、至少一个选择、对应代价、对应收益以及拒绝选项。收益必须与事件内容强相关，代价必须具体且合理。

【六、遗物设计原则】

遗物的作用是改变玩法，而不是单纯增加数值。优秀遗物应该改变资源流动、战斗方式、构筑方向或思考方式。每件遗物必须改变至少一种已有资源的使用方式。

【七、怪物设计原则】

怪物与玩家共享完全相同的底层规则。怪物的强项同时也是它的弱点。

怪物不持有法力，也没有[法限]。怪物面板直接以属性点计价（与轮回者同一货币；2026-09-11 裁定：独立"预算/法力"口径并入属性点，不再存在）；道纹配额仍按副本阶级单独给，阶级越高配额越多。怪物发动道纹不支付法力，只消耗行动。

1属性点=6血限=1攻击力，X²属性点=X攻击次数

【八、行动设计原则】

行动负责稳定成长，事件负责未知探索，两者不能混淆。

【九、代价原则】

第四宇宙不存在无代价成长。任何强收益必须伴随即时、持续、永久或机会成本中的至少一种。代价应当从玩家做出选择开始生效。

【十、随机原则】

随机负责创造问题，不负责决定结果。玩家失败应主要来自自己的选择，而不是运气。

【十一、情感表达原则】

第四宇宙不依赖角色喊口号表达主题。任何主题都必须通过规则、事件、选择和世界运行方式自然体现。轮回的核心在于执念与羁绊，玩家与角色应通过抉择与代数过程体悟生命的重量。

【十二、创作原则与绝对禁区】

不要思考"这样够不够酷"，而应思考"这样是否符合世界规则"。

禁止绝对无敌、绝对控制、无法反制。禁止机械降神、凭空捏造未持有道纹或擅自按回合递减跨战斗冷却。

禁止再次重复说明已有规则或说明

禁止出现“击杀怪物获得碎片（不是你失去碎片）”这种两者毫无关系和关联的脑残弱智说明！

规则高于身份，因果高于宿命，自由意志高于一切！

## AI角色扮演、协作规范与推演铁律

### 职责划分

1. AI 扮演本局全部角色，包括轮回者、怪物、[朋友]、[员工]、[临时朋友]与事件中的一切存在，并按各自身份、立场与可用资源作出当前最优决策。怪物依据《怪物准则》行动，[员工]依据工资与自身存活行动，敌对轮回者依据死斗胜负行动，各方立场互相独立，禁止相互放水。
2. DM 不替任何角色作出选择，只判断行为是否违规、裁定未见场景，并在触发特殊事件时介入。
3. 战斗中的数值对撞由引擎结算并由 AI 按结果快进，禁止逐条询问 DM。怪物为了生存和胜利会在陷入绝境时触发【逃跑与追击】或【进化】；仅在触发【许愿】【逃跑与追击】【员工叛变】等确需裁定的特殊事件时暂停等待 DM。正式随机统一由引擎随机源生成并留存记录，不向 DM 申请数字。
4. 局外阶段一次性完成全部3点精力的行动：不涉及随机的行动（修行、休整、学习、领悟等）直接写明所选档位与最终结果，禁止逐次询问。探索由引擎随机确定当前事件；【发现】由引擎随机列出3件未持有候选，再由对应角色显式选择1件，禁止自动取第一项或非法回退。结算后若仍有剩余精力则继续行动，精力耗尽后进入战斗。

### 角色性格塑造

先射箭，后画靶。

首次进入一阶副本时，不预先设定角色的完整性格、价值观或行为倾向。除基础背景、经历、目标和当前状态外，角色应根据自身掌握的信息自然行动、思考和交流。

一阶副本结束后，根据角色在实际行动中的表现归纳其性格特征，包括但不限于：风险偏好、人际倾向、道德底线、资源观、探索欲、情绪稳定性、决策习惯、表达方式以及对不同事件的特殊反应。性格特征必须从实际行为中归纳，不得为了符合某个预设人格而反向修改角色已经做出的选择。

后续副本中，将已经归纳出的性格作为角色的行为倾向而非绝对规则。角色可以因新的经历、情绪、关系和环境发生改变，必要时重新修正性格画像。性格描述不得强迫角色做出特定选择，只用于影响其在多个合理选择之间的偏好。

### 角色扮演要求

角色互动 → 用对话表达思考。
活人感 → 角色应像真实的人一样行动和交流，而不是单纯执行最优策略。角色可以根据自身性格、经历、当前情绪和处境自然产生闲聊、抱怨、吐槽、犹豫、自言自语、惊讶、后悔等反应。没有必要时不要强行解释自己的每个行动；独处时可以适度碎碎念，但不得为了表现活人感而刻意增加台词。

信息限制：角色只能根据自己已经获得的信息进行判断，不得因为系统知道真相而让角色提前表现出未知信息。允许角色误判、犯错、改变主意和事后反思。
关系塑造：角色与其他角色的共同经历会形成关系。关系可以表现为信任、戒备、依赖、竞争、厌恶、友情、亏欠等，并在后续决策中产生影响。关系应由实际互动逐渐形成，不得凭空设定。关系也可能因背叛、救助、共同战斗、冲突或长期相处而改变。

重要：性格是对行为的总结，而不是行为的剧本。不要为了证明角色具有某种性格而刻意制造对应行为。

每场副本或重要阶段结束后，生成简短行为画像；普通战斗仅记录具有明显性格意义的行为。
核心性格：
风险偏好：
人际倾向：
资源观：
道德底线：
情绪特点：
决策习惯：
语言习惯：
已观察到的特殊倾向：
尚无法确定的性格：

### AI协作规范

1. **越权禁令与工具授权**：未经明确指令，禁止修改或删除任何现有正文内容；遇到无法用程序硬编码或不可预见的创造性机制（如【许愿】、【自创法术】、【残韵实时插队】等），程序必须在此类事件触发点抛出中断（Interrupt），立刻停止数值推演并向 DM 提交现场状态等待人工裁定；禁止每轮对话新增无意义的分析文档或总结文档，分析直接以回答形式给出，结论直接落库。
2. **事实源与同步**：正文文件是事实源，归档、合集、记录一律为衍生物，与正文冲突时以正文为准；引用规则前必须先读取原文，禁止凭记忆复述或推测条文内容。
3. **设计合规**：新增设计前必须逐条通过《复杂度原则》，全部不可行才允许新增规则；提出任何效果前必须先检索全部现有道纹、遗物与法器，确认不存在同名、同效或下位替代；副本怪物池只能使用通用核心道纹、原始怪物道纹与转化道纹以及该副本自身的专属道纹；怪物面板必须符合该阶级的道纹数量与数量总值且组合唯一。
4. **代价与收益**：任何收益必须有代价且真实生效，禁止将“限制”伪装为代价；提出效果时必须自行验算其代价是否可被规避或抵消；禁止设计任何直接产出资源而无对应支出的机制（特别是碎片、属性点与精力）。
5. **效果撰写**：效果描述必须一句话写完，禁止附加解释或括号补充说明；禁止在效果中使用“你可以选择”之类无实际分支的措辞；严禁提供无人会选的选项；情报与计数类信息不得作为付费收益出售。
6. **命名**：命名须与该物件实际功能和世界观来源一致，禁止使用泛用直白名称；同一名称在全宇宙内唯一。
7. **叙事**：叙事文本中禁止使用破折号；禁止使用“不是……而是……”等先否定后肯定句式；战斗描写以博弈过程为主体，数值仅用于校验；道纹是概念而非固定动作；代价由规则直接扣除，不得描写为自残等具体动作。
8. **工作次序**：先道纹，再专属行动，再事件，最后法器；法器是通关奖励，必须在其他副本同样成立；不确定时提问；被否决的方案不得换名重提。
9. **测试默认**：用户说「测试」时，默认由 AI 通过 GameEngine 逐步点选手操并按战斗推演格式写战报；禁止把 TacticalAI 或批量通关当作默认测试；脚本仅用于测试机制是否生效（脚本轮回数据一律视为虚假数据——见推演铁律 6）；报告.md 口径按《文档管理规则》（当前有效状态在库内、历史归档 archive）。每次手操轮回结束后把道纹实战使用率等平衡观测写入《报告.md》；【死之传承】遗言必须经由 DM 审核确认无误后方可记入《死者之书.md》。

### 行文十诫

以下约束本项目任何描述：文档与回答（战报不含叙事，改编在外部进行）。

1. 实事求是：动笔前先查世界观设定与引擎事实，严禁杜撰。
2. 禁用“不是……是……”先否定后肯定的句式。
3. 禁止三词并列（如“高效、稳定、可拓展”）。
4. “此外”“然而”“值得注意的是”等高频词不得置于句首。
5. 句尾不得拔高，删去“彰显了”“反映了”“标志着”一类词。
6. 长短句混用，不得固定节奏。
7. 金句节制，一篇至多一两处。
8. 不得通篇堆砌“一定”“必然”“毫无疑问”。
9. 禁止“有人会说……但其实……”一类堵死反驳的写法。
10. 禁止假故事与假精确：事件与数字必须真实可查。

### 推演铁律

1. 每一次出手必须写明消耗或代价的具体数值，以及资源变化前后的实际数字。
2. 禁止在数值未耗尽时坐以待毙，任何角色在濒死时均须尝试其身份与资源允许的最优解。
3. 怪物在[战始]第一回合为白板状态，任何道纹须在其出手轮主动发动后方可生效。
4. 禁止发动未持有的道纹、未学会的法术、已进入冷却或已成为尘封的道纹。
5. 禁止跳过闪避判定，凡带[目标]的非必中判定，被选定方均须明确声明是否消耗速度闪避。
6. 脚本仅用于测试机制是否生效，凡是用脚本得到的轮回数据一律视为虚假数据；一切战斗测试与轮回推演必须由 AI 通过 GameEngine 逐步点选手操执行，按真实输入输出记录。真正做不到的情况下必须直接告诉用户并解释为什么，禁止胡编乱造和为了偷懒故意说做不到。
7. 胜负、命零、碎片奖励、晋级与封存等一切结算事实只能取自引擎真实返回值，一律由引擎事件驱动；引擎判负或判死时必须如实写入战报，AI 无权书写、改写或预写任何结局；禁止结论先行，不得先确定全胜叙事再补数值。
8. 战报禁止掺入叙事与文学描写，AI 不代写战报叙事；后台发生的事件不得遗漏，后台没有的事件不得新增；出手、道纹、X 值、消耗、伤害与资源前后数值必须与引擎输出严格一致；格挡跨回合保留（2026-09-10 起[回终]不再清空）、速度与法力仅战终复原等资源规则一律以引擎为准，禁止以通用直觉替代。
9. 怪物每回合的攻击出手与道纹发动必须按引擎列出的合法选项逐次结算、逐条书写，禁止复制同一段模板叙事充当多个回合的怪物行动，禁止叙事中出现持有关系不成立的效果。
10. 战报只保留后台数据（引擎逐次结算流水），叙事改编由用户在外部自行处理；战报先写入 报告.md，经用户核查确认后移入《故事文档.md》编号收录。
11. **死斗报告完整性（用户裁定 2026-08-26）**：任何涉及死斗（第8场PvP）的报告必须附完整过程——双方面板/先手/逐手行动（含守擂方行动日志）/每步战况/结果；**守擂零行动的死斗记录一律视为无效数据**（2026-08-26 已修复守擂木桩驱动与日志丢失双 bug，守擂现为通用预演决策）。死斗记录必须在批内原始捕获，不得依赖事后跨批重放（跨局持久状态漂移使逐位复现不可靠）。

## 战斗推演原子流水线与典型实战示例

### 七步原子时序切片流水线

```
【步骤 1：声明与付费】 行动方声明发动道纹/攻击，指定目标，扣除法力/支付代价
       ↓
【步骤 2：残韵插队窗口】双方可发动残韵（转换/反转/曲解）插队篡改当前道纹公式
       ↓
【步骤 3：预先响应窗口】检查「发动道纹前 / 受到伤害前」类法术（如：咎由自取、后发制人、先发制人）
       ↓
【步骤 4：防御声明窗口】目标声明是否消耗 1 点速度进行【闪避】（闪避成功则判定完全失效，跳过步骤5）
       ↓
【步骤 5：数值与效果落地】计算原始数值 → 结算格挡抵消 → 扣减实际生命 / 施加状态 / 产生回复
       ↓
【步骤 6：后置响应窗口】若实际扣减生命，检查「失去生命后」类法术（如：生生不息、以牙还牙、千刀万剐）
       ↓
【步骤 7：状态基准检查】检查存活、[命零]、癌变、凡庸、雕塑、崩解、撤退、救赎等全局状态
```

### 堆栈与残韵插队优先级

- 反应与插队动作压入动作堆栈顶层，后声明的先结算。
- 残韵作用于任意角色拥有的道纹时：该道纹永久变为变化后的道纹，施法者同时永久获得变化后的道纹；角色拥有的道纹保持唯一（重复则合并为1个）。
- 本次动作在结算前被残韵重写后，公式转为新道纹，代数 X 继承原发动的 X 数值，由原施法者继续执行。

### 循环法则与自驱动停机

- 允许法术终点结算（如自伤代价）再次满足该法术的启动条件从而进入下一轮循环。
- 每轮循环必须逐步完整推演法力扣减、效果生效与代价支付。
- 当法力耗尽、当前生命不足以支付代价、中间流程失效或目标死亡时，循环链立即自然中断。

### 典型实战场景示例

#### 场景 1：基础攻防（无法术、不闪避）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐2】（消耗 2 法力，基础伤害 4）
  [目标反应] B 选择不闪避
  [数值落地] B 受到 4 点伤害（生命 60→56，A 法力 20→18）
```

#### 场景 2：闪避交互（目标消耗速度闪避）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐2】（消耗 2 法力，基础伤害 4）
  [目标反应] B 声明消耗 1 点速度进行闪避（速度 8→7）
  [数值落地] 杀伐判定完全失效，B 未受伤害（生命维持 60，A 法力 20→18）
```

#### 场景 3：残韵插队逆转（永久改变目标道纹，施法者同时获得）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐2】（预定消耗 2 法力，对 B 造成 4 点伤害）
  [残韵插队] B 发动残韵【反转】插队：
    ├ A 拥有的道纹【杀伐】永久转化为【再生】（若 A 原本已持有【再生】，则唯一保留 1 个）
    ├ B 同时永久获得【再生】
    └ 本次动作被重写为【再生2】（A 消耗 2 法力，为目标 B [回复] 6 点生命，X 继承原值 2）
  [数值落地] A 消耗 2 法力（法力 20→18），A 失去【杀伐】并获得【再生】；B 永久获得【再生】；B 获得 [回复] 6 点生命（生命 40→46）
```

#### 场景 4：防御型反应法术（【后发制人】受到伤害前获得格挡）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐10】（消耗 10 法力，原始伤害 20）
  [预先响应] 【受到伤害前】B 触发法术【后发制人】（循环1）：
    └ B 对自身发动【庇护5】（消耗 5 法力）→ 获得 10 点格挡（格挡 0→10，B 法力 30→25）
  [目标反应] B 选择不闪避
  [数值落地] 杀伐造成 20 点伤害：10 点被格挡吸收（格挡归 0），穿透 10 点伤害
    └ B 实际失去 10 点生命（生命 60→50，A 法力 20→10）
```

#### 场景 5：交错反击型法术（【以牙还牙】受伤后先回血再反击）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐5】（消耗 5 法力，伤害 10）
  [目标反应] B 选择不闪避
  [数值落地] B 受到 10 点伤害（生命 60→50，A 法力 20→15）
  [后置响应] 【失去生命后】B 触发法术【以牙还牙】：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）→ [回复] 6 点生命（生命 50→56，B 法力 30→28）
    └ 步骤2：B 对 A 发动【杀伐4】（消耗 4 法力，B 法力 28→24）
        └ A 选择不闪避 → A 受到 8 点伤害（生命 60→52）
```

#### 场景 6：循环自伤法术（【千刀万剐】失去生命后自驱动循环至法力耗尽中断）
> 初始：A 生命 60/60；B 生命 35/60、法力 6/60（设定每轮循环取 X=2）。A 造成 1 点伤害使 B 生命 35→34 触发法术。

```markdown
出手1（A 造成 1 点伤害，B 生命 35→34）：

  【失去生命后】B 触发循环法术【千刀万剐】（第 1 轮循环，X=2）：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）
    │   └ B [回复] 6 点生命（生命 34→40，B 法力 6→4）
    ├ 步骤2：B 对 A 发动【血债2】（支付代价【流血2】）
    │   ├ B 支付流血 2 代价（B 生命 40→38）
    │   ├ A 选择不闪避
    │   └ A 受到 2 次 1 点伤害（共 2 点伤害，A 生命 60→58）
    └ 步骤3：B 因支付【流血2】再次失去生命 → 判定法力充足（剩余 4），触发第 2 轮循环。

  【失去生命后】B 触发循环法术【千刀万剐】（第 2 轮循环，X=2）：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）
    │   └ B [回复] 6 点生命（生命 38→44，B 法力 4→2）
    ├ 步骤2：B 对 A 发动【血债2】（支付代价【流血2】）
    │   ├ B 支付流血 2 代价（B 生命 44→42）
    │   ├ A 选择不闪避
    │   └ A 受到 2 次 1 点伤害（共 2 点伤害，A 生命 58→56）
    └ 步骤3：B 再次失去生命 → 判定法力充足（剩余 2），触发第 3 轮循环。

  【失去生命后】B 触发循环法术【千刀万剐】（第 3 轮循环，X=2）：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）
    │   └ B [回复] 6 点生命（生命 42→48，B 法力 2→0）
    ├ 步骤2：B 对 A 发动【血债2】（支付代价【流血2】）
    │   ├ B 支付流血 2 代价（B 生命 48→46）
    │   ├ A 选择不闪避
    │   └ A 受到 2 次 1 点伤害（共 2 点伤害，A 生命 56→54）
    └ 步骤3：B 再次失去生命 → 【中断检查】：B 当前法力为 0，无法支付【再生2】的 2 点法力，循环自然中断。

[最终本轮出手结算结果]：
  - A 生命：60 → 54（累计承受 6 点伤害）
  - B 生命：35 → 46（净回血 11 点）
  - B 法力：6 → 0（3 轮循环消耗完毕）
```

## 知识库维护

- **保留内容：** 现行工程约束、仍会影响后续实现的设计决定、当前有效的验证/操作经验、尚未解决或尚待裁定的事项。
- **删除内容：** 被替代的口径、废弃方案、受缺陷影响的统计、按日期堆叠的进度快照、已经落地并由代码和测试覆盖的修复过程。
- **更新时机：** 每次裁定、实现或验证改变现状时，在同一变更中更新本文件；事项解决后删除对应待办和修复叙述。
- **追溯方式：** 需要查看历史原因时使用 `git log`、`git show` 和相关提交，不在本文件复制历史流水账。
- **真实性要求：** 只记录能由当前代码、正文事实源、自动化测试或可复现命令验证的内容；无法复核的旧结论直接删除，不改写成肯定事实。

## 文档分工与事实源

- `README.md`：**只承载上手流程**——文档导航、五分钟上手、游戏流程（开局/第1–7场/最终死斗/阶级推进/自动触发列表）、战斗推演格式模板与推演铁律摘要。不承载规则条文、设计原则、世界观。
- `AI_EXPERIENCE.md#第四宇宙规则正文`：**通用规则条文的唯一事实源**——基础定义、核心规则、残韵作用、局外系统、怪物准则、代价、道纹体系、原始怪物道纹与转化道纹、特殊事件、通用事件池。`engine/events.py` 由此解析通用事件池，`engine/rule_sync.py` 由此解析道纹体系（切片锚点 `### 道纹体系` → `### 特殊事件`，改标题前先改解析器）。
- `故事文档.md`：世界观与底层因果法则设定正典，以及经认可的战报正典档案。
- `法术索引.md`：法术设计原则、三大法则（积木/循环/中断）、法术阶级规则、可学法术清单与自创法术标准语法的唯一完整说明。
- `死者之书.md`：可学法术数据的引擎事实源（`extract_spells_from_file` 读取此文件）与遗言事实源；法术效果正文的可读性权威说明见 `法术索引.md`。
- `物品索引.md`：遗物、消耗品和法器事实源。
- `副本索引.md` 及其链接文档：副本清单与副本规则；标记为未实现的草案不进入运行时。
- `engine/README.md`：精简架构和 API 参考。
- `AI_EXPERIENCE.md`：第四宇宙开发与维护规则（最高优先级）、设计原则、AI 角色扮演与协作规范、推演铁律与原子流水线、当前有效工程经验和限制；不承担世界观正文或实现历史归档职责。
- `报告.md`：**当前有效状态**统一文档（2026-08-24 起按 CF-4 统一口径；2026-08-26 增滚动规则）：只保留当前有效的结论、禁区清单与待办；**完整结论只记录最新一次任务**，下一任务出结论时清空上一任务的结论段（并入 `archive/report_history_*.md` 留痕）；历史报告整体归档 `archive/report_history_2026-08-24.md`。
- `archive/`：实验与变更流水档案（experiment_log.md）、已废止假设（hypotheses/）、手操战术库（tactics_handplay.md）；可检索，不常驻注入。
- 引擎可观察行为以当前代码和测试为准。正文、代码和测试冲突时不得擅自选择其一，必须按准则第 11 条停止并请用户裁定。

## 文档管理规则

> 统一口径（CF-4，2026-08-24）：**当前有效状态在库内，历史内容进 archive。** 不再同时执行"只留最新"与"按日期无限分节"两套规则。

1. 非常用过程信息默认进 `报告.md` 或对应 archive 档案，禁止为单次任务/单个 Bug/单次测试单独新建文档。
2. `报告.md` 只保留当前有效状态（结论、禁区清单、待办）；**完整结论滚动（2026-08-26 用户裁定）：只记录最新一次任务的完整结论，下一任务出结论时清空上一任务的结论段（并入 `archive/report_history_*.md` 留痕），禁区清单与待办滚动维护**；同日多次更新合并，每天最多更新一次。
3. 已失去时效的过程记录（旧测试轮、旧修复叙述、旧批次报告）整体迁入 `archive/` 对应档案，不在 `报告.md` 无限堆叠。
4. 新建文档前必须先判断信息能否进 `报告.md` 或已有 archive 档案；可以放进去就不得新建。正式规则文档、基础定义文档、README 不受此限。
5. 历史细节由 Git 历史与 archive 档案共同保存；任何归档必须保留批次编号、结论、数据文件引用、commit 与可追溯证据。
6. 被后续实验推翻的结论：从生产文档移除并按 D 类流程进 `archive/hypotheses/`（留原文、来源、被替代原因），不得与当前结论并列而不注明状态。

## 当前执行与验证口径

### 实现与问题报告

- AI 负责决策，`GameEngine` 是数值计算、状态变化和随机结果的事实源；不得脱离引擎伪造结算。
- 无法由程序判定的许愿、逃跑、真理眼等事项必须走 `Interrupt`，等待 DM 裁定。
- 描述问题统一使用三段：**问题**（错误现象与原因）、**方法**（修改路径与方式）、**结果**（修改后的可观察行为）。
- 修复完成后，问题叙述从本文件删除；长期有效的防错原则可抽象保留，但不得继续陈列已经修好的 bug。

### 测试含义

- 用户说"测试"时，默认含义是 AI 通过 `GameEngine` 逐步点选手操，并按战斗推演格式写入 `报告.md`；脚本仅用于测试机制是否生效（数据效力见推演铁律 6）。
- `TacticalAI`、`sim/pick_best_report.py` 等批量模拟是平衡工具，不得冒充手操测试；`pytest` 与引擎套件是回归验证，不等同于真实对局。
- 常用回归命令如下；只有实际运行并看到结果后，才能报告通过数量：

```bash
python -m pytest tests -q
python tests/test_engine.py
python sim/audit_monsters.py
```

### 状态机、API 迁移与回归防错

1. **规则改动先建追踪矩阵**：正文事实源→生产入口→状态字段→正常/边界/非法测试，缺一列不得宣称完成。
2. **阶段测试使用合法前置状态**；单测可直接构造 phase 但必须在注释声明绕过原因。
3. **API 变更视为全仓迁移**：先清点全部调用方，生产实现与调用方同变更更新。
4. **决策层与计算层分离**：计算层只列合法选项并验证提交；怪物阶段固定 prepare→提交→resolve。
5. **两阶段接口绑定快照**：resolve 只认对应 prepare 的选项快照做白名单。
6. **可选参数必须显式**：禁止"取第一项/最大值/自动闪避"或非法回退默认项。
7. **失败行动必须原子**：先完成全部校验再扣资源；失败后状态与随机历史不得改变。
8. **正式随机源唯一**：全部走 `DiceEngine` 并留可复现记录，禁止模块级 random 与注入结果。
9. **事件只能结算当前事件**：先校验条件、支付能力和显式目标，再产生效果。
10. **回归按层执行并设边界**：先契约测试再受影响文件最后全量；被中止的命令只能报告"未完成"。
11. **禁止为绿灯削弱新裁定**：旧测试与新裁定冲突时，不得加测试专用旁路或保留新旧双入口。
12. **阶段必须形成单向状态机**：各阶段唯一合法入口与一次性转换，重复调用不得再获资源。
13. **可用行动必须可直接执行**：`get_available_actions` 的输出必须能原样交给 `execute_action`。
14. **效果系统按触发矩阵验收**：授予、参数校验、触发、结算、清理五节点；未知效果先拒绝。
15. **极性不能替代生命周期**：清理时点由回合/战斗/轮回/永久/代价决定；面板增减登记来源与可逆 delta。
16. **触发听字段，不听途径**：效果挂在该字段唯一变更入口，禁止按物品名互判；特殊机制绕过统一入口须按开发规则 3 加注释。

## 当前有效的工程约束

- 一阶副本出怪数 `max(1, 战斗场数-3)`，七场序列 `1/1/1/1/2/3/4`；事实实现 `engine/monsters.py::compute_draw_count` 与 `_action_battle_start`。
- 一阶怪物面板属性点 60，审计公式 `ceil(血限/6) + 攻击力 + 攻击次数²`；改面板后必须跑 `python sim/audit_monsters.py`。（2026-09-11 扭曲40试点已 revert：AI 硬打喊难在先，难度不动，见报告。）
- 怪物困境信号≥1 触发强制二选一：进化借轮回者当前持有且自身未持有的纹（每场逃跑/进化限一次）；可选项只能由引擎列出。
- 异变 50 层触发【崩解】命零，累积统一走 `Entity.add_mutation`；调用方拿到 `collapsed=True` 必须交回 `_on_entity_death(..., ctx=_collapse_context(...))`。
- 【活血】只看本回合是否实际掉过 HP，不区分来源（DM裁定 2026-08-19）。
- 死斗按阶级封存（先来后到队列），胜者进阶封存不得再与同阶死斗。
- 手操轮回结束后，把道纹实战使用率与胜率等平衡观测写入《报告.md》（批量实验批次写入 data/experiments/）。
- 手操战斗必查残韵：`use_resonance` 不耗出手，可改写敌人未发动道纹（永久）并白嫖新纹；
  战始别把反转烧光（飞行怪留反转→坠落，或转换→滑翔自己飞）；剥光原始纹触发救赎可得半血员工（待命+工资+叛变，不再是免费朋友）。
  （2026-09-11 教训：两局零残韵连败，纠错后 3连胜。）
- 手操铁律（2026-09-11 B60r3 实测）：①蓝即拳=攻力取当前法力，花蓝普攻归零，
  法力池战斗内不回，奶/盾道纹不到斩杀线别碰；②速归 0=凡庸雕塑死（与血无关），
  闪避与攻次同池，保速即保攻，能吃别闪；③救赎线=⌈血限10%⌉， strip 原始纹＋压线接纳
  比杀满省 10%（还白嫖朋友）。
- 手操铁律②（2026-09-11 B60r4）：有朋友必下护卫（`command_ally 护卫 9`，无消耗不占出手，
  先吃头阵朋友）；护卫 9 只盖 9 击，变形 7 连击会打穿溢出，R3 前补命令或轮换；
  盟友是消耗品（死完救赎再招，员工注意工资与叛变），残血别心疼，轮回者无伤才是赢。
- 【死之传承】遗言必须经 DM 审核确认后方可记入《死者之书.md》；回音长廊涉及页面选择/写入同样必须停下来询问。
- 负岳碑与断尾求生使用"预声明后命中条件消费"接口；当前 API 不支持伤害结算中途插入交互。
- 阶段门禁以 `GameState.phase` 为准；道纹局外唯一例外是【消灾】。
- 【发现】/雇佣转化先由 `DiceEngine` 列候选再显式选择；【探索】免费发现1件/30碎片发现2件，事件队首制，候选待决时锁定其它推进。
- 局外【休整】用稳定引用分配；【修行】每点法力属性+2 法限；储能电池使用后立即 +12 法力。
- 【还债】员工走独立债务轨道（立即参战、无工资、一次付清离队）；裁定D：无力支付部分记为负债，触发阈值负债≥20。
- 【爆裂】按持有者 `[敌回终]` 递减；最终死斗仅四维完全相同时随机首手并逐回交换，否则属性优先，一方出手耗尽后另一方继续。
- **每轮工作结束必须提交并推送**：现行口径=《实验灾备协议》（2026-08-24 DM强制执行，见文末）。本条 2026-08-19 旧版（含过期分支名）已按 CF-1 处置，原文见 `archive/hypotheses/cf1_old_push_clause.md`。

## 现行手操经验

### 战斗

- **血债与格挡（核心规则纠正）**：本系统绝无"真实伤害/穿透伤害"。【血债X】每一次 1 点伤害均为普通伤害，必须优先被目标【格挡】完全吸收，格挡归零后才扣生命。
- **怪物代价类道纹准则（DM裁定）**：怪物无法力但发动代价类道纹必须真实支付；【冷却X】道纹（固执/束缚/迟滞）发动后进入冷却，本场及后续X场不得再发动，严禁单场复读；【流血X】扣自身生命（不足不能发动）、【衰老X】扣血限、【疲惫X】扣速度、【异变5X】累加异变。
- **斩伐闭环与开局流程**：先【发现】遗物（3 选 1）再从杀伐闭环【发现】候选显式选初始道纹（杀伐非默认起手，`format_setup_discovery` 缺候选会拒绝）；杀伐公式=消耗X、**5X**伤害（2026-09-10 起，原 2X）；【波及】属 11 节点杀伐闭环（2026-08-21 冲击改名波及，缓慢/慈悲/切割已删）。
- **关键机制口径**：残韵永久改写任意角色道纹且施法者同获（人类仍不得原始怪物道纹）；【第一杯】免疫癌变（钱袋已并入删除），癌变阈值=本场累计回复2×血限；【净化X】消耗5X使目标异变-X；救赎在怪物当前生命≤血限10%且无七种原始怪物道纹时触发；原始怪物道纹仅首次发动支付异变5X；癌变分流按角色类型结算（非怪物直接命零，怪物被吸收且全部档【休整】+8 可叠加）。
- **强化怪池占比报告纪律**（2026-08-24 回归最小恢复）：现行一阶池强化怪=罪孽2/12、扭曲2/12、龙心1/12；报告必须同时给静态池占比与实际样本（如单次2/6），不得以单次小样本代替总体出场率。
- **许愿（DM裁定）**：愿望没有固定可行范围；以能实现愿望但最符合扭曲本质的方式实现；代价与扭曲方式由愿望决定、不预先公开，由 DM 裁定。

## 当前限制与待裁定入口

- 二阶及以上副本仍未接入运行时；具体范围以 `副本索引.md` 为准。
- **【爆裂】反噬致死时 `_death_ctx.source` 的标注口径待裁定。** 现状记的是链根（如「杀伐」）而非直接机制（「爆裂」），与普通伤害致死「source = 链根」的既有约定一致，因果链本身完整（death → 反噬 hp_loss(subtype=`baolie_reflect`) → damage）。若裁定应改标为「爆裂」，需同时处理 `_apply_hostile_damage_inner` 中那段目前不可达的显式爆裂死亡 ctx 分支。
- **避风铃 / 守夜灯存在「combat.py 旧逻辑 + Hook」两套实现。** 引擎唯一分发方是 `CombatEngine._note_dodge` / `_grant_shouyedeng`；`CombatHookManager.apply_dodge()` 与 `apply_round_start()` 在 combat.py 中没有任何调用点，只有单元测试直接调用。两侧校验条件不同（封印／存活／轮回者／不朽之躯），合并任一方向都会改规则。在正式迁移裁定前，**禁止在 combat.py 接线这两个分发口**。
- 最终死斗的被动终音法器、初拥与龙族遗物已按双方所属轮回者分别结算；但黑金名片、罪业金库、教父左轮和真龙之心换取龙性等主动 action 仍只操作当前 `GameEngine.state` 的玩家侧资源。若要求封存对手主动使用这些能力，需要单独设计双方 action 接口。
- 当前平衡问题、赌命文本歧义、嫁祸目标策略和模拟数据适用边界等观测统一记录在 `报告.md`；本文件不复制易过期数值。


## AI 决策：完整后果优先原则（2026-08-19）

### 核心思想
不要把动作理解为"使用 X → 获得 Y"，而是：
"使用 X → 完整效果链 → 状态变化 → 资源变化 → 触发器 → 后续风险 → 最终局面"

**ActionPreview 是事实来源**——AI 不复制引擎规则（爆裂/癌变/残骸异变等），
决策层只负责理解预演 diff 中的数值变化、评估风险、比较收益。

### 风险口径（与 `engine/ai_preview.py::risk_classify` 同一套分级，数值一致）
LETHAL（完整效果链导致命零）与 CRITICAL（推入明确危险阈值：异变距崩解线 ≤5（即 ≥45），或 HP ≤ 血限10% 且动作继续扣血——生产规则 HP≤10%，DM 裁定 2026-08-24）动作一律拒绝，除非完整链确认能直接安全结束战斗；HIGH 降优先级、有安全替代选安全；MEDIUM 留意后续；LOW/SAFE 正常。
AI 必须观察所有累积状态（异变距 50 崩解的余量、HP/法/速归零缓冲、道纹层数与持续回合），禁止仅看局部收益的短视操作、禁止接近阈值仍为小收益堆叠；优先级：死亡 ≫ 丢资源 ≫ 当前回合收益下降 ≫ 普通收益差异。
允许冒险（赌一把/低血强杀/弃防换伤），但必须先经 ActionPreview 完整后果评估——"我知道会受伤但能打死"而非"我不知道会死"。

## 知识库更新协议（版本变更后必做，防"死板打法"）

AI 的"知识"分三处，版本变更（增删道纹/改公式/改机制）后必须同步，否则 AI 按旧知识死板出牌：

1. **`engine/ai_tactics.py`（实时决策器，2026-08-26 起无固定战术表）**：TACTICAL_ROLES / HIGH_VALUE_ENEMY_DAOWEN / STRATEGIES 固定顺序已删除（DM 裁决：防公式化打法）。战斗 AI 通过 ActionPreview 逐候选预演完整效果链，按"当前局势 + 角色性格（personality_traits）+ 可见信息"实时打分决策；新增/修改道纹只影响引擎 `calculate_*`，AI 侧零维护。
2. **`data/build_knowledge.json`**（自学习 build 知识）：版本变更后必须 `--reset` 重学（`PYTHONPATH=. python -m sim.build_learner --reset --generations 60 --runs 10 --random-seeds`），lessons 由 `sim/reapply_lessons_after_reset.py` 幂等重放（含基线合并与同头去重）。2026-08-26 防公式化清理：best/best_confirmed/trials/pair_scores 与打法结论类 lessons 已删，构筑候选池改为从 `DaoWenEngine._registry` 派生（非怪物原始道纹）。
3. **`sim/monster_targets.py::MONSTER_DAOWEN_*`**（怪物道纹优先级分组，`pick_monster_daowen_option`）：新道纹只改这里唯一入口，表外按机制组兜底。

## 生产配置摘要（2026-08-24 自 KB lessons 沉淀）

- **构筑学习器**：75% 确认精英回注 + 25% UCB 探索（双种子同序最优 2.62>2.02>1.78）；`best_confirmed` 须经 ≥2 次评估均值确认，单次 fitness 分不作决策依据。
- **深挖复评**：每 15 代对当前 top3 各追加 2 评——确认引擎与抗幻影稳定器；精英回注不配深挖=跨种子赌博。d0 绝育新知、d5 无增益减产。
- **新知识晋升门**：发现 + 独立随机种子复测 + 结构非近拷贝 + **普通个体迁移复现**，缺任一即个体性策略，不入库、不作决策依据。
- **库成熟期判据**：均分不再证明"在学习"（冻结≈活库），看产量线（新构筑率/候选管线/迁移门）；拓扑已结晶（封印+杀伐星形族），壳外未见更强家族。
- **死斗不通约**：PVE 均分不能代理死斗能力（top8 池 9.6–11.4%、库首 5.1%）；死斗=1–2 回合先手穿透+超时卫冕结构，需独立学习器（死斗专项为下阶段候选）。
- **环境层命门（十九批归因）**：终局悬崖 b4 4.4%→b5 24.3%→b6/b7 ≈56–57%；封印系异变经济（8X/次·阈50·CRITICAL45）在 b6+ 同时钳制输出与【残骸】救治；消耗品时机经影子实验证≈0、死法 99% 为敌方 hp_zero。

## 实验与变更档案

- `archive/experiment_log.md`：批次三~十九实验流水与 2026-08-21/08-22 规则变更全文（含批次×数据×commit 对照表）。
- `archive/hypotheses/`：已废止假设与陈旧值（波及过滤旧语义 D-01、CF-1 旧 push 条款），留原文+被替代原因。
- `archive/tactics_handplay.md`：手操战术库（组合矩阵细节/分级承伤/低X逼闪等迁出内容）。
- `archive/report_history_2026-08-24.md`：《报告.md》的历史报告部分。
- **知识库增量策略（DM 2026-08-24 第五阶段定案）**：525 行瘦身基线+回归最小恢复（2026-08-24 共2行）成立；此后采用**失败驱动增量**——仅在出现明确失败案例时才允许新增知识条目，禁止持续堆积；历史知识继续留在 archive/ 可检索。
- `data/experiments/`：批次实验数据与恢复清单（*MANIFEST.json）。
- KB lessons（`data/build_knowledge.json`）：技巧/心得/陷阱级经验，须带同种子配对证据，治理走 `sim/reapply_lessons_after_reset.py`（禁止手改 JSON 裸删）。

## 实验灾备协议（2026-08-24 DM强制执行，全批次适用）
1. **沙箱不是持久化介质**：/tmp、临时脚本/JSON/日志、未提交工作区、未推送commit 一律视为临时空间；重要结果不得只存在于上述位置。
2. **每批次分段持久化**：实验→数据归档→文档归档→Git commit→GitHub push→远端验证，缺一不可。
3. **数据先落仓内再提交**：/tmp/result.json 生成后立即复制到 data/experiments/<batch>_<name>.json，确认可读才继续。
4. **每批完成立即commit**：一个实验批次=至少一个可恢复commit；commit后记录 SHA/批次/数据文件/代码文件/测试结果/当前KB-gen/当前生产状态。
5. **未push不算完成**：区分 local commit 与 remote durable commit；仅GitHub远端验证成功才算已持久化。
6. **push前先同步**：git fetch origin → git status → git log --oneline --decorate -n 10；遇 non-fast-forward 禁止直接 force push，优先 merge（确需线性时 pull --rebase）。
7. **push后反向验证**：重新 fetch，git rev-parse HEAD 与远端一致，且 git log 确认本批commit位于远端历史中。
8. **每批建立恢复清单** data/experiments/<batch>_MANIFEST.json：batch/commit/parent_commit/kb_generation/kb_size/experiment_files/code_files/test_count/test_status/git_remote/remote_commit/timestamp_utc/timestamp_local——目标：沙箱下一秒消失也能仅凭GitHub恢复这一批。
9. **push失败立即停止继续实验**（token失效/认证失败/网络失败/被远端拒绝）：先解决持久化；实在无法push时至少整理成一个明确本地commit并在报告中标记 **NOT REMOTE-BACKED-UP**。
10. **每次开工先做重建检测**：git status / rev-parse HEAD / 远端rev / log -n 20；检查实验档案、KB、AI_EXPERIENCE、最近批次commit、工作区干净度；若 /tmp 清空+mtime全部一致+最近commit不在 → 判定可能发生沙箱重建，禁止直接继续，先走恢复流程。
11. **恢复流程**：查GitHub远端→找最后完整commit→clone/fetch→恢复代码/数据/KB/文档→跑完整测试→验证commit链→确认工作区后才允许继续。
12. **/tmp 只作中间计算空间**，不作唯一结果存储。
13. **报告三种状态**：LOCAL ONLY（仅本地commit）/ REMOTE BACKED（已push验证存在）/ REMOTE VERIFIED（重新fetch确认commit+数据+报告全部可恢复）——只有第三种才算本批正式结题。
14. **禁止改写远端历史**：禁止 git push --force（除非明确确认远端历史本身错误）；分叉时 merge 优先。
**最终完成判定**：实验数据/文档/代码均在仓内 + commit已生成 + 已push + HEAD==远端 + 新沙箱可经GitHub恢复 + 测试通过，才能报告"本批完成"，否则必须报告"本批未完成持久化"。（教训源头：2026-08-23 十七批提交6d9535a沙箱重建丢失git ref、十九批提交342eb38重建丢失后于次日以563466a重落，两次均被灾备要求追认为事故。）

## 工作方法红线：禁止空转（2026-08-31 DM 两次口头纠正后固化）

**失败案例**（符合失败驱动增量条件）：DM 于 2026-08-31 连续两次指出「怎么又 letsgo 循环？干什么？」「怎么不是繁忙就是空转」。随后我在生成回复时陷入「Let me write / Let me go / Emitting」重复模式数千次，无一字新增信息，被 DM 当场截获。**空转不只是多跑几次命令，它会在我的输出里直接漏出来。**

**三种具体违规形态（均已被抓现行）**：
1. **用工具调用代替思考**：为读一个函数拆成三次往返（grep 找行号 → sed 读一段 → 再 grep 找外层函数）。一次 `sed -n 'a,bp'` 即可。
2. **证据够了还不停手**：探针已经给出答案，还要再跑一次"确认一下"。判据：若下一次调用不产生新信息，就是空转。
3. **输出层面重复自己**：同一条消息里第二次解释同一个点，就该收尾。

**强制约束**：
- 要读函数就 `sed -n` 整段直读，禁止"先 grep 行号再读"的两步走。
- 探针**只跑一次**，跑完立即写结论。
- 判定准则：如果下一次工具调用只是「再确认一下」——**不做**。
- 发现自己在同一条消息里第二次解释同一件事 → 立即收束输出。
- DM 若看到我连续两次以上调用只为「定位行号」，有权直接打断。

**配套判据（与「读代码，不要靠跑 seed 循环下结论」同源）**：seed 循环证明的是确定性不是正确性；多次 grep 证明的是我没读完，不是我读对了。

---

## 死锁判卫冕 = 模拟器兜底，不是规则（2026-08-31 溯源确认）

**DM 质疑**：「死锁是什么东西？我可不记得我有规则提到过这个东西。」——质疑成立。

**三条证据**：
1. 规则文档中无此概念。全库 md 仅两处提及，且均为**问题记录**而非规则：`data/experiments/personality_diversity_report_2026-08-26-n400.md:98`（记作"已知问题：双乌龟死锁，挑战者 586 次空转，30s 墙钟超时判擂主卫冕"）与 `报告.md`（我方复测报告）。
2. 实现位于 `sim/duel_pvp.py:540-632`（`STALL_GUARD_STEPS` + `hp_frozen_rounds`），**在 sim/ 不在 engine/**。engine 中三处「死锁」字样全为代码注释，语义无关（0 费行动、待结算事件门禁）。
3. 出生证明 `tests/test_duel_pvp_guards.py:5`：「空转 598 步 → 30s 超时判卫冕。同时补：死锁防护」。

**真实来历**：程序跑不完被 30 秒墙钟超时强杀，为免空转 598 步而加的**提前判死锁**兜底，后被写成了**胜负判定**。

**由此确认的层级冲突**：

| 机制 | 层级 | 性质 |
|---|---|---|
| 凡庸 | engine | **游戏规则**：连续 5 回合未出手／未使敌对角色生命减少 → 凭空全身炸裂 |
| 死锁判卫冕 | sim | **模拟器防卡死兜底**：连续若干步零净变化 → 判擂主卫冕 |

实测（封印控制_23 vs 杀伐法攻_45，seed 1）：凡庸 `no_damage_rounds` 计到 **4**，阈值 `>= 5`，第 5 回合 tick 未发生——**兜底早一回合收场，规则没炸成**。

**定性**：程序的防卡死逻辑盖过了规则内的反乌龟机制，并顺带决定了胜负。**模拟器兜底不得成为胜负判定**，凡庸应优先。此项为待修缺陷。

**关联病根（已修，2026-08-31）**：该死锁的上游是**探针口径不同源**——`engine/ai_tactics.py::_digest_diff`
的 `kind` 判定用事件流，而 `dmg` 用被格挡吸收后的**面板净位移**；对方庇护盾使 X=1 伤害归零 ⇒
`probe["dmg"]=0` ⇒ `_daowen_candidates` 收割档条件 `dmg>0` 不成立 ⇒ 只生成 X=1 一档。
现已改读事件流 `damage_applied.raw_damage`（未扣盾）。**留存口径（防回归）**：探针的一切
效果量都必须与判定同源取自事件流；面板净位移会被格挡吸收遮蔽，不得当伤害口径。
由 `tests/test_ai_tactics.py::test_probe_dmg_not_masked_by_enemy_shield` 钉住；
死斗侧 A/B（30 组有序配对 × seed=1）确认此项只改「AI 是否出手」，不改僵持结局。

## 知识迁移：2026-08-31 报告清理（原 报告.md 第五/六/七节及待办④并入）

### 先发制人跳过闪避（已修，DM 选 1）
- 根因：README:161「凡带 [目标] 道纹，目标被选定时均可消耗 1 点速度闪避」；README:167/423「禁止跳过闪避判定」。先发制人的杀伐属 [目标] 非必中。
- 修法：`_fire_auto_reaction` 曾硬编码 `dodge: False`；改为 `_auto_reaction_dodge_decision` + `choose_dodge(entity=)` + `_spend_dodge_speed` + 每回合 `_dodge_budget_reset()`（`max_dodges=2`）。**先发制人数值未动**。
- 验证：封印控制_21 vs 速战速决_5，seed 1/3/7/11 —— 修复前全 `rounds=1`，修复后全 `rounds=2`（癌变收场）。
- 坑：自动路径的必中**只读不消耗**（`bizhong_remaining > 0` → 不闪避、不 `consume_bizhong`）；一旦消耗就会双重扣减 `resolve_attack:1504` 拥有的计数。

### 癌变（规则内击杀路径，不是 bug）
- 阈值 = `ceil(血限 × 2)`（血限 36 ⇒ 72）。代码：`engine/combat.py` 的 `cancer_threshold_of` / `check_cancer` / `_cancer_character`。
- **「治疗对手」是规则内击杀路径**，是血债/自残/再生这套血经济循环的自然天花板。
- `_on_entity_death` 曾把具名死因静默归为 `hp_zero`（吞掉癌变与崩解），已由 `NAMED_DEATH_MECHANICS` 修正。**今后再加特殊死亡机制必须登记进该映射。**

### 141.33 那手治疗杀 = 真发现，非碰巧（DM 已结案）
- `ai_tactics.py:524` 是**惩罚**敌方回血的（`-1.2 * enemy_heal`），该分数仍能存活，说明确由预演差异驱动产生。
- 预览 diff：`hp_before 32 → hp_after 0`，`heal_applied(heal_amount 108, actual_heal 4, overheal 104)`，`entity_died`。分数重建 `100 + 44.8 − 4.32 + 性格 ≈ 141.33`。
- DM 原话：「在没有任何前提的情况下学会了毒奶流，这就是我要的自主学习能力」。
- ⇒ **禁止**为毒奶流加任何道纹名/机制名特判。设计源头见「AI 不写死战术规则」条目。

### 死因必须写明（DM 2026-08-31）
- 起因：一次死因被折进模糊默认值，DM 要求「把所有死因都标明，比如说刚才那个情况 XX 因癌变而命零」。
- 实现：`sim/duel_pvp.py::death_attribution_note` 先走具名死因分支，再走通用分支；通用分支经 `_DEATH_CAUSE_LABELS` 译名（`mediocrity→凡庸`、`collapse→崩解`、`cancer/proliferation→癌变`）。
- **铁律：未登记的死因原文照显示，绝不静默吞掉。**（曾把内部英文名 `mediocrity` 直接漏进判词。）
- 已核实的死因必须能在事件流、日志、死斗归因三处都露出来，不得折进默认值。

### 凡庸优先于死锁兜底（2026-08-31 DM 裁定，已实施）
- 凡庸阈值提为常量 `engine/combat.py::MEDIOCRITY_ROUNDS = 5`；`sim/duel_pvp.py` 导入并令 `DEADLOCK_MIN_ROUNDS = MEDIOCRITY_ROUNDS + 1`，**两处不再各写各的**。
- 死锁命中与墙钟超时均返回 `winner=None / error=True / timeout=True`，**只报错不判胜负**。
- 复测（30 组有序配对 × seed=1）：凡庸 18/30、崩解 5、癌变 5、挑战者击杀守擂 2、**死锁 0/30**（改前：死锁 15/30、凡庸 3）。均回合数 4.6（改前 3.3）。

### `render_line` 默认用全局 `random`（未改，仅提示）
- `sim/duel_dialogue.py:139`：`rng = rng or _r`（`import random as _r`）。
- 同一进程内连跑两次死斗，**既有对白会漂移**（判定与动作不受影响）。做 A/B 复现必须先 `random.seed()` 重置，或显式传入 `rng=`。

### 战场公开频道（对白）契约（由 `tests/test_dialogue_channel.py` 26 条钉住）
- DM 三条裁定：① **台词可真可假，引擎绝不标记真伪**（原话「你都标出来了，还猜什么」）；② ~~完全不碰 AI~~ **已被反转** —— 「语言虽然没有实质作用，但有时候让敌人忌惮就是最大的作用」；③ **时机自由**，出手前说、出手后说都行。
- 姿态标签一律用**中性修辞**（施压/示弱/试探/夸口/随口），不得叫「虚张/真话/假话」——那等于替读者判了真伪。
- **信不信由听者性格定**；判定层不得偷看说话方真实 hp/法力/道纹（由 `test_belief_ignores_speaker_true_state` 钉住）。
- 台词**零数值效果**；独立随机源 `_DIALOGUE_RNG`，绝不消耗全局 `random` / 引擎 RNG。
- 实测性格表：同一句「我法力见底了」→ 轻信 +0.83 / 多疑 −0.42 / 冒险 +0.46 / 求稳 −0.17。`DIALOGUE_BIAS_CAP = 10.0`。
- 台词素材取自说话方**此刻真实持有**的道纹/血量/法力，但**是否兑现不作保证**（示弱可以是真弹尽粮绝，也可以是满法力装穷）。

### 《死者之书》遗言：此前只写不读（2026-08-31 补 `read_death_book`）
- 事实源是 `死者之书.md`（`engine/models.py:712` 注明 `death_book_legacies` 只是启动/审核后装回的缓存）；写入只有一条路：死之传承 → `_commit_death_ruling` → `DeathBookStore.append`（`engine/death_book.py`，**单句 ≤20 字**，DM裁定 2026-08-31 废止三段式）。
- **改前没有任何读取路径**：`death_book_legacies` 只被 `_serialize_full_character`（`engine/api.py:4122`）序列化、被 `_commit_death_ruling`（`:5080`）计数；`engine/ai_tactics.py`、`engine/ai_preview.py`、`sim/build_learner.py`、`sim/duel_pvp.py` 全都不读——轮回者与 AI 一条遗言都看不到，「死之传承」对模拟侧等于空转（书里早就躺着「纯叠盾五回合未扣敌血凡庸命零」这条教训，没人读得到）。
- 现在：`read_death_book` 动作（`engine/api.py::_action_read_death_book`）纯查询返回 `legacies/total_legacies/wisdom`，**不消耗精力、不掷骰、不改任何数值**；`sim/build_learner.py::_play`（新轮回者诞生处）与 `sim/duel_pvp.py::run_duel_pvp`（开战前）各读一次，死斗 log 里出现 `[死者之书] …` 行。
- **实测：读到 ≠ 会照做。** 补完读取路径后重跑擂台车轮战 11 局（seed=1），逐局胜方/回合数/死因与改前**完全一致**（含 速战速决_5 满血 36/36 把自己奶到癌变那一局）。原因：`engine/ai_tactics.py` 是打分式 AI，不吃文本。要让遗言真的改变行为，必须把三条自爆时钟接进候选打分（报告 待办 ④）。
- 书内新增一页遗言（走 `validate_legacy`）：「某人·罪孽都市·留训｜**不伤害他人，一味治疗，异变缠身都会死亡哦**」（正好 20 字）。
- **DM裁定 2026-08-31：遗言改单句**——一句话、≤20 字、除此之外没有其他限制；原「触发点／岔路／代价预算」三段式废止。
  写入端只认 `{"text": ...}`（旧三段式字段一律报错并说明裁定），**读取端**保留兼容：
  `parse_legacies` 把旧书页按第一句折叠载入，重写后自动变成单句格式（`tests/test_death_book.py` 22 例钉住）。

### 自爆的真正病根：AI 把回复打给对手（2026-08-31 实测）
- **12 局归因审计：838 点回复里 837 点落在对手身上，落在自己身上只有 1 点**
  （`python3 sim/self_preserve_experiment.py audit --pairs 12`，约 40 秒）。
  双方轮流把对手奶到癌变线（癌变），同时自己的伤害牌打向自己 → 五回合未使敌掉血（凡庸）。
- 机制在 `engine/ai_tactics.py::_probe`：它对 敌方/自身 两个目标各预演一次，
  按「效果方向分」`2.0*dmg + shield + heal + …` 择优并**整场缓存**。满血时自我回复的
  预演收益为 0，与打向敌人的空效果打平，先试的敌方目标胜出 →
  `再生` 被归纳成 `kind=tactician`、目标=对手（`_daowen_candidates` 的 else 分支）；
  `杀伐` 在挑战者手里被归纳成 `kind=shield`、目标=自己。
- 附带盲区：`engine/ai_preview.py::_diff` 的 `diff["player"]` 恒取 `state.player`，
  死斗里守擂侧 TacticalAI 的 actor 是敌方实体，其打分读到的是**挑战者的面板**
  （`_score_candidate` 全程按对手的血/法力/回复算分）。
- 自保时钟实验（`LJ_SELF_PRESERVE=1`，默认关闭）：把癌变/崩解/凡庸三条线接进候选打分，
  132 局前后对照 = 挑战 49→51、守擂 83→81、死因分布几乎不动（凡庸 83→81，崩解/癌变不变），
  自爆仍 132/132，抽样 6 局 `自保否决` 触发 0 次。**结论：不修目标归属，打分层怎么调都拦不住。**

## 规则变更：2026-09-10 DM 裁定批次（攻次攻力换算 / 出手固定2次 / 属性点 / 点金 / 格挡）

**攻次与攻力不再是独立面板（仅轮回者）**：`[攻击次数] = 当前速度`、`[攻击力] = 当前法力`，
经 `Entity.effective_attack_count()/effective_attack_power()` 读取；[朋友]/[员工]与怪物仍读自身面板。
直接后果：**法力归零 → 攻力 0 → 普攻 0 伤**；当前速度与当前法力同时归 0 才触发【雕塑】
（DM裁定：触发条件提升为「任一角色攻次与攻力都为0」，且判定读**当前值**而非上限）。
因此发动任何法力道纹都会**永久削弱本场剩余时间的普攻**，衰减由机会成本驱动，不是计时器。

**出手次数固定 2 次（轮回者）**，不再 `ceil(速限/3)`；[朋友]/[员工] = `ceil(攻次/3)`，怪物 = `ceil(速限/3)`。
`ai_tactics.remaining_actions()` 必须读 `action_count`，沿用旧公式会让 AI 按错口径分法力预算。

**属性点**：`2属性点 = 1[速限] = 1[法限]`、`1属性点 = 6[血限]`，25 点总预算不变；攻次/攻力池撤销。
属性点**可存储、随时兑换**（`state.attribute_points`，action `redeem_attribute_points`，仅战前）——
修行档位发的是整数点，攒够 2 点即可兑 1 速限或 1 法限，奇数余点留池。战内兑换被拒（等于白送法力池）。

**道纹数值**：法力改一池制后约束从「速率」变成「预算」，单次放大不再等于连续碾压。
- 【杀伐】`消耗X → 5X 伤害`（原 2X）。
- 【洗劫】改名【点金】并改制：`消耗8X 法力 → 获得X真碎片`，与伤害彻底脱钩，无目标、不挂状态
  （刻意不返回 `duration` 键：通用状态块以 `"duration" in calc` 为前提，带上就会凭空长出状态）。
  **状态【洗劫】与「造成伤害夺等量碎片」机制保留**，只剩【帮派令】[战始]发放（事件收益禁改）。
- 【格挡】不再[敌回终]清空，保留到被打掉或[战终]统一清除；【庇护】文案随之去掉「持续1回合」。
  注意：`副本/*.md` 的「<副本>怪物池」段落是 **`engine/monsters.py::parse_monster_pool` 的运行时活数据**，
  改道纹名必须同步改这里，否则怪物会带着注册表里不存在的道纹（本次 毒枭/窃贼 的 洗劫3 就中了这一条）。

**「AI 为什么不在斩杀线打空法力」的答案：是谨慎，但谨慎不是瓶颈。**
`_daowen_candidates` 确实会算收割档 `X=ceil(目标当前生命/每X伤害)`，但随后被
`x <= cap`（`cap = 法力//cost//剩余出手`）剪掉——构造用例中 法力8/剩2手/需X=6 时，
候选集只有 `杀伐X=4、杀伐X=1`，斩杀档**从未进入打分**。开关 `LJ_AI_HARVEST_DUMP=1` 让收割档只受
「买得起」约束后，该档出现；但 20 种子 96 次真实决策中候选集变化 **0 次**，通关率也不变。
真实原因是量级：**全池灌杀伐只占 boss 血限 6–11%**（法限 3/4/6 → 15/20/30 伤，boss 264–270 血），
所以「打空法力收掉它」这个选项在实战中几乎不存在；而两手里的第二手本来就允许用满全池。
结论：斩杀这条路的机制是通的，缺的是弹药——不要用「让 AI 更敢梭哈」去解释通关率为 0。

## 假账平反 + 三处修复 + 休整改制（2026-09-10 六审续，用户裁定批次）

### ② 平反：「68 盾/法力 51」假账是**诊断工具**的账，不是引擎的账
- 根因：`sim/duel_diff_trace.py` 的深度计数器只补丁 `ActionPreview.preview`，而预演
  主入口是 `preview_sequence`（`preview` 只是它的单步委托壳）、WinOnlyAI 整场推演走
  `_swap_world`（不经 ActionPreview，直接 execute_action）——两条路全漏记，副本世界
  的出手被打上 `[真实]` 印记。实测 seed1 一局 48 条 `[真实]`，第一条「守擂 4→0」
  下一行又回 4/36 = 回滚签名，全是推演世界。
- 修复：计数器改补 `preview_sequence`（两个口径都经它）+ `WinOnlyAI` 模块级
  `_PLAYOUT_DEPTH`（`_swap_world` 进出维护，`playout_depth()` 公开读取口）。
- **证据：修复后同一局只剩 1 条 `[真实]`（真实终结手，面板链一致），判定与修复前
  逐字节一致，不变量全绿——模拟世界从来干净，无需禁用 PvP 普攻候选。**

### ③ 尸体误判修复：`enemy_gone` 只认「动作前还活着」的敌人
- 根因：`ai_preview._diff` 不带动作前的存活位，多怪局首杀后场上尸体的 diff 条目恒
  `dead=True` → `ai_tactics._digest_diff` 的 `enemy_gone=any(dead)` 把「打自己」归纳成
  `remove`（+6.0 向性分压过真输出，2026-08-31 审计的「杀伐X=1 打自己」同源）。
- 修复（三件套）：`_diff` 补 `alive_before`；`_digest_diff` 只认
  `alive_before=True`（缺省回退 `hp_before>0`，兼容手工 diff）；连带发现并修复
  **自伤兜底**——无活敌时 杀伐 自指变体曾被兜底成 `tactician`（try_buff 拿它自残），
  新增 `kind="harm"` 拦截；`try_buff` 补「无活敌不出手」门（test_ai_buff_fixes 钉死
  的设计意图，旧版靠**双重误分类巧合**（杀伐→remove、龙鳞→shield）才绿）。
- 净效应：24 种子 cleared 逐种子持平（WinOnly 推演早已越过启发式误判）——修复是
  纯正确性收益，无训练回归。

### ④ 带伤续战成真：封存格式带上 mutation_count
- 用户裁定 2026-09-10：异变是**已实付的代价**，封存不得免费洗白崩解进度。
  `_serialize_entity_full` 补 `mutation_count`，`_deserialize_entity_full` 读回
  （旧档无键回退 0）。癌变 `total_healed` **不随封存走**：DM 已裁定它是局内减益、
  每场归零（api.py battle_end 注释），带它无意义。
- 事实澄清：异变在**单次运行内**跨场本来就持久（seed5 第 6 场累计 48 靠的就是它）；
  被封存丢弃的永久代价**只有异变这一个**，不是「所有代价都无法永久存在」。
- 顺带效应：轮回者带 48 异变续战 → 下一手封印 X≥1 仍会撞崩解线——AI 层崩解线
  否决（projected≥50 拒绝）现在有了真实的跨局意义。

### 休整改制：恢复额度=轮回者血限百分比（2026-09-10 用户裁定；同日二次裁定改三档）
- `engine/api.py::_pre_battle_xiuzheng`：tier1/2/3 = 血限×**20%/40%/60%**（向上取整，
  二次裁定前的 20%/60%/120% 作废）+ 永久休整加成（平加不倍增）；碎片代价 0/10/25
  不变。血限40 时 tier1=8 与旧固定值一致；高档位较旧 24/48 收敛（百分比随血限放大，
  固定值不随）。
- `sim/build_learner.py` 档位选择器同步：选「最小够用档」（base≥缺口的最便宜档），
  付不起（碎片+工资保留预算）降档，没有够用档买最高档兜底。
- README:235 规则行同步；6 处测试期望值改为按血限现算。

### 评分盲区③修复：法力损耗按攻力等价折价（2026-09-10 用户裁定批次）
- `_score_candidate`：`-0.12×mana_spent` → `-mana_spent×(0.12 + 0.5×攻次)`。
  「法力=攻力」的机会成本进分：满池梭哈盾在高攻次下净分为负，化雕塑路径
  （换席 seed 9/11/12：满法力一手全换盾→空手→回终化雕塑）在打分层被拦。
- **连锁澄清（重要）**：攻次×攻力改制后，满池普攻（攻次×攻力，零耗）常优于
  杀伐5X——旧测试钉的「满池必选杀伐」「低威胁=杀伐」是过时结论，已按不变量改写
  （不变量=「推进输出」，牌在 杀伐/普攻 间择优）。低威胁测试实测：满池普攻
  4×30=120 伤 vs 杀伐X=15=75 伤还烧 15 法力——普攻胜出是**正确**行为。
- **满血墙≠不可胜**：纯普攻 10 回合可磨死 258 血墙；墙打不动人时第 5 回合死于
  凡庸钟。`test_win_only_pve_playout_signs` 的「满血墙 ≠+1」物理上不再成立
  （剥 kit+5000 血依然 +1=墙死于凡庸），已改钉「预演零副作用」+首条逐字节泄漏守卫。
- 守卫测试：`tests/test_scoring_guards.py` 5 例（自伤=harm、remove 只认动作前活体、
  alive_before 回退口径、攻力折价随攻次缩放、低威胁梭哈盾<零耗普攻）。

### 坏死「付5烧0」查实：非 bug（2026-09-10，报告⑤结案）
- 坏死设计=禁疗 debuff（消耗5X → 目标「无法获得回复」持续X），**从不烧目标法力**。
  「付5」=施法代价，「烧0」=目标面板无位移=正常。旧实录「烧1」疑为残韵改写变体。
- 禁疗在战斗内管线多处消费点生效（combat.py:2665/2870/2897/2907）；实测低层
  `Entity.heal`/`State.apply_heal` 直调可绕过——**局外休整不受禁疗**是现行口径，
  是否扩到局外属规则面，报告留一行待裁定。
- 净效应复验：24 种子 纯AI层 56 / 自保开 59，与③修复前持平（rows 种子噪声内）。

### 自保时钟常开（2026-09-10 用户裁定）
- `engine/ai_tactics.py::SELF_PRESERVE` 默认**开**（`LJ_SELF_PRESERVE=0` 显式关复现旧行为）。
- 裁定依据：24 种子养蛊 A/B 两轮复验（纯AI层 56 → 开 59，+3 零副作用，rows 逐种子可复现）；
  DM 门槛的 132 局死斗对照因擂主农场 0/24 空转暂缺，留待农场恢复后补跑
  （`sim/self_preserve_experiment.py matrix`）。
- 连带：`tests/test_legacy_mentor.py` 的偏见夹逼测试原用 mutation_delta=99（越崩解线）
  构造 diff——自保一票否决正确拦截后返回 None，已把 diff 改到安全余量（=3）。
- 默认档抽检 seeds(2,5,23)=[0,5,6]，与 env 档 rows 逐位一致（默认=原实验档）。

## 正文行文红线：不写沿革、不写废案（2026-09-12 用户指令）

规则正文只写**当前生效**的口径，一律不写下列内容（用户原话：「很多废话就不要重复，直接删掉」）：

- 「X 已于某日删除／废止」「原口径是 Y」「旧写法……新写法……」一类沿革说明；
- 「DM裁定 2026-0X-XX」这类日期水印（裁定生效后它就是规则本身，不需要落款）；
- 对同一条规则在不同小节的重复陈述（正文一处写全，其它地方只链接）。

怕改回去的，记在本文件而不是正文里。当前已从正文删除、**不得再写回去**的内容：

| 已删内容 | 现状（正文只写这个） |
| --- | --- |
| 局外行动【领悟】 | 2026-09-10 删除，残韵不能凭精力白拿。残韵来源只有：开局必选1种、遗物【三相残韵盘】、乱葬岗「贴符加固」、遗忘书屋《自我剖析》、事件文案中「曲解/反转/转换 + 残韵」的通用解析。 |
| 「道纹归属规则」小节 | 2026-09-12 删除（与《原始怪物道纹与转化道纹》《全道纹索引》三处重复）。原始/转化道纹的归属、代价、单向变化与【原初X】借用口径，以《原始怪物道纹与转化道纹》和 `全道纹索引.md#道纹归属与学习规则` 为准。 |
| 「[血限]仅可在开局获得」 | 2026-09-12 用户裁定废止：局外【修行】与开局初始分配同口径，修行所得属性点同样可兑血限（`_redeem_attribute_points` 接受 `blood_points`，1点=6血限；【不朽之躯】在场时拒绝兑血限）。 |
| 攻次/攻力加点池、格挡[回终]清空、法力[回始]回填、杀伐 2X | 均为已废旧口径，正文不再并列陈述新旧两版。 |

## 反向禁区（从 报告.md 移入，2026-09-10 用户指令；禁止「顺手修好」，违反即自造规则）

- 不得给面板加自造上限：除【不朽之躯】外，① 禁给 `gain_shield()` 加「≤血限」clamp；
  ② 禁给「获得法力」加「≤法限」clamp。超上限是合法面板（曾两次误加，已由 `6985481`/硬伤1 撤销）。

## 化雕塑判定区分（2026-09-10 用户裁定：零散小项1「区别」）

- 死斗判负原因必须区分 被击杀/化雕塑/凡庸/崩解/自付代价，不许一律写「阵亡」。
- 落地：`sim/duel_pvp.py` 判定存活检查纳入离场标记（`is_sculptured`/`is_departed`，
  化雕塑是「攻次与攻力双0离场」不是命零）→ 专属文案「化雕塑（攻次与攻力双0离场，
  判负；非被击杀）」；`_DEATH_CAUSE_LABELS` 补 `sculpture→化雕塑`（凡庸/崩解/癌变/
  自付代价已有归因，2026-08-30 裁定的 `death_attribution_note`）。`duel_common`
  交替驱动同口径。守卫：`tests/test_duel_settlement_integrity.py::test_sculpture_loss_is_distinguished_from_death`。
- 报告其余零散小项（碎片封存时点/死斗带币/局外休整禁疗/132局对照）用户裁定「不管」，
  从报告移除。

## 手操铁律③：护卫=9次命中、必须每轮刷新（B60r4·B6R3死亡教训，2026-09-11）
- `command_ally 护卫9` 的9是**可重定向命中次数**（多轮累计消耗、多护卫共池），不是轮数。B6三怪 R1吃7次+R2吃10次=17次，
  两道护卫（R1巨囊+R2行者）共18次额度在R3见底，R3我方漏令护卫→鱼18+骨7连直击沈昼19HP→命零。
- B3/B4/B5单次护卫够用只因总命中少（8/4/4次），不是护卫常驻。**多怪多连击局：每轮round_start后第一件事就是补一道护卫**
  （坦克轮换：谁满血令谁），再谈输出。漏一次就可能死。
- 附带：骨天使开变形后每个攻击出手要提7次命中（7连），鱼R2衰败对被护卫玩家伤害为0（疑重定向/免疫，待查）。

## 手操铁律④：终局与怪阶段口径（B60r5通关，2026-09-11）
- R1怪无合法道纹（daowen须null）；R2起强制提交一个合法道纹（null会被拒）。
- 变形：首次发动当轮7连，之后轮次变回3连但仍须提变形（不提被拒）。
- 飞行没有曲解路径！剥飞行只能反转/转换（→滑翔）。动残韵前先查路径，别想当然。
- 残韵剥离成功施放者会获得新道纹（caster_gets_daowen），等于白嫖（滑翔即B2剥骨所得）。
- 多护卫叠加成池：B7双护卫9=18额度盖住四怪14连击，玩家全程0伤。连击>9的局直接双令。
- 终局（B7）不用救赎：员工再多也带不出轮回，直接集火秒杀（先杀最高秒伤）。
- 狂暴buff后怪可变多出手（蜈蚣R3被要求提2个attack_actions，每出手4击），坦克留余量。

## 手操铁律⑤：救赎接纳=待命员工（2026-09-11 用户令，免费朋友时代结束）
- 接纳得半面板满血[员工]，默认待命：当场参战须耗玩家1出手派遣；战终按2×(1+参战轮数)付工资
  （上限12/人）；全体存活员工攻击总值≥玩家当前生命+朋友攻击总值即叛变（镇压/让利/谈判）。
- 权衡点：1~2个便宜好养；3个起工资吃紧；5个以上基本叛变。r5式十朋友雪球在新规下=十员工，
  战终必叛变+工资破产——旧证据（data/handplay_20260911_b60r4/r5）仅保留走位与算伤参考，滚雪球规模须重测。
- 待命员工不吃工资、不长历练，但照样计入叛变——板凳也占"编制"，多了就解雇（+1黑名单）或干脆无视。
