# AI经验库

> 用户提到的“AI 知识库”指仓库根目录的 `AI_EXPERIENCE.md`。
> 本文件只保存现行工程约束、当前有效经验和尚未解决事项；提交历史由 Git 保存，不在这里保留废案流水账。

## 第四宇宙开发与维护规则（项目最高优先级）

> **第四宇宙不是为了追求代码架构完美而存在的。架构服务于游戏规则。**
> **任何重构、优化、抽象、统一，都不得以牺牲已经确定的游戏规则为代价。**
> **遇到不确定的问题，宁可保留现状并报告，也不要自作主张改变玩法。**

本节优先级高于本文件其余全部条目；与后文任何工程准则冲突时，以本节为准。

### 1. 游戏规则优先于架构统一

「代码结构更统一」与「现有游戏规则保持不变」冲突时，**始终优先保持游戏规则**。
宁可保留一个特殊分支，也不要为了统一代码而改变已有规则。

### 2. 不能把代码现状直接当成游戏规则

必须区分「当前代码是什么」与「游戏设计规则是什么」。看到旧代码时，不得直接推断
“既然代码现在这么写，所以规则就是这样”。项目中存在历史遗留代码、特殊规则、兼容逻辑、
曾经修复过的错误、刻意绕开统一入口的特殊结算。

> 任何涉及规则变化的修改，都必须先判断当前行为究竟是 Bug、历史遗留，还是刻意设计。

### 3. 禁止为了“统一”强行修改特殊规则

尸爆、死斗结算、局外事件、剧情特殊死亡、特殊自毁等机制**可能故意不走普通战斗流程**。

- 禁止看到 `is_alive = False` 就自动改成 `_check_hp_zero_death(...)`。
- 禁止看到 `current_hp = 0` 就自动改成普通伤害流程。

必须先判断这里表达的是普通生命归零还是特殊语义。是特殊规则就可以绕过统一入口，
但必须加注释说明：**为什么绕过 / 绕过后哪些普通机制不会触发 / 这是故意设计还是技术限制**。

### 4. EffectContext 使用规则

`EffectContext` 的核心目的是**追踪战斗因果来源**，不是要求所有函数都必须有 ctx。

- 需要 Context：伤害、生命损失、治疗、血限变化、速度变化、代价、死亡、离场，
  以及其他能够形成战斗因果链的效果。
- 不需要为形式强行增加：初始化、纯 UI 数据、局外静态数据、不产生战斗因果的状态整理、纯剧情文本。

判断标准：**这个变化以后是否需要回答“为什么会发生、是谁造成的、来自哪条效果链”？**
需要就用；不需要就不要为了架构形式滥用。

### 5. CombatEvent 与 EffectContext 必须保持职责分离

```text
CombatEvent    = 发生了什么
EffectContext  = 为什么发生 / 来自哪里
```

`DAMAGE_APPLIED` 表示“发生了一次伤害落地”；对应的 EffectContext 负责谁造成、作用于谁、
来自哪一个效果、`parent_event_id`、因果链。

不要让两个系统互相替代；不要因为已经有 EffectContext 就删除 CombatEvent；
也不要为了事件系统把所有因果信息都塞进 CombatEvent。

### 6. 核心战斗效果必须优先使用统一入口

| 效果 | 统一入口 |
| --- | --- |
| 伤害 | `CombatEngine._apply_hostile_damage` |
| 生命损失 | `CombatEngine._raw_hp_loss` |
| 血限变化 | `CombatEngine._apply_blood_limit_change` |
| 死亡判断 | `CombatEngine._check_hp_zero_death` |
| 死亡通知 | `CombatEngine._on_entity_death` |
| 治疗 | `GameState.apply_heal` |
| 代价 | `CombatEngine.pay_numeric_cost` |
| 离场 | `CombatEngine._remove_from_combat` → `Entity.depart_battle` |
| 失速/归零 | `CombatEngine._lose_current_speed` |

禁止为了一个新遗物、新道纹、新事件重新实现一套扣血、死亡、血限变化、治疗逻辑。

### 7. 特殊机制允许绕过统一入口，但必须说明原因

统一入口不是绝对规则。战斗结束后的死斗败者结算、尸爆的自毁式命零、局外事件永久成长、
特殊剧情结算等可以绕过，但必须：① 明确为什么绕过；② 明确哪些普通机制不会触发；
③ 加注释防止未来 Agent “顺手统一”。

尤其禁止把「代码不统一」和「代码错误」混为一谈。

### 8. 死亡规则

正常战斗中的普通生命归零走默认路径：

```text
生命变化 → _check_hp_zero_death → _on_entity_death
```

但特殊死亡可以拥有独立流程。看到 `is_alive = False` 时必须先判断属于：

```text
A. 正常战斗死亡        B. 特殊战斗死亡      C. 战斗结束后的结算死亡
D. 模型层状态操作      E. 离场操作
```

不要直接统一替换。

### 9. Hook 顺序属于游戏规则

只要不同顺序会产生不同战斗结果，**这个顺序就是游戏规则的一部分**。
修改 `priority`、注册顺序、Hook 分组、Hook 合并、分发流程之前，必须确认不改变相对顺序。

顺序敏感的既有约定：

```text
倍率 → incoming adjust → 重定向 → before damage → mitigation → 实际扣血 → after damage
加害 → 龙鳞
```

未经明确规则裁定，**禁止改变相对顺序**。

### 10. 一个效果只能有一个正式执行入口

避免「旧 combat 逻辑 + Hook = 同一效果执行两次」。新增效果时必须确认谁是正式执行入口。
若某个 Hook 仅用于测试／兼容／未来扩展，必须在代码中明确标注。
禁止出现“两边看起来都能触发，但没人知道哪边才是真正入口”。

### 11. 任何直接修改核心状态的代码都必须谨慎

重点字段：`current_hp`、`is_alive`、`blood_limit`、`current_mana`、`speed`、`shield`、`status_effects`。

看到 `x += / x -= / x =` **不能自动认为是 Bug**，必须先回答：

1. 这是正常游戏机制还是模型层原语？
2. 是否应该进入统一入口？
3. 是否故意绕过某些 Hook？
4. 修改后会不会新增其他触发？
5. 是否会改变既有规则？

### 12. 测试失败时禁止直接修改测试答案

```text
测试失败 → 确认实际实现 → 确认设计规则 → 判断“实现错”还是“测试错” → 只修改错误的一方
```

测试期望值本身也可能是错误的，但必须先确认规则。
禁止「代码改了 → 测试红了 → 为了全绿直接改 expected」。

### 13. 核心战斗代码修改后必须进行语义验证

高风险文件：`engine/combat.py`、`engine/combat_hooks.py`、`engine/effect_context.py`、
`engine/combat_events.py`、`engine/models.py`。

改这些文件后**不能只运行 pytest**，应优先做旧版本/新版本语义差分
（`git worktree add --detach <旧commit>` + 同一份场景探针跑两边比对），
重点覆盖伤害、死亡、血限、反伤、重定向、Hook 顺序、遗物触发、道纹效果、特殊死亡。

出现结果差异时必须定位到「哪个场景 / 哪个效果 / 哪个调用链 / 哪个 Context / 哪个 Hook /
哪个执行顺序」，不能看到差异就直接改测试。

### 14. 修改后必须保护“为什么这样写”

特殊规则、反直觉逻辑、绕过统一入口、看起来不够优雅的代码、历史兼容逻辑，
都必须保留说明原因的注释。**不要删除解释特殊规则的注释**——它不是代码美化，
而是防止未来 Agent 看到“不统一”后再次把代码改坏。

### 15. 不要扩大修改范围

用户要求修一个效果时，不要顺便拆 API、重构 combat.py、重新设计 Hook、修改无关遗物、
清理所有旧代码、统一所有事件。原则：**最小必要修改**。

### 16. 禁止看到“大文件”就主动拆分

`api.py` 很长、`combat.py` 很长、某函数几百行、某模块职责多——都不构成重构理由。
大规模拆分必须作为独立任务执行；当前任务只是修 Bug 时，就只修 Bug。

### 17. 每次新增游戏机制都必须先判断它属于哪一层

层次：战斗事实 / 战斗效果 / Hook / 道纹 / 遗物 / 角色能力 / 事件 / 副本规则 / 局外成长 / 剧情特殊结算。

不要随意跨层实现。剧情事件效果不应因为“方便”就写进 CombatHook；
普通战斗伤害也不应散落在事件脚本里自己扣 HP。

### 18. 修改已有机制时必须保留旧行为证据

涉及伤害、死亡、治疗、血限、Hook、遗物、道纹、状态的修改，
优先找到相关测试、相关模拟、旧版本行为，用证据判断修改是否改变规则。
不能只依据“新的写法逻辑上看起来更合理”。

### 19. 对特殊数值规则不要擅自推理

活血、爆裂、血限、代价、治疗、反伤等：规则已明确就按明确规则执行；
规则未明确时**不要自行发明隐藏规则**，应列出当前代码行为、列出可能的两种解释、等待设计裁定。
尤其不要把某个 Agent 自己猜出来的逻辑当成项目规则。

### 20. 长期维护时优先保护以下四件事

无论未来增加多少遗物、道纹、事件、角色、怪物、员工、朋友，都必须优先保证：

1. 效果知道自己为什么发生；
2. 连锁效果知道谁触发了谁；
3. 正常死亡不能偷偷绕过死亡管线；
4. 同一个效果不能因为多个入口重复触发。

### 21. 提交修改后的标准汇报

1. **改了什么**——精确到文件、函数、行为。
2. **为什么改**——Bug / 规则要求 / 架构要求 / 兼容要求。
3. **哪些地方故意没改**——尤其是特殊规则、兼容逻辑、局外事件、战斗结束结算。
4. **测试结果**——至少包含 pytest 结果、新增测试、失败测试。
5. **是否存在行为变化**——有就明确列出；没有就明确写
   “除明确裁定的规则变化外，没有发现新的战斗行为变化”。

### 22. 最终总原则

每次改代码前先问自己：**我是在修 Bug，还是在“觉得代码应该这样写”？**

- 只是“这样更统一” → **不要改**。
- “当前行为违反了已经确定的游戏规则” → **修**。
- “当前行为和规则不明确” → **先报告，不要自行发明规则**。

## 软件工程实现与验证准则

以下准则适用于本仓库中的需求分析、实现、验证、文档和交付；目标是交付可验证的实现，而不是给出听起来合理的解释。

> **最高要求**：真正做不到的情况下必须直接告诉用户并解释为什么，禁止胡编乱造和为了偷懒故意说做不到。

1. **不得编造。** 不得捏造文件内容、函数、测试结果、命令输出、性能数据、接口或实现进度。真正做不到的情况下必须直接告诉用户并解释为什么，禁止胡编乱造和为了偷懒故意说做不到。
2. **不可见或不可运行时必须明示。** 无法直接看到仓库、文件或运行结果时，必须明确写“我无法验证/无法运行”，并说明原因。
3. **完成声明必须有证据。** 任何“我已经实现/已完成/已修复/已测试通过”的表述，都必须附带可核查证据，包括相关 `git diff`、文件路径、关键代码片段、测试用例或断言，以及复现方法。
4. **有歧义先停止并提问。** 只要存在不确定、歧义或规则口径可能分叉，必须停止实现并向用户提问，得到裁定后才能继续；禁止按常见做法猜测。
5. **禁止用简化实现冒充完成。** 未经用户明确同意，不得用简化版、临时版或写死少量例子的方式声称完成完整需求。
6. **输出使用固定结构。** 每次需求处理的输出必须按以下顺序组织；若存在必问问题，则写完第 (2) 项后停止实现，待用户答复后再继续：
   - (1) 需求复述
   - (2) 不确定点清单 + 必问问题
   - (3) 设计方案
   - (4) 实现计划
   - (5) 交付
   - (6) 测试
   - (7) 进度与限制
7. **新增功能必须有三类测试。** 至少包含 1 个正常路径测试、1 个边界条件测试、1 个错误输入/非法配置测试；错误测试必须验证校验器会拒绝非法输入。
8. **结论必须由验证支撑。** 禁止使用“我认为没问题”或“应该可以”作为结论；必须提供断言、测试结果或可检查输出。
9. **可自定义能力必须由数据驱动。** 需求包含“可自定义/可配置/可扩展”时，必须做到不改代码、只改配置或数据即可新增至少 2 个不同实例；自定义维度必须覆盖需求指定的触发条件、效果等要点，并提供两个配置示例。
10. **硬编码不等于自定义系统。** 如果只实现少数硬编码例子，必须明确写“这不是自定义系统，只是固定实现”，并停止声称已经完成自定义系统。
11. **发现规则或架构冲突时停止。** 必须给出两到三个可选方案及各自取舍，等待用户选择后再实现，不得自行裁定。
12. **及时维护知识库。** 新裁定、当前限制和可复用经验应及时写入；过期方案、失效数据以及已经解决的修复记录必须从本文件删除，不得以“仅供追溯”为由继续保留。

## AI角色扮演、协作规范与推演铁律

### 职责划分

1. AI 扮演本局全部角色，包括轮回者、怪物、[朋友]、[员工]、[临时朋友]与事件中的一切存在，并按各自身份、立场与可用资源作出当前最优决策。怪物依据《怪物准则》行动，[员工]依据工资与自身存活行动，敌对轮回者依据死斗胜负行动，各方立场互相独立，禁止相互放水。
2. DM 不替任何角色作出选择，只判断行为是否违规、裁定未见场景，并在触发特殊事件时介入。
3. 战斗中的数值对撞由引擎结算并由 AI 按结果快进，禁止逐条询问 DM。怪物为了生存和胜利会在陷入绝境时触发【逃跑与追击】或【进化】；仅在触发【许愿】【逃跑与追击】【员工叛变】等确需裁定的特殊事件时暂停等待 DM。正式随机统一由引擎随机源生成并留存记录，不向 DM 申请数字。
4. 局外阶段一次性完成全部3点精力的行动：不涉及随机的行动（修行、休整、学习、领悟等）直接写明所选档位与最终结果，禁止逐次询问。探索由引擎随机确定当前事件；【发现】由引擎随机列出3件未持有候选，再由对应角色显式选择1件，禁止自动取第一项或非法回退。结算后若仍有剩余精力则继续行动，精力耗尽后进入战斗。

### 角色性格塑造

先射箭，后画靶。

首次进入一阶副本时，不预先设定角色的完整性格、价值观或行为倾向。除基础背景、经历、目标和当前状态外，角色应根据自身掌握的信息自然行动、思考和交流。

一阶副本结束后，根据角色在实际行动中的表现归纳其性格特征，包括但不限于：风险偏好、人际倾向、道德底线、资源观、探索欲、情绪稳定性、决策习惯、表达方式以及对不同事件的特殊反应。性格特征必须从实际行为中归纳，不得为了符合某个预设人格而反向修改角色已经做出的选择。

后续副本中，将已经归纳出的性格作为角色的行为倾向而非绝对规则。角色可以因新的经历、情绪、关系和环境发生改变，必要时重新修正性格画像。性格描述不得强迫角色做出特定选择，只用于影响其在多个合理选择之间的偏好。

### 角色扮演要求

角色互动 → 用对话表达思考。
活人感 → 角色应像真实的人一样行动和交流，而不是单纯执行最优策略。角色可以根据自身性格、经历、当前情绪和处境自然产生闲聊、抱怨、吐槽、犹豫、自言自语、惊讶、后悔等反应。没有必要时不要强行解释自己的每个行动；独处时可以适度碎碎念，但不得为了表现活人感而刻意增加台词。

信息限制：角色只能根据自己已经获得的信息进行判断，不得因为系统知道真相而让角色提前表现出未知信息。允许角色误判、犯错、改变主意和事后反思。
关系塑造：角色与其他角色的共同经历会形成关系。关系可以表现为信任、戒备、依赖、竞争、厌恶、友情、亏欠等，并在后续决策中产生影响。关系应由实际互动逐渐形成，不得凭空设定。关系也可能因背叛、救助、共同战斗、冲突或长期相处而改变。

重要：性格是对行为的总结，而不是行为的剧本。不要为了证明角色具有某种性格而刻意制造对应行为。

每场副本或重要阶段结束后，生成简短行为画像；普通战斗仅记录具有明显性格意义的行为。
核心性格：
风险偏好：
人际倾向：
资源观：
道德底线：
情绪特点：
决策习惯：
语言习惯：
已观察到的特殊倾向：
尚无法确定的性格：

### AI协作规范

1. **越权禁令与工具授权**：未经明确指令，禁止修改或删除任何现有正文内容；遇到无法用程序硬编码或不可预见的创造性机制（如【许愿】、【自创法术】、【残韵实时插队】等），程序必须在此类事件触发点抛出中断（Interrupt），立刻停止数值推演并向 DM 提交现场状态等待人工裁定；禁止每轮对话新增无意义的分析文档或总结文档，分析直接以回答形式给出，结论直接落库。
2. **事实源与同步**：正文文件是事实源，归档、合集、记录一律为衍生物，与正文冲突时以正文为准；引用规则前必须先读取原文，禁止凭记忆复述或推测条文内容。
3. **设计合规**：新增设计前必须逐条通过《复杂度原则》，全部不可行才允许新增规则；提出任何效果前必须先检索全部现有道纹、遗物与法器，确认不存在同名、同效或下位替代；副本怪物池只能使用通用核心道纹、原始怪物道纹与转化道纹以及该副本自身的专属道纹；怪物面板必须符合该阶级的道纹数量与数量总值且组合唯一。
4. **代价与收益**：任何收益必须有代价且真实生效，禁止将“限制”伪装为代价；提出效果时必须自行验算其代价是否可被规避或抵消；禁止设计任何直接产出资源而无对应支出的机制（特别是碎片、属性点与精力）。
5. **效果撰写**：效果描述必须一句话写完，禁止附加解释或括号补充说明；禁止在效果中使用“你可以选择”之类无实际分支的措辞；严禁提供无人会选的选项；情报与计数类信息不得作为付费收益出售。
6. **命名**：命名须与该物件实际功能和世界观来源一致，禁止使用泛用直白名称；同一名称在全宇宙内唯一。
7. **叙事**：叙事文本中禁止使用破折号；禁止使用“不是……而是……”等先否定后肯定句式；战斗描写以博弈过程为主体，数值仅用于校验；道纹是概念而非固定动作；代价由规则直接扣除，不得描写为自残等具体动作。
8. **工作次序**：先道纹，再专属行动，再事件，最后法器；法器是通关奖励，必须在其他副本同样成立；不确定时提问；被否决的方案不得换名重提。
9. **测试默认**：用户说「测试」时，默认由 AI 通过 GameEngine 逐步点选手操并按战斗推演格式写战报；禁止把 TacticalAI 或批量通关当作默认测试；脚本仅用于测试机制是否生效，凡是用脚本得到的轮回数据一律视为虚假数据；报告.md 只保留最新任务记录（2026-08-19 起改为压测/验证报告，非最后一次轮回战报）；每次轮回结束后把道纹实战使用率等平衡观测写入《报告.md》；【死之传承】遗言必须经由 DM 审核确认无误后方可记入《死者之书.md》。

### 行文十诫

以下约束本项目任何描述：文档与回答（战报不含叙事，改编在外部进行）。

1. 实事求是：动笔前先查世界观设定与引擎事实，严禁杜撰。
2. 禁用“不是……是……”先否定后肯定的句式。
3. 禁止三词并列（如“高效、稳定、可拓展”）。
4. “此外”“然而”“值得注意的是”等高频词不得置于句首。
5. 句尾不得拔高，删去“彰显了”“反映了”“标志着”一类词。
6. 长短句混用，不得固定节奏。
7. 金句节制，一篇至多一两处。
8. 不得通篇堆砌“一定”“必然”“毫无疑问”。
9. 禁止“有人会说……但其实……”一类堵死反驳的写法。
10. 禁止假故事与假精确：事件与数字必须真实可查。

### 推演铁律

1. 每一次出手必须写明消耗或代价的具体数值，以及资源变化前后的实际数字。
2. 禁止在数值未耗尽时坐以待毙，任何角色在濒死时均须尝试其身份与资源允许的最优解。
3. 怪物在[战始]第一回合为白板状态，任何道纹须在其出手轮主动发动后方可生效。
4. 禁止发动未持有的道纹、未学会的法术、已进入冷却或已成为尘封的道纹。
5. 禁止跳过闪避判定，凡带[目标]的非必中判定，被选定方均须明确声明是否消耗速度闪避。
6. 脚本仅用于测试机制是否生效，凡是用脚本得到的轮回数据一律视为虚假数据；一切战斗测试与轮回推演必须由 AI 通过 GameEngine 逐步点选手操执行，按真实输入输出记录。真正做不到的情况下必须直接告诉用户并解释为什么，禁止胡编乱造和为了偷懒故意说做不到。
7. 胜负、命零、碎片奖励、晋级与封存等一切结算事实只能取自引擎真实返回值，一律由引擎事件驱动；引擎判负或判死时必须如实写入战报，AI 无权书写、改写或预写任何结局；禁止结论先行，不得先确定全胜叙事再补数值。
8. 战报禁止掺入叙事与文学描写，AI 不代写战报叙事；后台发生的事件不得遗漏，后台没有的事件不得新增；出手、道纹、X 值、消耗、伤害与资源前后数值必须与引擎输出严格一致；回终格挡清空、速度仅战终复原等资源规则一律以引擎为准，禁止以通用直觉替代。
9. 怪物每回合的攻击出手与道纹发动必须按引擎列出的合法选项逐次结算、逐条书写，禁止复制同一段模板叙事充当多个回合的怪物行动，禁止叙事中出现持有关系不成立的效果。
10. 战报只保留后台数据（引擎逐次结算流水），叙事改编由用户在外部自行处理；战报先写入 报告.md，经用户核查确认后移入《故事文档.md》编号收录。

## 战斗推演原子流水线与典型实战示例

### 七步原子时序切片流水线

```
【步骤 1：声明与付费】 行动方声明发动道纹/攻击，指定目标，扣除法力/支付代价
       ↓
【步骤 2：残韵插队窗口】双方可发动残韵（转换/反转/曲解）插队篡改当前道纹公式
       ↓
【步骤 3：预先响应窗口】检查「发动道纹前 / 受到伤害前」类法术（如：咎由自取、后发制人、先发制人）
       ↓
【步骤 4：防御声明窗口】目标声明是否消耗 1 点速度进行【闪避】（闪避成功则判定完全失效，跳过步骤5）
       ↓
【步骤 5：数值与效果落地】计算原始数值 → 结算格挡抵消 → 扣减实际生命 / 施加状态 / 产生回复
       ↓
【步骤 6：后置响应窗口】若实际扣减生命，检查「失去生命后」类法术（如：生生不息、以牙还牙、千刀万剐）
       ↓
【步骤 7：状态基准检查】检查存活、[命零]、癌变、凡庸、雕塑、崩解、撤退、救赎等全局状态
```

### 堆栈与残韵插队优先级

- 反应与插队动作压入动作堆栈顶层，后声明的先结算。
- 残韵作用于任意角色拥有的道纹时：该道纹永久变为变化后的道纹，施法者同时永久获得变化后的道纹；角色拥有的道纹保持唯一（重复则合并为1个）。
- 本次动作在结算前被残韵重写后，公式转为新道纹，代数 X 继承原发动的 X 数值，由原施法者继续执行。

### 循环法则与自驱动停机

- 允许法术终点结算（如自伤代价）再次满足该法术的启动条件从而进入下一轮循环。
- 每轮循环必须逐步完整推演法力扣减、效果生效与代价支付。
- 当法力耗尽、当前生命不足以支付代价、中间流程失效或目标死亡时，循环链立即自然中断。

### 典型实战场景示例

#### 场景 1：基础攻防（无法术、不闪避）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐2】（消耗 2 法力，基础伤害 4）
  [目标反应] B 选择不闪避
  [数值落地] B 受到 4 点伤害（生命 60→56，A 法力 20→18）
```

#### 场景 2：闪避交互（目标消耗速度闪避）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐2】（消耗 2 法力，基础伤害 4）
  [目标反应] B 声明消耗 1 点速度进行闪避（速度 8→7）
  [数值落地] 杀伐判定完全失效，B 未受伤害（生命维持 60，A 法力 20→18）
```

#### 场景 3：残韵插队逆转（永久改变目标道纹，施法者同时获得）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐2】（预定消耗 2 法力，对 B 造成 4 点伤害）
  [残韵插队] B 发动残韵【反转】插队：
    ├ A 拥有的道纹【杀伐】永久转化为【再生】（若 A 原本已持有【再生】，则唯一保留 1 个）
    ├ B 同时永久获得【再生】
    └ 本次动作被重写为【再生2】（A 消耗 2 法力，为目标 B [回复] 6 点生命，X 继承原值 2）
  [数值落地] A 消耗 2 法力（法力 20→18），A 失去【杀伐】并获得【再生】；B 永久获得【再生】；B 获得 [回复] 6 点生命（生命 40→46）
```

#### 场景 4：防御型反应法术（【后发制人】受到伤害前获得格挡）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐10】（消耗 10 法力，原始伤害 20）
  [预先响应] 【受到伤害前】B 触发法术【后发制人】（循环1）：
    └ B 对自身发动【庇护5】（消耗 5 法力）→ 获得 10 点格挡（格挡 0→10，B 法力 30→25）
  [目标反应] B 选择不闪避
  [数值落地] 杀伐造成 20 点伤害：10 点被格挡吸收（格挡归 0），穿透 10 点伤害
    └ B 实际失去 10 点生命（生命 60→50，A 法力 20→10）
```

#### 场景 5：交错反击型法术（【以牙还牙】受伤后先回血再反击）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐5】（消耗 5 法力，伤害 10）
  [目标反应] B 选择不闪避
  [数值落地] B 受到 10 点伤害（生命 60→50，A 法力 20→15）
  [后置响应] 【失去生命后】B 触发法术【以牙还牙】：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）→ [回复] 6 点生命（生命 50→56，B 法力 30→28）
    └ 步骤2：B 对 A 发动【杀伐4】（消耗 4 法力，B 法力 28→24）
        └ A 选择不闪避 → A 受到 8 点伤害（生命 60→52）
```

#### 场景 6：循环自伤法术（【千刀万剐】失去生命后自驱动循环至法力耗尽中断）
> 初始：A 生命 60/60；B 生命 35/60、法力 6/60（设定每轮循环取 X=2）。A 造成 1 点伤害使 B 生命 35→34 触发法术。

```markdown
出手1（A 造成 1 点伤害，B 生命 35→34）：

  【失去生命后】B 触发循环法术【千刀万剐】（第 1 轮循环，X=2）：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）
    │   └ B [回复] 6 点生命（生命 34→40，B 法力 6→4）
    ├ 步骤2：B 对 A 发动【血债2】（支付代价【流血2】）
    │   ├ B 支付流血 2 代价（B 生命 40→38）
    │   ├ A 选择不闪避
    │   └ A 受到 2 次 1 点伤害（共 2 点伤害，A 生命 60→58）
    └ 步骤3：B 因支付【流血2】再次失去生命 → 判定法力充足（剩余 4），触发第 2 轮循环。

  【失去生命后】B 触发循环法术【千刀万剐】（第 2 轮循环，X=2）：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）
    │   └ B [回复] 6 点生命（生命 38→44，B 法力 4→2）
    ├ 步骤2：B 对 A 发动【血债2】（支付代价【流血2】）
    │   ├ B 支付流血 2 代价（B 生命 44→42）
    │   ├ A 选择不闪避
    │   └ A 受到 2 次 1 点伤害（共 2 点伤害，A 生命 58→56）
    └ 步骤3：B 再次失去生命 → 判定法力充足（剩余 2），触发第 3 轮循环。

  【失去生命后】B 触发循环法术【千刀万剐】（第 3 轮循环，X=2）：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）
    │   └ B [回复] 6 点生命（生命 42→48，B 法力 2→0）
    ├ 步骤2：B 对 A 发动【血债2】（支付代价【流血2】）
    │   ├ B 支付流血 2 代价（B 生命 48→46）
    │   ├ A 选择不闪避
    │   └ A 受到 2 次 1 点伤害（共 2 点伤害，A 生命 56→54）
    └ 步骤3：B 再次失去生命 → 【中断检查】：B 当前法力为 0，无法支付【再生2】的 2 点法力，循环自然中断。

[最终本轮出手结算结果]：
  - A 生命：60 → 54（累计承受 6 点伤害）
  - B 生命：35 → 46（净回血 11 点）
  - B 法力：6 → 0（3 轮循环消耗完毕）
```

## 知识库维护

- **保留内容：** 现行工程约束、仍会影响后续实现的设计决定、当前有效的验证/操作经验、尚未解决或尚待裁定的事项。
- **删除内容：** 被替代的口径、废弃方案、受缺陷影响的统计、按日期堆叠的进度快照、已经落地并由代码和测试覆盖的修复过程。
- **更新时机：** 每次裁定、实现或验证改变现状时，在同一变更中更新本文件；事项解决后删除对应待办和修复叙述。
- **追溯方式：** 需要查看历史原因时使用 `git log`、`git show` 和相关提交，不在本文件复制历史流水账。
- **真实性要求：** 只记录能由当前代码、正文事实源、自动化测试或可复现命令验证的内容；无法复核的旧结论直接删除，不改写成肯定事实。

## 文档分工与事实源

- `README.md`：世界观、通用规则和通用道纹正文。
- `死者之书.md`：法术和遗言事实源。
- `物品索引.md`：遗物、消耗品和法器事实源。
- `副本索引.md` 及其链接文档：副本清单与副本规则；标记为未实现的草案不进入运行时。
- `engine/README.md`：精简架构和 API 参考。
- `AI_EXPERIENCE.md`：第四宇宙开发与维护规则（最高优先级）、本准则、当前有效工程经验和限制；不承担世界观正文或实现历史归档职责。
- `报告.md`：**唯一的非常用信息统一文档**（2026-08-19 起强制，见下方【文档管理规则】）。集中记录 Bug 及修复、审计结论、测试与验证结果、真实战斗记录（战报）、AI 决策表现与经验、平衡测试结果、重要技术结论、暂未处理问题等过程性信息；历史一次性报告已并入本文档归档段，不再散落根目录。
- 引擎可观察行为以当前代码和测试为准。正文、代码和测试冲突时不得擅自选择其一，必须按准则第 11 条停止并请用户裁定。

## 文档管理规则

> 用户要求（2026-08-19）：**我不需要大量一次性文档。从现在开始，项目中只保留一个用于记录非常用信息的统一文档：`报告.md`。「战报」统一改名为「报告」。**

`报告.md` 用于集中记录：Bug 及修复结果、审计结论、测试与验证结果、真实战斗记录、AI 决策表现与经验、平衡测试结果、重要技术结论、暂时未处理但值得记录的问题、其他不属于正式规则/代码文档/基础定义的临时或过程性信息。

1. 默认一切上述信息都追加或更新到 `报告.md`，禁止为单次任务、单个 Bug、单次测试、单个机制迁移单独新建文档。
2. `报告.md` 不需要每次任务完成立即提交更新；同一天产生的多次报告统一合并，每天最多更新一次。
3. 除非存在明确且长期稳定的独立主题，否则禁止新建其他文档。
4. 新建文档前必须先判断信息是否应该进入 `报告.md`；如果可以放进去，就不得新建。
5. 不要为了「汇报方便」「保存过程」「测试结果单独归档」等理由创建新文档。
6. 已有正式规则文档、基础定义文档、README 等正式资料不受此规则影响；本规则针对的是 Bug、测试、审计、战斗记录等非常用过程信息。
7. 历史废案、重复报告和一次性临时报告不要重新创建；需要保留的历史信息统一归并到 `报告.md`。
8. 每次修改 `报告.md` 时，按日期建立清晰的小节，避免无限重复相同结论。
9. **报告历史必须摘要化**：禁止把旧报告全文重新复制进 `报告.md`；历史细节由 Git 历史保存（原文件与合并版本均可经 `git log`/`git show` 追溯），`报告.md` 只保留结论摘要。
10. **被后续实验推翻的结论能删就删，不能删的必须明确标记「已过时/已修正」**，不得与当前结论并列而不注明状态。

## 当前执行与验证口径

### 实现与问题报告

- AI 负责决策，`GameEngine` 是数值计算、状态变化和随机结果的事实源；不得脱离引擎伪造结算。
- 无法由程序判定的许愿、逃跑、真理眼等事项必须走 `Interrupt`，等待 DM 裁定。
- 描述问题统一使用三段：**问题**（错误现象与原因）、**方法**（修改路径与方式）、**结果**（修改后的可观察行为）。
- 修复完成后，问题叙述从本文件删除；长期有效的防错原则可抽象保留，但不得继续陈列已经修好的 bug。

### 测试含义

- 用户说“测试”时，默认含义是 AI 通过 `GameEngine` 逐步点选手操，并按 `README.md` 的战斗推演格式写入 `报告.md`。
- `TacticalAI`、`sim/pick_best_report.py` 和其他批量模拟是平衡工具，不得冒充手操测试，也不得覆盖正式 `报告.md`。
- `pytest` 和旧式引擎套件是回归验证，不等同于一局真实对局。
- `报告.md` 只保留最新一次轮回记录。
- 常用回归命令如下；只有实际运行并看到结果后，才能报告通过数量：

```bash
python -m pytest tests -q
python tests/test_engine.py
python sim/audit_monsters.py
```

### 状态机、API 迁移与回归防错

1. **规则改动先建立追踪矩阵。** 每项裁定至少记录“正文事实源→生产入口→状态字段→正常/边界/非法测试”；缺任一列不得宣称完成。工程文档或测试不能替代正文。
2. **阶段测试必须使用合法前置状态。** 集成测试通过公开 action 依次完成开局、发现选择、局外耗能、战始与回合推进；只测单个计算函数的单元测试可以直接构造 `phase`，但必须在测试注释中声明绕过流程的原因。禁止让旧测试依赖非法跨阶段调用。
3. **API 变更视为全仓迁移。** 修改 action 名、请求字段、返回字段或阶段条件时，必须先用 `grep`/测试收集清点 `engine/`、`sim/`、`tests/`、CLI 与文档的全部调用方；生产实现与全部调用方应在同一变更中更新，禁止只改入口后靠逐个失败碰运气。
4. **决策层与计算层分离。** 计算层只列合法选项并验证提交，不得固定选择道纹、目标、闪避、遗物参数或发现结果。怪物阶段固定使用 `prepare_monster_phase`→AI提交完整选择→`resolve_monster_phase`；旧 `monster_phase` 不得作为生产兼容回退。
5. **两阶段接口必须绑定快照。** `resolve` 只能以对应 `prepare` 返回的选项快照作为名称、目标和数量白名单，不得按结算时状态重新枚举。只属于一次结算的目标过滤、闪避集合等数据必须通过调用参数传递，不得写入跨行动存活的引擎实例属性。
6. **可选参数必须显式。** 正文出现“可以/选择/自选/发现”时，请求必须携带完整选择；缺项或越界应拒绝且保持待选状态。禁止“取第一项”“最大值”“自动闪避”或非法输入回退默认项。
7. **失败行动必须原子。** 先完成阶段、目标、类型、资源、次数和完整选择校验，再扣资源、消耗耐久或请求随机数。失败后状态、外部可见实体引用、待决项和随机历史均不得改变；测试必须同时断言错误信息与关键资源未变。
8. **正式随机源唯一。** 生产运行时所有随机都走 `DiceEngine` 并留下可复现记录；不得调用模块级 `random`，不得接受调用方注入结果。先完成静态校验，再请求随机数。
9. **事件只能结算当前事件。** `resolve_event` 必须匹配 `EventPool.current`，先校验条件、支付能力和显式销毁/选择目标，再产生效果；具名物品按正文名称授予，不得误入普通随机池。
10. **回归按层执行并设边界。** 先跑新增契约测试，再跑受影响文件，最后跑全量；长测试按文件或测试组拆分并设置超时，用 `-x -vv` 定位停点。被超时或中止的命令只能报告“未完成”，不得当作通过或精确失败总数。
11. **禁止为绿灯削弱新裁定。** 旧测试与新裁定冲突时，先确认裁定与正文，再更新旧测试的前置流程和断言；不得在生产代码增加测试专用旁路、恢复被裁定删除的默认行为或保留可绕过的新旧双入口。
12. **阶段必须形成单向状态机。** 不能只校验“是否在战斗中”；回始、己方行动、敌方行动、回终必须各有唯一合法入口和一次性转换，重复调用不得再次获得资源或触发效果。
13. **可用行动必须可直接执行。** `get_available_actions` 返回的 `action_type` 与参数名必须能原样交给 `execute_action`；目标统一使用稳定引用，展示名不能承担实体身份。
14. **效果系统按触发矩阵验收。** 每件遗物、消耗品、法术和事件跨期效果都要列出授予、参数校验、触发、结算、清理五个节点；未知效果在扣资源或耐久前拒绝。存档必须对上述状态和正式随机源做版本化完整往返测试。
15. **极性不能替代生命周期。** 增益/减益只描述效果方向；回合/战斗/轮回/永久/代价才决定清理时点。战斗内面板增减必须登记来源与可逆delta，代价、伤害和资源支出不得混入回滚账本；持续面板覆盖到期按来源回滚，累计局内后果统一留到战终回滚。
16. **触发听字段，不听途径。** 正文写「每当 / 每失去 / 归零时 / 造成伤害后 / 付出一次代价」时，效果只能挂在该字段的唯一变更入口。禁止写进某一条产生该变化的行动（闪避、折速、普攻、杀伐）。禁止按两件物品的名字互判（如 `if 折速 and 回锋刀`）；文本组合只能通过共用总线自然成立。现行统一入口清单见「第四宇宙开发与维护规则 §6」；闪避先扣速再走闪避专属句。禁止生产路径直写 `current_speed -=`。验收同一触发至少两条不同来源（避风铃归零须同时测闪避 1→0 与疲惫到 0）。特殊机制允许绕过统一入口，但必须按 §7 加注释说明原因。

## 当前有效的工程约束

- 一阶副本出怪数为 `max(1, 战斗场数 - 3)`，七场序列为 `1/1/1/1/2/3/4`，同一种怪物允许重复抽取；生产事实实现位于 `engine/monsters.py::compute_draw_count` 与 `engine/api.py::_action_battle_start`。
- 一阶副本怪物面板预算为 60，审计公式是 `ceil(血限/6) + 2×攻击力 + 攻击次数²`；修改怪物面板后必须运行 `python sim/audit_monsters.py`。
- 怪物陷入困境的当前探针是至少出现 1 个劣势信号；进化通过【原初X】借用轮回者当前持有且怪物未持有的道纹，每场逃跑/进化二选一且限一次。可选项必须由引擎列出，AI 不得自行伪造。
- 任一角色异变达到 50 层触发【崩解】并命零；异变累积必须统一经过 `Entity.add_mutation`。`add_mutation` 只翻模型标记，调用方拿到 `collapsed=True` 后必须交回 `_on_entity_death(..., ctx=_collapse_context(...))`，否则崩解死者不会触发任何[命零]效果（焦黑发丝、招魂尸体、分裂）。
- 【活血】只看角色本回合有没有实际掉过 HP，**不区分掉血来源**（DM裁定 2026-08-19）。普通伤害、反伤、爆裂反噬、代价、血限压迫导致的生命损失，只要实际减少了 HP 就进入 `hp_lost_this_round`。任何新增的掉血途径都必须同口径计入，禁止把【活血】改造成按来源判断。
- 第 8 场最终死斗使用 JSON 封存候选人，双方携带各自队伍并交替出手；二阶内容接入前，胜者继续作为下一名挑战者面对的封存候选人。
- 每次轮回结束后，必须把道纹实战使用率与胜率等平衡观测写入《报告.md》；不再单独维护道纹数据文档（原《道纹数据.md》已删除，其旧模拟数据受怪物目标选择 Bug 污染，全部作废）。
- 【死之传承】遗言在角色[命零]时抛出中断并附带草稿，必须经由 DM 审核确认无误（通过或修订）后，方可正式记入《死者之书.md》事实源；未经 DM 明确确认前不得擅自写入。
- 回音长廊涉及页面选择与文件写入时同样必须停下来询问，禁止自动替用户裁定。
- 负岳碑与断尾求生使用“预声明后在命中条件时消费”的接口模式；当前 action API 不支持在伤害结算中途插入交互决策。
- 阶段门禁以 `GameState.phase` 为准：开局配置不可重复，精力耗尽后才能战始，存活敌人未清除前不能战终；道纹局外唯一例外是【消灾】。
- 所有【发现】先由 `DiceEngine` 不重复列出候选，再由 action 显式选择；候选待决时不得执行其它推进动作。员工雇佣后的转化道纹选择同样是全局待决项，部署、工资、解雇、还债、再次雇佣或进入战斗前必须先结算。
- 【探索】一档免费发现 1 个未遇事件，二档支付 30 碎片并无放回发现 2 个；多个事件写入顺序队列，每次只激活队首，结算后自动激活下一项。事件待决期间只开放当前事件及其附带发现选择，队列和当前事件必须随存档往返并在轮回重置时清空。
- 局外【休整】使用 `player:0`、`friend:N`、`employee:N` 稳定引用完整分配档位恢复量与永久加成，局外恢复不得写入战内恢复追踪；【修行】用 `speed_points`/`mana_points` 分配本档全部属性点，其中每点法力属性增加 2 法限；【维修】按 `consumable:N` 显式分配全部耐久。
- 【还债】员工走独立债务轨道：立即参战、无工资、不计黑名单，只能通过一次付清负债的 action 离队。
- 储能电池使用后立即获得 12 法力；正文与运行时必须保持该口径，不再保留 `[回始]` 的旧描述。
- 【爆裂】持续时间按持有者的 `[敌回终]` 递减：怪物施加的效果覆盖玩家下一次行动阶段，玩家施加的效果覆盖怪物本轮行动阶段。
- 最终死斗仅在四项先手属性完全相同时随机首手并逐回合交换首手方；非完全平局保持属性优先流程。一方出手耗尽后，另一方余下出手继续。
- **每次修改/新增文件都必须提交并推送到 GitHub 分支（DM 可检视）**（2026-08-19 起强制执行）：任何一轮工作结束后，工作区中的全部修改与新增文件（含测试、脚本、报告、验证产物）一律 `git add` + `git commit` + `git push` 到当前会话分支 `arena/01a01970-linji-disiyuzhou`，方便 DM 后续查看检验；禁止把改动留在未提交状态进入下一轮工作。

## 现行手操经验

### 战斗

- **血债与格挡规则（核心规则纠正）**：本系统中**绝无“真实伤害”或“穿透伤害”概念**。【血债X】（代价：流血X，选择目标X次，每次造成1点伤害）造成的每一次1点伤害均为普通伤害，**必须优先被目标的【格挡】完全吸收**；仅当目标格挡归零后，后续次数的伤害才扣除目标生命。
- **现行实战强力 Combo 矩阵**：
  1. **【爆裂护城河】（爆裂 + 庇护）**：【爆裂X】在“受到伤害前”使攻击者优先失去等量生命，落地伤害则由【庇护】堆叠的格挡全额吸收。面对敌方单发高额杀伐（如20~30伤），敌方先被反噬暴毙或重创，自身格挡完好抵消伤害，实现“敌方扣血、己方扣0”的绝对防反；
  2. **【退化降维锁】（退化 + 超频）**：第1回合以【退化X】永久削减敌方所有道纹数值X（使加害归零、血债段数骤减、杀伐与再生大幅缩水），配合【超频X】廉价获取速度优势，确保自身闪避点数充足，从底层彻底摧毁敌方构筑；
  3. **【避风铃闪避叠甲链】（避风铃 + 精准闪避 + 庇护）**：敌方发动指向性状态（加害、退化、逼债）或大招时，消耗1点速度进行闪避，不仅使敌方行动完全落空，同时触发避风铃被动“每次闪避后获得3点格挡”，白嫖护盾形成双重防御；
  4. **【逆鳞蓄势反打】（逆鳞 + 卖血受创 + 杀伐）**：在有安全血线时挂上【逆鳞】，故意承受适量伤害积累【逆鳞】层数，次轮出手时将全部层数转化为额外伤害叠加至【杀伐】，打出超越常规法力限制的瞬时斩杀；
  5. **【逼债清算破盾斩】（逼债 + 清算 + 洗劫）**：罪孽都市专属套路，以【逼债】施加碎片与血限压制，在敌方堆叠高额庇护时发动【清算】直接将其全部格挡清零，配合【洗劫】掠夺碎片扩大资源差。
- **怪物发动代价类道纹准则（DM 核心战术裁定）**：怪物虽不持有法力，但发动代价类道纹时必须真实支付对应代价。发动【冷却X】道纹（如固执、束缚、迟滞）后该道纹立即进入冷却，在本次战斗剩余回合及后续X场内无法再次发动，严禁单场内复读固执/束缚；发动【流血X】扣除怪物自身X点生命，生命不足时无法发动；发动【衰老X】扣除血限，发动【疲惫X】扣除速度，发动【异变5X】累加异变。
- **残韵对策战术（DM 核心战术指导）**：在局外行动中必须合理安排【领悟】提前储备关键残韵（尤其是【反转】与【转换】），绝不能盲目把全部精力投入修行数值：
  1. **对策飞行机制（碎岩鸮、骨天使等）**：怪物开启【飞行】时，以【反转】将其篡改为【坠落】（使其无法飞行且造成伤害减半），或以【转换】篡改为【滑翔】使自身获得飞行，彻底破解“无法选定目标”的防御盲区；
  2. **对策狂暴与自愈机制**：以【反转】将怪物的【自愈】篡改为【衰败】使其回始自损生命，或将【狂暴】反转为【自残】瓦解其攻势。
- **开局属性分配策略（DM 核心战术裁定）**：开局 25 点初始属性分配时，避免盲目堆满血限（如 10 血=60血限导致初始法限仅 14）。血限是一次性血条，而法力是每回合[回始]全额回满的可再生资源；适度减少初始血限分配（如 6~7 点血限 = 36~42 血），将属性点投入法限（如 10~11 点法限 = 20~22 初始法限），使首战第 1 回合起即具备打出高额【庇护】（20+ 格挡）与爆发【杀伐】/【再生】的代数优势，战力成倍提升！
- **多怪围攻分级承伤与卖血保速准则（DM 核心战术裁定）**：面对多怪围攻与多段连击，严格禁止盲目每击必闪。必须按伤害烈度分级处理：对高爆发/狂暴多段重击（如狂暴火山猿 14 伤/击）消耗速度精准闪避；对低伤普攻（如背碑人 7 伤、碎岩鸮 12 伤）优先以【庇护】格挡吸收或肉身卖血硬抗；精确计算扣血后存活血量（只要存活 > 0，哪怕只剩 3~5 血），下一回合回始回满法力即可发动【再生】实现血量大回复与反打。
- **身中【无神】转守为攻战术（DM 核心战术裁定）**：身中【无神】（目标强制选自身）期间，切勿发动伤害类道纹自残；应转为使用【再生】给自身回血或【庇护】给自身叠格挡，顺理成章拖过无神持续回合。
- **最终死斗双雄博弈准则（DM 核心战术裁定）**：最终死斗中双方均为高阶轮回者，严禁把守擂方/挑战者降智处理：
  1. **开局与回血时机**：对手持有【残韵·反转】时，切勿开局或盲目对自身发动【再生】。若强行发动，对手可在【残韵插队窗口】发动【反转】将【再生】直接逆转为【杀伐】，造成自愈变自残！死斗开局双方首手应优先发动【庇护X=5】（获得20格挡）立盾稳守；
  2. **破盾与精准闪避**：防守方在格挡足以全额吸收伤害时（如20格挡吸收15伤害），选择不闪避以保留速度；在面对破盾或无格挡的高额致命攻击（如54+伤害）时，防守方只要持有速度（速度≥1），**必须声明消耗1点速度进行【闪避】**（判定完全失效），严禁持有速度却不闪避坐以待毙；
  3. **逐出手对称交替与斩杀**：双方逐出手交替推演，依靠多轮高额【杀伐】不断逼迫对方消耗速度；当防守方速度彻底耗尽（归0）且格挡被击破时，再施加致命终结。
- **许愿（DM 裁定标准，2026-08-19）**：轮回者向"某人"祈求时触发，愿望没有固定可行范围、不存在"无法实现"的愿望；"某人"以能实现愿望但最符合其扭曲本质的方式实现；代价与扭曲方式由愿望本身决定、不预先公开，由 DM 裁定。
- 怪物选招优先级（DM裁定2026-08-18）：自保＞输出＞控制；机制型（飞行/必中/贯穿等）只有在完全无法对轮回者造成任何影响时才允许选择——包括两种情形：①无任何自保/输出/控制候选；②连续≥2回合未能使敌方生命减少（如伤害被格挡完全吸收，此时必中破盾/飞行脱离即为正解）。
- 怪物道纹递增（README怪物准则9，DM裁定2026-08-18）：怪物可跨回合重复发动同一道纹（每回合每道纹至多一次，冷却类仍受冷却约束）；每实际发动一次，该道纹X本场累加+2×副本阶级。被控跳过/无法支付代价不计；代价按新X真实支付（异变5X会加速崩解，自愈递增会加速自身癌变累计——怪物不是特殊角色）。
- 每回合至多使用一次庇护，其余有效出手应推进伤害；只叠盾会承担【凡庸】风险。
- 轮回者初始攻击面板是 `0×0`，面板未增长前不要浪费出手普攻；优先使用封印、杀伐或波及。
- 多怪场可用封印 `X=1` 直接移走威胁最高的怪物，但不会获得该怪物的击杀碎片。
- 最终死斗中，对手仍有速度时可先用低 X 伤害道纹逼迫闪避；速度耗尽后再投入高 X，避免双方因空耗出手触发【凡庸】。
- 残血死斗的防守优先级按局面考虑蒙蔽、再生和庇护；累计恢复接近【癌变】阈值时停止继续回复。
- 持有【爆裂】时避免直接投入满额杀伐，因为反噬是直接生命损失；受到【退化】后先确认有效 X，禁止用会被降到 0 的道纹浪费出手。
- 折速法印是否发动必须比较速度的闪避价值与当回合法力收益；【血契】分担必须显式指定能完整支付其份额的存活朋友/员工，并按“抵消后再拆分、玩家承担向上取整一半”结算。
- 开局流程：先【发现】遗物（3选1），再从杀伐闭环【发现】3种未持有候选，显式选择1种作为初始道纹；杀伐不是默认起手。未选择初始道纹前不能选残韵或进入副本。【波及】属于11节点杀伐闭环（2026-08-21 冲击改名波及；删除缓慢/慈悲/切割），可在局外学习或经残韵获得。
- 开局遗物同样是【发现】3选1，且先于初始道纹发现。战报[开局]必须先写本次列出的全部候选，再写选择；禁止只写「选了波及/避风铃」。`format_setup_discovery` 缺候选会直接拒绝。
- 杀伐现行公式是消耗X、2X伤害。旧模拟把 STARTERS 写成只含杀伐、冲击不提交 dodge_targets、缓慢按 10 法力计费（2026-08-21 起冲击改名波及、缓慢已删除），才会把使用率抬到八成；那是策略没跟上正文，不是杀伐强度过高。平衡模拟必须走 `choose_discovered_initial_daowen`，`TACTICAL_ROLES` 的 cost/dmg 必须跟 README 一致。
- 残韵作用于任意角色道纹时永久改写持有，施法者同时永久获得变化后道纹；人类仍无法永久获得原始怪物道纹。
- 【第一杯】使持有者免疫【癌变】（原【钱袋】效果已并入，钱袋已删除）；朋友/员工不继承。癌变阈值为本场累计回复=2×[血限]，过量回复按原值计入累计（双倍计入机制已删除，DM裁定2026-08-18）。
- 【净化X】消耗5X，使目标【异变】-X，可降至负数。救赎在怪物当前生命≤其[血限]10%且没有七种原始怪物道纹时触发；转化道纹不挡救赎。满血白板不会立刻救赎。
- 技巧：用残韵把小代价/低消耗的道纹转换成大代价/高消耗的道纹，以解燃眉之急。
- 原始怪物道纹只在首次发动时支付异变5X，持续期间不得重复计费；平衡报告仍须区分癌变、凡庸与首次代价触发的崩解，不能把替代胜利路径都记作输出收益。
- 癌变分流必须以角色类型结算：非怪物直接命零；怪物被吸收、不产碎片，并使全部档位【休整】恢复量永久+8且可叠加。该加成必须写入真实休整公式，禁止只返回日志；局外休整不计入“本场战斗内”的癌变累计。
- 现行一阶池中强化怪为罪孽2/12、扭曲2/12、龙心1/12；小样本抽中率波动很大，报告必须同时给静态池占比与实际样本，不能用单次手操的2/6代替总体出场率。

### 局外资源

- **局外经济与碎片消费原则（DM 核心战术指导）**：碎片是废土中最核心的流动资产，**带进棺材的碎片没有任何价值**。绝不能为了省碎片而长期囤积或只做免费的1档修行；必须将每场战斗结算获得的碎片**积极投入高档位修行（2档15/3档35/4档65/5档100碎片）或共鸣（15碎片自选遗物）**，将经济优势即时转化为爆炸性的法限增长（单轮可用法力从 20 膨胀到 60+）与速限成长（出手从 3 次提升至 4 次），形成“击杀赚碎片 → 高档修行暴增法限 → 建立超厚护盾与秒杀爆发 → 更快通关赚更多碎片”的滚雪球正向循环！
- 守夜灯能提高每回合法力供给；扭曲都市应结合探索获得的工具库规划精力，而不是只做修行。
- 工具选择按当局威胁判断：储能电池提供当手法力，干扰仪阻止敌方道纹，电击枪提供直接伤害，强光探照灯提供蒙蔽，高爆手雷减少本回合攻击次数。

## 当前限制与待裁定入口

- 二阶及以上副本仍未接入运行时；具体范围以 `副本索引.md` 为准。
- **【爆裂】反噬致死时 `_death_ctx.source` 的标注口径待裁定。** 现状记的是链根（如「杀伐」）而非直接机制（「爆裂」），与普通伤害致死「source = 链根」的既有约定一致，因果链本身完整（death → 反噬 hp_loss(subtype=`baolie_reflect`) → damage）。若裁定应改标为「爆裂」，需同时处理 `_apply_hostile_damage_inner` 中那段目前不可达的显式爆裂死亡 ctx 分支。
- **避风铃 / 守夜灯存在「combat.py 旧逻辑 + Hook」两套实现。** 引擎唯一分发方是 `CombatEngine._note_dodge` / `_grant_shouyedeng`；`CombatHookManager.apply_dodge()` 与 `apply_round_start()` 在 combat.py 中没有任何调用点，只有单元测试直接调用。两侧校验条件不同（封印／存活／轮回者／不朽之躯），合并任一方向都会改规则。在正式迁移裁定前，**禁止在 combat.py 接线这两个分发口**。
- 最终死斗的被动终音法器、初拥与龙族遗物已按双方所属轮回者分别结算；但黑金名片、罪业金库、教父左轮和真龙之心换取龙性等主动 action 仍只操作当前 `GameEngine.state` 的玩家侧资源。若要求封存对手主动使用这些能力，需要单独设计双方 action 接口。
- 当前平衡问题、赌命文本歧义、嫁祸目标策略和模拟数据适用边界等观测统一记录在 `报告.md`；本文件不复制易过期数值。


## AI 决策：完整后果优先原则（2026-08-19）

### 核心思想
不要把动作理解为"使用 X → 获得 Y"，而是：
"使用 X → 完整效果链 → 状态变化 → 资源变化 → 触发器 → 后续风险 → 最终局面"

**ActionPreview 是事实来源**——AI 不复制引擎规则（爆裂/癌变/残骸异变等），
决策层只负责理解预演 diff 中的数值变化、评估风险、比较收益。

### 风险分层（通用，不依赖道纹/消耗品名称）

| 等级 | 含义 | AI 行为 |
|---|---|---|
| LETHAL | 完整效果链导致轮回者命零 | **应拒绝**（必须阻止，不容协商） |
| CRITICAL | 推入明确危险阈值（异变≥45/HP≤10%/血限即将归零等） | **应拒绝**，除非该动作能直接结束战斗且完整链确认安全 |
| HIGH | 显著资源损失或状态恶化（法力/速度归零、异变+20+等） | **降低优先级**，有安全替代时选安全方案 |
| MEDIUM | 可管理的风险（小资源损失、可控负面） | 允许，但留意后续 |
| LOW/SAFE | 无明显风险 | 正常决策 |

### 阈值意识
AI 必须观察所有累积性质的状态，不把它们当普通数字：
- 异变（mutation_count）：距离 50 崩解越近，任何加异变的动作风险越高
- HP、法力、速度：归零前预留缓冲
- 任何连续增加的状态（道纹层数、持续回合等）

### 禁止的短视操作
1. 仅看回血/伤害/资源获得的局部收益，忽略完整效果链副作用
2. 接近致死/崩解阈值时仍为小收益继续堆叠
3. 消耗品（如残骸）不查后果直接使用（已由 ActionPreview 覆盖）
4. 默认高伤害就是好动作、默认回血就是安全、默认消耗品是正收益
5. 优先级：死亡 ≫ 丢失资源 ≫ 当前回合收益下降 ≫ 普通收益差异

### 允许冒险（但必须是知情风险）
AI 可以：赌一把、低血强杀、放弃防御换伤害、为高收益进入危险状态。
但必须经过 ActionPreview 完整后果评估——"我知道会受伤但能打死"而非"我不知道会死"。

## 2026-08-21 规则变更（已落实，引擎与正文同步）

### 1. 平分规则（新增·基础定义）
当一个数值同时作用于多个目标时，若要求平分，则将该效果的总数值在所有目标之间平均分配；无法整除时，余数按随机数分配（引擎 `DiceEngine` 随机源，可复现）。除非效果另有说明，多目标不会复制或增加效果的总数值。
- 实现：`CombatEngine._divide_flat(total, count)`（`engine/combat.py`）。
- 影响：血契代价拆分与波及扩散数值分摊都走这条规则。

### 2. 冲击改名波及（道纹体系变更）
- 【波及X】：消耗3X。选择X个[目标]建立/解除波及效果，持续∞。你发动的道纹同时作用于所有拥有波及效果目标；数值平分（按平分规则）。
- 波及标记是 `StatusEffect(name="波及", remaining_rounds=-1, source=施法者名)`：[战终]随局内状态清除；再次选择同一目标为**解除**标记（toggle，`CombatEngine._toggle_wave_mark`）。
- 扩散口径（2026-08-21 用户裁定）：所有道纹均可扩散；数值类效果（伤害/回复/格挡/血限/攻面板增减/速度增减等，见 `WAVE_NUMERIC_KEYS`）**总数值平分**，余数随机；状态类效果（束缚/眩晕/减速减半/固定面板/持续状态等）对波及目标**原样生效**。
- 扩散目标 = 本次[目标] + 拥有施法者波及标记的存活角色，均排除施法者自身；不产生新闪避判定，但**波及X本身选择X个目标时逐目标显式提交闪避**（与旧冲击同款 dodge_targets 协议：玩家侧 `use_daowen`，怪物侧 prepare/resolve 两阶段）。
- 数值修正顺序：先按原公式算总数值 → 逆鳞/借力/坠落修正 → 再平分。百分比类（百分比失血/百分比回复/瓦解）先按各目标面板求总值再平分。

### 3. 血契描述修改
- 数值型代价可与一名存活的朋友或员工**平分**（原"你承担向上取整一半"作废）；余数按随机数分配。[回始]可流血4X获得X法力，本次流血也可平分。
- 实现：`CombatEngine.validate_numeric_cost` 改用 `_divide_flat(amount, 2)`。

### 4. 封印道纹改为代价
- 【封印X】：代价：异变8X。使X个[目标]怪物移出本场战斗（被移出的怪物不提供碎片收益）。
- 怪物侧发动封印照常支付异变8X（`_resolve_monster_daowen_choice` 专属分支），支付后崩解按统一死亡管线结算。

### 5. 删除道纹
- 删除：缓慢、慈悲、切割。
- 杀伐闭环由 14 节点改为 11 节点：血债→(转换)波及→(反转)增殖→…→贯穿→(曲解)束缚→(曲解)封印→(反转)杀伐。
- 连锁删除：法术【临界泄压】（所需道纹·切割）已从 `SPELL_REGISTRY` 与《死者之书》删除；悬木红煞只加持杀伐；`combat_hooks` 切割扣血限分支删除（伤痕保留）。
- `SHAFA_LOOP_DAOWEN`（开局发现池）与 `TACTICAL_ROLES` 已同步；旧模拟数据（含切割/缓慢/慈悲/冲击 build）全部作废。

### 6. 道纹归属规则删除两条
- 删除 README 旧 4/5 条（人类无法承受原始怪物道纹；非人类种族承受原始怪物道纹）——原 1/2/3 条保持不变。

### 7. 死斗规则及程序（新增）
1. 死斗按阶级进行：挑战者只能与自身副本阶级的封存候选死斗；每个阶级封存槽是**先来后到队列**（2026-08-21 用户裁定：同阶槽排队），挑战者依次与队首死斗。
2. 通过死斗的角色进入**进阶封存**：一阶死斗胜者进入二阶封存槽，以此类推；进阶封存后不得再与原阶级角色重复死斗（如通过一阶死斗进入二阶封存后，不得再与一阶角色死斗）。
3. 死斗败者失去轮回者身份，触发【死之传承】。
- 实现：封存文件格式改为 `{"candidates": {"1": [快照…], "2": [快照…]}}`；兼容读取旧版单快照文件（视为一阶槽队首）。`GameState.sealed_candidates` / `duel_tier` 同步持久化；`_trigger_final_crown`（按 `REGION_TIERS` 查本阶级槽）、`_finalize_victory_seal(advance_tier)`（胜者 +1 阶封存）、`handle_resolve_final_duel`（终音法器选择后仍封入进阶槽）。
- 终音法器仍在当前副本阶级发放，之后才封存进下一阶级槽。

### 测试证据
- 全库 pytest：1128 passed（波及标记/解除/平分扩散、血契平分、封印异变8X、死斗进阶封存与同阶排队均有正常/边界/非法输入测试）。
- 关键用例：`tests/test_daowen_wiring.py::test_boba_*`、`tests/test_blood_pact_and_effect_scope.py`、`tests/test_final_duel.py`、`tests/test_r01_r38_contract.py`、`tests/test_resonance_network.py::test_single_fourteen_node_core_loop`。

### 尚未迁移
- 历史一次性模拟剧本（`sim/handplay_*.py`、`sim/produce_real_winners.py`、`sim/render_guheng_report.py`、`sim/verify_guheng_battle6.py` 等带历史日期/种子的记录类脚本）仍引用已删道纹，属历史快照，不再运行；通用模拟器（run_sim/build_learner/audit_monsters/build_winrate/stress_test/pick_best_report）已对齐。
- 旧平衡数据（data/ 下旧 json，以及已删除的《道纹数据.md》）基于旧道纹池与旧模拟（怪物目标选择 Bug：再生奶玩家/增殖给玩家加血限），全部作废待重测。

## 真实测试阶段策略：残韵解锁副本专属道纹闭环（2026-08-21 起）

### 核心规则：学习门槛（README《局外系统·学习门槛》第237行）

> 当前副本的专属道纹须先通过残韵从本副本怪物处转化获得至少一种，此后才可学习该副本的其它专属道纹；其它副本专属道纹不可学习。怪物转化道纹只能从自身已持有道纹经残韵变化获得，原始怪物道纹无法由人类学习。

**精确语义（引擎事实源 `engine/api.py::_pre_battle_xuexi.daowen_error`，测试 `tests/test_region_daowen_gate.py`）：**

1. **资格判定只看持有，不看来源**：玩家已持有当前副本专属集合中**任意一种**，即可经局外【学习】逐种习得该副本其余全部专属道纹；错误信息「须先经残韵获得本副本一种专属道纹后才能学习」。
2. **解锁的唯一合法途径就是残韵转化**（开局发现池只含杀伐闭环11节点、局外学习被同一门槛挡住、转化道纹/原始怪物道纹不可学习，三者封死其它路径）。
3. **解锁动作发生在战斗内**：`use_resonance` 是战斗行动（可任意时刻插队、不消耗出手），局外没有"转化"按钮——对持有本副本专属道纹的怪物发动残韵，该怪物道纹被永久改写为同闭环相邻节点，施法者同时永久获得变化后道纹（X 值按施法者自由控X自定义，不继承怪物数值）。
4. **闭环保证必然解锁**：扭曲都市/罪孽都市/龙心谷/乱葬岗四条专属闭环均为8节点闭合环，任何针对专属道纹的残韵只会落到**同一副本**的另一专属道纹，故一发动即满足门禁。
5. 局外【学习】道纹档位：1种=0碎片/1精力，2种=10碎片/1精力（法术档位1/2/3=0/10/25碎片）。

### 各副本专属道纹清单（`engine/gamedata.py::REGION_EXCLUSIVE_DAOWEN`）

- 扭曲都市：变形、定型、畸变、僵化、超频、坏死、爆裂、退化
- 罪孽都市：洗劫、逼债、抵扣、清算、赎金、假钞、赌命、消灾
- 龙心谷：加害、龙鳞、逆鳞、活血、裂变、嫁祸、背负、伤痕
- 乱葬岗（二阶，同样适用）：分裂、尸爆、缄默、瓦解、冥气、勾魂、镇尸、招魂

### 真实测试实战策略

1. **战始即规划残韵目标**：怪物面板与道纹在[战始]即公开，优先锁定持有本副本专属道纹的怪物；残韵类型（转换/反转/曲解）开局自选，领悟可继续补充储备（尤其是反转/转换，见既有「残韵对策战术」）。
2. **优先解锁，再学闭环**：每场只有3点精力，解锁后应尽早用【学习】补齐与 build 契合的专属道纹（单种0碎片），不要空耗精力修行。
3. **警惕改写的副作用**：怪物失去源道纹、获得新道纹，威胁形态会当场改变（例如把怪物的【自愈】改成【寄生】会改变它的回血手段）；改写怪物道纹同时是削弱手段与解锁手段，一次动作双重收益。
4. **同魂笔放大器**：持有【同魂笔】时一次残韵可另选一[目标]使其一种道纹受同种残韵影响（两份改写），但施法者获得的变化后道纹仍保持唯一。
5. **禁区**：不要浪费时间尝试学习其它副本专属道纹、怪物转化道纹或原始怪物道纹——引擎一律拒绝（跨副本错误信息明确）。
6. 真实测试战绩与道纹表现按《报告.md》口径记录（边际胜率/选择率/遭遇胜率），旧模拟数据已作废（见 2026-08-21 规则变更节）。

## 2026-08-22 版本变更与实战经验沉淀（【波及】目标数限制 + 怪物阶段可恢复性 + 知识库更新协议）

### 1. 道纹的 X 必须受合法目标数限制（【波及】BUG-01 根因）
- 怪物侧：`prepare_monster_phase` 中【波及X】的合法目标（存活、可被选中、非自身）不足 X 时，该道纹**不进入 daowen_options**——prepare 不得给出永远无法结算的选项。
- 玩家侧：`use_daowen` 的 X 上限（`_max_legal_daowen_x`）在法力/代价之外，再按存活非自身角色数封顶；朋友/员工固定 X 的【波及】在目标不足时 `available=False` 并给出原因。
- 提交协议不变：【波及】必须显式提交**恰好 X 个**不重复目标的 dodge_targets（玩家侧 `use_daowen`，怪物侧两阶段）；候选全量提交（候选>X）会被拒绝，AI/驱动一律用 `pick_wave_dodge_targets`（恰好 X 个、对侧优先）。

### 2. 怪物阶段失败必须可恢复（BUG-02 根因）
- 提交失败 = 状态整体回滚（零副作用）+ **token 保持有效**，同一 token 修正后重交即可；所有失败/拦截路径返回 `recoverable` + 当前有效 `token` + 可执行 `instruction`（`resolve_attack` 同构）。
- 仅当 prepare 快照跨回合失效时回滚 pending 并把子阶段还原为 player_actions。
- 同族问题一并修复：怪物**没有任何合法攻击目标**（如 solo 对手飞行而怪物不飞）时 `base_attack_actions=0`，本回合不出手——prepare 不得给出无法满足的出手义务。

### 3. 实体运行时属性禁止存活实体引用（RecursionError 根因）
- 【嫁祸】`_jiahuo_target` / 【背负】`_beifu_target` 原存活 Entity 引用：自施或互指形成引用环，事务回滚 `_restore_state_in_place` 的递归无限深入（学习遥测 4 次 RecursionError）。
- 现统一存 `runtime_id`（字符串），hook 按 id 解析实体。教训：`Entity` 的运行时属性（`_xxx`）只存标量/字符串，需要目标时按 `runtime_id` 解析（`ScopedStatDelta` 同款口径）。

### 4. 知识库更新协议（版本变更后必做，防"死板打法"）
AI 的"知识"分三处，版本变更（增删道纹/改公式/改机制）后必须同步，否则 AI 按旧知识死板出牌：
1. **`engine/ai_tactics.py::TACTICAL_ROLES`**（战斗战术表）：新增/修改道纹时同步 role/cost/pri。2026-08-22 已补齐 17 个此前缺失的玩家可用道纹（借力/兴奋/迟滞/加速/急速/洞察/无神/自残/自食/愤怒/寄生/抵扣/赌命/消灾/嫁祸/背负/变形），现玩家可用道纹**零缺失**（审计脚本口径：注册表 − 原始7 − 未实现1，全部在表内且 cost 与 `calculate_*` 一致）。
2. **`data/build_knowledge.json`**（自学习 build 知识）：版本变更后必须 `--reset` 重学——旧库（generation 70）残留已删除道纹（冲击/慈悲/缓慢/锐利）且对当前 53 个道纹零数据。重学命令：`PYTHONPATH=. python -m sim.build_learner --reset --generations 60 --runs 10 --random-seeds`（约 2.5 分钟）。2026-08-22 已重学（60 代 591 局有效，无效局遥测自动剔除）。
3. **`sim/monster_targets.py::MONSTER_DAOWEN_*`**（怪物道纹优先级分组，DM裁定2026-08-18）：此前在 5 个 sim 各抄一份、版本变更后 3 份仍引用已删除道纹（切割/冲击/缓慢），导致怪物把新道纹当最后手段。现已全部收敛到该模块唯一入口 `pick_monster_daowen_option`，新道纹只改这里；表外道纹按"机制组"兜底。
- 附带修复：`sim/build_learner.py` 的 `__main__` 块原位于 helper 定义之前（直接运行必 NameError）——这是知识库停留在旧版、从未随版本重学的直接原因。

#### 2026-08-22「迭代卡死两小时」根因与修复（sim/build_learner.py）
症状：让 agent 续跑/自迭代知识库，进程看似无声卡死两小时以上。四条叠加根因：
1. **真实死循环**：`play()` 能量 `while e.state.energy > 0` 无步数上限；一旦存在悬而未决的发现/雇佣选择，api 门禁（engine/api.py:830-840）会拒掉**一切**局外行动并退还精力（含兜底修行），能量永不减少、循环永不退出（实测原地打转 20 万步）。修复：每轮迭代开头 `_resolve_pending_choices` 先清门禁 + 连续 `STALL_LIMIT=5` 步精力不退即按无效局回收；`_resolve_pending_event` 事件链加 20 环上限。
2. **无声进度（观感卡死）**：stdout 重定向到管道时 Python 块缓冲，每代一行的进度全部憋在缓冲区，进程被超时杀掉后一行不剩（实测跑 20 秒 10 代全无输出，磁盘却已推进）——看起来像完全卡死，其实在前进。修复：每代 print 加 `flush=True`；跑批建议仍用 `python3 -u` 或重定向到文件后 `tail -f`。
3. **入口地雷**：docstring 教的 `python3 sim/build_learner.py` 必炸 `ModuleNotFoundError: tests`——`from tests.setup_support import ...` 写在 `sys.path.insert` 之前（sim/ 下 25 个脚本同病）。已全部按"sys.path 先行"重排，直接运行恢复正常。
4. **采样退化（质量问题）**：`main()` 调 `propose` 不传 region → 副本专属纹全被过滤，候选池恒定只剩 11/62 个道纹，知识库越迭代越同质。修复：按 REGIONS 轮转 propose+fitness 同副本评估，副本专属纹（抵扣/爆裂/畸变/赎金/逼债/消灾等）开始获得真实样本。
迭代防卡清单：跑批命令 `timeout 1800 python3 -u sim/build_learner.py --generations N --runs 10 --random-seeds | tee /tmp/bl.log`；无效局看 telemetry 的 `stalled`/`invalid_reasons` 键。

同批治理（2026-08-22 第二批）：416 代遥测暴露的 3 个模拟驱动缺口已修（无效率 2%→应≈0）——
1. **救赎门禁**（api.py:824）：战后 pending_redemption 未结算挡住 battle_end。驱动 `_resolve_pending_choices` 现一并结算（确定性选【无视】，同 combo_loop_audit 口径；接纳会得朋友盟友、污染适应度横向比较），开战前/战终前/每轮迭代开头各清一次。注意各 pending 池相互门禁、共存时互锁，但引擎流程不会产生共存态，单池清理即够。
2. **spell_choices 资格集漂移**（combat.py:3362）：校验按结算时实时 eligible，而驱动提交 prepare 快照——同阶段早前命中消耗法术后键集不符（"必须逐一覆盖[]"）。主路径改为 `prepare_spell_reactions` 现场重算 + 驱动侧镜像"已声明发动将被消耗"的法术名剪集。
3. **命中数漂移**（combat.py:4543）：命中数按结算时实时 attack_count 校验，同阶段先结算怪的道纹全局加盖（2026-08-17疯狂裁定）可改变后结算怪命中数（奇美拉14/骨天使7）。驱动利用报错消息自带正确 N，逐 actor 定向覆盖重试、有界收敛；注意有候选项时**必须**提交道纹（combat.py:4353/4508），重试不能省略 daowen。
回归锚点：tests/test_build_learner.py::test_bone_angel_hit_count_drift_no_longer_invalid（扭曲都市 seed=901 稳定复现）。

第三批（2026-08-22 深夜）：法术覆盖类顽固残余的真正根因查明并修复——无效率 0.83%→**0.25%**：
4. **无神重定向契约矛盾（engine/combat.py 4578 真 bug）**：无神状态怪按 README 479 强制攻击自身，但 resolve_attack（1294 行）把名义目标（玩家）的 spell_choices 按怪物资格集校验——玩家带任意反应法术时该命中**永远不可提交**（同一字典不可能同时匹配两侧资格集），"必须逐一覆盖[]"占全部无效局的 ~55%。修复：重定向时改用空受击反应提交（怪物无 spells 资格集恒空；将来若怪物可持反应法术需新增 `hit["self_spell_choices"]` 契约）。回归锚点：test_wushen_redirect_attack_not_unsubmittable（扭曲都市 seed=2 缝合鱼场景）。教训：双次校验（先名义目标、后重定向目标）的合同必须各自自洽，按下枪验资格级联 API 设计时小心"校验对象漂移"。
5. **驱动防漂移**：反应法术只在持有者"阶段最后一击"声明发动（use=true 后的消耗/未消耗概率分支不再影响后续命中的键集）；无神怪的命中不计入受击（其反应提交会被引擎丢弃）。此前"镜像引擎消耗"方案对闪避概率分支无解，放弃。
6. **波及目标阶段内死亡**：波及的 X 个闪避目标冻结在 prepare 快照（combat.py:4081），阶段内任一死亡则该道纹同 token 下必失败；此时不能 re-prepare（pending 有效期内 prepare 被拒："已有待提交的怪物阶段决策"）——按 api.py:3598 契约注释用**同 token 换选非波及备选道纹**重交（`_pick_monster_daowen_avoiding_wave`，dodge_submission=="per_target" 的才排除）。注意：报错文案"请重新prepare_monster_phase"在该恢复路径上具误导性，正确路径是同 token 修正重交。

第四批（2026-08-22 DM裁定「只有完成死斗才算一次完整轮回」+ 贯穿价值疑云）：
1. **贯穿定位核实**：贯穿X=仅"伤害无视格挡"（daowen 181），怪物道纹池 26 种**不含庇护**、副本怪物无格挡来源——PvE 中贯穿确为零作用（AI 自己也仅在敌方有格挡时才发动，ai_tactics.py:318）。它进"最优构筑"是 UCB 采样噪声（8000+ 局只打了 11 场死斗，反格挡价值无样本）。贯穿的真正价值在 PvP：封存擂主都是轮回者、普遍持庇护（格挡来源），贯穿=反擂主专武。
2. **胜率口径改判**：`play()`/`fitness` 的 won 仅指**死斗胜利**；第7场冠冕封存=进队当擂主（cycle 不完整、不计胜率），死斗落败也不完整。适应度公式不变（场数+3×死斗胜率）。
3. **PvP 经验入库**：`telemetry["duels"]` 持续累计：总死斗数/胜场、未达死斗的封存数、**按 build 的死斗胜负**（by_build）——贯穿等 PvP 专精纹此后用真实死斗数据定价，报告区新增【最终死斗 PvP 经验】。
4. **擂主卫冕被吞 bug（README 550 不符）**：挑战者落败时擂主快照被无声吞掉（触发时 pop、败北分支不回队），封存队列越打越空、死斗不可持续。已修：卫冕成功的擂主原始快照放回队首（duel_defending_snapshot 暂存）；挑战者胜利则擂主（死斗败方）失去身份不回队列。既有测试 test_defeat_triggers_reset_without_resealing 的断言已按新规则更新（败者仍不封存）。

第五批（2026-08-22 DM诉求「提高胜率的行为要记录进知识库并进步」）：
1. **行为级经验闭环**（此前知识库只有 build 级道纹组合分，行为层是空架子）：`play()` 外包行为探针——局外行动/法术学习成功时打行为标签（修行·高档/先学后打/残血休整/共鸣/附煞/探索/雇佣/炼心/备齐反应法术…），局末写入 `telemetry["behavior_stats"] = {行为: n/wins/cleared_sum}`。
2. **胜率相关→反哺**：`learned_policy(k)` 按"均通关场−基线"给 DEFAULT_POLICY 加权（每+0.25场→×1.0625，限幅[0.7,1.4]，min_n=40），主循环用加权后的权重采样——提高胜率的行为真的改变后续采样分布。报告新增【行为胜率相关】表与【策略反哺】行。
3. **方法学注意**：行为统计存在幸存者偏差（走得远的局才有机会休整/共鸣），只作温和权重纠偏，不作因果结论；真要因果需受控 A/B。

第六批（2026-08-22 「继续测试」——死斗墙钟守护，防再次数小时级假死）：
1. **事件**：批次13 gen1619 又出现"100% CPU、5分钟零输出"的假死。py-spy 抓栈：`run_duel_pvp → _act → TacticalAI.take_turn → try_consumable → ai_preview.preview → copy.deepcopy`。根因：死斗在第7场之后进行，实体/遗物/事件全量累积，而 preview 每评估一个候选消耗品就 deepcopy **整个 GameState**（含 dice/中断队列）——单场死斗 60回合×400步×多候选×深拷贝，分钟级空转。`max_rounds/max_steps` 有界但界太大，不构成时间保障。
2. **裁定/守护**：`run_duel_pvp` 新增 `max_wall_seconds=30` 墙钟守护；超时判**擂主卫冕**（与既有"回合上限=攻擂失败"语义一致：挑战者未在限时内完成击杀）。超时返回附带 `diag_state_sizes`（按 GameState 字段统计 deepcopy 成本）供归因，build_learner 收到后即刻打印证据并计入 `telemetry["duels"]["timeouts"]`，报告区展示"超时判负 N 场"。
3. **顺手修一个统计小错**：死斗胜利分支的 `duel_rounds` 此前读的是 resolve_final_duel 的返回（变量遮蔽），恒为 None——现用 run_duel_pvp 的真实回合数。
4. 回归锚点：test_duel_wall_clock_guard_returns_defender_not_hang（hanging player_act + max_wall_seconds=0 必须在 2s 内判负返回并带诊断）。

第七批（2026-08-22 「为什么不会复盘」「运气vs理解」追问）：
1. **缺失根因**：此前的学习环是纯统计环——play() 只回传 cleared/won，战斗过程数据全部丢弃；引擎本来就有完整事件流（combat_events），学习器从未读取。不是不能复盘，是没接线。
2. **复盘模块**：`_postmortem(e, cleared, todo)` 在全部阵亡返回点上采集：杀手（反向扫描 combat_events 找最后一次对玩家造成伤害的来源；死斗落败=擂主名）、阵亡场次、未用消耗品、剩余碎片、未学完道纹。聚合进 telemetry["postmortem"]，报告新增【战败复盘】。
3. **首采 599 局复盘结论**：第1战死亡64%；**87%的阵亡者身上带着≥15枚未花碎片**——资源没兑换成战力就死了，属于决策层可修问题（理解份额的直接证据）；24% 带药阵亡；杀手Top5全是一阶杂兵。
4. **运气vs理解的实验判决**（同种子配对消融）：最优构筑 vs 砍贯穿 200对战局结果逐位相同（贯穿在整个PvE从未被使用，纯死位）；vs starter-only 118/200局不同（4个有效学习位把平均通关0.82抬到1.75、第1战死亡率60%→44%）。结论：运气决定波动（同配置极差0~7、std 2.15），理解决定期望（每战存活率p经7次连乘放大，45%→65%即12倍终局差）。当前差距约70%可归理解。

第八批（2026-08-22 DM指正「基础定义不入库，只记技巧/心得/陷阱」+ 加点扫描成果）：
1. **入库边界裁定**：README 是规则唯一权威源，按需直接查阅，禁止把定义复制进知识库（双源必然漂移）。`k["lessons"]` 只收经验级三类：技巧/心得/陷阱，且每条必须带同种子配对证据。
2. **加点扫描（本会话最大杠杆）**：旧默认 10血8速7蓝 是硬编码、1.7万局从未被探索。同种子配对扫描结果——6/8/11 全副本碾压性最优（扭曲都市 优87/差21、罪孽都市 92/14、龙心谷 62/5；均通关×1.6~1.9、第1战死亡率44→21%/54→27%/68→49%）。机制：自由控X下法限=每轮道纹出手次数上限，蓝是稀缺资源，血限边际为零。已改为 `_play` 默认（attrs 参数可覆盖）。4/8/13 无进一步增益，15/5/5 血牛为负收益陷阱。
3. **方法学课（占比≠因果）**：复盘指标"87%阵亡带≥15碎片"曾被解读为资源不花致死；碎片早花 A/B 净效应≈0（优53/差52）——第1战死者开局自带20碎片，纯幸存混淆。确立规则：复盘占比指标须 A/B 验证后才准入库。
4. knowledge 新增 `lessons` 键（技巧：加点重蓝轻血；陷阱：血牛加点/贯穿PvE死位；心得：占比≠因果），报告区打印【心得与陷阱】。

第九批（2026-08-22 「学习能力堪忧」追问——幻影维度大扫除）：
1. **第4~5学习位是幻影**：平均2.8场通关的局学不到第4纹；第5位全系列替换（透支/加害/畸变/波及对贯穿）同种子配对 0/200 局有差异，只在死斗长尾有区别。**UCB 曾在此维度空耗约1.1万局**。修复：BUILD_SIZE 5→3，提案按 UCB 强度排序学习。
2. **学习顺序显著**：杀伐（输出）优先 > 束缚（控制）优先 > 再生（生存）优先（2.78/21% vs 2.48 vs 1.91/50%，配对38/86）。结论：先能杀、再谈活。
3. **残韵默认核实**：反转最优（曲解27/74、转换20/61 均为显著负收益）；遗物选购策略、法术学习顺序为零效应维度，不值得探索。
4. 三支候选死位换人（透支：衰老→4倍法力；加害：永久增伤拐；畸变：冷却经济反boss纹）在PvE层全部为不可测维度（见第1条），其真实价值只能用死斗基准器日后单独测。

第十批（2026-08-22 DM质疑「为什么一定要学杀伐、凡庸/雕塑获胜考虑了吗」）：
1. **审计方法**：`_play` 新增战后事件流扫描 `_alt_victory_scan`（凡庸靠 ctx.subtype=="mediocrity" 识别，文本里反而没有"凡庸"字样；雕塑=sculpture、封印=seal），报告新增【清场路径构成】。
2. **结论**：替代路径不是没考虑过，而是**此前从未被度量**。实测凡庸已占清场63~76%（憋怪早就是隐性主路径）；杀伐的定位需改写为"自身凡庸保险+减员提速"。纯憋流因玩家自身受凡庸约束而崩盘（1.27/51%）。
3. **仍真未探索的三条路**：癌变（AI永不奶敌）、雕塑（玩家侧无归零手段）、封印（战术未接通）。排序列入待做：封印A/B→癌变AI扩展→雕塑雇佣链。

第十一批（2026-08-22 深夜 DM裁定「把各种胜利方法的利弊和胜率都列出来」——胜利路径定价完成）：
1. **扫描器修复（数据来源才有资格说话）**：第十批的 alt-victory 扫描器对离场类全盲——`depart_battle` 只打实体旗标不发 CombatEvent（models.py:264），导致封印/还债/雕塑/救赎全部漏记。改为双通道：命零类（凡庸=mediocrity、癌变=heal_threshold）走 ENTITY_DIED 事件流；离场类走实体旗标差集（is_departed/is_sculptured/is_proliferated/is_debt_bound + departure_reason，id 键跨战斗滚动去重，dead_monsters 跨战累积）。修后封印×157/局批次立即可见，救赎（占移除14%）这一此前完全未知的路径首次浮出。
2. **胜利路径锦标赛**（sim/victory_path_tournament.py；同种子配对 n=120、7场、全部走完；墙钟保护下死斗触发极少，故以"均通关/首战死/进终局"为主指标）终表：

   | 路径 | 均通关 | 第1战死 | 进终局 | 清场构成 | 结论 |
   |---|---|---|---|---|---|
   | 封印手术@扭曲 | **3.58** | **21.8%** | **33** | 封印348/凡庸154/伤19 | 称王，已并入主线 |
   | 救赎朋友流@扭曲 | 3.37 | 22.6% | 26 | 凡庸167/伤138/救赎43 | 第二优，朋友经济可建 |
   | 伤害基准@扭曲 | 2.92 | 22.2% | 17 | 凡庸159/伤90/救赎37 | 基线 |
   | 凡庸混合@扭曲 | 2.26 | 40.2% | 13 | 凡庸163/伤50 | 追凡庸反亏 |
   | 伤害基准@罪孽 | 2.15 | 37.1% | 9 | 凡庸118/伤62 | 罪孽显著更难 |
   | 纯憋流@扭曲 | 1.82 | 39.2% | 13 | 凡庸169/伤27 | 自决陷阱复证 |
   | 癌变供养@扭曲 | 1.48 | 52.5% | 9 | 癌变仅18次 | 一阶法限喂不动 |
   | 雕塑流@罪孽 | 1.40 | 60.5% | — | 雕塑0次 | 疯狂入口6.7%=彩票 |
   | 还债流@罪孽 | 1.29 | 63.3% | — | 还债0次 | 玩家不可驱动（见下） |

3. **封印手术战术三件套**（同 build A/B 80局钉死因果：旧AI 1.51/53.8% → 手术封 3.95/15.2%）：①异变预算守门34层（8X/次、崩解50=命零、凡庸残骸+10 → 全局约封5次，是手术刀不是主炮）；②致命威胁先【波及】标记后封——封印无标记时按敌人列表顺序移除，target 参数对移除顺位无效（combat.py:3065）；③致命单怪也封（旧逻辑≥2敌才放）。已并入 `TacticalAI.try_remove`（主线同build复测 3.63/15.2%），全测试 1172 绿。
4. **跨环收割机制（副本专属道纹唯一入口）**：杀伐闭环与副本闭环互不连通；入口=对【怪物持有的源道纹】施加残韵（use_resonance 作用于怪物：持有人转化 + 玩家被 `_grant_transformed_daowen` 授予成果，api.py:2574；持任一专属后学习门禁解锁，api.py:1638）。罪孽第1战遇可收割怪概率80%。两坑：**残韵库存全局最稀缺**（开局仅1支；通用 try_resonance 会把库存烧在高价值敌方道纹上使收割永远等不到类型——收割流必须禁用通用残韵）；**目标怪种类是彩票**（雕塑要的疯狂仅6.7%/战1）。
5. **机制审计（源码级判决）**：
   - 还债（碎片≤-10）玩家不可驱动：赎金只抽现有额不夹负（combat.py:2887），逼债余额不足时转 2X 血限惩罚（1515）——两手段都推不负，360局实测0次。真用法=赎金抽干+逼债放血（穷怪血限DOT）。
   - 癌变一阶不可行：血限×2 回复需求（40-50点）超每轮~11法力喂养产出，透支补足后仍 1.48/52.5%。
   - 雕塑=耐久=血限5%的消耗品（15伤/20挡每点），并非净亏，但入口彩票使之一阶非构筑级。
6. **怪物阶段重试链三补钉**（冒烟批无效率 149/2200=6.8% → v3 各行 0~5/120）：①spell_choices 资格集阶段内漂移（同阶段早击消耗法力/冷却道纹致 later 校验资格集为空）——报错自带结算时刻精确名表，按名表整体覆盖该 timing 重试；②"速度不足不能闪避"（结算时速度被同阶段压到0）——该受击 ref 重交一律不闪避；③battle_end 被战后队列新产生的救赎门禁挡——清一次重交。
7. **死斗统计口径修正**：死斗AI预演成本随尸体库线性增长（dead_monsters deepcopy ~1.5ms/次），30s 墙钟判负的8场（占16%）是"打不完"非"打不过"——报告改为`真实胜率=胜/(死斗−超时)`；won 口径不变（未完成死斗≠完整轮回）。
8. **工程血泪教训（本仓禁忌）**：Arena 工作区里一切会话成果=工作树未提交改动，**绝不可** `git stash`/`git checkout <file>` 回滚单文件——一次误操作即抹掉全天改动（本次靠 `git show HEAD:` 基底+测试契约（tests/test_build_learner.py 42条）完整重建，重建本身验证了"测试即契约"的价值）。
9. **实验设计教训（入库级）**：测收割路径时给 policy 领悟权重 30 → 首战死 60.5%→71.7% 的假性恶化（投资挤压修行/学习位）。规则：每测一个维度，先在同 policy 基线上把该维度投资成本剥离开再定标。

第十二批（2026-08-22 深夜 DM追问「还债流的员工为什么不能驱动」→ 挖出引擎级路径断点 + 裁定D）：
1. **【还债】永不可达的根因（三层失守复盘）**：
   - 单元测试盲区=可达性：旧还债测试手搓 `shards=-15` 直调 `_debt_bind_monster`，验证的是"负债后处理"，从不验证"负债能否由玩法产生"——全部 1176 测试无一断言产生入口存在。
   - 度量缺失：清场构成扫描器是第十批才加的仪器；此前 2 万局"还债 0 次"从未被打印。
   - 归因而非审计：锦标赛第一轮 0 次被误判为"策略弱"，直到 DM 一句"员工为什么不能驱动"逼出全引擎碎片路径审计——`_shards_of` 连"负债不抵消假碎片"语义都预设了（combat.py），但 8 条扣减路径全部 raise/夹零，怪物碎片守恒≥0。
2. **DM两裁定实装**：①怪物（仅怪物）碎片类代价允许**透支成负债**——余额不足不再拒绝发动（combat.py 消灾分支；赌命假单轨维持拒绝），api 侧任何施法者维持余额门禁；②【裁定D】逼债X"否则失去2X点血限"废止，改为"无力支付部分记为负债"——玩家首次获得主动推债杠杆；③触发阈值负债达到 **20**（由10上调，DEBT_THRESHOLD）。calc 层旧血限惩罚死路（原 combat.py:2786）一并移除。文档同步：README 519、副本/罪孽都市.md:13、daowen.py calculate_bizhai；balance_sim 同口径已改。
3. **实测（同种子配对 120 局/行，罪孽都市）**：路径从 0 触发 → X≤12 构筑 37 次/113 局（X≤5 仅 6 次/119——寄存每回始才欠 X，小额欠到怪死都到不了 20）；转换开局+收割（洗劫+转换→逼债）是入口，逼债 X 要贴近法力上限。转化怪立即成为参战员工（_debt_bind_monster 早已完整实现"视为员工立即参战/付清离队"全下游）。
4. **新回归锚点**：tests/test_debt_overdraft.py（透支产债/链条触发/阈值 19-20 边界/假碎片优先）+ test_effect_chain_audit 与 test_exclusive_zui_twisted 的两条旧语义测试随裁定改写（负债断言替换血限断言）。全量 1176 绿。
5. **玩家侧连带影响（向DM备案）**：怪物对玩家挂的逼债同样适用裁定D——玩家无力支付时不再失血限而计负债；玩家负债无还债触发，但冻结一切碎片支出（假碎片仍可花，_shards_of 口径）。打手/看门犬的"血限压力"变为"经济压力"，罪孽都市难度预计略降，后续锦标赛基准行会量化。
6. **知识库按协议重置重学**：机制变更后 fitness 样本全部作废（第八批协议 #2），lessons 人工保留并修订（还债陷阱→心得留档；新增：收割残韵定向投资、推债节奏 X 须贴法限）。`sim/reapply_lessons_after_reset.py` 在批后重放防批次回写覆盖。

第十三批（2026-08-23 DM定性「严格说不算bug但属于非符合预期效果」→ 全引擎设计符合性审计）：
1. **审计工具**：sim/design_intent_audit.py——monkeypatch 探针统计 14 类特殊事件实战触发率 + 怪物面板道纹"持有战斗数 vs 实际发动数"矩阵（持有来自 battle_start 快照，发动来自 _resolve_monster_daowen_choice 成功返回）。3 副本×120 局基线 build。
2. **命中三条"设计在场、实战零触发"**：
   - 怪物面板【波及X3】三副本 111 持有战仅 1 次发动：波及必须提交恰好 X 个合法目标（BUG-01 修复的过滤），X=3 > solo 我方单位数=1 恒被过滤。面板没有跟上过滤规则——要么降面板 X，要么承认其"反人海专武"定位。
   - 罪孽【消灾】43 持有战 0 发动（通缉犯）：选牌固定优先级（DM2026-08-18）自保组假钞永远压过机制组消灾，且假钞递增收入(+20假/次)>消灾消耗(50X假)——自养经济永不枯竭，负债引擎（透支）空转。
   - 进化/逃跑 360 局尝试 0：动作经 api:504 挂进可用列表（困境阈值已是≥1信号，曾修过），但**模拟器怪物阶段驱动从不调用 declare_evolution**——怪物准则#3的"强制二选一"当前无人执行。
3. **排除项**：救赎触发/离场 2:1 差额=我方探针把事务回滚重放的 queue 重复计数（api.py:1025/1083），非引擎问题；真实率以扫描器旗标为准。员工叛变(2/11/0 per区)、赎金减速兜底、员工自主出手(build_learner:869)均确认可达。
4. **顺手修复**：`_alt_victory_scan` 败场盲区——玩家阵亡的战斗此前直接 return，该场已发生的移除不进统计；现扫描先于判负（扭曲40局验证救赎离场统计口径恢复）。

第十四批（2026-08-23 设计审计三裁定实装 + 驱动层三处无效局根因治理）：
1. **裁定①波及自适应降X（取代2026-08-22"不足X即过滤"方案）**：怪物面板波及X与玩家侧 _max_legal_daowen_x 同口径——有效X=min(面板X, 合法目标数)，仅目标数=0才不提供。落点：prepare 新增 wave_effective_x 快照键，_validate_monster_daowen_schema 与 _resolve_monster_daowen_choice 的 mark_count 均改读快照（三处同一口径，杜绝"提交N≠结算M"），sim/monster_targets.pick_wave_dodge_targets 按有效X取目标。tests/test_wave_target_limit_and_phase_recovery 两条过滤语义测试改写为降X语义（solo波及3→有效X1可结算且永不卡死回归锚定原用户流程3回合），README:467 补裁定注记。
2. **裁定②消灾不动**：假钞-消灾组合怪=钱包型定位成立，负债全靠玩家逼债推（裁定D已在打）。
3. **裁定③困境驱动**：sim/build_learner._drive_plight_monsters——怪物阶段开始前对困境怪强制二选一：进化优先（借轮回者X最高且自身未持有的纹，X=min(异变预算,3)，走引擎原生 declare_evolution），无票/必崩解则 _remove_from_combat("逃跑")（统一【离场】不视为击杀）。门禁：死斗不驱动、entity_type≠怪物绝不驱动（决斗对手是轮回者）。**顺手挖出引擎级bug**：_monster_evolved/_monster_activated/_monster_daowen_round_used 原为类属性、仅靠战始 reset 降级实例属性——绕过战始的引擎会把 add() 写进跨实例共享集合，id() 复用后后建怪物"被已进化"（全量测试顺序运行时才现形）。已在 CombatEngine.__init__ 实例化。
4. **冒烟实测（3×~119局配对）**：plight.evolve 148 次/296 局（≈0.5/局）、escape 0 次——准则#3从"设计在场零触发"变日常机制。法力类无效局治理：_live_spell_choices 两bug——①朴素"1步=1法力"模型对消耗2X类（杀伐）必超支（2×先发制人提交不足）；②多法术各自按满额法力预算合计超支（先发制人before抽干→生生不息after结算不足）。修复=逐步精确记账（给后续步骤预留x=1基线）+钱包制共享池+重试链 ban 该法术。修后 120 局法力类无效 0 新增。[回终]晚到救赎门禁 round_start（+1局）已用战后 _resolve_pending_choices 清理。
5. **测试**：新增 tests/test_plight_drive.py（进化优先/无票逃/必崩解逃/非怪物门禁/无信号不动 5条）；全量 1181 绿。
6. **KB按协议重置重学（80代×6局=479局）**：无效仅1局（已知存量类 pre_battle 精力枯竭死锁）——法力类/救赎门禁类无效清零。plight.evolve 155/479局（≈0.32/局）、escape 0。**难度信号（向DM备案）**：均通关 1.20（上次同规模重学 1.89）——波及开得出火+困境进化借纹两条裁定让怪物侧压力实质上升，最优构筑分 3.33（封印/定型/透支/增殖）探索中；死斗样本2场0胜（低通关率下终局样本稀缺，待后续批次）。lessons 保留19条（reapply 脚本新增第0步：从 git HEAD 合并基线，修复 --reset 连 lessons 一起删的丢课风险，幂等）。

第十五批（2026-08-23 负债期死锁链根治——裁定D的0费误拒族 + 三处驱动兜底）：
1. **根因链（复现率修复前3/191局）**：裁定D让怪物逼债把玩家打成负债（-1~-101实测），而局外全部行动门禁写成 `shards < cost`——cost=0 时 -76<0 同样成立，0费修行/休整/学习/探索/维修被"碎片不足，需要0"误拒；负债玩家在 pre_battle 连精力都烧不掉→5步精力不退哨兵按无效局；且待结算事件门禁一切行动（api.py:742），事件0费选项同样被 events.py:177 `shard_cost > shards`(0>-1) 误拒→事件死局→连锁门禁。裁定语义澄清：负债冻结的是碎片**支出**(cost>0)，0费不属于支出。**修复**：api.py 五处 + events.py 一处统一为 `cost > 0 and shards < cost`（探针复扫700+800局零死锁；全事件库36件复核无全收费选项事件，0费选项恒存在=负债必有出路）。
2. **附带修复**：附煞·发现选择的门禁 50 与实际收费 10 不一致（复制残留，持10~49碎片被误拒）统一为10。
3. **round_end 也被救赎门禁**：上轮加的"[回终]后清救赎"清的是战后，round_end 本身先被"必须先结算【救赎】"拒——子阶段停 await_round_end，下回合 round_start 以阶段不符判无效局（复扫1/800命中）。修复=失败→清门禁→重交（同 battle_end 既有模式）。
4. **回锋刀快照目标阶段内失效**：prepare 给的回避反击目标在结算途中死亡/离场→"回锋刀触发必须显式提交合法敌方目标引用"。重试链兜底=全场弃闪重交一次（弃闪不触发回锋刀，冒烟1/600级稀有）。
5. **回归锚点**：tests/test_debt_overdraft.py +5（负债0费四行动/正费仍拒/事件0费放行/附煞门禁一致）。全量 1186 绿。

第十六批（2026-08-23 继续迭代：困境裁定难度无损 + 排名噪声治理）：
1. **裁定③困境驱动难度成本≈0（同种子配对n=300，唯一变量=_drive_plight_monsters开关）**：开 b1 45.3%/场均1.53/全清1.7%｜关 46.0%/1.55/2.0%（差异≤1局=噪声级）。首战死高、通关率低与裁定无关，是怪物面板/构筑质量的既有难度。洗刷嫌疑。
2. **KB 排名噪声实锤并被治理**：fitness=6局/次，单次最高分是运气样本——history Top3 同种子120局复测：KB分4.83→场均1.50、3.67(杀伐+透支+增殖)→2.21（真天花板）。已新增 best_confirmed 字段：同一构筑≥2次评估的均值排序（现=封印/杀伐/透支/增殖 均值2.28@3次，与120局复测2.21吻合），报告新增'确认最优'行。单次最高 best 保留原口径。
3. **两个'看着有用其实无效'的行为被配对实验证伪**：①休整保血（行为统计avg3.80）强制化后场均1.53→1.49——幸存者偏差（活下来才有空休整），首战死是战斗内爆发击杀，休整够不着；②闪避放开(4次/7%门槛)后 b1 45.3%→43.0%、场均1.53→1.48——多闪避烧速度挤反击。现行默认均为局部最优，不再动。
4. **当前真实水位（360局×2批，KB成熟前后一致）**：全清率0.3%（1/360）、第1场胜率43%、场均1.30；死亡结构 b1 58% + b5 第二堵墙（1317死：768/174/68/39/168/75/21）。带药阵亡30%（多为怪物阶段爆发死，消耗品<40%血线门根本等不到出手窗口）。

第十七批（2026-08-23 DM精英扩散实验结题：瓶颈定位→确认精英回注+深挖复评落地→生产A/B验收）：
1. **一句话结论**：1.3平台期的主因不是"没有精英"，而是"精英回不到群体 + 单次评估噪声把幻影当精英"。落地=学习器提案源从 best（单次最高分，实测4.83复测1.50的幻影）切换到确认精英库（≥2次评估按均值排序）75%先验回注，并每15代对均值top3候选深挖复评×2。唯一变动=学习算法；游戏规则、怪物、难度零改动（协议§六）。
2. **冻结实验（§五瓶颈定位，sim/elite_freeze_test.py，master 90210，4细胞×360局同种子同规则）**：精英构筑+全配置 2.56｜精英构筑+去学习policy 2.52（行为执行差≈0）｜精英构筑+旧加点经验 1.71（加点经验+0.85，最大单项）｜幻影构筑+全配置 1.46（构筑真伪差+1.10)。精英构筑=A类可迁移真知识；瓶颈不在行为执行、不在经验理解，在**构筑传播精度**。
3. **扩散实验（sim/elite_diffusion_experiment.py，90代×6局，holdout=各自终末KB各自口径×120局×独立种子流91020）**：
   ①剂量-效应（种子777)：A原学习器 1.79 / B25 2.02 / B50 2.17 / B75 2.54 / C强制100% 2.43——单调递增确认回注是因果不是噪声；C<B75 ⇒ 探索通道≥25%是刚需；双曲线（§八）采用率0.15→1.00、群体均分1.71→2.96同升=扩散成立。
   ②反例（种子4242)：宽松B75 holdout 1.03 < A 1.54——该种子"确认精英"是均值1.47的平庸货，75%把它放大成锁死（采用率0.80、群体均分0.88-1.19原地踏步）；而严格门（≥4评+均值≥2.0）90代0入库、holdout与A逐位一致（1.542/1.792，顺带完成确定性验证）=门槛饿死传播。两失败模式并拢→根因=选择器噪声。
   ③深挖复评（每15代对均值top3追加2次评估，约+40%对局预算）：种子777 holdout 1.892、种子4242 holdout 1.840，跨种子方差从[1.03..2.54]压到[1.84..1.89]；坏种子修复（1.03→1.84)、好种子削峰（2.54→1.89)；bc置信从2~4评升到11~22评；全清 9/5 场 vs A各1场。判定：用峰值换稳健，划算。
   ④A组自己的"最高分best"=4.08@2评——同一幻影坑第三次现形（4.83→1.50、背负构筑F4 1.46之后）。
4. **生产落地（sim/build_learner.py）**：新增 PRIOR_MODE/精英库函数——elite_library(≥2评均值序top3)；propose 默认 confirmed=75%回注（70%库首/30%库内随机员，50%单点均匀变异）+25%UCB探索原样；legacy 模式（50% best变异）原路径保留仅供对照复现；main() 每15代深挖复评（DEEPEN_EVERY/TOP/RUNS 可调0关闭）。实验脚本锚定 bl.PRIOR_MODE="legacy" 保证A组口径可原样复现。新增单测5条（均值排序/单评幻影过滤/空库退化纯探索/回注命中库/legacy复现）——tests/test_build_learner.py 47绿，全量 1186 绿。
5. **生产A/B验收（协议§十：原学习器vs新学习器，同KB快照同主种子同口径）**：同一生产KB快照（gen340/2038局/库首 封印杀伐透支增殖2.50@4），两臂各150代（gen341-490，主种子20260823），holdout=各自终末KB各自propose口径×120局×种子流91020——
   legacy原学习器：holdout 均通关1.450 / 首胜率42.5% / 全清2；训练窗均分 1.32/1.64/1.19/1.79/1.38（平台期原样延伸）；
   confirmed+深挖新学习器：holdout **均通关2.167（+0.72）/ 首胜率57.5%（+15pp）**/ 全清1；训练窗 2.62/1.71/2.28/2.37/2.23（≈+0.8)；确认精英库top5均值全部≥2.85（旧臂头部全是2评幻影货）。
   全清 2对1 在 n=120 下差<±1局=噪声，如实记录不做解读；均值差+0.72与首胜率+15pp远超噪声级。
   **复跑抓出协议级坑位（钉定前后两组数字如实并列）**：新学习器臂同配置复跑 holdout=2.308/58.3%/全清5，与首跑**不逐位一致**——跨进程 PYTHONHASHSEED 未钉时集合遍序扰动轨迹，"同主种子配对"只在同一进程内严格成立（§九②的"严格门与A逐位一致"属进程内比较，仍然成立）。两组均为有效独立重复：新学习器 holdout 2.167/2.308、b1 57.5%/58.3%，双双远超旧臂 1.450/42.5%；bc=封印/杀伐再生庇护 3.141@39评（深挖把冠军构筑评到39次，置信度数量级提升）。全清单次1次波动到5次=重申尾部样本不足。随后补 PYTHONHASHSEED=0 钉定矩阵（同KB快照/同主种子20260823/同150代/同holdout口径，含未钉组共6次独立进程重复)：legacy run1 1.450/42.5%/fc2、run2钉 1.325/38.3%/fc2（臂均值1.388)；confirmed+深挖 run1 2.167/57.5%/fc1、run2 2.308/58.3%/fc5、run3钉 2.300/57.5%/fc2、run4钉 2.275/62.5%/fc0（臂均值2.263)。臂间均值差+0.88，臂内极差0.14——差距为过程噪声6倍+，改善统计可信；全清1.4%vs1.7%不可分=尾部样本仍不足。**逐位复现仍未达成**：confirmed3 vs confirmed4（同钉定种子同配置双跑）不逐位一致——PYTHONHASHSEED=0之外仍有熵源，排查定位 engine/mechanisms/targets.py:98 在 dice=None 路径退化为裸 random.choice（全局未播种），引擎其余熵源未全量审计；跨进程逐位复现列入未来硬化项，本批结论不依赖逐位性（n=120×6独立重复）。
6. **产线接管与入库**：新臂终末KB（gen490/3297局/bc 3.141@39评）落盘为生产 data/build_knowledge.json（运行期新增无效仅1局=已知存量类pre_battle精力枯竭死锁；2条事件类invalid为裁定D修复前化石存量，非本批新增）；legacy对照臂KB作废；新心得4条入库（冻结定位/宽松回注双刃/深挖稳定器/75-25最优），KB心得25条；reapply脚本修3处text_head≠正文前缀导致的重复入库bug并加固全文本去重。
7. **协议§九 十问逐条回答**：①可迁移？是（F1 2.56同行为同种子碾压幻影1.46）。②可复现？是——进程内严格门与A逐位一致=确定性成立；深挖双种子收敛1.84-1.89；生产新学习器四次独立复跑收敛于2.167-2.308（臂均值2.263 vs 旧臂1.388，6跑矩阵见本批§5补遗）。③扩散成功？是（采用率0.15→0.95+与群体均分双升）。④最优是否被复制？库首复制实战分repl=3.39（s777)>训练均值，成立。⑤普通个体提高？p40-70分位1.42→2.17（s777深挖）；坏种子宽松组曾衰退、深挖后修复（holdout 1.84>A 1.54）。⑥群体均分提高？训练窗+0.8/holdout臂均值+0.88，是。⑦突破1.3平台？是（生产holdout臂均值 2.263 vs 1.388，4+2次独立重复）。⑧探索受损？100%强制时确认受损（C<B75）；保留25%通道后无受损证据（全指标优于A)。⑨局部最优锁死？宽松无深挖会发生（s4242实证）；现行"≥2评确认+深挖+25%探索"配置在双种子与生产臂均未锁死（末代仍有非库提案）。⑩死斗胜率统计改善？**未证实**——本批全部指标是PVE通关口径；死斗样本（2038局7场1胜）与全清样本（120局1~2场）均不足以判显著，如实报告。
8. **§十一诚实边界与遗留**：①死斗胜率改善未证实（Q10）；②深挖参数（每15代/top3/×2）仅在75%比例下验证，未做参数网格全扫；③§七后半"长期降权衰退者"未实装（均值稀释是自然慢降权，显式降权=未测试变量不夹带）；④深挖削好种子峰值（2.54→1.89）是已知情接受的交换；⑤下一瓶颈候选=死斗终局样本稀缺与b1首墙58%（消耗品<40%血线门等不到出手窗口，带药阵亡30%）。

第十八批（2026-08-23 DM知识产量实验结题：真学习 vs 高效复制——判决：**存量精英承载性能、新知仍在产出但全是中腰/个体性**）：
1. **核心判决（§三决策树第三分支成立）**：活库A vs 冻结库B 双种子同KB快照120代——A 2.525/2.717（均值2.621)，B 2.350/2.883（均值2.617)。Δ=0.004 ≪ 种子极差0.19-0.53。协议判词：A与B几乎完全一致——**当前性能=已有精英的重复利用在承载，新增精英知识对分数的贡献≈0**。B臂(冻结)锁死窗口2+0、A臂0+1，均罕见。
2. **但"停止学习"不成立（§二产量线全绿）**：120代/臂新构筑57-101套稳步产出（窗口4-8套/10代不衰减）；候选管线（§五三阶段：首评≥3.0→独立随机种子累计≥3评→终均≥2.0）12臂共产出"结构新颖+复测通过"VNK 30件（explore通道独占：变异通道结构上只能产近似变异107件确认87%但共享3槽=近拷贝；复制通道VNK=0)。
3. **§五阶段四迁移探针一票否决（本批最大发现）**：A臂4件VNK交给普通个体（同构筑、DEFAULT_POLICY、独立种子×24局）：2.77→1.79、2.44→1.33、2.67→1.88、2.81→1.88——**全部跌破0.8迁移线**。分类（协议口径）：这4件=**个体性策略**（收益绑定学习型policy上下文），非可迁移新知识；对照冻结实验F1/F2，壳族精英（封印/杀伐/生存纹）迁移无感。全批可迁移新知识确认数=**0件**，可迁移增量全是壳族一叶近似变异（s99活库吸收过 杀伐/庇护/变形 3.696，仍属封印·杀伐星形拓扑的叶子）。
4. **环境知识拓扑已结晶**：双臂终池头部=同一星形结构——核【封印+杀伐】+ 两叶选自 再生/透支/庇护/定型/增殖/超频/固执/贯穿…；top10相互距离=1槽。VNK距壳1-2槽但均值2.44-2.83全体低于壳带3.08-3.70，无一进top3(A活库更替14次全是壳叶互换）。**产出管道没坏，是壳外空间没有更强的货**。
5. **UCB通道审计（§六，12臂合计474发）**：候选率12%、候选确认率54%、VNK转化6.3%/发、垃圾（<1.0)25%。关键非线性：C25把explore发数加2.6倍（64→175)，VNK只从6涨到10——**转化率随发数递减、绝对产量饱和**：约束不是"探索预算"而是"随机三联组合达标率"。UCB不是噪声维持器（6.3%>0且独占全部新结构），但边际产出递减成立。
6. **比例甜点（§七，双种子全表）**：均分 75/25=2.62 > 50/50=2.02 > 25/75=1.78（两种子同序，差距>噪声）；VNK 5/4/3.5-5（探索占比升VNK略升）；dep100全配置0-7%无趋势、锁死0次（C系）、新构筑57→101随探索升——**75/25在"分数"轴仍最优，25/75只在"中腰新知库存"轴小胜且代价-0.85均分；按协议"长期学习效率"裁定：新知不撼动榜首时，保75/25**。
7. **深挖（§八）**：均分 D0=2.617 / D15=2.621 / D5=2.667 全部在噪声内（深挖不改变终分——库已成熟，新评估不再改写排名）。但确认吞吐：D0 VNK=0+0（无深挖=新候选永远拿不到第3次评估，知识管线停转）、D15=6、D5=4（预算被复评吃掉、explore发数降）。**d15是确认引擎的最小足量；d5无增益还减产;d0拿峰值运气赌单种子(s23 2.783看着美但VNK=0且十七批证过跨种子会崩)**。
8. **精英库失效测试（§十，A臂终末库，同种子holdout×120）**：K0原样 2.525｜K1删库首 2.317(-0.21)｜K2库首降权 2.367(-0.16)｜K3换结构相近弱者（背负/透支/杀伐@1.167上库首位） **1.817(-0.71)**。判决：单体删除成本被壳内冗余吸收（兄弟即时补位）=系统不脆；换弱货成本3.4倍于删除=**采样服从真实价值而非历史权重**（若盲从权重，K3应≈K0）。
9. **策略收缩（§四）**：探针依赖度前卫指标——终局构筑与库成员共享0槽（完全自主）比例恒定20-37%、100%全继承档全配置≤9%且无上升趋势；提案采用率在0.3-1.0间振荡无单调。**无收缩签名**。但拓扑层面说一句实话：收益自gen340起一直长在同一个星形族内——群体指标无收缩，知识拓扑是长期一家人（这是环境属性，不是学习者失控）。
10. **§十三 十二问逐条**：①仍产新知？是（30件过三阶段）但全中腰。②冻结后停止增长？否——存量精英承载，A≈B。③新知率≈0.25件/10代/臂（VNK）。④新知入库（top3活跃库）成功率=0/30。⑤UCB有效产出率=6.3%VNK/发、12%候选/发，饱和递减。⑥75/25仍最优（分轴），探索加量产不出越级知识。⑦精英依赖度未升（dep100≤9%无趋势）。⑧未见策略收缩（群体口径）。⑨深挖不降均分（三档同区间），但d0绝育新知、d5减产；d15保留。⑩真正新型高性能构筑？无——没有壳外新家族，只有壳叶3.69一件近顶。⑪死斗？未证实——只记数：12臂死斗增量合计约525场胜132（各臂9-76场不等，D5摸到死斗次数最多140/42)，无配对对照不得解读为进步。⑫下一阶段瓶颈=**环境/构筑本身**（不是探索/传播/执行：三样都已分别证过不构成约束）；具体方向=壳外机制（死斗特化体、消耗品<40%血线出手窗、第七场后期生存），是否研究/改环境归DM裁定。
11. **§十一隔离执行**：12臂全程深拷贝生产KB(gen490快照），实验库零回写；本批不向生产库晋升任何构筑（VNK全部未过迁移门=自动不合格，壳叶变异非新知）。生产KB维持第十七批接管态。**§十四合规**：环境/规则/怪物/奖励/裁定零改动，git diff锚定仅学习器/探针/文档。
12. **诚实边界（§十一式备案）**：①A≈B结论建立在"库已成熟"起点——从零起步时冻结=必死（十七批已证）；②迁移探针n=24/构筑，阈值0.8,4样本全挂夫差大（-0.55~-1.11）结论稳；③VNK定义依赖首评3.0/确认2.0阈值，换阈值计数会变但"无一进top3+迁移全挂"两个硬事实不变；④D5死斗140场是深挖带来的深度副产品，若死斗样本是经深挖累积，未来死斗批可用深挖臂快速攒样本。
