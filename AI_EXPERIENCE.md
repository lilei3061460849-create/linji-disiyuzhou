# AI经验库
> 每次测试后记录教训，避免重蹈覆辙。DM确认后计入。

## 出怪公式（已确认）
- 出怪数量 = 战斗场数，一阶副本直接-3，最低1
- 一阶7场：1/1/1/1/2/3/4只
- 允许重复抽选同一怪物种族
- 第5场开始出现多怪，必须在此之前建立防御体系

## 平衡改动（2026-08-03，针对0%通关率）
针对“只有杀伐一条路且伤害失衡”做的配套改动：

### 1. 降服（新增非杀伐胜利路径）
- 怪物连续3回合未能对轮回者造成伤害→触发降服，记录当前面板化为消耗品（耐久1）
- 使用降服品→召唤其作为临时朋友作战（战终离去），被降服怪物不产碎片
- 胜利路径从此不止杀伐一条；降服成本是“速度/格挡”，与杀伐的“法力”是不同资源池
- 代码：`engine/combat.py` 的 `settle_taming/_subdue_monster/summon_tamed_friend`，`api.py:_action_consume_item`

### 2. 攻击次数平方消耗（设计预算）（设计预算）
- 公式（已定稿）：1法力=8血限，1攻击力=2法力，X攻击次数=X²法力
- 可分配法力 = 攻击力×2 + ⌈血限÷8⌉ + 攻击次数²；道纹按[道纹数量]/[数量总值]单独配额
- 一阶预算定为60（可调）；现有36面板已全部重做合规，仅6个超标面板被压到60：
  - 千手蜈蚣 6×8/120→6×6/96（保留"多手"，输出48→36）
  - 眼树 1×26/96→1×25/72、狙击手 1×28/96→1×24/84（保留单发高伤）
  - 血肉巨囊/断翼巨像 2×20/150→2×20/126（压血限）
  - 追求者 8×2/96→6×2/72（事件怪，保留多段弱击）

### 3. 怪物出手拆分（已取代旧"出手固定为1"）（解决"道纹用不上"）
- 怪物每回合：1次攻击出手 + 1次道纹出手（互不抢夺，均固定不随回合增加）
- 攻击出手发动一轮攻击；道纹出手发动一次道纹。活力+X、狂暴+1作用于攻击出手
- 代码：`engine/battle_flow.py:get_monster_actions`（返回{attack,daowen}）

### 4. 多路径胜利（4条非杀伐路径，均不产碎片）
| 路径 | 触发 | 产物/效果 | 资源池 |
|------|------|----------|--------|
| 降服 | 连续3回合未造成伤害 | [怪物名]召唤物(1/1)→临时朋友 | 速度/格挡 |
| 雕塑 | 攻击次数或攻击力归0 | [怪物名]雕塑(X/Y, Y=血限5%)，1耐久=15伤害或20格挡 | 弱化/僵化 |
| 增生 | 累计恢复≥血限×1.0(占位，过量双倍) | 吸收进死者之书，休整恢复+8 | 再生/滋养 |
| 还债 | 负债≤-10(占位) | 视为员工，还清负债后离队 | 碎片/经济 |

- 代码：`engine/combat.py` 的 `settle_victory_paths/_subdue/_sculpture/_proliferate/_debt_bind`、`use_sculpture`、`summon_tamed_friend`
- 消耗品统一 `kind`：normal/summon/sculpture（`engine/models.py:Consumable`）
- 所有阈值（增生倍率/还债线/雕塑数值）为占位初值，需经测试调整

## 实战模拟测试（2026-08-03，sim/balance_sim.py + sim/run_sim.py）
两个模拟器：balance_sim测单场各路径；run_sim测7场全通关率（多怪/HP跨场/局外休整修行）。

### 7场全通关率（真正的"通关率"，用户目标30%）
玩家标准构筑(60HP/14法/8速, 杀伐→学庇护/再生/冲击)，局外3精力休整+修行，HP跨场带。

**修复region池bug + AI激进修行（花碎片买高阶修行档）后的各副本真实通关率：**

| 副本 | 通关率 | 评价 |
|------|--------|------|
| 扭曲都市 | 8.3% | 太难：怪高攻(18/20/25)+变形/活力/飞行/僵化疯狂涨 |
| 罪孽都市 | 77.3% | 太易：怪低攻(6-14)+碎片经济(钱袋/还债)→激进修行雪球 |
| 龙心谷 | 32.0% | 接近30%目标 |
| **三副本平均** | **39.2%** | |

**关键发现：三副本严重失衡。** 罪孽的碎片经济+低攻怪让玩家激进修行雪球碾压；扭曲的高攻涨型怪前期就把玩家打死；龙心谷恰好。
- 出怪曲线-3(1/1/1/1/2/3/4)已实装(原-2通关率仅6%)
- AI激进修行(碎片买高阶档,不囤积)：符合"碎片不转面板就是废物"原则；但暴露罪孽经济过强
- region池bug已修：每副本只出本池12只怪(之前罪孽会出龙心谷怪)
- 待DM定夺：是否调平三副本(罪孽削经济/扭曲降怪攻)，或接受副本难度梯度

### 单场各策略胜率（balance_sim，38怪×50局）
| 策略 | 单场胜率 | 主导路径 |
|------|---------|---------|
| 杀伐 | 42% | 几乎全靠降服 |
| 叠盾闪避 | 87% | 降服 |
| 弱化(雕塑) | 97% | 雕塑30%+降服70% |
| 奶敌(增生) | 87% | 降服92%+增生8% |
| 赎金(还债) | 87% | 降服85%+还债15% |

### 核心发现
- 单场胜率(42-87%)≠通关率(6-23%)：多怪场+HP跨场流失才是真正难度
- 降服在单场过强(8速度闪避+庇护)，但在多怪场几乎用不出来(挡不住所有怪)→多怪靠杀伐/AOE冲击
- 占位阈值定值：增生1.0倍血限、还债-10碎片、雕塑15伤/20挡
- 控场道纹已实装：蒙蔽(玩家伤害道纹归零)/坏死(禁疗)/减速(速度减半)/僵化 均接入引擎判定与模拟器激活；控场使通关率约-2%

## 引擎补全进度（2026-08-03，20/20测试通过）
引擎从~15%提到~90%实装率，现已端到端可玩（sim/engine_trace.py全程用GameEngine API驱动生成战报证明）。
已补10项：①道纹效果全接通 ②局外行动(休整/修行/学习/共鸣)真实生效 ③遗物系统(12件全实装:
  钱袋/避风铃/回锋刀/守夜灯/折速法印/鲜血契约/买路财/同魂笔/卖身契/无所求/三相残韵盘)
  ④怪物回合(run_monster_phase自主驱动) ⑤法术系统(9法术自动触发) ⑥飞行免选中+裂变分次
  ⑦活血钩子 ⑧事件系统(解析28事件/探索触发/选项结算) ⑨员工叛变检查+死之传承 ⑩5遗物补全。
新增api动作: monster_phase(怪物回合)、resolve_event(事件结算)。
战报: 战报_完整轮回.md 由engine_trace引擎驱动生成(龙心谷7场HP4险胜, 触发守夜灯/降服/多怪ramp)。
剩余收尾: 撤退/最终冠冕死斗(第8场)/初拥之夜/逆鳞嫁祸背负/消耗品具体效果。

## 多次测试通关率（run_sim, 300局/副本, 激进修行）
扭曲都市 8.3% | 罪孽都市 77.3% | 龙心谷 32.0% | 平均 39.2%
（注: run_sim用并行模拟逻辑测统计; engine_trace用引擎本体生成单局战报。两者互补。）

## 引擎补全进度第二阶段（2026-08-10，pytest 90/90 + 旧套件20/20）
承接"剩余收尾"清单，本阶段陆续补齐：随机数自动化、员工经济系统、雇佣转化道纹、
追求者事件、听指令行动、出怪系统、出手预算、撤退机制。测试统一放在 tests/test_*.py，
用 `python -m pytest tests/` 跑；旧的 tests/test_engine.py 保持不动、原生跑法仍可用。

### DM裁定记录（与正文冲突/需要正文同步的，已改正文，此处只记结论）
- 随机数规则：由"玩家/DM提供池内数字"改为"引擎自身生成随机数(DiceEngine.auto_roll)"，
  正文已同步。手动流程(create_pool/resolve_pool/random_number action)保留作调试/强制裁定入口。
- 出怪数量公式：AI_EXPERIENCE.md旧记录"-3"与正文"-2"冲突，DM裁定采用"-3"
  （一阶7场序列1/1/1/1/2/3/4），正文已同步更正。

### 出怪系统（engine/monsters.py）
从README解析三副本各12怪物面板（复用了sim/balance_sim.py已验证过的正则解析逻辑），
[战始]按上述公式随机抽取(允许重复，只用当前副本自己的池)，真正把Entity放进state.enemies
（此前只返回"请抽取怪物"的提示文字，从不实际出怪）。战斗背景确认为纯叙事占位，无机制效果，不做处理。

### 员工经济系统（出战支援/工资/黑名单/解雇）
[员工]默认待命(is_deployed=False)，需`deploy_employee`消耗1出手部署才计入战场与工资。
[战终]按"存活+已部署+非还债"计算工资(2×(1+实际出场回合数)，上限12)，阻塞战终直到逐个
`pay_employee_wage`(pay/refuse)决策完毕；拒付强制离队+计入黑名单(累计3次触发`is_blacklisted`，
封禁雇佣)。`dismiss_employee`自由解雇同样计黑名单。每场战斗单独部署，战终重置待命状态。
还债转化的员工("视为其参战")独立于此套经济，不需部署、不产生工资。
雇佣本身此前是纯占位(从不创建员工对象)，现已真正创建；DIY预算强制`atk_bundles≥1`
（攻击次数最低为1，防止出现出手预算永远为0的"纯沙包"盟友——出手次数=攻击次数/3）。

### 雇佣diy转化道纹 + 龙心谷"追求者"事件
雇佣后引擎用auto_roll从19个转化道纹中抽3个候选，`choose_hired_daowen`选1个赋予。
"追求者"事件选项1创建真实员工(8×2/96,逆鳞2,活血3,固执3)；选项2登记
`state.forced_monsters_next_battle`，[战始]时真正作为额外怪物加入(出怪系统落地后一并接通)。

### 朋友/员工听从指令行动
`use_daowen`新增`actor`参数，可指定已参战[朋友]/[员工]代为发动：免法力(与怪物同规则，
只消耗出手)，且必须指定非自身目标(哪怕该道纹本可自打)。`attack`本就支持`attacker`参数，
无需改动即可指挥。玩家自身发动道纹的法力制+默认自身目标完全不变。

### 出手预算校验
`action_count`按entity_type分流：轮回者=速限/3，[朋友]/[员工]=攻击次数/3(此前只认速限，
对没有速限的盟友永远算出0，是真实bug)。消耗1出手：attack/use_daowen/deploy_employee(扣玩家)/
declare_wit/declare_escape，全部校验通过后才真正扣减；不消耗：consume_item/use_resonance。
按回合重置。

### 撤退机制
任意[朋友]/[员工]即将被外部伤害(攻击或敌对道纹，不含自身代价)打到命零时，自动撤退：
伤害清零、保留当前生命、退出本场战斗，[战终]重置可参加下一场。判定用扣除格挡后的实际
伤害，格挡够用则不触发。新增`CombatEngine._apply_hostile_damage()`作为统一入口，
替换了原来5处直接调用`take_damage`的伤害结算点(普通攻击/裂变/道纹直伤/血债多段/自残反弹)。

### 员工叛变三分支（镇压/让利/急中生智）
镇压完全复用现有战斗体系：不新增任何战斗计算逻辑，只是把state.employees整体(保留完整
面板与道纹)搬进state.enemies当"出怪"结果，之后走标准的round_start/attack/use_daowen/
monster_phase/round_end流程；resolve_rebellion_battle(outcome)只结算胜负后果——胜利保留
碎片(不产生额外击杀奖励，原文只说"保留财产"没说加送)，失败/撤退清空碎片+存活叛徒永久离开。
让利=全局wage_bonus+5，叠加在12碎片封顶之后。急中生智复用了enums.py里早就定义但从未
使用过的STAFF_MUTINY中断类型，走标准Interrupt交DM裁定，不在引擎里编造谈判成功判定逻辑。
三分支默认都要求`state.rebellion_active`(即战终检查命中)，`force=True`对应原文"被效果强制
触发时跳过数值检查"的分支，供未来其他机制强制触发用。

### 测试过程中抓到的真实bug(非production代码本身编造，均有对应回归测试锁定)
- `current_round`实际在`round_start()`时递增(不是`round_end()`)，`deployed_at_round`最初算错1格。
- 工资pay/refuse决策后直接`del`会导致下次`battle_end`重新计费，改用None哨兵值标记已决策。
- `use_daowen`传入不存在的target名字时会静默把目标退回施法者自己，已改为明确报错。
- 老测试`test_daowen_effects_wired`连续发6次道纹从未考虑出手预算，加上预算校验后炸了——
  给它单独调大速限，不影响其"验证道纹效果落地"的本意，不代表生产代码有问题。

### 遗物池5件坏掉/缺失的道具修复
审计发现遗物池13件里此前只有7件真正生效，逐个修复：
- 血誓戒：此前完全没有代码钩子(只有名字)。新增`Entity.blood_oath_used_this_round`
  (回始归零)+`CombatEngine._pay_bleed_cost()`统一入口，替换了道纹代价"cost_hp"分支与
  鲜血契约自身流血代价的两处直接`take_damage`调用，确保不管流血代价来自哪里都能触发。
- 买路财：此前只有`buyaicai_escape_cost()`算数字，没有真正执行撤退的入口(唯一调用方是
  一条只验证计算结果的测试)。新增`retreat_via_toll` action，真正扣碎片/生命、清空战场。
- 同魂笔：此前的代码会生成一条"XXX亦受影响"的日志，但从未查找第二目标实体、也没有修改
  任何人的dao_wen——是纯粹的假日志。按"残韵作用于非轮回者拥有的道纹时，仅改变本次结算，
  施法者永久获得变化后的道纹"这条通用残韵规则重写：新增`second_source_daowen`参数指定
  第二目标身上要生效的具体道纹，用`ResonanceEngine`已经算出来的`caster_gets_daowen`标志
  (而不是自己猜)决定是否真的让施法者获得新道纹；第二目标自身的dao_wen全程不被修改。
- 钱袋：`process_relics`里写了`on_monster_death`触发分支，但全代码库没有任何地方调用
  `process_relics("on_monster_death")`，是彻底的死代码。改为在`battle_end`的标准击杀奖励
  循环里直接叠加结算，用的是新增的`Entity.battle_start_blood_limit`快照(见下条)。
- 忘忧香：遗物池此前只注册了12件，第13件忘忧香连名字都没有。补上定义+对应的"忘忧"
  局外行动(失忆X种指定道纹，换30/55/80碎片)。

### 顺带修的一个更早的既有bug：标准击杀碎片奖励用错了"血限"
原文写"怪物[战始][血限]×2%"，但`battle_end`原来的公式直接读`monster.blood_limit`，这是
**当前**血限，如果战斗中血限被改变过(比如增殖类道纹)，奖励就会算错。新增
`Entity.battle_start_blood_limit`(通过`__post_init__`默认等于构造时的blood_limit，只在
真正需要"重新快照"的地方才手动赋值)，标准奖励公式与钱袋加成都改用这个快照值。这个问题
不是我在这轮引入的，是审计遗物时顺带发现的既有bug，一并修了并补了专门的回归测试锁定。

### 最终的冠冕 / 第8场最终死斗（用户逐条确认过设计）
持久化：跨GameEngine实例共享一个JSON文件(默认data/sealed_candidate.json，已加入.gitignore，
构造函数sealed_candidate_path可覆盖，测试用不同路径互相隔离)，完整封存player+friends+
employees+relics+resonance+shards+death_book_wisdom；Entity的完整序列化没有直接复用
to_dict()(那是给展示用的摘要，会丢失道纹x_value等信息)，专门写了_serialize_entity_full/
_deserialize_entity_full往返无损。
死斗范围：双方各自带队伍(朋友/员工)一起参战，不是严格1v1对手的player+friends+employees
整体作为state.enemies。
交替出手：新增state.in_final_duel/duel_turn("player_side"/"opponent_side")，attack/
use_daowen新增_check_duel_turn_or_error校验，非当前出手方一律拒绝；死斗中declare_escape
直接拒绝("无法逃跑")。先手顺序只在死斗开始时用速限→法限→血限→当前生命比较一次决定谁先手，
之后严格交替，不逐回合重算。
二阶及以上未实现：胜者与败者都触发state重置为空白GameState(等待新轮回者)；额外地，
胜者会被重新完整封存并写回同一个JSON槽位，形成"擂台循环"(下一位挑战者依然会遇到这个胜者)，
而不是尝试接入不存在的二阶内容；败者不封存，槽位保持空缺。
踩到的真实bug：两个轮回者默认都叫"轮回者"(setup_attributes的name默认值)，死斗对手直接
复用这个名字会导致所有按name查找目标的代码(全引擎皆是)把伤害错发给自己。修复：
_restore_side_from_snapshot对手阵营中与挑战者一侧重名的实体强制改名(追加"（对手）")。
这不是死斗专属问题的补丁，而是"两个默认同名角色对战"这一必然场景下引擎能正确工作的前提，
值得记录以防未来别的地方复用同一套按name查找的逻辑时踩同样的坑。

### 遗物池5件坏掉/缺失的道具修复
审计发现遗物池13件里此前只有7件真正生效，逐个修复：
- 血誓戒：此前完全没有代码钩子(只有名字)。新增`Entity.blood_oath_used_this_round`
  (回始归零)+`CombatEngine._pay_bleed_cost()`统一入口，替换了道纹代价"cost_hp"分支与
  鲜血契约自身流血代价的两处直接`take_damage`调用，确保不管流血代价来自哪里都能触发。
- 买路财：此前只有`buyaicai_escape_cost()`算数字，没有真正执行撤退的入口(唯一调用方是
  一条只验证计算结果的测试)。新增`retreat_via_toll` action，真正扣碎片/生命、清空战场。
- 同魂笔：此前的代码会生成一条"XXX亦受影响"的日志，但从未查找第二目标实体、也没有修改
  任何人的dao_wen——是纯粹的假日志。按"残韵作用于非轮回者拥有的道纹时，仅改变本次结算，
  施法者永久获得变化后的道纹"这条通用残韵规则重写：新增`second_source_daowen`参数指定
  第二目标身上要生效的具体道纹，用`ResonanceEngine`已经算出来的`caster_gets_daowen`标志
  (而不是自己猜)决定是否真的让施法者获得新道纹；第二目标自身的dao_wen全程不被修改。
- 钱袋：`process_relics`里写了`on_monster_death`触发分支，但全代码库没有任何地方调用
  `process_relics("on_monster_death")`，是彻底的死代码。改为在`battle_end`的标准击杀奖励
  循环里直接叠加结算，用的是新增的`Entity.battle_start_blood_limit`快照(见下条)。
- 忘忧香：遗物池此前只注册了12件，第13件忘忧香连名字都没有。补上定义+对应的"忘忧"
  局外行动(失忆X种指定道纹，换30/55/80碎片)。

### 顺带修的一个更早的既有bug：标准击杀碎片奖励用错了"血限"
原文写"怪物[战始][血限]×2%"，但`battle_end`原来的公式直接读`monster.blood_limit`，这是
**当前**血限，如果战斗中血限被改变过(比如增殖类道纹)，奖励就会算错。新增
`Entity.battle_start_blood_limit`(通过`__post_init__`默认等于构造时的blood_limit，只在
真正需要"重新快照"的地方才手动赋值)，标准奖励公式与钱袋加成都改用这个快照值。这个问题
不是我在这轮引入的，是审计遗物时顺带发现的既有bug，一并修了并补了专门的回归测试锁定。

### 文档分工（本轮起）
README.md只放世界观正文，改动需要DM签收；engine/README.md只放精简的架构/API参考，
不放设计取舍的长篇说明；具体的设计取舍、DM裁定记录、测试驱动出的bug修复，统一记在本文件。

### 三副本终音法器 / 初拥之夜 / 真龙之心（用户逐条确认过设计，"如果没有就一块做了"）

这是原始审计清单里最后、也是工程量最大的一项：死斗胜利后领取的10件终音法器
(扭曲都市4件/罪孽都市3件/龙心谷3件)、猩红尖牙触发的初拥之夜(9选1)、真龙之心解锁的
独立"龙性"资源与8种龙族特性。三者被绑在一起做是因为猩红尖牙→初拥之夜、真龙之心→
龙性经济这两条链路都发生在"领取终音法器"这同一个入口之后，拆开做反而会互相依赖。

**设计取舍/用户裁定记录：**
- 二阶及以上副本(如血族的"永夜庭")不存在，因此"每击杀一名血族触发初拥之夜"这类以
  二阶内容为前提的重触发条件，实现上按`entity_type == "血族"`原样写出判定逻辑，
  但在当前引擎下永远不可达——这是已知且被接受的现状，不是需要修的bug。这与"赤族"
  (玩家通过血族尖牙奴役的怪物，`entity_type == "赤族"`)是两个完全独立的实体类型，
  击杀赤族不算击杀血族，两者刻意不共用判定。
- 真理眼："这个就中断问我"——未做任何自动判定，触发后直接抛`Interrupt`交DM裁定
  目标的应答是否属实，与急中生智等既有中断机制走同一套`submit_ruling`流程。
- 寒冰法力：核心澄清是"施加法力"指持有者(必须是玩家自己)每次因发动道纹而**支付**的
  法力数额，累加到**目标**身上(哪怕目标是自己)，不是什么"打掉对方法力池"的机制
  (代码库里没有任何道纹有伤害对方法力值的效果，这点专门grep确认过)。实现为
  `Entity.mana_inflicted_this_round`按目标累加，每跨越一个10的整数倍叠加1层【无力】
  (复用既有状态，无力已经在`action_count`属性里生效，没有发明新机制)。
- 真龙之心的工作量确实不小("总不能删了吧")，全量实现：龙性经济(6衰老=2枯竭=1萎缩=
  12龙性)+8种特性一个不少，包括容易被跳过的"龙威"——这条实际上不需要写任何代码，
  因为`run_monster_phase`里怪物攻击目标本来就硬编码只打玩家本人，从未实现过"怪物选中
  朋友/员工"的目标分配逻辑，所以"敌方必须优先选择自身为目标"天然恒定满足。专门写了
  一条回归测试锁定这个断言，防止未来给怪物加目标选择逻辑时不小心破坏这个前提。

**测试驱动出的3个真实bug（都是先跑通`python3 -c`临时脚本发现，再补的回归测试）：**
1. 震岳龙躯的`dragon_body_shield_rounds`只有写入没有任何地方递减——一旦激活就是
   "永久护体"而不是文档写的"持续X回合"。补在`combat.round_end()`里每回合-1。
2. 黑金名片：校验"负债不能超过50"之前，代码已经先把所有敌方的血限减半改掉了——
   即便校验失败返回`success: False`，敌人也已经白得了减半效果，碎片却没扣。
   这是"先做副作用、后做校验"的经典顺序错误，修复为先算总代价、校验通过再真正改状态。
3. 龙族利爪"自身每完成一次行动后，攻击次数+1"最初挂在共享的`_consume_action_or_error`
   里，在动作**执行前**就先长了一次——对【攻击】来说是致命的，因为
   `calculate_round_attack`马上就要用(已经涨过的)攻击次数去要求对应数量的
   `target_selections`，导致本次攻击自己都满足不了自己刚提出的要求。拆成
   `_apply_dragon_claw_growth()`独立调用，攻击这一条特意挪到`calculate_round_attack`
   读取旧攻击次数**之后**，其余几个调用点(use_daowen/deploy_employee/declare_wit/
   declare_escape，均不依赖攻击次数做本次自身校验)维持"消耗出手后立即增长"不变。

**已知的局限/一致性问题(disclosed，非本轮修复范围)：**
- `_serialize_full_character`封存的对手快照里，`artifacts_owned`/`first_embrace_traits`/
  `dragon_traits`只做记录保真，不会在死斗中真正让对手一侧触发这些特性的被动效果——
  全代码库的这些trait判定都硬编码在`self.state`(当前操作方)上，还没做成能对双方分别
  独立生效的形式。要修需要更大的重构，本轮不做，仅记录在`_serialize_full_character`
  docstring与本文件两处。
- 血族尖牙的"衰老20"代价是直接`player.blood_limit -= 20`，没有像道纹的衰老代价那样
  经过`_offset_with_dragon_heart`走共心环/龙心抵消——这是因为它不是道纹系统里的
  "代价"，是一个独立action，目前按字面"代价：衰老20"实现为直接扣除。
- 教父左轮/共心环文档写的是"[战终]耐久回满"/"[战始]选定"，实现上把两者都放在
  下一场的`_action_battle_start`里做重置，效果等价(即：这一场用完，下一场开始才能
  再选/满弹)，只是代码里少一个显式的战终清理点，不影响可观察行为。
- 负岳碑/断尾求生都是"预声明，命中相关判定时才真正消耗"的模式(`declare_fuyuebei_toll`/
  `declare_tail_sacrifice`)，因为当前基于action调用的引擎架构不支持真正的
  "伤害结算过程中途插入决策"式中断，只能退化为提前声明。
