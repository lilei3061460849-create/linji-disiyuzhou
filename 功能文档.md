# 功能文档 · 未完成项与待平衡项总账

> 基线：2026-08-10，**双血统合并后**（PR #6）。
> 合并原委：本会话分支（019feb69，裁定①-⑬+三副本调平）与 main（PR#5 谱系=会话019fe6ff，
> 撤退/冠冕/初拥/员工经济/出怪系统等）同源于 9e949eb（2026-08-04 快照）。合并策略：
> **规则层全取本分支裁定①-⑬；功能层全留 main；降服按裁定⑩在合并结果中删除（含 main 侧实现与测试）。**
> 口径声明：数值均来自并行逻辑模拟器（`sim/`）。全程=种子2026、每副本300局；单体=种子42、每策略1800局。
> main 侧历史文档（`功能实现状态.md`、`IMPLEMENTATION_AUDIT.md`、`战报_格式合规示例.md`）描述的是**合并前**谱系状态
> （含"降服已实现"等过期表述），按"README+本文档为准"原则不回改，仅在此声明。

## 〇、合并后验证快照（2026-08-10 当轮复跑）

| 项 | 结果 | 复现命令 |
|---|---|---|
| legacy 引擎测试 | 24/24 通过 | `python tests/test_engine.py` |
| main 谱系 pytest 套件 | **195/195 通过**（合并中按裁定⑩删 1 个降服测试；按裁定⑥更新 1 个旧面板字面预期） | `python -m pytest tests/ -q` |
| 全程通关率（300局×3） | 扭曲31.0% / 罪孽29.7% / 龙心谷29.3%，**平均30.0%**（与合并前完全一致） | `python sim/run_sim.py all 300` |
| 进化/崩解计数（900局） | 进化 2919 次、崩解 5658 次（未被合并扰动） | 同上 |
| 面板审计（预算60/道纹3/总值8） | 36只怪 0 违规 | `python sim/audit_monsters.py` |
| 语法 | engine/sim/simulate.py/main.py 全部可编译 | `python -m py_compile ...` |

**合并已完成项**（旧清单 F1/F2/F3/F8 关闭）：撤退机制、最终冠冕死斗（第8场）、初拥之夜、员工经济/失信黑名单
——由 main 谱系带入，`tests/test_retreat.py / test_final_duel.py / test_first_embrace.py / test_employee_economy.py`
全部通过。出怪规则已统一为"场数−3、允许重复"（main 侧 DM 裁定，与本会话模拟器实测口径一致，`run_sim.py:284`）。

## 一、未完成功能（合并后真实清单）

| # | 项 | 规则依据 | 现状（证据） | 缺口 / 待办 | 依赖裁定 |
|---|---|---|---|---|---|
| F1 | 凡庸特殊事件 | README:504-505（降服的替代机制，裁定⑩） | 合并后引擎/模拟器对「凡庸」「残骸」仍零引用 | ①「连续五回合未出手/未能使敌对角色减血」追踪；②炸裂死亡结算；③授予残骸(1/1)（恢复20+异变10） | 无。异变计费管线已就绪（`_action_consume_item` 正则解析「获得异变N」，有测试），触发与授予补上即生效 |
| F2 | 专属道纹引擎结算③档 | 三副本怪物池 | 引擎已接两档（状态口径：加害/龙鳞/裂变/贯穿/活血/坏死/畸变；效果公式：赌命/清算/逼债/赎金/洗劫/退化/定型/变形/超频）；**零实现档**：逆鳞/嫁祸/背负/衰败/抵扣/消灾/伤痕/增殖（combat.py:21-23 仅集合枚举）；怪物自主激活优先级仅覆盖原始道纹系（combat.py:1094） | ③档引擎实现+怪物发动专属道纹编排；24种完整怪物侧效果目前只在 `sim/balance_sim.py`，双端口径有漂移风险 | 是否引擎实装、优先级 |
| F3 | 工具库使用效果 | 扭曲都市工具库清单 | 引擎：授予端已实装（`TWISTED_TOOL_LIBRARY`+附赠发现，测试齐）；**8件工具效果只在 `sim/run_sim.py tools_phase`** | 引擎侧使用结算（耐久/干扰仪封激活/水枪清持续等8件） | 无，纯实现 |
| F4 | 通关战报生成器 | 用户指令（会话内被合并指令打断） | 旧版 `战报_完整轮回.md` 为汇总式输出，违反 README§六，已删除并由 `sim/format_trace.py` + `engine/battle_report.py` 取代 | 已裁定：走引擎API、龙心谷 | ✅ 已完成，产物 `战报_格式合规示例.md` |
| F5 | relic_of_choice 死字段 | README 遗物机制 | 合并后仍仅 `models.py` 声明、代码零消费（main 谱系的遗物修复测试未接线此字段） | 接线或删 | 删 or 接 |
| F6 | 活力四方案死代码 | 裁定⑫已否决甲/乙/丙/丁 | `sim/balance_sim.py HUOLI_MODE` 开关保留四候选，默认 `current` | 删候选代码或留档 | 删 or 留 |
| F7 | 文档一致性 | README | ① README:318-319「全程自动触发」漏列凡庸(:504)/癌变(:507)/崩解(:538)；②命名漂移：事件「癌变」vs 代码注释「增生」(`is_proliferated`，models.py) vs 怪物道纹「增殖」，三者指不同物；③ `engine/README.md` 与正文未全面复审 | 补列表、统一命名、复审 | 命名以哪个为准 |
| F8 | **simulate.py 口径**（合并引入的新注意项） | — | main 谱系的独立模拟器 `simulate.py`（消费 gamedata，面板已随合并同步为预算60）**未含裁定①-⑬机制**（无崩解/异变计费/原初X/专属道纹层），其战斗结果与本会话 `sim/` 的 30% 口径不可混用 | 改造对齐 / 标注归档 / 删除 | 三选一 |

## 二、待平衡 / 待裁定（改任何一条 ⇒ 标注的重测范围必须重跑）

| # | 事项 | 现行口径与数据（可核查） | 影响面 / 重测范围 |
|---|---|---|---|
| B1 | **赌命文本歧义** | README「失去30%[血限]的当前生命」；引擎实现=**当前生命×30%**（combat.py「赌命：当前生命百分比」注释自证）；字面另一读=血限×30%（满血约3倍狠） | 改读法 → 罪孽都市全套重测 |
| B2 | **嫁祸目标策略** | 模拟默认 `JIAHUO_POLICY="player_first"`（依规则#6无道德底线）；`ally_first` 龙心谷约 +3pp | 改策略 → 龙心谷重测 |
| B3 | **崩解自毁是否送分** | 持续计费使高数值原始道纹怪约2-3回合自崩解；900局崩解5658次=玩家主动击杀之外最大怪物减员源。「代价自平衡（禀赋）」还是「可苟等死（漏洞）」未裁定 | 若改计费/阈值 → 三副本30%全重测 |
| B4 | **单体路径失衡** | 单体：雕塑100%/增生97.2%/还债97.2% vs 杀伐66.7%；四策略对毒枭全0%（唯一活力持有者） | 若给怪物反制 → 单体+全程全重测 |
| B5 | **活力悬崖红线（存档）** | 历史实测：全池×活力1=0.0%；4只×活力3=0.3%；毒枭独持活力2=29.7%；≥2只×活力≥2=全灭区 | 新增/调整任何活力怪 ⇒ 全量重测 |
| B6 | **模拟结论有效性边界** | 30%成立前提：脚本玩家策略+激进修行；扭曲每局固定2件工具；单体口径60碎片；撤退未模拟（机制已在引擎，未进模拟器）；困境探针≥1信号（裁定⑦）；出怪场数−3允许重复 | 任一前提变更 ⇒ 对应副本重测 |
| B7 | **败亡节奏（供体感裁定）** | 罪孽/龙心谷败亡集中第6-7场（26/21%、26/24%），扭曲分散；进化2919次/900局 | 是否期望节奏属设计裁定 |

## 三、复现命令汇总

```bash
python tests/test_engine.py        # legacy 引擎测试，基线 24/24
python -m pytest tests/ -q         # main 谱系套件（需 pip install pytest），基线 195/195
python sim/audit_monsters.py       # 面板审计，基线 0/36 违规
python sim/run_sim.py all 300      # 全程通关率（种子2026），基线 31.0/29.7/29.3 平均30.0
python sim/balance_sim.py          # 单体1800局×4策略（种子42），基线 66.7/100/97.2/97.2
```
