# 功能文档 · 未完成项与待平衡项总账

> 当前基线：2026-08-14，分支 `arena/019ffa62-linji-disiyuzhou`。
> 口径声明：本文档只登记**尚未解决**的功能与待平衡项；历史实现过程见 `AI_EXPERIENCE.md`。
> 未实现副本草案虽已纳入文档索引，但不会进入当前运行时。
> 用户说「测试」时，默认 AI 通过 GameEngine 手操点选，不得用脚本批量通关当测试。

## 〇、验证快照（2026-08-14 当轮实跑）

| 项 | 结果 | 复现命令 |
|---|---|---|
| pytest 全库 | **581 passed** | `PYTHONPATH=. .venv/bin/pytest -q` |
| 初始杀伐/合并闭环/异变单次计费手操 | **10 PASS** | `PYTHONPATH=. .venv/bin/python /tmp/manual_initial_loop_billing.py` |
| 癌变怪物休整永久加成手操 | **5 PASS** | `PYTHONPATH=. .venv/bin/python /tmp/manual_cancer_rest_bonus.py` |
| 删除持续计费后的跨副本复测 | 固定种子原计划6场；扭曲都市首场阵亡，实际完成5场。0场因持续异变崩解结束；2场锐利命零、2场癌变、1场玩家命零 | `PYTHONPATH=. .venv/bin/python /tmp/manual_merged_loop_six.py` |
| 文档校验 | **17份文档、136条链接** | `validate_markdown_documents('.')` |

## 一、未完成功能

二阶副本【乱葬岗】已完成实现并接入运行时（包含12只怪物与专属道纹）。`副本索引.md` 已登记永夜庭、沉沦海、荒疫古城与巴别塔四份未实现规则草案；其预算与机制仍待后续接入。
## 二、待平衡 / 待裁定（改任何一条 ⇒ 标注的重测范围必须重跑）

> ⚠️ **旧平衡数据全部作废**：此前的「总体通关率 13.0%」「精英 build `杀伐+僵化+庇护+加害+缓慢+再生` = 59.0%」
> 等结论，建立在**跨副本混搭专属道纹**之上。副本专属道纹门禁修复后该 build 已不合法，
> 实测其在三副本各 200 局的成绩为 **0.0% / 0.0% / 0.0%**（半数道纹学不到）。
> 任何引用旧数字的结论都不得再作为裁定依据。

| # | 事项 | 现行口径与数据（可核查） | 影响面 / 重测范围 |
|---|---|---|---|
| **B1a** | **道纹口径最新裁定（2026-08-16 落实）** | 杀伐消耗X→3X伤；冲击消耗X→全体2X伤；锐利消耗3X→血限/生命-5X；缓慢改代价冷却X、若目标出手次数≤2则无法出手持续X；血债改代价流血X、选择目标X次每次1伤（破速克制固执）。 | 三副本全量重测 |
| **B1** | **庇护/再生仍是生存地基，旧数据待重跑** | 再生已改为消耗X回复6X，初始道纹已统一为杀伐，怪物异变也改为仅首次发动计费，因此旧3X及旧起手下的300局封禁反事实全部失效。删除持续计费后的固定种子复测中，扭曲都市首场即命零，说明此前怪物自毁曾掩盖生存压力 | 暂不据旧胜率削弱；按新初始构筑和单次异变计费重跑封禁反事实 |
| **B2** | **后续学习道纹的边际差异待重测** | 锐利不再是起手，而是杀伐14节点闭环中的后续可学习道纹；旧“第5位各300局”数据同时受到旧初始道纹、再生3X和持续异变自毁影响，已经失效 | 按“自动杀伐起手→局外学习锐利/其他候选”的合法构筑重新比较边际价值 |
| **B3** | **赌命口径已裁定落实** | 用户已裁定：代码口径改为 **血限×30%**（`math.ceil(blood_limit * 0.3)`），严格按字面血限百分比结算 | 罪孽都市已同步代码 |
| B4 | 嫁祸目标策略 | 模拟默认 `JIAHUO_POLICY="player_first"`；`ally_first` 龙心谷约 +3pp | 改策略 ⇒ 龙心谷重测 |
| B6 | 活力悬崖红线待重测 | 旧数据包含持续异变计费与怪物自毁，已不能代表现行“首次发动付费、持续免费”口径；活力持续增攻且不再因维持而崩解，理论威胁反而更高 | 按三副本现行怪物池重新测试活力1/2/3及多怪叠加 |
| **B7** | **【凡庸】的战术后果（已裁定保留）** | 按 README 中该条件的"/"取或语义：连续五回合未出手**或**未使敌方生命减少即[命零]。后果：堆格挡完全免伤时，怪物会因"未使敌方生命减少"自爆。**用户已裁定这是正当的耗死战术，保持不变** | 已裁定，不改。若日后调整 ⇒ 三副本全重测 |
| B8 | 模拟结论有效性边界 | 现行数据前提：`sim/build_learner.py::learnable_candidates(region)` 按副本过滤可学道纹（门禁后 40 个候选中 26 个不能经局外【学习】获得，不过滤则 build 退化同质）；AI 为 `TacticalAI`；每局 7 场 | 任一前提变更 ⇒ 对应数据全部重跑 |

## 三、复现命令汇总

```bash
.venv/bin/python -m pytest tests -q
.venv/bin/python tests/test_engine.py
.venv/bin/python sim/audit_monsters.py
.venv/bin/python sim/pick_best_report.py --candidates 30 --out data/batch_report.md   # 平衡工具，不得覆盖 战报.md
.venv/bin/python sim/dominance_test.py --games 120
.venv/bin/python sim/balance_sweep.py --games 60               # 改数值必同步 TACTICAL_ROLES
```

> 用户说「测试」时，默认 AI 通过 GameEngine 逐步点选手操并写战报，不是跑上面这些脚本。
> 平衡结论铁律：**纸上谈兵无效，任何修改范围必须由真实对局数据得出**；
> 受 bug 影响的数据一律作废，不得作为平衡依据。
