# 功能文档 · 未完成项与待平衡项总账

> 基线：2026-08-10，分支 `arena/019feb69-linji-disiyuzhou`（合并入 main 的快照）。
> 本文件只登记两类账：**§一 没做完的**、**§二 需要平衡/待平衡裁定的**。已完成内容见 README 与 AI_EXPERIENCE.md，不重复。
> 口径声明：本文所有数值均来自并行逻辑模拟器（`sim/`，与引擎共享规则参数但非引擎端到端实测）。全程模拟=种子2026、每副本300局；单体=种子42、每策略1800局。

## 〇、合并基线验证快照（2026-08-10 当轮复跑）

| 项 | 结果 | 复现命令 |
|---|---|---|
| 引擎测试 | 24/24 通过 | `python tests/test_engine.py` |
| 面板审计（预算60/道纹3/数量总值8/池许可） | 36只怪 0 违规 | `python sim/audit_monsters.py` |
| 全程通关率（300局×3副本） | 扭曲31.0% / 罪孽29.7% / 龙心谷29.3%，**平均30.0%** | `python sim/run_sim.py all 300` |
| 进化/崩解计数（同批900局） | 进化 2919 次（≈3.2次/局）、崩解 5658 次（≈6.3次/局） | 同上 |
| 单体战斗（1800局×4策略） | 杀伐66.7% / 雕塑100% / 增生97.2% / 还债97.2%；全部策略对毒枭=0% | `python sim/balance_sim.py` |

## 一、未完成功能

| # | 项 | 规则依据 | 代码现状（可核查证据） | 缺口 / 待办 | 依赖裁定 |
|---|---|---|---|---|---|
| F1 | 撤退·买路财（执行端） | README:319「撤退」列入全程自动触发；买路财规则 | 引擎仅有**费用计算** `engine/combat.py:1207 buyaicai_escape_cost()` 与行动描述罗列 `engine/api.py:648`；「逃跑与追击」已实现（`api.py:984 _action_declare_escape`）；模拟器未建模撤退 | 撤退执行动作、撤退后该场与副本进度结算；当前通关率口径不含撤退 | 撤退后果细则（怪物是否保留/再遇等） |
| F2 | 最终的冠冕（第8场死斗） | README:311、544 | 仅有字段 `engine/models.py:444 sealed_candidate`（无引用）；`run_sim.py` 固定7场；引擎零实现 | 封存、双候选死斗、胜者结算全套 | 无（规则文本已完整），纯实现 |
| F3 | 初拥之夜 | README:547-556（9选项） | 引擎/模拟器 grep 零命中 | 触发（得到/持有猩红尖牙、击杀血族）、9选项、除9外限选1次 | 无，纯实现 |
| F4 | 凡庸特殊事件 | README:504-505（裁定⑩以凡庸替代降服） | **引擎与模拟器零引用**（grep 无「凡庸」「残骸」）；降服代码已全删 | ①「连续五回合未出手/未能使敌对角色减血」的战斗内追踪；②炸裂死亡结算（轮回者直接死/怪物命零）；③授予残骸(1/1)（恢复20+异变10） | 无。注：异变计费管线已就绪（`_action_consume_item` 自动解析「获得异变N」并有测试），触发与授予补上后异变10自动生效 |
| F5 | 专属道纹·引擎侧发动编排 | 三副本怪物池专属道纹 | 引擎三档：①状态口径已接——加害/龙鳞/裂变/贯穿（combat.py:153-187）、活血（:279,393-395）、坏死禁疗（:453-454,802-831）、畸变标记（:320-352）；②效果公式已接——赌命（:824）、清算（:839）、逼债（:850,883）、赎金/洗劫（:886）、退化/定型（:445-451）、变形/超频（:867,930-931）；③**零实现**（仅 combat.py:21-23 集合枚举）——逆鳞/嫁祸/背负/衰败/抵扣/消灾/伤痕/增殖。且怪物自主激活优先级仅覆盖原始道纹系（combat.py:1094） | 第③档的引擎实现 + 怪物发动专属道纹的编排；引擎/模拟器两套结算存在口径漂移风险，实机端到端目前用不了这批道纹（24种完整的怪物侧效果只在 `sim/balance_sim.py`） | 是否引擎实装（工作量大）；先实装哪些 |
| F6 | 工具库使用效果 | 扭曲都市工具库清单（README） | 引擎：授予端已实装（`TWISTED_TOOL_LIBRARY` + 事件附赠发现，测试覆盖 正常/非法选取回退/全持有边界）；**8件工具的效果只在 `sim/run_sim.py tools_phase`** | 引擎侧使用结算（耐久、干扰仪封激活、水枪清持续、探照灯蒙蔽等8件） | 无，纯实现 |
| F7 | 通关战报生成器 | 用户指令（2026-08-10 会话内提出后被合并指令打断） | 旧稿 `战报_完整轮回.md` 是**降服时代产物**（满屏★taming），机制已失效，**不能作为现行结算证据**，仅留档 | 两个未答裁定：①生成链路——A全程模拟器 / B引擎API驱动 / C两份；②副本——任一 / 扭曲 / 罪孽 / 龙心谷 | 上述两问，答后即做 |
| F8 | 失信黑名单机制 | README 对应机制文本 | 死字段 `engine/models.py:447-448 blacklist_level / is_blacklisted`（代码零引用） | 机制接线（员工离队累计→黑名单）或删字段 | 删 or 接 |
| F9 | 遗物选择字段 | 同上 | 死字段 `engine/models.py:455 relic_of_choice`（代码零引用） | 接线或删 | 删 or 接 |
| F10 | 活力四方案死代码 | 裁定⑫已否决甲/乙/丙/丁四种活力文本口径 | `sim/balance_sim.py:484-487 HUOLI_MODE` 开关保留四套候选实现，默认 `current` | 删除候选代码或留作档案 | 删 or 留 |
| F11 | 文档一致性修订 | README | ① README:318-319「全程自动触发」列表漏列三个条件触发事件：凡庸（:504）、癌变（:507）、崩解（:538）；②命名三足漂移：事件名「癌变」（README:507，恢复量致死）vs 代码 `is_proliferated` / 注释「增生」（models.py:217-219、combat.py:584）vs 怪物道纹「增殖」（池面板，扭曲都市）——三者指不同物；③ `engine/README.md` 与 `README.md` 未全面复审 | 补列表、统一命名、全文复审 | 命名采哪个为准 |

## 二、待平衡 / 待裁定（改任何一条 ⇒ 标注的重测范围必须重跑）

| # | 事项 | 现行口径与数据（可核查） | 影响面 / 重测范围 |
|---|---|---|---|
| B1 | **赌命文本歧义** | README「失去30%[血限]的当前生命」两种读法；引擎实现=**当前生命×30%**（combat.py:824 注释自证「赌命：当前生命百分比」）；字面另一读=血限×30%（满血时约3倍狠） | 改读法 → 罪孽都市全套重测 |
| B2 | **嫁祸目标策略** | 模拟默认 `JIAHUO_POLICY="player_first"`（依规则#6无道德底线）；改 `ally_first` 龙心谷约 +3pp（29.3%→≈32%） | 改策略 → 龙心谷重测 |
| B3 | **崩解自毁是否送分** | 持续计费使高数值原始道纹怪自缴异变、约2-3回合自行崩解；900局崩解5658次，是玩家主动击杀之外的最大怪物减员源。「代价体系自平衡（特性）」还是「玩家可苟等死（漏洞）」未裁定 | 若改计费/阈值 → 三副本30%全部重测 |
| B4 | **单体路径失衡** | 单体口径：雕塑100%、增生97.2%、还债97.2% vs 杀伐66.7%——三条移出路径单体层几乎无反制；同时四策略对毒枭全0%（全场唯一0%怪=唯一活力持有者） | 若给怪物反制（如抗移出）→ 单体+全程全重测 |
| B5 | **活力悬崖红线（存档）** | 历史实测：12只全池×活力1=0.0%；4只×活力3=0.3%；毒枭独持活力2=29.7%；≥2只×活力≥2=全灭区（0.3-10.7%）。活力数量/数值是10pp级震荡源 | 今后新增或调整任何活力怪 ⇒ 全量重测，本条为调平红线 |
| B6 | **模拟结论的有效性边界** | 通关率30%成立的前提：①玩家=脚本策略（杀伐焦点/致命庇护/低血再生/≥3怪冲击）+激进修行；②扭曲都市按每局固定2件工具（前2场各1精力探索）；③单体口径玩家初始60碎片；④撤退未建模（F1）；⑤困境探针=≥1信号（裁定⑦） | 任一前提变更 ⇒ 对应副本重测；实机DM操作差异±若干pp属预期 |
| B7 | **败亡节奏（供体感裁定）** | 败亡分布：罪孽 第6-7场 26%/21%，龙心谷 26%/24%（后期陡）；扭曲 分散在第2-7场（2-16%）；进化2919次/900局 | 是否为期望节奏属设计裁定，数据存档 |

## 三、复现命令汇总

```bash
python tests/test_engine.py        # 引擎测试，基线 24/24
python sim/audit_monsters.py       # 面板审计，基线 0/36 违规
python sim/run_sim.py all 300      # 全程通关率（种子2026），基线 31.0/29.7/29.3 平均30.0
python sim/balance_sim.py          # 单体1800局×4策略（种子42），基线 66.7/100/97.2/97.2
```
