# 报告（统一过程信息文档）

> 口径（CF-4，2026-08-24；2026-09-10 五审按用户指令清空）：本文件**只留未解决/未判定的问题**。
> 已结案结论与逐手实录 → `archive/report_history_*.md`；机制知识 → `AI_EXPERIENCE.md`。

# 待裁定：未解决问题清单（2026-09-10 五审）

## ① 养蛊管线：怪物墙（**裁定更新 2026-09-10 四审：走道纹深度，A~D 全不取**）

> **用户裁定（2026-09-10 四审）**：「倾向于用其他道纹解决，不靠道纹就能通关反而违背设计——
> 那就是纯粹数值对撞」。方案A(削怪物HP)/B(回蓝)/C(输出系数)/D(出怪构型) **全部不取**。
> 落地动作：**事件拒绝选项 → 随机获得一种残韵**（裁定同日下达，已实装，见下）。

### 已实装：拒绝→随机残韵（DM 裁定 2026-09-10）

- **机制**（`engine/api.py::_action_resolve_event`）：真拒绝选项
  （`_is_reject_option_text`：REJECT_OPTION_KEYWORDS 或「拒绝：」前缀；带代价的
  「拒绝改造」类不算）结算成功后，引擎 dice 随机授予 转换/反转/曲解 之一入
  `State.resonance`（与既有事件授予同存储，局中残韵插队可耗）。同 seed 同选项
  同结果（可复现）。测试 `tests/test_event_reject_resonance.py` 3 例。
- **实测（24 种子养蛊，WinOnly 全链 + 新机制）**：7 通关 **0/24**；cleared 分布
  `[5,0,0,0,4,0,1,0,0,0,4,5,2,5,0,0,0,0,1,5,2,4,2,0]`（与上轮持平，个位噪声）；
  拒绝奖励发放 **30 次**（≈1.25 次/局），类型 曲解11/反转11/转换8（均匀）。
- **结论（如实）**：机制生效但**量不够**——两处断点：
  ①**获取频率**：模拟器 AI 几乎不探索（探索在局外 policy 权重低、精力被修行/学习挤占），
  平均每局只摸到 1.25 个事件；②**输出链**：残韵改写的是道纹质量（范围/阈值/爆发），
  但改写后伤害仍走 X=法力——一池法力 12-16 的每场输出预算没变，204-252 血的墙
  靠现有法力预算依然磨不动。
- **五审实测（用户「去试就知道了」，同日）**：「探索」提为局外优先（每场第一个窗口
  免费摸事件，`build_learner` AI 层；探索发现的事件当场结算——**踩坑留痕**：早期版本
  continue 跳过循环尾的事件结算块，未决事件门禁一切局外行动→死锁哨兵 24/24 速死，
  已修）。结果：事件 1.25→**3.1/局**，拒绝残韵 **2.9/局**（深局 12 个/局），三型均匀
  （反转20/曲解25/转换24）——但 **7 通关仍 0/24**，cleared 合计 40→38（种子噪声内持平）。
- **结论（证据闭合）**：残韵**频率**不是瓶颈。残韵改写的是道纹质量（范围/阈值/爆发），
  伤害仍走 X=法力——一池法力 12–16 封死单场输出预算（60–80 vs 墙 204–252），残韵再多
  也变不出更多法力。**例外即线索**：游戏里已存在**无 mana 伤害路径**——固执→反转→血债
  （代价=流血，不走法力；死斗实录已实锤该链可行）。PvE 里 AI 几乎从不把它拼出来。
- **下一步（AI 层，待跑）**：让 WinOnly 提案器识别「无 mana 伤害路径」——候选生成时
  对 残韵转化→流血代价类道纹（血债流）给结构性探测（预演里区分 cost_type=流血 的
  伤害与 cost_type=消耗 的伤害），验证「道纹深度破墙」是否成立。这仍是 AI 层，
  不改任何规则数值；若拼出链后仍破不了，则该构筑方向本身缺强度，回用户裁。

### 原方案菜单（四审全部不取，留档）

9,000+ 局扫描 0 个 7 通关胜者（旧报告 2026-09-10 ①，归档）；现行死斗选手全部来自旧计价绝版快照。

现状数字（引擎实测）：扭曲都市前 6 场怪物墙 ≈**1776 总 HP**（单只 204–258，攻次×攻力
1×8~4×5），第 7 场 4 怪围攻再 +900；新计价玩家单场输出预算 ≈**60–80**（法池 12–16 一池制
×杀伐 5X）+普攻零头 → 7 场累计 ≈500–700。**缺口 ≈×3**。WinOnly 全默认后 cleared 分布已
抬升（最多 5），但 0/24 到 7。四个方案（可组合），任一都动规则面、等您点头才动：

- **方案A（推荐）：怪物面板重校**——`副本/*.md` 数据按新输出定标：单只 HP 204–258 →
  **60–90**（≈2–3 次有效出手杀一只），第 7 场围攻总量 ≈280–360；攻次×攻力维持不动
  （死亡压力保留）。只改数据文件+怪物生成读取，战斗规则零改动；PvP 死斗完全不受影响
  （怪物不进死斗）。可逆性最好，改回即回滚。
- **方案B：回始回蓝**——法力一池制改为[回始]回填 1/3 法限 → 全程输出 ×~2.5。动
  `combat.py` 回始时序（部分撤销 2026-09-10 一池制裁定，需您明示幅度）。联动：PvP 死斗
  也回蓝 → 死斗更胶着，现有死斗结论（先手 R1 秒杀）会被稀释。
- **方案C：输出系数回调**——杀伐 5X→10X（DM 2026-09-10 注记嫌 12 夸张才定 5）或普攻
  公式加成。单点改动 `daowen.py`。联动：死斗秒杀线更近（26 血方对 4 血方更极端）。
- **方案D：出怪构型**——第 7 场 4 怪围攻 → 2 怪/单 boss（`副本/*.md` 流程数据）。
  只削峰值压力，前 6 场墙仍在，单独用大概率不够。
- 组合建议：**A 单独即可闭合缺口**（1776→~600 ≈ 玩家 7 场输出 500–700）；要留终局压力
  可 A+D。B/C 会改死斗生态，慎选。

9,000+ 局扫描 0 个 7 通关胜者（旧报告 2026-09-10 ①，归档）；现行死斗选手全部来自旧计价绝版快照。
原三选一（重校怪物/回调计价/维持现状）并入本菜单。

## ② 普攻候选在预演世界「挥空」的假账路径（**未修，待裁定**）

`LJ_AI_BASIC_ATTACK=1` 下 `[真实] resolve_attack` 的变化被回滚（预演世界账面混入实录；
同源「68 盾/法力 51」假账机制），深度计数器未拦住。真实世界该普攻 0 伤（法力空=攻力 0）。
待裁定点：修预演隔离，还是禁用 PvP 内普攻候选。裁定前不动。
复现：`python3 sim/duel_diff_trace.py [挑战者] [守擂] [seed]`。

## ③ `enemy_gone` 尸体误判：探针把「打自己」判成 remove（**未修，待裁定**）

`_diff` 的 enemies 含动作前已死者，`_digest_diff` 不区分「本来就死/这一手打死」→
场上有尸体时自指变体白得 remove +6.0 向性分，压过真输出（2026-08-31 实测：有尸体时
杀伐X=1 打自己）。影响面=多怪战斗首杀之后；1v1 死斗不触发。
修法候选：`_diff` 补 `alive_before` 或 digest 只认 `hp_before>0`——行为变更，待裁定。

## ④ 带伤续战做不了：封存格式缺「崩解／癌变」进度条（**未做，待裁定**）

`_serialize_entity_full`（api.py:4062-4079）不写 `mutation_count`/`total_healed`，
反序列化静默清零 → 「带伤续战」是假的。要真实现须扩封存格式（写读两端），属规则口径改动。

## ⑤ 自保时钟：默认关；病根已修，是否重跑 132 局对照（**待裁定**）

`SELF_PRESERVE` 保持默认关（132 局对照几乎无效：候选池里「能掉血」候选恒 0，加权加不到）。
当时查出的病根——回复牌被探针钉在敌人身上——**已于 2026-09-10 修复**（heal 意图口径 +
窄幅并列裁决）。要不要重跑前后对照给数据，待裁定。

## ⑥ 杀伐自指倾泻的评分盲区（**定位完成，未修，待裁定**）

死斗实录实锤（换席 seed 9/11/12）：挑战者预演如实看到「法力 16→0、盾+25」，仍一手把
全部法力（反转下=全部攻力）换成盾 → 空手 → 回终「化雕塑」阵亡（hp 3–4/36、盾 46–66 完好）。
评分层：盾收益 +1.1/点 ≫ 法力损耗 −0.12/点，且无「法力=攻力」折算。修法属评分层
（法力损耗按当前攻力等价折价），待裁定后动。

## ⑦ 回终「化雕塑」× 死斗判定的交互（**规则面观察，待裁定**）

3 局长局死因均为化雕塑，判定文案只写「挑战者阵亡」，不区分 被击杀/化雕塑/凡庸。
规则是否应区分/豁免，待裁定。

## ⑧ 坏死 × 法力一池制：实测「付5烧0」（**待查**）

现行实录：坏死X=1 付 5 法力，目标法力面板无位移；与旧实录「烧 1」不一致，
疑有状态/残韵改写参与。规则面观察，未动。

## ⑨ 评分守卫测试未写（**待办**）

「0 效候选不得压过预演已验证必杀」应种子化进 tests/（素材：seed2 压制期权 119.24 vs 必杀 107.6，
本轮已修但无守卫测试防回归）。

## ⑨+ 胜负唯一计分=一切打分默认（**已实装，用户裁定 2026-09-10 三审：不只死斗**）

- **用户裁定（2026-09-10，长期有效）**：评价口径只有胜负——胜 +1 / 败 −1，无论什么手段
  什么战术。启发式效用分（得分 119.24 之类）不再作为好坏标准，只允许当**提案裁剪器**。
- **实装**：`sim/win_only_ai.py::WinOnlyAI`——候选由基类生成并启发式排序取前 4，逐候选在
  引擎世界深拷贝上真实打出本手，再以基类 TacticalAI 把整局推演到底，按结局 ±1 取最优
  （平/未决=0）。世界切换镜像 `ai_preview` 换世界口径；`run_duel_pvp` 新增 `resume=True`
  续跑模式（常规调用行为不变，base 口径 md5 复验一致）。
- **±1 实测（8 seeds × 两席，双方都用 WinOnly）**：正席 26 血方 **+8/8**，换席 4 血方
  **−8/8**——与启发式口径的 48/48 结构性结论完全一致。**在用户自己的计分下，胜负仍由
  面板结构决定，不是打分函数的锅**；要让 4 血方有得赢，须动规则面（见①）。
- **已设为默认（三审扩围：PvE 战斗、死斗、养蛊链全默认）**：`run_duel_pvp` 默认
  WinOnlyAI（守卫钉死）；`build_learner._play` PvE 默认 WinOnlyAI；遗言桥降级为
  **提案器**（`win_only_cls(LegacyAwareAI)`：遗言偏见仍影响「试什么」，「选什么」只认
  整局推演的 ±1）。推演内环钉死基类防递归；`LJ_WIN_ONLY=0` 总开关退回启发式
  （千局级扫描用，正常跑不必）。守卫测试 `tests/test_win_only_ai.py` 5 例。
- **全链实测（WinOnly battles+duel，普攻武斗构筑 24 种子）**：7 通关仍 **0/24**，但
  cleared 分布整体抬升（5/0/0/0/5/0/1/0/0/0/4/5/2/5/1/0/0/0/1/4/2/4/4/0，多个种子
  0→4/5），性能 ~7.6s/种子（可接受）。**推演判分本身让选手活得更久**——胜负导向
  直接兑现了。7 通关仍差最后一段，缺口在怪物墙（见①，待您选方案）。

## ⑩ 碎片：局外花光已修；两处残留待裁定（**部分已修**）

- **已修（sim 层 2026-09-10，用户质询「局外为什么不把碎片花完」）**：`build_learner` 的
  spend_shards 原逻辑只买 修行tier3/tier2 且 tier2 被 todo 门控——后期一窗收入可 >100
  （奖励=ceil(战始血限×2%)+5×道纹数/怪），3 精力×35 的花费容量必然攒钱。改为**档位阶梯
  从高到低买**（tier6=150→tier2=15），窗口内花到 <15 零头为止；实测深局支出 145/局、
  剩余 4（≈98% 花掉）。**窗口账（用户核对）**：7 场 × 3 精力 = 21 个局外窗口，容量
  21×最高档 150 = 3150 ≫ 全程收入；花不完的不是容量，是**活得短**（多数局第 1 场即死，
  只用上 1–3 个窗口、收入本来就近零——死了没有「该花没花」）。
- **残留①（规则面，待裁定）**：快照=第 7 场通关瞬间（engine final_crown 流），**最后一场
  的奖励必然冻结**在快照里，之后没有局外窗口。要清零需改封存时点（允许通关后一个
  花光窗口）——动引擎流程，待裁定。
- **残留②（规则面，待裁定）**：死斗**内**碎片经济缺位依旧——双方实体 `shards=0`，卡组
  无碎片代价牌（赌命/消灾不在双方道纹内，发动也读实体自身碎片=0）。要接入需规则裁定
  （带币入场/赛前购买）。
- 历史注：存量快照 普攻武斗_69/429 的 93/174 剩余碎片是旧链化石（当代链复现 cleared=0，
  第 1 场即死），不可复现、用完即绝版。
- **「花钱数值必高」A/B（同种子同构筑，唯一差异=花不花碎片，2026-09-10 实测）**：
  seed1 花钱 cleared 0→**5**（速 5→12、攻 8×18）；seed11 速 5→8、攻 5→8；seed20 同为
  cleared=4 时 速6/法6 优于 速5/法3。**数值面结论成立：新计价下碎片直接买 速/法=买攻次/攻力，
  花钱角色面板全面更高。**但如实记录反例：seed12 不花钱反到 cleared=6 > 花钱 4——存活还有
  出怪运气方差；且 花光口径+WinOnly 全开后 24 种子仍 **0/24 个 7 通关**（多数第 1 场死于
  204–252 血怪，见①规则面）。「7 通关结构性不可达」的瓶颈在怪物侧规则，不在花钱与打分。

---

# 当前禁区清单（证据不足，禁改；遇新证据可重启评估）

- 血僵 4×19/270、尸霸 5×15/264 数值强度：强度确认，「玩家侧策略空间」存疑；数值禁改，重启需血限型构筑复测。
- 乱葬岗全池 0/8 each：二阶对一阶构筑碾压，结构性难度裁定待 DM。
- 冥气削速限的速度：组合放大口径维持（机制=速限压制型，已复现）。
- 事件收益数值：探索因果效应 ≈0（CI 含 0），禁改事件收益/风险；重启需 300+ 局多副本或静态扫描。

## 反向禁区（禁止「顺手修好」，违反即自造规则）

- 不得给面板加自造上限：除【不朽之躯】外，① 禁给 `gain_shield()` 加「≤血限」clamp；
  ② 禁给「获得法力」加「≤法限」clamp。超上限是合法面板（曾两次误加，已由 `6985481`/硬伤1 撤销）。

---

# 移交后需复跑

- `python -m pytest tests -q`：**1436 passed, 3 xfailed**（基线）
- `python3 sim/duel_chronicle.py SEED [--swap]`：死斗逐手实录（前后面板+参数+判定；
  skip=battle_start/battle_end/resolve_ally_phases）。正典对局=普攻武斗_69 vs 普攻武斗_429。
  同 seed 复跑逐字节一致（md5 已验）。当前格局：26血/速12 方两席 48/48 全胜（详见归档 2026-09-10）。
- `python3 sim/duel_diff_trace.py [挑战者] [守擂] [seed]`：逐次 diff + 面板不变量自检。
- `python3 sim/breed_and_duel.py --breed N --per-build 2 --seed-start S`：养蛊（**会先清空
  `data/breed_winners/`，续养前必须备份**）。当前池 12 档。
- `python3 sim/self_preserve_experiment.py audit --pairs 12`：回复流向归因审计（约 40 秒）。
