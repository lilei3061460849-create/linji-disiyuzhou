# 报告：养蛊死斗——用当前引擎重新养蛊两个真实胜者，再做第8场死斗（2026-08-30）

> **口径**：本报告只记录「当前引擎重新养蛊 → 第8场死斗」这一条线的完整过程与实测结果。
> **来源**：所有角色均来自 `data/breed_winners/`（当前引擎 `_play(battles=7)` 逐种子跑出的通关快照）；
> 死斗由 `run_duel_pvp` 逐手真实结算。**不使用旧快照、不编造数值、不去重旧报告。**
> 旧 `data/real_winners/winner_0X.json`（玄夜/青梧、贾凡等）与当前引擎不配对，已不作为基准（见下文《为何重新养蛊》）。

---

## 为何重新养蛊

旧基准 `data/real_winners/winner_0X.json` 是旧引擎/旧平衡的产物。用**当前代码**重放其真实胜者种子，只得到 `cleared = 5 / 0 / 2 / 0`（seed 76/82/127/301），**到不了 7 场**。所以：

- 这批旧胜者快照与当前引擎**不配对**，不能再当"真实一阶胜者"基准；
- 任何"拿旧胜者打一轮死斗 / 靠 bug 法术过关"的旧结论都不可复现，弃用；
- 本报告改由**当前引擎重新养蛊**，产出**可复现**的通关胜者，再做死斗。

为此新增了养蛊+死斗脚本 `sim/breed_and_duel.py`（含 `breed()`、`run_breeder_duel()`），并修复了死斗 PvP 驱动 `sim/duel_pvp.py`（见下文）。

---

## 一、养蛊：逐种子跑当前引擎的 `_play(battles=7)`

**流程**（`python3 sim/breed_and_duel.py --breed 5`）：

1. 对每个构建（杀伐法攻 / 封印控制 / 速战速决 / 血厚耐打）× 逐 seed，调用 `build_learner._play(starter, learn, "扭曲都市", seed, battles=7, attrs=...)`。
2. 只有 `cleared == 7`（通过全部 7 场一阶）才视为"通关胜者"。
3. 从封存槽 `sealed_candidate_path` 读出该局**真实跑完 7 场**的快照（含面板/道纹/法术/残血），写入 `data/breed_winners/`，并带上 `origin={build,seed,cleared}` 可追溯。

**产出**：6 个当前引擎可复现的通关胜者（`data/breed_winners/`）：

| 文件 | 血 | 法 | 速 | 道纹 | 法术 |
|---|---|---|---|---|---|
| 封印控制_21 | 36 | 36 | 12 | 再生/封印/庇护/杀伐/衰败 | 先发制人/生生不息/后发制人 |
| 封印控制_23 | 36 | 30 | 12 | 再生/庇护/弱化/杀伐/束缚/畸变 | 先发制人/生生不息/后发制人 |
| 封印控制_58 | 36 | 34 | 13 | 僵化/再生/封印/庇护/杀伐 | 先发制人/生生不息/后发制人 |
| 杀伐法攻_45 | 36 | 34 | 14 | 再生/庇护/自残/血债 | 后发制人 |
| 杀伐法攻_58 | 36 | 32 | 11 | 庇护/弱化/杀伐/畸变/超频 | 先发制人/后发制人/生生不息 |
| 杀伐法攻_78 | 36 | 30 | 10 | 封印/庇护/畸变/蒙蔽 | 后发制人/生生不息 |

> 清一色速度 10–14——这本身就是"速度是通关刚需"的实证（见《三、为什么不用速度》）。

---

## 二、死斗：`run_breeder_duel` + `run_duel_pvp` 对称 PvP

### 2.1 死斗进入（`run_breeder_duel`）

用**扭曲都市（tier=1）**搭建（乱葬岗 tier=2 与单胜者封存槽不匹配，会读不到二阶队列而 `outcome="sealed"`）：

- `GameEngine(db, seed, sealed_candidate_path=守擂快照)` → 挑战者走 `setup_attributes → finish_initial_daowen → setup_choose_resonance(反转) → setup_choose_region("扭曲都市") → load_winner(挑战快照)`。
- `energy=0; current_battle=6` → `start_battle` → 清场 → `battle_end` → `final_crown outcome="duel_start"`。守擂主将出现在 `state.enemies`（轮回者），`in_final_duel=True`，先手由 `duel_turn` 决定。
- 之后交 `run_duel_pvp` 做**对称 PvP**：挑战者侧由 `player_act()` 驱动，守擂侧由 `_resolve_opponent_one()` 驱动，双侧都按轮回者规则（法力制 / 出手≈速限/3 / 道纹 X / 逐出手交替）。

### 2.2 死斗驱动修复（`sim/duel_pvp.py`）

之前死斗**跑到一半就判"死斗死锁"返回**，不是打不死、而是驱动提前拦截。定位到两处：

- **误判死锁**：本回合法力耗尽后，双方面板连续 60 步零变化，`run_duel_pvp` 立即判"死斗死锁判卫冕"，在 `round_end`/次回 `start_round` 回蓝**之前**就返回，导致真实对耗没机会打满。
  - 修复：本回合零变化 → `break` 让 `round_end` → 次回 `start_round` 回满法力、重置出手，继续对耗；**只有"回始回满后仍连续两回合面板零净变化"才判真死锁**（如 1 血 0 牌互瞪）。
- **次回 `round_start` 撞 subphase 守卫**：守擂侧由 `_resolve_opponent_one` 驱动、不把 `combat_subphase` 推回 `await_round_end`，次回 `round_start` 撞 guard（当前为 `player_actions`）失败，法力永不满。
  - 修复：在 `round_end` 前显式设 `combat_subphase = AWAIT_ROUND_END.value`。

### 2.3 残血继承（不人为回满）

死斗双方**继承第 7 场战终的真实损耗**。养蛊胜者快照里 `current_hp` 是真实残血（如 封印控制_21=36 满血、封印控制_23=7、杀伐法攻_45=4、杀伐法攻_58=5、封印控制_58=14），**不是人为回满**——这是"损耗影响死斗"的真实规则。

### 2.4 挑战者策略：先防御/控制、再输出

守擂常持【先发制人】（受到伤害前用杀伐反打）。挑战者若不设防、一上来就杀伐，会被**瞬间反杀**（满血也扛不住）。故 `_make_breeder_act` 改为：
- 每回合先做一次防御/控制（庇护加盾，或封印/束缚/衰败压制守擂）；
- 再输出：代价型（血债/自残，不耗法）优先，杀伐用 `mana_budget = 剩余法力 // 剩余出手` 均摊，避免一发打光法力后整回合空转。

### 2.5 死斗实测结果

用快照原始残血、固定种子（两次 seed 结果一致，可复现）：

| 配对（挑战者 vs 守擂） | 挑战者残血 | 守擂残血 | 结果 | 判定 | 分胜负回合 |
|---|---|---|---|---|---|
| 封印控制_21 vs 封印控制_23 | 36 | 7 | 挑战者胜 | 守擂主将阵亡 | 第1回合 |
| 封印控制_23 vs 封印控制_21 | 7 | 36 | 挑战者胜 | 守擂主将阵亡 | 第1回合 |
| 杀伐法攻_45 vs 杀伐法攻_58 | 4 | 5 | 守擂方胜 | 挑战者阵亡 | 第1回合 |
| 杀伐法攻_58 vs 杀伐法攻_45 | 5 | 4 | 守擂方胜 | 死斗死锁（回始回填后双方面板连续零净变化） | 第2回合 |
| 杀伐法攻_45 vs 封印控制_23 | 4 | 7 | 守擂方胜 | 挑战者阵亡 | 第1回合 |
| 封印控制_58 vs 杀伐法攻_58 | 14 | 5 | 守擂方胜 | 挑战者阵亡 | 第1回合 |

**几点如实说明**：

- **多数能在当回合分出胜负**，且两次 seed 结果一致（可复现），不是驱动写死、也不是随机碰运气。
- **挑战者策略必须"先防御/控制、再输出"**：守擂持【先发制人】，挑战者无脑先手杀伐会被反杀；先下庇护/封印后就反制守擂。这是真实法术反击机制——"为什么不能无脑先手杀伐"的答案。
- **存在"防御均势死锁"**：杀伐法攻_58 vs 杀伐法攻_45 陷入真死斗——双方都持庇护/再生/后发制人，互相磨不死，回满法力后仍零净变化，判"死斗死锁（回始回填后仍零净变化）"。这不是 bug，是**纯防御型对耗的自然结果**（两位都是保命流）。
- **角色名"贾凡（对手）"**：不同种子养出的胜者在引擎里都继承封存槽默认名"贾凡"，只是面板/道纹不同；这是命名层，不影响死斗真实性、不影响结算。

---

## 三、为什么不用速度？速度到底多宝贵

用实测数据回答（25 点预算、扭曲都市、60 种子采样、`_play` 杀伐/庇护/再生）：

| 配置 | 场均通关 | ≥3 场率 | 出手≈ |
|---|---|---|---|
| 标准 6/8/11 | 1.90 | 28.3% | 3 |
| 低速 6/5/14 | 1.38 | 18.3% | 2 |
| 高速 6/11/8 | 2.00 | 30.0% | 4 |
| 血牛 9/5/11 | 1.45 | 18.3% | 2 |
| 速法 6/10/9 | 2.03 | 28.3% | 4 |
| 极速 6/13/6 | 1.83 | 31.7% | 5 |

**结论**：

- 速度**决定出手次数**（≈`speed_limit/3`）与闪避池，是**通关的核心属性**——所以养蛊真胜者清一色速 10–14，且"标准/高速/速法"都比"低速/血牛"好。
- **但不是越高越好**：极速 6/13/6 场均 1.83，反而低于 速法 6/10/9 的 2.03。原因：把点数堆满速度后，**法力/庇护/再生的预算被挤占**，出手次数虽多但招架不住（过度闪避烧光速度、挤占反击资源——旧 KB 教训）。
- 所以"为什么不用速度"的答案不是"速度没用"，而是：**速度要用，但不能极端堆**；最优在速 10–14（能多出手且还留足法力/血线），过高挤占生存与输出导致整体通关率回跌。

---

## 四、结论

- 养蛊环节：用当前引擎 `_play(battles=7)` 重跑，可复现抓出 6 个 `cleared=7` 的通关胜者存入 `data/breed_winners/`（可追溯 `origin`）。
- 死斗环节：`run_breeder_duel` 让两个真胜者经扭曲都市直达 `duel_start`，再由修复过的 `run_duel_pvp` 对称 PvP 真实结算；多数配对当回合分出胜负，两次 seed 可复现；存在一例"防御均势"真死锁（保命流互磨天然结果，非 bug）。
- 速度：核心属性、决定出手与闪避，但**不可极端堆**；最优在速 10–14。
- 旧 `data/real_winners/winner_0X.json` 已弃用（当前代码重放不可复现），不再作为"真实胜者"基准。
