# 报告（待解决问题清单，唯一正本）

> 口径（CF-4）：本文件**只列未解决、待用户裁定的问题**。解决一项删一项。
> 已结案实录 → `archive/report_history_*.md`；r8 终局报告 → `data/handplay_20260911_b60r8/报告.md`。
> 当前状态（2026-09-11）：r8 扭曲60 验证终局 **6/7（B6），B7R7 阵亡**。

## 一、待拍板

| # | 问题 | 状态 |
|---|---|---|
| 1 | **波次出怪（用户提案）**：不再一次性全出，每3轮+1怪直到上限。算术结论：**能解B7**（r8实测84/轮：R1-R3杀爬行者吃60 → R4-R6杀第二只吃40 → R7-R9熔孢子吃28 → R10-R12杀奇美拉吃36，总184 < 承伤池370；每波3轮内清掉，场上不超2怪）。待批准实施。选项：A全战斗启用（B2-B6同步变简单）/ B仅3+怪战斗启用（B1/B2不变） | 待批准 |
| 2 | 闪避耗速（出手数＝速度，战终才回） | 挂起，用户：看1的情况 |
| 3 | 衰败永久：查实**可闪避**（道纹对象填dodge True，耗速1，README:161；B7R6是我填false吃满，且当时速度0也闪不起）。反制存在，约束在速度资源（并入#2） | 待确认关闭 |
| 4 | **退场占位（重写）**：部署位只有2个。员工残血会自动退场，退场后不能攻击也不能护卫，**但还占着部署位、还按部署轮数拿工资**；换一波坦要花玩家2个出手（整轮0输出，B7R6实测）。等于残血坦卡死位置，换坦代价一整轮。改法：退场自动腾位（换坦低成本）vs 保持（换坦是战术成本） | 待拍板 |
| 5a | 怪崩解自杀（用户令：怪也不要出现nc情况）：e3连发原始飞行异变50崩解。以后手操/AI提交怪道纹时规避自杀（异变+5X≥50不选原始）。是否加引擎护栏待定 | 待定 |
| 5b | 轮回者崩解对称：查实规则已对称——封印代价异变8X（通用道纹，玩家可学），`add_mutation`注释"任一角色达50直接命零"，`api.py:5084`有玩家检查。用户说得对：没异变道纹≠不触发 | 关闭，无需改 |
| 6 | 冻结三项：扭曲40试点 / 血限成长 / 1:1兑换 | 仍冻结 |
| 7 | 飞行引导②：反制＝残韵知识＋库存，新手/AI易漏（教学问题） | 保留 |

## 二、引擎异常复现结论（已全查清，待删）

| # | 异常 | 结论 |
|---|---|---|
| 1 | 行者B5战终62→53莫名−9 | **机制正常**：B5R3回始衰败tick打沈昼9点，被行者背负挡下（`shuaibai_tick`+背负联动，trace实锤）。附带证实背负可挡道纹tick |
| 2 | 被背负（r8末72）效果 | **纯标记无结算作用**：护卫X=盟友下X次代挡（`_beifu_left`次数池），"被背负"只是玩家身上的显示标记（极性误标debuff易误读，可选小改极性） |
| 3 | 拒付黑名单1影响 | **无异常**：3次拒付才禁雇佣（`blacklist_level≥3→is_blacklisted`），r8仅1次；且只禁雇佣不禁接纳 |
| 4 | B7R2反转剥飞行被拒 | **我的参数错误**：残韵改写正确动作是`use_resonance`（r5成功先例：转换飞行→滑翔），我误塞进`spell_choices.before`（反应法术槽，空）。附带：B7复盘更优线＝use_resonance剥原始（狂暴/强化/飞行）→变可救赎，待波次验证时用正确姿势 |

## 三、陈年待办（用户记忆正确，待办节②③④全已处理）

- ②`enemy_gone`探针误判：已修（`alive_before`分支在`ai_tactics.py:361`）。
- ③带伤续战封存：已修（`mutation_count`入封存；`total_healed`经DM裁定为局内量、战终归零）。
- ④自保时钟：已合并且默认开（`SELF_PRESERVE`默认1；`_probe_cache`机制还在，属AI内部口径）。
- sim残留③普攻挥空（预演显示层假账，真实战斗不受影响）＋④养蛊走向三选一（"无7通关"前提已被r5证伪）→ 请示关闭。
