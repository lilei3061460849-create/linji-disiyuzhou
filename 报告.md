# 养蛊胜者（可靠数据）

> 来源：`data/breed_winners/`（当前引擎 `_play(battles=7)` 逐种子跑出的 `cleared==7` 通关胜者，确定性可复现）。旧 `data/real_winners/*` 与当前引擎不配对，弃用。
>
> ⚠️ **死斗过程、死斗结果表格、死斗切片一律不再写**——死斗数值目前是假账（见下方「待办」），写出来就是误导。本阶段报告只呈现可复现的养蛊胜者。

## 养蛊产出（6 个可复现通关胜者）

| 文件 | 血 | 法 | 速限 | 出手 | 道纹 | 法术 | 死斗前准备残韵 |
|---|---|---|---|---|---|---|---|
| 封印控制_21 | 36 | 36 | 12 | 4 | 封印/杀伐/再生/庇护/衰败 | 先发制人/生生不息/后发制人 | 无 |
| 封印控制_23 | 36 | 30 | 12 | 4 | 束缚/弱化/杀伐/畸变/再生/庇护 | 先发制人/生生不息/后发制人 | 无 |
| 杀伐法攻_45 | 36 | 34 | 14 | 5 | 血债/庇护/自残/再生 | 后发制人 | 无 |
| 杀伐法攻_56 | 36 | 25 | 13 | 5 | 封印/庇护/再生/弱化/定型/无神 | 后发制人/生生不息 | 曲解1、转换1（战终三相残韵盘再+） |
| 速战速决_5 | 36 | 28 | 14 | 5 | 杀伐/庇护/加速/自食/再生 | 先发制人/后发制人/生生不息 | 无 |
| 速战速决_6 | 36 | 22 | 15 | 5 | 杀伐/庇护/弱化/再生 | 先发制人/后发制人/生生不息 | 无 |

---

# 待办（尚未落实的改动，作为下个会话的交接清单）

> 这些是用户明确要求、但**尚未动手改代码**的项。每一项都写清「现状 / 改哪里 / 为什么现在没动 / 验证方式」。新会话按此清单推进，不要凭空改坏引擎。

## 硬伤 0：报告死斗内容不写

- ✅ 已删：死斗结果表格、「最精彩一战」完整过程、死斗切片。全用本说明替代。
- 原因：死斗结算数值经逐次 diff 核验为假账（见硬伤1），写出来就是误导用户。

## 硬伤 1：死斗结算数值假账（必须先修，才能让死斗可信）

**已核实的假账证据**（`封印控制_21` vs `杀伐法攻_45`，`run_duel_pvp` 逐次 diff）：
1. 满血 36 的挑战者盾堆到 **68**（`挑战hp=36 盾=68 法=0`），格挡远超血限 → 几乎无敌、双方只盾变血量恒 36。
2. 守擂 4 血靠 `再生X` 一回合内跳到 34 又被打回（血量跳变）。
3. 守擂 `血债X=3` 的**流血代价反噬**把自己流到 `hp 4→1→0`，判定却写「守擂主将阵亡」——**没人打它，它自己被血债流死**。
4. 法力从 0 涨到 37（> 法限 34），法力假账。

**已定位的根因**（精确到函数）：
- `engine/models.py:484 gain_shield()` —— `self.shield += amount`，**无上限 clamp** → 盾可堆超血限。
- `engine/api.py:2362 _action_use_daowen()` —— `x = params.get("x",1)`，`x < 1` 被拒 → AI 侧 `_daowen_candidates` 已强制 `x>=1`，**假账不是 X=0 造成**；但守擂者快照道纹 `x_value=0` 仍被别处提取。
- `engine/combat.py` 回始（约 1705-1713）—— `current_mana += mana_limit` 回满法力，但**不回满速度**。
- `engine/models.py:389 take_damage()` —— 格挡扣减 `absorbed=min(shield, remaining)` 本身正确，盾可正常消耗；68 盾是「没被打、一直叠」累积的。

**改法（建议）**：
1. `gain_shield` 加上限 clamp（盾 ≤ 血限），杜绝 68 盾。
2. 死斗里 `血债X` 的「流血X」代价反噬致胜：若守擂者因自己血债流血·命零，应判「守擂者阵亡」（守擂方身份），而非「挑战者胜」。
3. 死斗回始是否回满速度需明确（用户已选：**不回满**，速度被扣就持续保留、只在战终回满）。
4. 修干净后，**重新用真实面板数据选一场对局写进报告**（用户明确「根据程序输出的数据写出来」）。

**验证**：重跑死斗，核对逐次 diff：双方 hp 只在真实掉血/回血时变；盾 ≤ 血限；法力 ≤ 法限；血债不再靠流血反噬致胜；判「双方按速限交替出手」。

## 硬伤 1b：允许道纹 X=0（视为无效=轮回者有权不发动）

- 用户明确：**"允许 X 为 0，X=0 时忽略描写、视为无效，这样轮回者才能拒绝发动不想发动的道纹。"**
- 现状：`engine/api.py:2362 _action_use_daowen` 已 `x < 1` 拒绝（`X必须≥1`），AI 侧候选也 `x>=1`。
- 改法：死斗/指令驱动侧，让「轮到我攻、但不想发当前道纹（X=0）」合法 = 跳过该道纹、不打（视为放弃那次道纹发动，而非报错）。需与「轮空/让行」语义统一。

## 硬伤 2：怪物用「必中」逼玩家用残韵（大方向已确认，方案已落定）

### A. 必中改成「层数制、攻击+道纹通用」
- 现状：`engine/daowen.py:275 calculate_bizhong(x)` = 消耗异变5X，自身下 X 次攻击附带必中。`engine/combat.py:1053/1063 grant_bizhong/consume_bizhong` 只出现在 **攻击结算**（`resolve_attack` 里 `consume_bizhong`）。
- 改法：`consume_bizhong(attacker)` 接入**道纹结算路径**——施法者选择消耗 1 层，使该次攻击**或道纹**无法被闪避/速度抵消。攻击与道纹共用层数，不浪费。

### B. 怪物 debuff 带「必中」（消耗必中层才不可抵抗）
- 现状：怪物道纹（减速/勾魂/镇尸/封印/衰弱）直接 `_lose_current_speed`/`add_status` 落到玩家，**无「必中」环节**。
- 改法：怪物施放道纹前若持必中层，**选择消耗 1 层**则该 debuff 命中不可被玩家抵抗；否则按原规则（可被速度/残韵解除）。给部分怪物面板挂「必中N」层（按强度）。

### C. 勾魂改为「目标无法获得法力，持续 X」
- 现状：`engine/daowen.py:931 calculate_gouhun(x)` = 消耗X，`[回始]`目标失去 2X 法力，永久。
- 改法：去掉 `round_start_mana_drain`，改新增持续状态「勾魂」（`remaining_rounds=x`），效果：目标回始**不获得法力**。在 `combat.py` 回始法力回填处（`current_mana += mana_limit`）加判断。
- **携带怪一起改**（用户确认：寄骨蝇等乱葬岗勾魂怪物构筑同步），另需**接入转化打断**。

### D. 转化（resonance）打断已生效 debuff + 真实生效（必须做）
- 现状：`engine/api.py:2645 _action_use_resonance` 只 `_permanently_convert_daowen(holder, source, dest)` 改怪物牌面；**已生效的玩家 debuff 不会被解除**。
- 用户已拍板：**转化时【不】清除已生效的 debuff、速度回始【不】回满**（`no_clear` / `no_refill`）。即：转化只改怪物牌面（后面该怪物不再施放源道纹），玩家身上已挂的减速/勾魂等**按自然规则继续**（速度被扣就持续保留）。
- **真实生效**：转化成功 → 怪物下回合按新道纹（dest）结算（`_permanently_convert_daowen` 已支持，确认怪物 AI 会用新道纹即可）。

### E. AI 策略/权重/数值一律不动
- 用户明确 `ai_strict`：只加机制压力（怪物必中 debuff），不碰局外行动权重、不碰 `_w()` 性格调制、不碰 AI 战术。靠压力让 AI 自学习用残韵。

## 硬伤 3：对白必须能干涉当前战局，否则不废话

- 用户明确：**"我希望任一方说话能干涉到当前情况，否则就不要废话。"**
- 现状：`sim/duel_dialogue.py render_line` 只是往 log append 字符串，**不触发任何引擎结算**——不挑衅、不干扰对手、不改自己行动。性格只在 `_w()` 微调出招权重。
- 改法：让台词进入结算，产生真实可核对的效果，且体现性格差异：
  - 高 `expression_style`+/`risk_preference`+ → 对对手施加可结算的**减攻/增伤**（如「你破绽大开」→ 对手下回合 -X 攻击）。
  - 高 `reaction_pattern`-/`emotional_stability`+ → 给自己真实收益（回蓝/减伤/闪避值）。
  - 只限性格维度强的角色触发，无性格的平庸局不刷台词。
- **验证**：台词出现后，对手面板/后续决策真实改变（可查 diff）；不同性格角色的台词效果不同。

---

# 移交后需复跑

- `tests/`: 全量 1339 passed（`test_full_flow` 曾因 `/tmp/linji_tests` 共享状态偶发失败，隔离/重跑通过，与本轮改动无关）
- `run_duel_pvp` 复现：双方按速限交替出手、名字互异（如 `祁连 vs 阮烟`）、有对白；**但数值假账未修，勿凭死斗面板下结论**。
