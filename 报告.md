# 报告

> 性质：**正文 ↔ 引擎 差异核查清单（2026-09-16）**，供逐条裁定。
> 基准：`AI_EXPERIENCE.md`「第四宇宙规则正文」为唯一规则事实源，逐项比对引擎实现（`engine/`）。
> 旧内容（2026-09-15 白夜 vs 陆沉死斗战报）已归档为
> `archive/report_history_2026-09-15_白夜VS陆沉死斗.md`。
> 用法：每条给「建议」→ 裁定【改代码】/【改正文】/【搁置】。
> **剩余待裁定：仅 B3**（见第二节）。

---

## 〇、本次已按裁定处理（A1–A5 / B1 / B2 / B4 / B5 / D1–D3）

| 项 | 裁定 | 处理 |
| --- | --- | --- |
| **A1** 【无所求】属性点比价 | 改成现版本公式 | ✅ 已修。改按正文 `2属性点=1速限=1法限`，且**入属性点池**而非直接改面板（《物品索引》原文即「永久获得1点属性点」）。旧实现按「1属性点=1速限=2法限」直接加速限/法限，速限 2 倍、法限 4 倍，且把速限与法限做成 1:2。 |
| **A2** 怪物出手次数 | 删掉，属早期历史遗留 | ✅ 已删。正文「怪物 = [速限]/3」删除（怪物面板不含[速限]，该式恒为 0），改为「怪物每回合固定 2 次出手＝1 次攻击 + 1 种道纹」，与引擎 `single_round_action_count` 同口径。 |
| **A3** 事件文本「属性点」被吞成 +1 速限 | 修复 | ✅ 已修。改为 `state.attribute_points += N` 入池，兑换决定权交回玩家。 |
| **A4** 事件文本「获得N点[法限]」给 2N 点 | 修复 | ✅ 已修。改为 `mana_limit += x`，与速限同口径。 |
| **A5** 真拒绝判定 | 改成分词/开头匹配，排除带代价收益的选项 | ✅ 已修。关键词须在**开头**（可带「N.」序号），且正文含「失去/获得/流血/属性点…」等实质得失字样即判为「拒绝改造」、不算真拒绝。统计由 34 → **33**：原【遗落的赌局】「下注生命：流血X。50%获得2X[碎片]，50%无事发生。」系误判（含实质得失）。 |
| **B1** 副本专属行动 | 众所周知的既定设计 | ✅ 已在正文《局外系统》补「副本专属行动」声明（与通用行动共用精力池），不再列为差异。 |
| **B2** 24 种道纹不在正文 | 补上 | ✅ 已于正文新增「### 副本专属道纹」小节，24 种全部写入效果正文；另注明 8 种闭环中间节点见《全道纹索引》。 |
| **B4** 碎片公式被副本改写 | 以正文为准，禁止改写公式 | ✅ 已修。`shard_reward` 恒等于正文公式 Σ(⌈战始血限×2%⌉+道纹数×5)，**任何事件增益不得乘入/并入**；角斗场双倍、悬赏金、三回合彩头改为 `event_bonuses` 独立列项发放。 |
| **B5** 死斗守擂侧不受出手预算约束 | 见下方问答 | ✅ 已修并封顶，实测 2 次（1 攻 + 1 纹）。 |
| **D1** 血炼周天 / 血溅五步 | 补流程 / 删空名字 | ✅ 血炼周天已补内置流程（失去生命后→再生→透支→循环，loop=True）；血溅五步已从《死者之书》《法术索引》删除。 |
| **D2** 正文举例用了未定义的【加害】 | 直接用【杀伐】举例 | ✅ 已改为【寄生】、【杀伐】。 |
| **D3** 测试污染 `死者之书.md` | 同意 | ✅ 新增 `tests/conftest.py` 自动夹具，把默认死者之书路径重定向到 pytest 临时副本；仓库文件保持只读。 |

### 同批改动的法术机制（2026-09-16 用户裁定）

- **法术无需学习**：内置法术不再写进 `entity.spells`，改为按持有道纹实时推导
  （`CombatEngine.BUILTIN_SPELL_DAOWEN` 为唯一事实源，`api.SPELL_REGISTRY` 降为别名）。
- **轮回者消耗 1 次主动出手，二选一**：
  - `use_spell` — 发动一种法术，该法术生效，此后在其触发时点自动结算；
  - `define_spell` — 自定义一种法术，立即生效，创建即可用。
  - 卸下（`disarm`）不花出手。局外自创入口已取消。
- **镇魔印/封印抑制**判据由「已学习」改为「已装配」，保住该法术
  「把一次主动出手换成回合结束免费暂离」的原意。
- 测试：全量 1551 项，失败 8 项，**与改动前基线完全一致**（既有 AI 打分/招架故障），零回归。

---

## 一、B5 问答：守擂方能一直出手吗？

**不能。**实测结论（脚本复现，守擂为封存轮回者时）：挑战者 2 次、守擂 2 次，回合正常结束。

但原代码确实有一处隐患，现已修复：

- `_duel_side_can_act()` 对守擂侧的怪物写的是「**存活未撤退即有余手**」，
  而真正的出手门禁走的是 `Entity.action_count`——怪物面板不含[速限]，该值**恒为 0**。
- 两个口径不一致的后果是：怪物**既被 0 预算卡死、一次都出不了手**，
  本函数又坚称它「还有余手」，于是回合永远结束不了（**卡死**，不是无限出手）。
- 另有一处放大风险：`_consume_action_or_error()` 对怪物**无条件 return None**，
  死斗中怪物的 `actions_used_this_round` 永不增长——一旦上面的 0 预算被绕过，
  就会变成真正的**无限出手**。

修复：新增唯一口径 `_action_budget_of()`，怪物按正文「每回合 1 次攻击 + 1 种道纹」
（= 2）计预算；`_duel_side_can_act`、`_action_prepare_attack`、`_consume_action_or_error`
三处共用它；死斗中怪物照常记账。实测封顶在 2 次。

> 注：死斗的守擂方通常是封存轮回者（非怪物），怪物分支属防御性代码。

---

## 二、B3 是什么？（原文说明）

B3 指的是**引擎里有 25 个战斗行动接口，正文里一条条文都没有**。

`engine/api.py` 共 63 个 `action_type`（如 `attack`、`use_daowen`、`define_spell`）。
正文写清楚了其中约 38 个；下面这 25 个只在代码里存在，规则文档没有对应条文：

`upgrade_doctor`、`choose_sha_qi`、`choose_hired_daowen`、`repay_debt_employee`、
`lianxin_in_battle`、`retreat_via_toll`、`read_death_book`、`resolve_ally_phases`、
`activate_duel_relic`、`choose_terminal_artifact`、`select_shared_dragon_heart`、
`declare_fuyuebei_toll`、`unlock_dragon_trait`、`pay_for_dragon_nature`、
`activate_dragon_body`、`use_dragon_wings`、`devour_monster`、`declare_tail_sacrifice`、
`use_blood_wings`、`blood_feast`、`enslave_as_chizu`、`use_truth_eye`、
`fire_godfather_revolver`、`use_black_card`、`use_crime_vault`

**为什么值得单列**：这些不是内部函数，而是 AI 可以调用的**玩家级行动**。
一个行动若没有正文条文，它的代价、目标要求、能否在战斗中用就全靠读代码才知道，
推演时无法引用规则。

**其中只有 3 个跨副本**（其余都是副本专属，条文在各副本文档里）：

| 行动 | 说明 | 建议 |
| --- | --- | --- |
| `choose_terminal_artifact` | 终音法器（死斗胜利后按副本发放） | 升格进正文 |
| `activate_duel_relic` | 死斗遗物发动 | 升格进正文 |
| `read_death_book` | 翻阅《死者之书》 | 升格进正文（正文已有《死者之书》条目，补行动口径即可）|

- 另：B1 已按你的说明确认为既定设计（每个副本自带专属行动），故 B1 不再列为差异。

---

## 三、C 类：已逐项核验一致（对照项，可放心）

| 条文 | 引擎位置 | 结果 |
| --- | --- | --- |
| 休整 20/40/60%，碎片 0/10/25 | `api.py` 休整档位 | 一致 |
| 修行 1/2/3/4/5/6 点，0/15/35/65/100/150 | `api.py` 修行档位 | 一致 |
| 学习转化道纹 1/2 种，0/10 | `api.py` 学习档位 | 一致 |
| 共鸣 发现/自选15碎片；探索 1次/2次(30) | `api.py` | 一致 |
| 癌变阈值 血限×2；吸收怪物→休整+8 | `models.py` / `combat.py` | 一致 |
| 崩解 异变 50 层 | `models.py:476` | 一致 |
| 凡庸 连续 5 回合 | `models.py:479` | 一致 |
| 雕塑 15 伤害 / 20 格挡 | `combat.py` | 一致 |
| 碎片 ⌈战始血限×2%⌉＋道纹×5 | `api.py` 战终 | 一致 |
| 出怪数量 ＝ 场数−3，最低 1 | `monsters.py:compute_draw_count` | 一致 |
| 波次 R1 首只，R4/R7/R10 增援 | `api.py` / `combat.py` | 一致 |
| 法力一池制（战始给满、回始不回填、战终复原） | `api.py` 战始 | 一致 |
| 属性上限 clamp（当前≤上限，全局无条件） | `combat.py:clamp_immortal_body` | 一致 |
| 招架（每次受伤−当前法力、下回合禁用、不耗出手） | `api.py` / `models.py` | 一致 |
| 道纹递增 +2×副本阶级 | `api.py` | 一致 |
| 事件池 36 个 / 33 含真拒绝 / 33 个真拒绝选项 | `events.py` | 与正文数字（收紧后）一致 |
| 消耗品使用不消耗出手 | `api.py` | 一致 |
| 怪物池 48 只（4 副本各 12） | `monsters.py` | 一致 |
| 轮回者固定 2 次出手 | `models.py:action_count` | 一致 |
| [朋友]/[员工] ＝ ⌈攻击次数/3⌉ | `models.py` | 一致 |
| 开局 25 属性点上限校验 | `validator.py` | 一致 |
| 精力 3 点 | `api.py` | 一致 |

---

## 四、回归测试

新增 `tests/test_rulings_2026_09_16.py`（21 项）钉住本次裁定：

- A5：13 组真拒绝用例 + 事件池统计（36/33/33）
- B2：正文「副本专属道纹」小节必须含全部 24 种
- B4：命零公式不被 `arena_double_loot` 缩放；悬赏金为独立列项
- B5：守擂怪物出手封顶在 2 次
- D1：血炼周天内置流程（再生→透支→循环）；血溅五步已从两处文档删除

另新增 `tests/conftest.py`：自动夹具把默认《死者之书》路径重定向到 pytest 临时副本，
仓库文件不再被测试写入。
