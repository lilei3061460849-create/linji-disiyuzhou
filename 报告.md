# 报告

> **本报告已清空上一轮手操复盘内容，改为「未处理事项交接清单」**，供下一轮对话直接接手。
> 生成时间 2026-09-17。分支 `arena/01a0a566-linji-disiyuzhou` 已合并入 `main`。
>
> 每条都写清：**现状 → 为什么是问题 → 建议做法**，并给出复现/定位命令，避免下一轮重新调查。

---

## 〇、环境注意事项（每次新会话必读）

| 项 | 内容 |
| --- | --- |
| `.venv` | **不在快照内**，会话恢复后第一条命令就要重建：`python3 -m venv .venv && .venv/bin/python -m pip install -q -r requirements-dev.txt` |
| 回归命令 | `.venv/bin/python -m pytest tests -q --no-header`（约 7.5 分钟） |
| **测试基线** | **8 failed / 1591 passed / 3 xfailed**。这 8 项是**既有失败，与本轮无关**，不要误判为回归 |
| 8 项既有失败 | `test_win_only_ai.py`×4、`test_ai_basic_attack_candidate.py::test_at_one_by_one_daowen_still_wins`、`test_ai_tactics.py::test_ai_can_declare_parry_under_lethal_threat`、`test_build_learner.py::test_valid_and_invalid_are_separated`、`test_unified_ai.py::test_ai_player_is_the_combat_and_high_level_entrypoint`（均为 AI/mock 类） |
| 分支纪律 | 本会话固定分支 `arena/01a0a566-linji-disiyuzhou`；若要在新会话继续，需新建会话分支 |

---

## 一、本轮已落地（仅作对照，**无需再处理**）

| 项 | 内容 |
| --- | --- |
| 点金 | 定为 **消耗 8X**（推翻此前 3X 结论），程序/正文/测试四处同步 |
| 超频 | 改为**自由选择目标**（选到谁给谁加速度），移出 `self_targeted` 名单 |
| 分裂 | 重做为**双参数 X/Y 即时创生**：代价衰老 X×10Y，创造 X 个 10Y 血限复制体（引擎首个双参数道纹） |
| 全力 | 原名【强化】，改为「攻击力**锁定**＝其法限，持续X」——花法力不掉输出。弱化不变 |
| 全速 | 原名【迟滞】，改为「攻击次数**锁定**＝其速限，持续X」。因全局钳制规则，语义**由减益翻转为增益**，故改名 |
| 变形 | 改为「[目标]当前速度↔当前法力互换」，超出上限部分**蒸发**；目标可选（不填则自身） |
| 龙族利爪 | 改为「攻击力＝当前法力×2」（旧版对轮回者完全无效） |
| 缄默面具 | 由「[战始]获得20X法力」改为「[战终][法限]+X」，机制层新增 `Phase.BATTLE_END` 相位 |
| 员工转正 | 出场并存活满 3 场战斗后转为[朋友] |
| 术语 | 员工叛变 → 员工背叛（仅中文层，英文 `rebellion` 保留） |

**口径**：`effective_attack_power` = 当前法力 → 龙族利爪×2 → 全力锁定法限（最后生效，压过倍率）；
`effective_attack_count` = 当前速度，【全速】时锁定为[速限]。均走状态层，对全体角色同口径生效。

---

## 二、⚠ P1：35 条道纹的 `summary` 消耗数字与程序不符

`summary` 是**战斗日志里展示给玩家和 AI 的文本**，但其中 35 条写的是过期硬编码倍率。
`docstring` 与 `cost` 字段是一致的，**只有 `summary` 是错的**。

复现命令（会持续产出最新清单）：

```python
import sys, re; sys.path.insert(0,'.')
from engine.daowen import DaoWenEngine
DaoWenEngine.register_all()
for name in DaoWenEngine._registry:
    r = DaoWenEngine.resolve(name, 1)
    amt = next((r[k] for k in ("cost","cost_mutation","cost_blood_limit") if k in r), None)
    m = re.search(r'(消耗|冷却|异变\+|流血|衰老|疲惫)(\d+)', r.get("summary",""))
    if m and int(m.group(2)) != amt:
        print(f'{name}: 实际{amt}X / summary写{m.group(0)}X')
```

| 道纹 | 程序实际 | summary 写的 | | 道纹 | 程序实际 | summary 写的 |
| --- | --- | --- | --- | --- | --- | --- |
| 伤痕 | 2X | 5X | | 波及 | 2X | 3X |
| 借力 | 3X | 10X | | 活血 | 1X | 2X |
| 兴奋 | 2X | 5X | | 清算 | 2X | 5X |
| 冥气 | 2X | 5X | | 滑翔 | 2X | 5X |
| 净化 | 2X | 5X | | 爆裂 | 2X | 3X |
| 加速 | 5X | **20X** | | 瓦解 | 3X | 10X |
| 坏死 | 2X | 5X | | 眩晕 | 5X | **20X** |
| 嫁祸 | 4X | 15X | | 缄默 | 1X | 2X |
| 定型 | 2X | 3X | | 背负 | 2X | 5X |
| 寄生 | 3X | 10X | | 自残 | 3X | 10X |
| 尸爆 | 3X | 10X | | 蒙蔽 | 2X | 5X |
| 弱化 | 2X | 3X | | 裂变 | 2X | 3X |
| 急速 | 5X | **20X** | | 贯穿 | 2X | 5X |
| 愤怒 | 2X | 5X | | 赎金 | 3X | 10X |
| 抵扣 | 3X | 10X | | 退化 | 2X | 5X |
| 招魂 | 3X | 10X | | 镇尸 | 2X | 5X |
| 无力 | 3X | 10X | | 龙鳞 | 2X | 5X |
| 无神 | 5X | **20X** | | | | |

**建议**：纯数据修正、零风险，建议优先做。改完应加一条**一致性测试**（遍历注册表，断言 summary 数字 == cost 字段），防止再次漂移。

---

## 三、⚠ P2：三只怪物战术被道纹重做打崩（**需 DM 裁定**）

本轮改的道纹一共嵌在 **25 只**怪物面板里，但**只有扭曲都市写了战术注释**，所以只有这 3 只的破损是"看得见"的。

面板格式为 **血限/攻击力/攻击次数**（可用骨天使 7+5=12 → 变形换 12击×3 反推验证）。

### 扭曲都市（已实现的一阶副本，优先处理）

| 怪 | 面板 | 原本 | 现在 | 问题 |
| --- | --- | --- | --- | --- |
| **骨天使** | 222/7/3，变形，全力，飞行 | 3击×7(21)；全力后 12击×3(36) | 变形后 **3击×3(9)** | ①新变形对"攻力>攻次"的怪是**自残**（超出速限的部分蒸发）<br>②【全力】改为锁定法限后不再是"+X攻力"，「先全力堆到12」不成立 |
| **奇美拉** | 228/9/2，变形，必中，飞行 | 2击×9(18)；变形后 9击×2 | 变形后 **2击×2(4)** | 专破【固执】（单击上限1）的"多段低伤"效果**完全失效** |
| **眼树** | 192/13/1，定型，全速，弱化 | 全速压攻次到1＋弱化削攻力＋定型锁死＝废掉对手输出 | 【全速】变增益 | 连招**反向** |

### 永夜庭

| 怪 | 面板 | 问题 |
| --- | --- | --- |
| 许知微（绝食修女） | 2×22/300，饥饿4，失血2，自愈3，蒙蔽3，**全速3** | 全速由减益变增益 |
| 温澜（赤族） | 1×11/186，共罪1，**全速2**，【诅咒】 | 同上 |

**根因**：新【变形】专克「法力 > 速度」的目标（这正是用户要的"喝汤"效果：20/3/10 → 20/3/3，凭空失去 7 法力），
但对「攻力 > 攻次」的怪**自用就是自残**——因为旧版"互换后总量不变"，新版被速限钳掉的部分会**蒸发**。

**待裁定**：给这三只（及永夜庭两只）换道纹？还是改面板数值？还是接受它们变弱？

---

## 四、⚠ P3：【高爆手雷】的「攻击次数-1」对轮回者无效

用户问过，尚未修。

| 效果 | 对怪物 | 对轮回者 |
| --- | --- | --- |
| 15 点伤害 | ✅ | ✅ |
| 本回合攻击次数-1 | ✅ | ❌ **无效** |

**两层原因**：

1. 玩家侧命中数走 `effective_attack_count()`（＝当前速度），这条路径**根本不读**「手雷减攻」状态；
2. 就算改读遗留字段 `attack_count`，轮回者的该字段不写穿、与真实速度脱节，仍无效。

**同类隐患**：怪物侧现在能生效，靠的是 `monster.attack_count - get_status_value("手雷减攻")`
（`engine/combat.py` 的 `base_hits_per_attack` / `hits_per_action` 两处）——读的也是遗留字段
而非 `effective_attack_count()`。有写穿兜底所以能跑，但与【全力】【全速】是同一个坑。

---

## 五、⚠ P4：条件触发式道纹的冗余清单（用户要求找出）

判据：**带有「XX 时」触发条件、不代表一个单纯概念、效果部分已有独立道纹**（本条只是"触发器＋那个效果"的打包）。

### 5.1 效果部分已有独立道纹（最该处理）

| 道纹 | 触发条件 | 效果 | 同效果的独立道纹 |
| --- | --- | --- | --- |
| **自愈** | [回始] | 回复血限 10X% | **滋养**（公式逐字相同 `ceil(血限×10X/100)`，自愈＝滋养的每回合订阅版） |
| **兴奋** | 每次出手后 | 速度+1 | 超频（速度+X） |
| **急速** | 每闪避两次 | 速度+1 | 超频 |
| **加速** | — | 获得的速度翻倍 | 超频 |
| **洞察** | 每次闪避后 | 下回合法力+10 | 透支 / 搏命 |
| **伤痕** | 每次失去生命后 | 血限-X | 瓦解（血限-10X%） |
| **活血** | 每累计失去2生命 | 回终回复1 | 再生 / 滋养 |
| **爆裂** | 受到伤害后 | 攻击者失去等量生命 | 杀伐 |
| **冥气** | 每失去一次速度 | 速限-2 | 减速 |
| **尸爆** | [命零] | AoE 伤害 | 杀伐 |
| **眩晕** | 受到伤害后解除 | 无法出手 | 束缚 |
| **逆鳞** | 每失去1生命叠层 | 下次伤害+层数 | 加害 |
| **寄生** | 受到伤害时 | 转化 20X% 为回复 | 加害反向＋再生 |
| **赎金** | **若无碎片**（否则分支） | 失去X速度 | 减速 |

### 5.2 三组纯重复 / 高度重叠（建议优先合并）

1. **自愈 vs 滋养** —— 回复公式逐字相同，自愈只是"每回合版"
2. **加速 / 超频 / 兴奋 / 急速** —— 四条全挤在「加速度」这**一个**概念上
3. **加害 vs 龙鳞** —— `+X` 与 `-X`，纯粹对称

### 5.3 元机制类（不是概念，是倍率器/开关）

| 道纹 | 本质 |
| --- | --- |
| 波及 | 复制器：你发动的道纹同时作用于所有波及目标，数值平分 |
| 缄默 | 开关：禁止全场[命零]触发的效果（针对尸爆/分裂） |
| 裂变 | 结算方式：伤害改为分X次结算（与【固执】单击上限1互动） |
| 定型 | 锁：攻次与攻力无法被改变 |
| 退化 | 每次发动道纹时该次数值-X（无直接对应道纹） |
| 逼债 | [回始]二选一：失碎片否则失血限（复合概念） |

---

## 六、⚠ P5：怪物战术文档覆盖率仅 7%（架构决策）

| 副本 | 怪物数 | 有战术注释 |
| --- | --- | --- |
| 扭曲都市 | 20 | **12** |
| 其余 7 个副本 | 160 | **0** |
| **合计** | **180** | **12（7%）** |

**这是 P2 的深层原因**：耦合是真实的、结构性的（25 只面板直接嵌入道纹名），
但 93% 的耦合是**隐形**的——调道纹后，没写注释的怪物会**静默改变行为**，没有任何东西提示。

**两个方向待选**：

- **A：给 180 只怪物补战术注释** —— 彻底，但工作量巨大，且以后每次调道纹都要同步 180 份文档
- **B：反过来，怪物面板只写道纹、不写战术** —— 战术由道纹语义自动推导。调道纹＝自动调所有相关怪物，无需逐只改；代价是丢失「这只怪该怎么打」的设计意图记录

（我倾向 B，但 A 的价值在于保留设计意图。需 DM 裁定。）

---

## 七、⚠ P6：B3 —— 战斗行动接口正文无条文

引擎 64 个 action 处理器中，**47 个在规则文档里一次都没出现过**（排除内部 helper `_action_budget_of` 后为 **46 个**）。
这些不是内部函数，是 AI 可以调用的玩家级行动；没有条文，其代价/目标要求/能否在战斗中使用就只能读代码。

> **注**：旧报告写「完整清单见 `archive/清单已裁定项_2026-09-16.md` 的 B3 原文」是**死链**——
> 该文件里没有 B3，25 项完整清单从未落盘。以下为 2026-09-17 重新扫描的**实时清单**。

复现命令：

```python
import io,re,glob,os
acts=set()
for p in glob.glob('engine/**/*.py',recursive=True):
    acts|=set(re.findall(r'def _action_([a-z_]+)\s*\(', io.open(p,encoding='utf-8').read()))
acts -= {'budget_of'}   # 内部 helper，非玩家行动
docs=''.join(io.open(p,encoding='utf-8').read()
             for p in ['AI_EXPERIENCE.md','全道纹索引.md','物品索引.md','法术索引.md',
                       '副本索引.md','故事文档.md']+glob.glob('副本/*.md') if os.path.exists(p))
print(sorted(a for a in acts if a not in docs))
```

**跨副本的 3 个（优先级最高，旧报告已标）**：

| 行动 | 说明 | 建议 |
| --- | --- | --- |
| `choose_terminal_artifact` | 终音法器（死斗胜利后按副本发放） | 升格进正文 |
| `activate_duel_relic` | 死斗遗物发动 | 升格进正文 |
| `read_death_book` | 翻阅《死者之书》 | 升格进正文（正文已有《死者之书》条目，补行动口径即可） |

**其余 43 个**：

```
appease_rebellion        blood_feast              choose_discovered_item
choose_discovered_relic  choose_first_embrace     choose_hired_daowen
choose_sha_qi            consume_item             declare_escape
declare_evolution        declare_fuyuebei_toll    declare_tail_sacrifice
declare_wish             deploy_employee          devour_monster
dismiss_employee         enslave_as_chizu         fire_godfather_revolver
lianxin_in_battle        negotiate_rebellion      pay_employee_wage
pay_for_dragon_nature    prepare_attack           repay_debt_employee
resolve_ally_phases      resolve_event            resolve_final_duel
resolve_rebellion_battle resolve_redemption       retreat_via_toll
select_shared_dragon_heart                        setup_attributes
setup_choose_initial_daowen                       setup_choose_region
setup_choose_resonance   suppress_rebellion       unlock_dragon_trait
upgrade_doctor           use_black_card           use_blood_wings
use_crime_vault          use_dragon_wings         use_truth_eye
activate_dragon_body
```

（其中 `setup_*` 属开局流程，`resolve_*` 属死斗/背叛/救赎结算，可成批补条文。）

---

## 八、建议处理顺序

1. **P1（35 条 summary）** —— 纯数据修正、零风险、玩家可见，建议先做并加一致性测试
2. **P2（三只怪物）** —— 已实现的扭曲都市副本里有 3 只处于破损状态，需 DM 裁定
3. **P4（道纹冗余）** —— 尤其「自愈 vs 滋养」「加速/超频/兴奋/急速」两组
4. **P3（高爆手雷）** —— 小修，顺带把怪物侧改读 `effective_attack_count()`
5. **P5（战术架构）** —— A/B 方案定调后再动
6. **P6（B3，46 个接口）** —— 量大，可成批处理

---

## 九、本轮产物

| 文件 | 内容 |
| --- | --- |
| `tests/test_fenlie_split.py` | 分裂双参数回归（6 项） |
| `tests/test_quansu_bianxing.py` | 全速/变形回归（9 项） |
| `engine/mechanisms/triggers.py` | 新增 `Phase.BATTLE_END` 相位（为【缄默面具】改版） |
| `engine/mechanisms/builtins.py` | 新增 `DRAGON_CLAW` 机制；`SILENT_MASK` 移至战终 |
