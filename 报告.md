# 报告（统一过程信息文档）

> 口径（CF-4，2026-08-24；滚动规则 2026-08-26；**清理 2026-08-31**）：
> 本文件**只保留当前有效状态**——未决待办、禁区清单、复跑命令。
>
> - 已结案的机制知识与结论 → 迁入 `AI_EXPERIENCE.md`（知识库）
> - 历史留痕 → `archive/report_history_*.md`
> - **已处理项直接删除，不划线占位**（划线等于没删，仍占版面）

---

# 结论：最新一次任务（2026-08-31 · 探针 `dmg` 口径）

- **改了什么**：`engine/ai_tactics.py::_digest_diff` 的 `dmg` 由**面板净位移**改读**事件流
  `damage_applied.raw_damage`（未扣盾）**合计，与同函数 `kind` 判定同源同口径。写成
  `max(面板位移, 事件流原始伤害)` 只为撤退／断尾求生／爆裂压制这类**不发伤害事件**的路径
  保留旧的 `dmg=0`；**无任何道纹名/机制名特判**。
- **引擎事实**（4 血 2 盾木桩，杀伐X=2）：
  `damage_applied {raw_damage: 4, actual_damage: 2, shield_absorbed: 2, hp_after: 2}`；
  修复前同一手是 `杀伐X=1`、盾吃光、`hp_after: 4`（整手零伤害）。
- **验证**：
  - `tests/test_ai_tactics.py` 新增 2 条（`probe["dmg"]==2` + 收割档 X=2 回到候选；
    纯输出局 AI 改出 X=2 并真实掉血）——两条在**修复前必挂**，修复后通过。
  - `python -m pytest tests -q`：**1389 passed**。
  - 死斗 A/B：`data/breed_winners` 六个胜者两两有序配对（30 组）× seed=1，逐组走
    `sim/duel_diff_trace.py::traced_duel`，修复前后各跑一遍——仅 1 组变化
    （速战速决_5 vs 封印控制_23：3 回合 → 2 回合，胜方与死因同为挑战者胜／守擂【癌变】命零）；
    胜负分布（挑战者 12／守擂 18）、死因分布（凡庸 18／崩解 5／癌变 5／血债 1／击杀 1）、
    死锁 0/30 两侧完全一致。—— 与原判定一致：**修的是 AI 能不能正常出手，不是消除僵持**。
- 原待办 ① 原文已按 CF-4 迁入 `archive/report_history_2026-08-31.md`。

---

# 待办：未决事项

> 只留**真正未决**的项。

## ② `enemy_gone` 把「动作前就已阵亡的敌人」也算作消失 → 探针把「打自己」判成 remove（**未修**，待裁定）

修待办 ① 时在测试台复现（2026-08-31，与本次改动无关，修复前后同样复现）：

- `engine/ai_preview.py::_diff` 的 `enemies` 按索引列出 `before.enemies` **全部**条目
  （含动作前就已 `dead` 的），而 `engine/ai_tactics.py::_digest_diff` 的
  `enemy_gone = any(e.get("dead") for e in enemies)` 不区分「本来就死」与「这一手打死」。
- 于是只要场上留着一具尸体，**任何**指向自身的预演变体都满足
  `enemy_gone and enemy_hp_loss<=0 and not hit_enemy` → `kind="remove"`，
  而 `_probe` 的方向分给 remove 加 **+6.0**，压过真输出变体的 `2.0×dmg`。
- 实测（`_engine(learn=())`、清残韵、加一只 20 血木桩，唯一差别是场上有无死者）：
  - 无死者：`probe kind=damage target=石背熊 dmg=2` → `杀伐X=4` 打怪，玩家血 60 → 60
  - 有死者：`probe kind=remove target=贾凡 dmg=0` → `杀伐X=1` **打自己**，玩家血 60 → 54
- 影响面：多怪战斗首杀之后（尸体留在 `state.enemies`）即成立；1v1 死斗不触发
  （怪一死战斗就结束），故 30 组死斗 A/B 与现行 1389 条测试都看不到它。
- 待裁定点：`enemy_gone` 是否应改为「动作前存活、动作后消失」（`_diff` 已给出
  `hp_before/hp_after`，但没给「动作前是否存活」——要么 `_diff` 补一个
  `alive_before`，要么 `_digest_diff` 只认 `hp_before>0` 的条目）。属**行为变更**，
  且改的是探针语义而非某条正文规则，按最小必要修改口径先报告、不顺手改。

---

# 当前禁区清单（证据不足，禁改；遇新证据可重启评估）

> 维护口径（2026-08-30 清理）：**只留仍然有效的条目**。已结案、无提案、
> 或性质属于「规则裁定」的一律下架（归档留痕见 `archive/report_history_2026-08-30.md`）。

- **血僵 4×19/270 与尸霸 5×15/264 数值强度**：加样 12+12 全部 1 回合阵亡——强度确认，
  但「玩家侧策略空间」存疑（一阶加点无生存窗口）；数值暂仍禁改，重启需血限型构筑复测。
- **乱葬岗全池（执念/纸人/哀嚎者/腐疫鼠 0/8 each）**：同上口径全灭——二阶整体数值对
  一阶构筑碾压，非个别怪问题；结构性难度裁定待 DM。
- **冥气削速限的速度**：组合放大口径维持（机制=速限压制型，已复现）。
- **事件收益数值**：审计证据显示探索因果效应 ≈0（CI 含 0），**禁改事件收益/风险**；
  重启需 300+ 局多副本或事件收益上限静态扫描。
- **勾魂改版后的强度**：旧版「永久扣蓝」已删除，新版（持续X回合无法获得法力）
  是否仍需调强度**证据不足，禁改**——需玩家构筑样本复测。

## 反向禁区（禁止「顺手修好」，违反即自造规则）

- **不得给面板加自造上限**：除遗物【不朽之躯】外，
  ① 禁止给 `gain_shield()` 加「格挡 ≤ 血限」clamp；
  ② 禁止给「获得法力」加「≤ 法限」clamp（回始是加法，允许 > 法限）。
  正文只规定格挡「抵消等量伤害」，从未写它不得超过[血限]——**叠到超过上限是合法面板**。
  ①②各曾误加过一次，已分别由 `6985481` / 硬伤1 反向裁定撤销，勿再改回去。

---

# 移交后需复跑

- `python -m pytest tests -q`：**1389 passed**（1356 + 对白 26 + 闪避 2 + 死因 2 + 凡庸优先 1
  + 探针 dmg 口径 2）
- `python3 sim/duel_diff_trace.py [挑战者] [守擂] [seed]`：死斗逐次 diff + 面板不变量自检 +
  战场公开频道实录；双方名字按**性格反差最大**配对（`_pick_contrasting_names`）
- `python3 sim/breed_and_duel.py --breed N`：养蛊胜者（快照是第 7 场通关瞬间的状态，
  多数残血、核心道纹已被残韵转掉；六个里有四个仍持杀伐、两个满血）
- 对白不碰数值的端到端 A/B：`tests/test_dialogue_channel.py`
