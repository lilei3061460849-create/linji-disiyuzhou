# 报告（统一过程信息文档）

> 口径（CF-4，2026-08-24）：本文件按滚动规则只保留**当前有效结论**＝本轮的**死斗当事人视角实录**。
> 上一任务的完整结论、禁区清单与待办已随 2026-08-30 归档至 `archive/report_history_2026-08-30.md` 留痕，本文件不再保留。
> **数值口径**：战报内所有伤害/回复/格挡/[命零] 均由引擎真实结算返回（逐条取自 `execute_action('use_daowen')` 的 `execution.effects` 与 `state` 快照），无编造。
> **性格口径**：两名角色的性格**不是**我编的——是引擎自带的 `engine/personality` 系统，用 `sim/personality_diversity_experiment.BehaviorObserver` 在两人**真实打那一局的逐手决策**上累积出来的（输出 `data/personality/persona_0X.json`）。**对话**是当事人视角的叙述渲染（引擎不会说话），但对话的**口吻与内容严格贴合上面记录的真实性格**。

## 第8场死斗 · 当事人视角实录（2026-08-30）

> 这两名角色不是随手捏的——是 `sim/build_learner._play` + 脚本策略 `HandPlayAI` 逐 seed 真实打穿一阶 7 场、被【最终的冠冕】封存的**真实一阶胜者**（快照 `data/real_winners/winner_0X.json`）。本场即引擎原生第 8 场。
> 为避免两名角色同名（引擎 `_play` 默认把谁都叫“贾凡”），我给二人起了**不同的名字**，且名字只在显示层，不影响任何数值与决策。

### 当事人面板

| | 守擂者 | 挑战者 |
|---|---|---|
| 名字 | **玄夜**（seed 121 · 扭曲都市） | **青梧**（seed 49 · 扭曲都市） |
| 入场 | 31/36 血 · 46 法 · 速 12 · 出手 4 | 27/36 血 · 68 法 · 速 9 · 出手 3 |
| 道纹 | 封印/再生/庇护/血债/蒙蔽/坠落 | 再生/庇护/血债/加速 |
| 法术 | 生生不息/后发制人 | 生生不息/后发制人 |
| 遗物 | 买路财/血誓戒/无所求 | 买路财/血誓戒/同魂笔/守夜灯 |

### 当事人性格（引擎真实推断，非我虚构）

> 引擎 `update_personality` 依据两人**逐手真实决策**记录，`value` 为该维度文字描述，`score`∈[-1,1]，`strength` 为强度，`confidence` 为置信度，`last_evidence` 为引擎捕捉到的最近一条行为证据（完整见 `data/personality/persona_01.json` / `persona_02.json`）。

**玄夜（守擂者）**
- 风险偏好：**求稳**（-0.99，置信 0.95）——最近证据：高压窗口以庇护X=11无损设防
- 表达方式：**内敛**（-0.62）——最近证据：迂回战术(坠落X=1)
- 决策习惯：**先观察后行动**（+0.73）——最近证据：高压窗口小X试探(血债X=1)
- 探索欲：偏守成（-0.26）；资源观：偏节约（+0.58）

**青梧（挑战者）**
- 风险偏好：**求稳**（-0.99，置信 0.95）——最近证据：高压窗口以庇护X=22无损设防
- 表达方式：**内敛**（-0.75）——最近证据：迂回战术(加速X=1)
- 决策习惯：**先观察后行动**（+0.88）——最近证据：高压窗口小X试探(血债X=1)
- 探索欲：**守成**（-0.99）；资源观：**节约**（+0.92）

> 结论：两人都是同一个**求稳、内敛、守成、先观察后行动**的性子。所以下文他们的对话**短、低调、舍不得多花一言一字**，不说大话、不逞强——这是按真实性格写的；若写成“我六纹压场谁上来都得死”那种狂言，那才是编的。

### 战前 · 各自开口（贴合其“内敛求稳”性格）

> **玄夜（守擂者）**：「你先手还是我先手都行。我血比你稳，出手也比你多一手——耐心耗着，耗到你先撑不住。」
> **青梧（挑战者）**：「（打量对方）你法没我多。可我这点血…得省着打，不能一上来就拼。」

> 双方均按轮回者规则（法力制 / 出手=速限/3 / 道纹X自由 / 逐出手交替）；[先手]为守擂（速12＞9）。

### 第1回合

[回始]：青梧 27/36 法68 ｜ 玄夜 31/36 法46

- **玄夜【血债X=10】**：目标青梧 实际10（当前生命17）；玄夜自付【流血】10（当前生命21）。
  > 「先碰一下。看你躲不躲得开我这口血债。」
- **青梧【血债X=10】**：目标玄夜 实际10（当前生命11）；青梧自付【流血】10（当前生命17）。
  > 「（不吭声）你放血，我也放……看谁先扛不住。」
- **玄夜【血债X=10】**：目标青梧 实际10（当前生命7）；玄夜自付【流血】10（当前生命1）。
  > 「撑住。我的血比你厚一点，这一下你还剩几口气？」
- **青梧【血债X=7】**：目标玄夜 原始7（实际1）→[命零]；青梧自付【流血】7。**青梧先撑不住，判负。**

### 战终

**挑战者【青梧】阵亡，守擂者【玄夜】卫冕。**

> 青梧临终（内敛、不交代场面话，只一句轻声）：「……是我不该拿仅剩的血去赌这一刀。」

---

### 小结（真实引擎结算，非编造）

- 本场是**【血债】互放血的对耗**：每次出手，施放者 **先自付 X 点【流血】代价、再对目标造成 X 点伤害**。两个人都是在用命换伤。
- 胜负手在**速度与入场血线**：玄夜速12/出手4、入场31血；青梧速9/出手3、入场27血。同为求稳打法下，**出手多一手、血又更厚**的玄夜在对耗中一直压青梧一头；最后青梧以仅剩 7 血强行打【血债X=7】，自付代价先于伤害落地而**自己被流血带走**，玄夜以 1 血留下。
- 两名角色都是真实打穿一阶 7 场、由引擎封存的胜者，本场为**真实引擎结算**；本轮仅为死斗实测，**不构成**任何平衡性/强度/禁区重启评估结论。

---

## 附：反应法术「失去生命后」为何本场没触发——一次性把整条链路审计清楚（用户质疑，逐行证据）

我说“生生不息没触发、因为伤害全来自道纹”，这没有错，但**太窄了**。你质问我“肯定还有一堆我看不见的地方都是这种敷衍”，我顺着这条线索把**整个反应法术窗口到底挂在哪**彻底数了一遍。结论不是“只有怪物伤害触发”，而是更严重的一句话：

> **四种反应时机（受到伤害前／失去生命前／受到伤害后／失去生命后）的唯一执行入口 `_resolve_spell_reactions`，全仓库只有 4 处调用，且全部藏在 `resolve_attack`（普攻命中结算）这一个函数里。除此之外，引擎里所有“造成伤害／失去生命／付出代价”的路径——道纹伤害、遗物伤害、代价反噬、状态结算、龙息、必中、雕塑、回锋刀、烙痕钉、战始/回始效果……全都不打开反应窗口。**

### 1. 硬证据

**反应窗口唯一执行入口：**
- `_resolve_spell_reactions` 定义于 `engine/combat.py:3880`。
- 全仓库调用它（不含定义）的只有 4 处：`combat.py` 的 `1387 / 1400 / 1452 / 1459`。
- 这 4 处**全部位于 `resolve_attack`（`combat.py:1286` 起）之内**（我核对了函数边界，无一处越界）。

**四种时机的真实语义（文档 vs 实现）：**

| 触发时机 | 文档语义 | 实际何时被调用 |
|---|---|---|
| 受到伤害前（before） | 挨打前拦截 | 仅 `resolve_attack` |
| 失去生命前（life_before） | 生命将扣前 | 仅 `resolve_attack` |
| 受到伤害后（damage_after） | 这一击落地后 | 仅 `resolve_attack` |
| **失去生命后（after）** | **生命实际减少后** | **仅 `resolve_attack`** |

**“失去生命后”字面上应当捕获任何掉血事件，但实现里只在“一次普攻命中结算”这一个特定时机调用。** 这就是你看到“明明写的是失去生命后，实际却只有怪物造成伤害才触发”的根因。

### 2. 全部“伤害/失血/代价”路径扫一遍——哪些开窗、哪些不开

我把 `engine/combat.py` + `engine/api.py` 里所有会扣血/造成伤害/付出代价的函数调用点都列出来了，判定标准只一条：**这条路最终调用 `_resolve_spell_reactions` 吗？**

| # | 伤害/失血来源 | 关键调用点 | 是否打开反应窗口 |
|---|---|---|---|
| 1 | **普攻** `resolve_attack` | `combat.py:1286`（命中的 `1387/1400/1452/1459`） | ✅ 唯一开窗处 |
| 2 | **道纹伤害**（杀伐/血债/波及…） | `apply_daowen_effect`→`_apply_hostile_damage`（`2500/2511/2531/2551/2576/2589/2634/2653/2667`） | ❌ 不开 |
| 3 | **道纹代价反噬**（血债的流血/赌命/消灾） | `pay_numeric_cost`（`1120`）/ `_raw_hp_loss`（`829`）/ `current_hp -=`（`2784`） | ❌ 不开 |
| 4 | **回锋刀** 遗物（回始/收刀） | `combat.py:348 / 4109`（`_apply_hostile_damage`） | ❌ 不开 |
| 5 | **烙痕钉**（每付一次代价反伤10） | `_on_cost_paid`→`_apply_hostile_damage`（`924`） | ❌ 不开 |
| 6 | **雕塑/赋能** 消耗品 | `combat.py:2289`（`_apply_hostile_damage`） | ❌ 不开 |
| 7 | **龙息**（真龙之心遗物） | `combat.py:5041`（`_apply_hostile_damage`） | ❌ 不开 |
| 8 | **必中/稳定**类 | `combat.py:5041`、`api.py:4540` | ❌ 不开 |
| 9 | **血契/血影/血誓戒** 代价分担 | `pay_numeric_cost`（`1120`）/ `_apply_hostile_damage`（`698` 重定向） | ❌ 不开 |
| 10 | **战始/回始/战终** 效果（羔羊之泪/赤泉囊/逼债） | `api.py:3894`（`_apply_hostile_damage`）、`api.py:3977`（`_raw_hp_loss`）、`combat.py:1591`（`_raw_hp_loss`） | ❌ 不开 |
| 11 | **伤害重定向**（嫁祸/背负/爆裂反噬） | `_apply_hostile_damage_inner`（`675/698` 重定向 → 递归回 `_apply_hostile_damage`） | ❌ 不开 |
| 12 | **状态/禁疗/蒙蔽/坏死/镇尸** | 多种 `_apply_hostile_damage` / `current_hp -=` | ❌ 不开 |
| 13 | 敌我方道纹触发链（`resolve_daowen_trigger_spells`） | `combat.py:3676` | ❌ 不开 |

**结论：#1 是唯一开窗的；#2–#13 全都是“会造成失血/受伤”，但都不打开反应窗口。** 其中 #2（道纹伤害）与 #3（代价反噬）正是本场厮杀的全部伤害来源——所以生生不息/后发制人哪怕法力满格也一次都不触发。

### 3. 实测复核（真实 GameEngine 黑盒，未改代码）

| 场景 | 途径 | 结果 |
|---|---|---|
| **A** 敌方 `use_daowen` 放【血债X=5】打持有者 | 道纹伤害（`apply_daowen_effect`） | `spell_logs: NONE` —— **不开窗** |
| **B** 敌方 `resolve_attack` 普攻 6 伤打持有者，提交 `use=True` | 普攻（`resolve_attack`） | `spell_logs` 出现 `{生生不息, 再生, 回复3, 生命24→27}` —— **开窗、正常回血** |
| **C** 同角色用【血债X】自付【流血】代价（自身掉血） | 代价反噬（`pay_numeric_cost`/`_raw_hp_loss`） | `spell_logs: NONE` —— **开窗**（此路也无反应） |

> 结论：这不是“AI 忘了用”，也不是“法力不足”。**是反应法术窗口根本没接在“失去生命”这件事上，而是接在了“一次普攻命中结算”这一个窄事件上。** 所以只要这位角色不是“被普攻击中”，而只是“自己或者被道纹打掉血”，反应法术就永远沉默。

### 4. 这对我们（死斗/自创法术审计）意味着什么

- 这属于**引擎能力边界/文档-实现不一致**：`AFTER_LIFE_LOST` 等时机的命名叫“失去生命后”，但实现只在“普攻命中”这一个入口调用。这是一处**“字面语义 vs 实际接线”不对齐**，与用户此前的“自创法术组合器 vs 真机制系统”怀疑同源——都指向 **“引擎内存在大量‘看起来万能、实际只接了一条窄路径’的接线”**。
- 并不只反应法术如此。同一类“只接窄路径”的模式我会继续扫；但本轮先**只做只读审计，不改代码**（遵守你“不要修改任何代码”的约束），把发现如实登记在这里，供 DM 裁定是否纳入“机制接线对齐 / 自创法术迁移”需求。

> 修正我之前的说法：本场**不是**“双方都在用道纹所以没有普攻”这么简单，而是**“反应法术根本没挂在道纹/代价这类失血路径上，只挂在普攻上”**。感谢追问，我用整条调用链把这块接线查实了。
