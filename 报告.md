# 2026-08-19：AI 决策重构后 10000 seeds 全流程压力测试报告

## 测试规模
- 驱动器：`sim/stress_test.py`（GameEngine + TacticalAI + ActionPreview +
  alt_path + choose_pre_battle + 雇佣/救赎/存档续战，monkeypatch 校验）
- 重构内容：新增 `ActionPreview.risk_classify()` 通用风险分类器（LETHAL→SAFE，
  不依赖道纹/消耗品名称）；`try_consumable` 接入预演安全过滤；`_cast` 记录
  风险等级。**未改 CombatEngine、未改规则数值、未加道纹特判**
- 监控修复：区分 preview 副本执行与真实执行（消除已知残骸崩解假阳性）
- **10000 seeds（2000–6999 + 7000–11999）/ 10000 轮回 / 21463 战斗 / 118615 回合**
- 原始数据：data/stress_report_batchA.json、data/stress_report_10000_v2.json

## 聚合结果
| 项 | 值 |
|---|---|
| 胜 / 负 | 11521 / 9942 |
| 引擎异常 | **0** |
| AI 非法动作 | **0** |
| AI 明显自杀 | **0** |
| 消耗品崩解自杀（原已知问题） | **0** ✅ 已修复 |
| ActionPreview 不一致 | **0** |
| ActionPreview 状态泄漏 | **0** |
| 实体引用漂移（含嫁祸/activated/round_used） | **0** |
| 资源异常（HP/mana/speed/shards/energy） | **0** |
| 规则异常（is_alive 与 hp 矛盾等） | **0** |
| 战前休整异常 | **0** |
| 存档 / 续战异常 | **0** |
| 雇佣 / 救赎异常 | **0** |
| 测试脚本异常（C 类） | 208（164 战斗结束检测边界 + 44 战前精力循环） |
| 正常战败 | 9916 |

## 关键结论
1. **残骸崩解问题确认修复**：10000 seeds 中消耗品崩解自杀 0 次（重构前 206 次）。
   通用风险分类器让 `try_consumable` 预演后拒绝 LETHAL/CRITICAL 级消耗品，
   不依赖"残骸"名称。
2. **无新引擎 / AI / 预演 / 资源 / 规则问题**：10000 seeds 随机样本下核心系统全绿。
3. 非 KNOWN 条目（208 条）均为压测驱动器边界（战斗已结束但循环未检测、
   战前精力循环卡 pending），不影响核心结论。

## 新发现问题列表
**无。** 全部指标与重构前一致或改善（残骸 206→0）。

## 最终结论
AI 决策质量重构（完整后果优先 + 通用风险分层）在 10000 seeds / 21463 战
随机全流程压测下**零引擎异常、零预演泄漏/不一致、零资源/规则异常**，
并成功消灭已知的消耗品崩解自杀问题（206→0），未引入任何新问题。
