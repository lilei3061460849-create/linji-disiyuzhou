# 2026-08-19：删除急中生智事件，新增特殊事件【许愿】

## 变更内容

### 删除【急中生智】
- 移除玩家可触发动作 `declare_wit`（引擎/combat.py `initiate_wit`、enums
  `WIT_OF_DESPERATION`、api 注册/分发/出手预算全覆盖）
- README 独立规则段、特殊事件列表、员工叛变选项、逃跑与追击措辞、
  困境检查、怪物许愿资格同步移除/改写
- 保留员工叛变·谈判（原"急中生智谈判"改名"谈判"，机制不变）

### 新增特殊事件【许愿】（轮回者向"某人"祈求时触发）
规则（DM 裁定标准）：
1. 轮回者许下一个愿望，消耗一次出手；愿望没有固定的可行范围，
   不存在"无法实现"的愿望；
2. "某人"会以能够实现愿望、但最符合其扭曲本质的方式实现愿望；
3. 愿望的代价与扭曲方式由愿望本身决定，**不预先公开**。

实现：
- `engine/combat.py::initiate_wish(wisher, wish_text, target)` → Interrupt(WISH)
- `engine/api.py::declare_wish`（wish_text 必填 + 可选 target_ref，消耗 1 出手，
  经 dragon_claw 增长；替换原 declare_wit 全部注册点）
- `engine/enums.py`：WISH = "许愿"
- 引擎只提交现场状态（许愿文本/目标/当前 HP/道纹/回合），实现方式与代价
  完全由 DM 裁定——符合"不预先公开"规则

## 验证
- pytest **1128 passed, 5 skipped**（test_action_budget/test_engine/test_f7/
  test_rebellion 已更新为 declare_wish；test_engine 完整流程含许愿触发→DM裁定→先例查询）
- 手动链路验证：declare_wish 出现在可用动作 → 触发许愿中断（类型=许愿、
  上下文含 wish_text）→ 缺 wish_text 拒绝 → DM 裁定 → 先例可查
