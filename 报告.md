# 报告

> **当前未处理事项只在第三节《设计待办》**（D1–D3 方向已定/暂不实装，D4 待重标，D6/D7/D12 已裁定不改）。
> **等裁定的＝D13（生成把方括号不统一带进规则正文，8 处）、D14（六处 `AI_EXPERIENCE.md:1254` 锚点早已失效）**；
> 等平衡测试的＝D11（赌命 30% 基数）；等重做的＝D8（活血）；只影响未实现内容的＝D9（波及固定X）。
> 第 4.6 节的 **Q1–Q12 已于 2026-09-18（夜）逐条裁定并实装**，落点与读数见第六节；该表保留为观测存档。
> 第五节是同日（晚）两条更正的实装记录。
> 新硬伤按「现状 → 为什么是问题 → 建议做法」并附复现/定位命令的格式，进第三节（需裁定的另开表）。

## 一、上一轮待办的落点（仅作索引，无需再处理）

| 项 | 结论 | 落点 |
| --- | --- | --- |
| P1 35 条道纹 `summary` 与程序不符 | 全量同步（裁定 sync_all） | `全道纹索引.md`（生成器区域专属道纹改 `sorted()`，重生成可复现）、`副本/*.md`、`物品索引.md` |
| P2 三只怪物战术被道纹重做打崩 | 怪物 AI 依据道纹语义自行推导战术 | `AI_EXPERIENCE.md`《怪物准则》第 9/10/11 条、`archive/monster_tactics_2026-09-17.md` |
| P3 高爆手雷对轮回者无效 | 重做为「全场 20 伤害；伤害后生命≥50%[血限]给【无力2】，否则【无力1】」 | `engine/api.py`、`物品索引.md`、`tests/test_grenade_action_budget.py` |
| P4 条件触发式道纹冗余清单 | 逐条核实：仅「加害/龙鳞」确证纯对称重复（裁定保留），4 条论断被推翻，另挖出「缄默是死状态」 | `archive/P4_daowen_redundancy_verification_2026-09-17.md` |
| P5 怪物战术文档覆盖率 7% | plan_b | `archive/monster_tactics_2026-09-17.md` |
| P6 战斗行动接口正文无条文 | 分六批补齐：60/64 有专属四要素条文，余 4 条指路专题正文 | `AI_EXPERIENCE.md`《行动接口口径（全部行动接口的四要素条文）》 |
| 附：缄默无消费方 | 已实装（全场封禁「由[命零]触发的效果」） | `engine/combat.py`、`engine/daowen.py`、`tests/test_jianmo_silence.py` |
| 附：怪物出手预算不校验 / AI 用不了需目标的工具 | 两处接口不可达已修 | `engine/combat.py::prepare_monster_phase`、`engine/ai_tactics.py::try_consumable` |
| D4 滋养口径待裁定 | 用户裁定（2026-09-18）＝「消耗2X。使[目标]受到的恢复量翻倍，持续X」——做成**放大器**，不清零累计、不抬阈值、不豁免癌变 | `engine/daowen.py::calculate_ziyang`、`engine/models.py::apply_heal`（统一回复入口结算）、`tests/test_ziyu_ziyang_rework.py` |
| D5 自愈口径待裁定 | 用户裁定（2026-09-18）＝「代价：冷却X。恢复[目标]25X%已损生命」；ROUND_START 上的自愈机制整体删除 | `engine/daowen.py::calculate_ziyu`、`engine/combat.py`（`heal_missing_percent` 分支＋波及平分＋`_monster_max_daowen_x` 崩解线封顶）、`engine/mechanisms/builtins.py`、`机制迁移台账.md`《已回撤》 |
| 附：癌变能否被绕开 | 用户裁定：不加任何新豁免途径，唯一免疫仍是遗物【第一杯】（刻意的强度限制）；滋养×2 同时把癌变进度×2 是预期张力 | `AI_EXPERIENCE.md:396`、`tests/test_ziyu_ziyang_rework.py` |
| 附：残韵 19 条回边不可达 | 选 A：`use_resonance` 改 target-first（自由选目标→再选其身上的道纹），未给目标才回落施法者本人 | `engine/api.py::_find_resonance_holder`、`tests/test_resonance_target_first.py` |

## 二、环境注意事项（每次新会话必读）

- **用户工作原则（2026-09-18）：非必要不修改。** 用户已多次遇到「改来改去最终改回原版」，
  所以现在任何"顺手改一改""统一一下口径"的冲动都必须先报给用户裁定，不得自行动手；
  评估结论若是不改，也要在本节或三节留一行存档，避免下次会话重新提。
- `.venv` 不进快照：新会话需 `python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt`。
- **既有失败基线 9 条（AI/mock 类），不是回归**，逐条为：
  `test_ai_basic_attack_candidate::test_at_one_by_one_daowen_still_wins`、
  `test_ai_tactics::test_ai_can_declare_parry_under_lethal_threat`、
  `test_build_learner::test_valid_and_invalid_are_separated`、
  `test_monster_daowen_x_free::test_heal_x_scales_with_missing_hp`、
  `test_unified_ai::test_ai_player_is_the_combat_and_high_level_entrypoint`、
  `test_win_only_ai`（4 条：`test_win_only_includes_parry_in_real_candidate_path`、
  `test_collapse_line_veto_rejects_mutation_suicide`、`test_collapse_line_veto_allows_safe_cast`、
  `test_survival_tiebreak_prefers_later_death`）。
- 全量回归口径（最新，③-full 之后）：`9 failed / 1723 passed / 3 xfailed`，**约 5 分钟**（313s；变慢的原因与逐条数据见 6.13「性能核查」——是打的局数变多，不是每步变贵）。
  历史口径：`9 failed / 1706 passed / 3 xfailed`（约 4–5 分钟；2026-09-18 夜 Q1–Q12 实装后实测。
  1697 → 1706 ＝ 本轮新增 9 条用例：`test_monster_phase_order.py`(5)、`test_guard_command.py`(2)、
  `test_employee_economy.py`(2)；9 条失败仍与上面的基线名单逐条同名）。
- **改一条规则后先跑 `PYTHONPATH=. .venv/bin/python sim/check_rule_change.py`（约 7 秒）**，别每次都等 4–5 分钟全量：它会一次做完「索引同步（重跑生成器比哈希）＋12 文件守卫测试子集（对照基线只报新增失败）＋废案扫描＋引擎口径速览」，`--full` 才跑全量。遇到「同 seed 两次跑不一致」跑 `sim/diag_repro.py`（7 层定位＋账本污染审计），不要再靠反复重跑猜层。两者用法与反向对照见 6.11。
- **新增账本（战斗运行态容器）只改一处**：`engine/ledger_isolation.py::COMBAT_LEDGERS` —— AI 预演隔离、死斗推演隔离、`sim/diag_repro.py` 的 L7 垃圾键审计同源；`tests/test_ledger_isolation.py` 会**静态**扫 `engine/combat.py` 的容器创建点，未登记即失败。详见 6.12。
- 平衡模拟：`.venv/bin/python sim/run_sim.py all 20000`（种子固定 2026，约 1 分钟）；
  工具库只在扭曲都市发放，改道具后另两副本应保持不变，可当镜像未外溢的反证。

## 三、设计待办（未实装，**不是**现行口径；2026-09-18 用户令开此节）

> 本节收纳「方向已定但口径未裁定」与「暂不实装」的设计。正文（`AI_EXPERIENCE.md`）与
> `全道纹索引.md` 按红线只写现行口径，所以未实装设计一律记在这里，不进正文。

### D1 攻伤×当前生命 —— 不做全局公式（用户已裁定），只允许遗物/道纹级覆盖

- 裁定：**不动核心伤害公式**（普攻伤害 = [攻击次数]×[攻击力]，攻击力 = 当前法力）。
- 允许的形态：做成**遗物或道纹**级别的局部覆盖，复用 `engine/models.py::effective_attack_power`
  的状态层机制（例如「攻击力 = 当前生命×k%」），只对该持有者生效。
- 为什么不做全局（四方案结论存档）：a 比例乘区 → 双向死亡螺旋＋血限双扣；b 绝对加成 →
  怪物血限 192~552 会碾平玩家；c 阈值反螺旋 → 残血增伤留翻盘，但改的是全局节奏；
  d 遗物/道纹级覆盖 → 唯一不污染 79 只怪物输出、不与审计口径
  `ceil(血限/6)+2×攻次+2×攻击力` 双计、不要求 `sim/balance_sim.py`（自带 `m.attack_power`
  伤害模型，不跟随核心公式）重标的方案。
- 实装时需一并处理：血限在审计口径里已计价，覆盖式攻击力要避免与「血限→攻力」双计。

### D2 美杜莎之眼 —— 从 `物品索引.md` 草案区移入此处（用户裁定：先移到设计待办，标明原因）

原文（巴别塔草案，移入前逐字保存在此，未实装、引擎零引用）：

> ### 美杜莎之眼
> 选择一名[目标]，你可以消耗等同其10%当前生命的[石块]耐久，使其立刻触发【雕塑】。雕塑对轮回者同样生效。
> > 来源：巴别塔草案

**为什么移出来（现行规则下它的三个前提都已失效）**：

1. 「雕塑对轮回者同样生效」是**冗余句**：2026-09-09 DM裁定后 `combat.py::_can_be_sculptured`
   对任何角色都返 True，`settle_victory_paths` 第二个循环确实在雕塑玩家侧。
2. 它卖的效果**能被免费复现**：属性统一后 攻次=当前速度、攻力=当前法力
   （`AI_EXPERIENCE.md:1235`），把目标两个池子同时打到 0，回终自动雕塑。速度是唯一行动池
   （减速 10X%／疲惫／透支／萎缩溢出都在抽它），法力被普攻抽（攻伤=当前法力）。
3. **代价算不过来**：石块耐久 = ceil(血限×5%)（`combat.py::_sculpture_monster`），
   遗物却要 ceil(目标当前生命×10%) → 对同阶满血目标一次要 **2 块石块**；怪物血限 192~552
   即单次 19~55 点耐久，而每点耐久本来值 15 伤害／20 格挡，等于烧掉 300~800 伤害换一个
   **不给碎片**的移除，且移除后只返还 ceil(血限×5%)（付 2 拿 1）。文案还**没有部分支付条款**：
   耐久不够就直接不能用。只有目标已掉到石块来源怪血限 ~50% 以下才付得起，而那时直接杀更划算。
4. 仅剩的独占价值：①「立刻」，不必等回终、不必自己耗资源把两池打空；②对满血高攻目标也能强推；
   ③死斗里雕塑对手是列名的多路径胜利（`api.py` 回终 note「多路径胜利（雕塑/癌变/还债）已结算」）。

**处置意向（用户原话意旨）**：等实测**轮回者雕塑流**时发现缺哪个机制，就把这件遗物的效果
改成那个缺失的机制——比现在凭空瞎想要靠谱。届时可直接复用的现行【雕塑】口径：
`AI_EXPERIENCE.md:414-415`（任意角色攻次与攻力同时归 0 → 化为雕塑消耗品 X/Y，Y=ceil(血限×5%)，
每点耐久 15 伤害或 20 格挡；不视为击杀、不提供碎片）。

### D3 尸爆 —— 重做待议（用户：现有方案都看不上，先放待办）

现行正文：「X：消耗3X。[命零]对所有敌方[目标]打出自身[血限]的10X%伤害。」

**无论怎么重做都必须一并修掉的硬伤**：

1. **侧别 bug**：`engine/combat.py` 的尸爆结算固定打 `self.state.get_all_enemy_side()`，
   而它恒返 `state.enemies`（永远是玩家视角的敌方）→ **怪物（不死骸骨／尸霸）发动尸爆时炸的是自己那一侧的怪物**。
   修法：侧别改为相对施法者（「你方之外的所有存活角色」）或相对尸体。
2. **母子语义脱节**：【分裂】已于 2026-09-17 重做为即时结算（造 X 个 10Y 血限复制体、代价衰老 X×10Y），
   尸爆作为它的转化产物仍绑在 [命零] 上，母道纹已经没有"死后触发"的身份了。
3. **代价时点怪账**：3X 法力在"人都要死了"的那一刻支付；若改主动引爆则应在发动时预付。
4. 与【缄默】的边界：缄默封「由[命零]触发的效果」，尸爆整条都在封禁范围内
   （`combat.py` 已有 `qianmo_blocked` 分支）；若重做为非[命零]触发，需明确缄默还封不封。

**已被否的方案存档**（勿再提）：P1 点尸引爆／P2 同归于尽 50X%／P3 每尸炸随机敌；
2026-09-18 批次：尸-A 连锁（任一敌方命零→其余敌方全体）／尸-B 延迟引爆（下一[回始]炸，尸体被移出则不炸）／
尸-C 主动自爆（立刻[命零]，打「你方之外」全体）。

### D4 自愈/滋养重做的连带待办 —— 单卡评分与承载怪战术需按新语义重标

- `data/daowen_single_scores_20260914.json`（`protocol.kind = single_daowen_baseline`，runs=6）是**旧语义**下测的：
  自愈当时排 51、pve 0/6 胜（旧版是[回始]自奶→自我癌变定时器，见 D5 事实2）。重做后该分数不可再用于平衡决策。
  重测入口：`.venv/bin/python sim/build_learner.py --audit-all-daowen --audit-runs 6 --audit-output data/daowen_single_scores_<日期>.json`
  （单卡基准不代表构筑内边际贡献；滋养现在自身不回血，单卡跑分必然偏低，读分时要记住它是**放大器**、
  价值来自与治疗来源配对——与自愈2 配对即满血复活）。
- 5 只承载怪（白骨祭坛、肠水母、血肉巨囊、打手头目、背碑人）的战术口径需复核：怪物发动自愈＝
  **冷却X 场＋家族税异变5X**，且必须显式选定[目标]（`prepare_monster_phase` 会给 `target_options`，含它自己）；
  崩解封顶已把家族税计入（`combat.py::_monster_max_daowen_x`），异变层数高时可用 X 会自动收窄到 0（＝摘除该选项）。

### D5 本轮查实的现行事实（存档，防丢失）

1. **滋养是双刃刀（×2 放大器）**：结算点在 `models.py::apply_heal`（统一回复入口），覆盖**战斗内**的一切来源
   （道纹／消耗品／寄生…）；过量部分同样翻倍计入 `total_healed` → 对怪＝癌变进度×2（癌变不给[碎片]、
   局外【休整】永久+8），对轮回者/[朋友]/[员工]＝更快直接[命零]。同名重复施放**不增强倍率**（恒×2），只叠加持续时间。
   **它不加成局外【休整】**（用户 2026-09-18 指出，已查实）：滋养是 `StatusEffect(scope=BATTLE)`、持续X回合，
   [战终]统一清场只保留 COST/RUN/PERMANENT 三种 scope（`api.py:5292-5313`），而【休整】是战前行动
   （`api.py:1408 _pre_battle_xiuzheng`）→ 两者永不同时在场。此前正文与两处代码注释误写「覆盖…休整」，已更正。
2. **旧自愈机制是怪物的自杀定时器，已整体删除**：旧版[回始]自奶走统一 `heal` 动词、过量照原值累计，
   癌变阈值只有 ceil(血限×2) → 承载怪 ceil(20/X) 回合后必然自我癌变（X=3→7、X=5→4、X=9→3），
   叠加异变5X/次与崩解线50，第二次发动还会直接崩解；两条路都永久离场且不给[碎片]。
   新版按**已损生命**计价、代价改冷却，两条自毁路径都消失。旧【自愈】状态即使残留在存档里也不再有任何
   [回始]结算（`tests/test_shuai_bai_migration.py::test_legacy_ziyu_status_is_inert_and_shuaibai_still_first` 钉死）。
3. **怪物「家族税」已删除**（用户令 2026-09-18，实装见第五节 5.1）：现行口径＝怪物发动道纹只按该道纹
   **自身正文代价**支付，与轮回者/[朋友]/[员工]同口径走统一代价总线 → 自愈对怪物只付【冷却X】、不产生异变层数；
   狂暴/全力/疯狂/减速/必中/飞行 的【异变5X】是它们**自身**的代价（不是家族税），照旧支付、照旧受崩解线封顶。
4. **尸爆侧别 bug**（详见 D3）。

### D6 冷却X 改「每回合计时＋跨场继承」—— 已评估，用户裁定**不改**（2026-09-18）

用户提议把【冷却X】从「X 场战斗」改成「每回合-1、归零即可再用、剩余冷却跨场继承」，
评估后以「太麻烦、非必要不修改」否决。存档评估结论，勿再提：

- **现状即已跨场**：递减点在 `engine/api.py` 的 [战终] 统一清理块（只遍历 轮回者/[朋友]/[员工]，
  **怪物不递减**＝一场一次），实例常驻、[战始] 不重置；闸门只有 `models.py::can_use()`；
  `X_LIMITS["冷却"]=7`。
- **共用这个代价类型的一共 5 条**：固执X（冷却X｜单次失血上限1，持续X）、束缚X（冷却2X｜无法行动，持续X）、
  全速X（冷却X｜攻次=速限，持续X）、畸变X（冷却X｜[回终]扣目标 攻力×攻次 的[血限]，持续X）、
  自愈X（冷却X｜恢复25X%已损生命，瞬发）。
- **只换计时单位会正面破红线**：实测战斗长度（`sim/run_sim.py` 300 局/287 场，粗模型仅作量级参考）
  均值 8.6 回合、中位 7、p90 13；而上面 4 条的**冷却≈持续**，按回合计时后 固执1＝每回合重开
  （接近绝对无敌）、束缚1/2＝目标 50% 回合无法行动（永久控制）、全速X/畸变X＝100% 覆盖，
  直接违反 `AI_EXPERIENCE.md:731`「禁止绝对无敌、绝对控制…擅自按回合递减跨战斗冷却」与
  L279《怪物准则》「严禁单场内重复复读发动」。要改就必须同时重标 5 条的冷却倍率
  （建议红线：冷却回合数 ≥ 持续回合数×3，控制类再+1），连带改 4 处正文、
  `test_daowen_cost_consistency.py::SUMMARY_LEAD["冷却"]`（硬钉「冷却N场」）、
  `validator.py:440`／`watchdog.py:149` 措辞、索引与台账、`sim/balance_sim.py` 激活模型。
- 顺带查实的自愈自平衡性质（存档）：按回合计时时，自愈X 的**每回合回复率＝25X%÷X＝恒为已损25%**，
  与 X 无关（X 大＝大爆发＋等更久），所以它本身不需要重标；怪物侧复读会被家族税异变5X/次
  自然限死（自愈1 每回合复读＝10 回合撞崩解线50）。

### D7 `api.py` 的失败事务回滚只覆盖 3/9 本账本 —— 已查证，未改（属另一裁定）

- 位置：`engine/api.py::_snapshot_combat_runtime`／`_restore_combat_runtime`（供**失败事务回滚**，
  不是副本世界隔离）。
- 现状：只快照/回滚 `_monster_activated`、`_resonance_rewrites`、`_sanxiang_consumed` 三项；
  `_monster_daowen_round_used`、`_monster_evolved`、`_split_clones_spawned`、`_effect_chain_depth`、
  `_dodge_counts`、`_dodge_round` 不在内 → 一次失败提交回滚后，这几本账可能保留提交过程中的写入。
- **为何没顺手改**：它的口径与「副本世界隔离」不同——回滚会重建实体对象，所以它把 id 键翻成
  `(kind, index)` 稳定引用、还原时再翻回 id；扩覆盖面＝改回滚行为（可能让「本回合已发动」被回滚掉，
  进而改变怪物能否再发动），属行为改动，按「非必要不修改」先报裁定。
- 已做的防护：`tests/test_ledger_isolation.py::test_api_rollback_ledgers_are_a_subset_of_the_spec`
  钉住「它覆盖的账本名必须在权威清单内」，防两套口径彻底分叉（覆盖范围可以不同，名字不能陌生）。
- 若要处理，建议顺序：先写一条复现用例（构造一次失败提交，断言 `_monster_daowen_round_used`
  是否残留、残留是否造成可观测后果），确认有真后果再决定扩不扩。
- **2026-09-18 ③-full 之后的更新**：账本键改成 `runtime_id`，本函数里那套「id → `(kind, index)` →
  回 id」的翻译已**删除**（runtime_id 本身跨对象稳定，不需要翻译），实现见
  `engine/api.py::_snapshot_combat_runtime`／`_restore_combat_runtime`（改为按在场名册的 runtime_id
  过滤，语义与原来一致：不在名册的条目照旧丢弃）。**覆盖面仍是原来那 3 项，一行没扩**——
  扩不扩仍属另一裁定，本条继续存档。

### D8 【活血】需要重做 —— 承露盏已取代其作用（用户裁定 2026-09-19，本轮只记档）

- 裁定：活血记入待办**重做**，理由是遗物【承露盏】已经取代它的作用。本轮**不改口径、不动结算**
  （按「非必要就不修改」，重做属规则改动，等用户给新口径）。
- 两者对照（同为「掉血 → 换资源」，承露盏是更完整的一版）：
  | | 触发条件 | 产出 | 累计范围 | 余数 |
  |---|---|---|---|---|
  | 【活血】道纹 | 每累计失去 2 点生命 | [回终]获得[回复1] | 每个完整回合内 | [回终]清空 |
  | 【承露盏】遗物 | 每累计失去 10 点生命 | 获得 1 点法力 | 本场累计 | 滚存 |
  - 承露盏：`engine/api.py:1598`（正文口径）、`engine/combat_parts/damage_death.py::_settle_chenglu`（结算）。
  - 活血：`engine/daowen.py::calculate_huoxue`（结算 `hp_lost_this_round // 2`）、`AI_EXPERIENCE.md:1030`
    （DM裁定 2026-08-19：只看本回合是否实际掉过 HP，不区分来源）。
- 顺带记档（重做时一并定，本轮不改）：活血的结算是 `// 2`（向下取整）。项目整数规则
  （`AI_EXPERIENCE.md:203`）写的是「所有计算都向上取整」，但「每满 N 计一次」这类计数型效果
  用 ceil 会变成「掉 1 点也算一次」，语义相反 → 需要一条明确的例外口径，而不是就地改成 ceil。
- 本轮已做的两件小事（都不改行为）：
  1. `engine/daowen.py::calculate_huoxue` 的 docstring 里补了「待重做」注记（代码注释不受正文红线约束）；
  2. `AI_EXPERIENCE.md:350` 那行简写口径（「每累计失去2生命…持续X」）与 `副本/龙心谷.md:12`、引擎首行
     不一致（漏了「每个完整回合内」「余数[回终]清空」），按上轮搬进引擎的现行口径同步了一遍。

### D9 面板把 X 写死的【波及】在可标记目标不足时无法结算 —— 只影响未实现副本，记档观察

- 2026-09-19 删掉「自适应降X」后，结算目标数严格等于本次发动的 X。对 `x_free` 面板
  （现行所有已实现副本的波及都不写 X）没有影响：发动方自己把 X 收到可标记目标数以内即可。
- 但**固定 X** 面板（`monsters.py:111`：写了数字就 `x_free=False`）在可标记目标 < X 时，
  提交数凑不出来 → 结算报错。目前只有两个**未实现**草案是这种写法：
  `副本/巴别塔.md:120-128`（测绘师 波及4、断层匠 波及5/波及4…）、`副本/永夜庭.md:139`（宁晚 波及3）。
- 本轮不改（未实现副本不进运行时，`tests/test_rule_sources.py:40` 钉着「巴别塔＝未实现」）。
  **实现这两个副本时要一并决定**：面板去掉 X（跟现行口径一致，推荐），或给固定 X 补一条
  「目标不足时怎么算」的明确规则。
- 驱动侧已有兜底：`sim/build_learner.py` 的 `wave_retry` 会在波及报错时同 token 换一条非波及
  道纹重交，怪物不会因此卡死（攻击选项始终在）。

### D10 波及实验查出：自我增益类道纹在「互标」时把自己的状态送给对方 —— 用户裁定**选案 A**，已实装（2026-09-19，见 6.21）

- 出处：2026-09-19 用户令做的波及扩散实验（见 **6.20**，原始矩阵
  `data/experiments/wave_spread_2026-09-19.md`，探针 `sim/probe_wave_spread.py`）。
- 现象：轮回者与怪物互相挂上波及标记后，轮回者发【滋养】（正文＝「使**贾凡**受到的恢复量翻倍」），
  状态实际落在**怪物**身上，自己一点没拿到。同类共 13 条：借力、兴奋、加速、急速、活血、滋养、
  爆裂、龙鳞、背负、庇护（状态部分）、全力（状态部分）、变形（状态部分）、逆鳞（状态＋层数初始化）。
- 机制（`engine/combat_parts/daowen_effect.py`）：
  - `:57-67` 组扩散名单 `wave_status_targets`：`[本次目标（**若不等于施法者**）] + 自己挂出去的波及目标`
    → 本次目标是自己时，自己被剔除，名单里只剩对方；
  - `:982-989` 状态类分支 `for et in wave_status_targets` 无条件按这份名单挂状态 → 挂到对方身上；
    而它自己的注释写的是「状态类效果对每个拥有波及效果的目标（**含本次[目标]**）原样生效」
    → **实现与注释意图相反**（本次目标＝施法者时并不含）；
  - 数值类分支（伤害/治疗/格挡…）另有 `if key in wave_pieces` 门槛，只有有效目标 ≥2 才平分，
    所以 1v1 里数值留在提交的目标上、状态却跑了 → 同一条道纹被劈成两半
    （【庇护】＝格挡给自己、庇护状态给对方；【全力】＝面板锁定给自己、全力状态给对方）。
  - `:959` 还有一份 `self_targeted` 白名单（自食/飞行/滑翔/狂暴/必中/固执/贯穿）走「状态永远挂施法者」，
    **不受波及影响** → 同样是自我增益，两类道纹行为相反，取决于它落在哪个分支。
  - 另：【贯穿】在这次实验里 `wave_spread` 报了扩散目标（实验怪、实验怪乙），实际状态仍挂在自己身上
    → 日志与事实不符（`wave_spread` 是按名单记的，不代表每个分支真按名单落地）。
- **用户裁定（2026-09-19）＝选案 A**：扩散名单保留施法者自己，自我增益自己照常拿到、波及目标额外各得一份。
  用户对副作用（1v1 自施时数值平分，一半治疗/格挡给敌人）的原话：「要是担心波及当内鬼，直接不用不就行了？」
  —— 即接受该代价，不为它加保护。实装细节与验证见 **6.21**。
- 当时的三个改法（存档，A 已选）：
  | 选项 | 怎么改 | 改完之后的后果 |
  | --- | --- | --- |
  | A 照注释意图修 | 扩散名单保留施法者自己（本次目标＝自己时不剔除） | 自我增益自己照常拿到，**对方也拿一份**；1v1 里有效目标变 2 个 → 数值型开始平分，于是【自愈】【再生】会把一半治疗分给敌人、【杀伐】打对方时若对方也被自己标记则……（要逐条重测，平衡面改动最大） |
  | B 自我类不参与扩散 | 本次目标＝施法者时整条道纹不进扩散名单（等于把 `self_targeted` 白名单的做法推广到所有自施情形） | 自己的增益自己拿，波及目标不受影响；只有显式指定他人为目标时才扩散。行为简单可预期，但正文「你发动的道纹同时作用于所有拥有波及效果的目标」要加一句例外（自施时不扩散） |
  | C 不改代码，改文档 | 保持现状，把正文/summary 逐条写清「自施时状态落到波及目标身上」 | 零行为改动、零平衡影响；但要改 13 条道纹的文案，而且【滋养】这类「使自己……」的正文得改口径；战术上互标会变成一种反制手段（对手给你挂标记后，你的自我增益反而资助对手），需要玩家能预判 |
- 我的观察（不作为裁定）：C 的文案代价最小但把反直觉行为固化成正则；B 最符合玩家直觉、改动面可控；
  A 语义最贴「同时作用于」字面，但会让治疗/格挡类把一半送给敌人，几乎肯定要重做平衡。
  另外无论选哪个，`wave_spread` 日志按名单记而不按实际落地记（贯穿那例）都建议一并修成「记实际落地」。
- **`wave_spread` 日志的裁定（2026-09-19）：先不动，只记档。** A 落地后这份日志的假阳性面变大了
  （12 条道纹现在报「扩散→甲,乙」但实际对波及目标什么都没做，见 6.21 第 4 条），仍未改代码，
  留在此处备查；将来若要修，改法是「按实际落地记」而不是「按名单记」，发射点 `daowen_effect.py:80`（数值分支）
  与 `:85`（`elif len(effective) >= 2` 的状态分支）。

### D11 【赌命】30% 的基数（血限 vs 当前生命）—— 用户裁定：等平衡测试时一并定（2026-09-19）

- 现状：实现是 `combat.py::math.ceil(tgt.blood_limit * pct / 100)`＝30% **血限**
  （pct 本轮已改成读引擎声明字段 `duming_hp_pct`，不再硬编码）；旧发布正文写的是 30% **当前生命**。
- 本轮已把发布正文（引擎首行、summary、`副本/罪孽都市.md`）统一对齐到**实现**口径，零行为改动。
- 用户裁定：基数到底按哪边，**等平衡测试时一起调整**（与「平衡数据等所有规则完善了再测」同批）。
- 重跑平衡时要一并决定的两点：
  1. 30% 按血限（对残血目标可能直接打死，数值稳定可预判）还是按当前生命（残血目标只掉一点，几乎打不死）；
  2. 若改按当前生命，`duming_hp_pct` 的语义要跟着改，相关测试（`tests/test_exclusive_zui_twisted.py:228-243`）同步。

### D12 引擎首行的定义术语方括号不统一（29 条命中，约 23 条该加）—— 用户裁定**只记档、不改**（2026-09-19）

- 出处：①-B 第一刀（见 **6.22**）把副本区域正文改成引擎生成后，引擎首行的写法**直接变成玩家正文**，
  于是这处既有的不统一显形了。不是本轮引入的——`全道纹索引.md` 一直按同一份首行发布。
- 实测（`sim/daowen_doc.py::RULES` 全量扫，排除已在 `[]`／`［］`／`【】` 内的命中）：**29 条道纹**的首行里
  有未被括号包住的定义术语（[目标]／[血限]／[碎片]／[假碎片]／[回复]／[回始]／[回终]／[法限]／[速限]）。
  逐条看过，分两类：
  | 类别 | 条数 | 例 | 该怎么处理 |
  | --- | --- | --- | --- |
  | 术语当名词用，按文档惯例该加括号 | 约 23 | 伤痕「使目标…血限-X」、畸变「回终使…的血限」、坏死「无法获得回复」、点金「获得X个碎片」、全力「等同其法限」、狂暴/无力「回始…」、逼债/清算/赎金/消灾/赌命/假钞 的碎片类 | 首行补方括号（纯文案，零行为） |
  | 动词或散文用法，**不该**加 | 6 | 再生「为[目标]**回复**4X点生命」（动词）、飞行「选为**目标**」、无神「选择**目标**时」、封印「一个**目标**怪物」、超频 注「**目标**可自由指定」、波及「由你挂上波及效果的**目标**」 | 保持原样 |
- **用户裁定（2026-09-19）＝只记档、不改**（选项：保持现状；不影响任何行为，引擎按代码走不按括号）。
  本节留档防重提：将来若有人再提「正文括号不齐」，先看这里的两类清单——6 条是动词/散文用法，加了反而错。
- 当时评估的改动面：这是 23 条首行的文案批改，会连带重生成索引与四区正文；`WORDING_STOP` 已把 `[]（）` 排除在
  措辞守卫的 bigram 之外，所以**不会**触发措辞漂移；数字守卫的 24 字归属窗口理论上会因字符变长而位移，
  改完必须复跑（代价 token 都在名字紧后方，风险低）。按「非必要就不修改」，不裁定就不动。

### D13 生成把 D12 的方括号不统一发布进了规则正文（8 处）—— 待裁定

- 现状：三节改成引擎生成后，正文里 8 处术语少了方括号（清单见 6.23「已知代价」第 1 条），
  另有约 5 处数字旁空格消失。D12 裁定的是「不改引擎首行」，当时后果只落在索引；现在后果落在
  **规则正文**（AI 与玩家读的那份），所以要重新确认一次。
- 可选做法与后果：
  | 案 | 做法 | 后果 |
  | --- | --- | --- |
  | A 保持现状 | 什么都不做 | 正文与引擎首行逐字一致（单一真源最纯）；代价是 `[回始]` 这类**时点术语**在狂暴/无力两行变成裸词，读者要靠上下文认；与文档《基础定义》的方括号惯例不齐 |
  | B 渲染层补方括号 | 在 `sim/daowen_doc.py` 加一张术语表（就用《基础定义》里已定义的 `[战始][战终][回始][回终][敌回始][敌回终][命零][目标][碎片][血限][法限][速限][员工][朋友][回复]`），渲染时把裸词补成带括号；引擎首行不动 | 正文/索引/副本区域三份派生文档一起变整齐（都要重跑生成器）；D12 的「不改引擎」仍成立；风险＝补错位置（如「回复量翻倍」会变成「[回复]量翻倍」），需要一张例外表＋守卫 |
  | C 改引擎首行 | 直接把 8 条首行补上方括号 | 与 D12 的裁定相反（要用户明确翻案）；好处是连 `summary`、AI 提示词、索引一起齐 |
- 我的建议：**A**（本刀先不动）。理由：这 8 处不影响任何行为与数值，`[回始]` 的语义在句子里仍完整；
  B 要新增一张术语表＋例外表＋守卫，是「为了排版齐」引入的新维护面，与「非必要就不修改」冲突。
  若将来发现 AI 因为裸词误判时点，再走 B（渲染层，不碰引擎）。

### D14 六处代码注释的 `AI_EXPERIENCE.md:1254` 锚点已失效（早于本刀）—— 待裁定

- 出处：`engine/api.py:2512`/`:3854`、`sim/ally_targets.py:10`、`tests/test_ally_commands.py:13`/`:82`、
  `tests/test_ally_daowen_x_free.py:9` 都写「微光者按 AI_EXPERIENCE.md:1254 是一池制、[回始]不回填」。
  实测 HEAD（本刀之前）的 1254 行是 ai_preview 死斗盲区笔记，与该口径无关；真实位置是 706 与 725 行
  （`grep -n "一池制" AI_EXPERIENCE.md`）。
- 为什么现在提：本刀让三节变成生成块，**任何指进这三节的行号锚点从此必然漂移**（口径一改行数就变）。
  本刀已把唯一一条被自己推移的锚点（`test_daowen_cost_consistency.py` 的 `:391`）改成按节名引用。
- 可选做法与后果：
  | 案 | 做法 | 后果 |
  | --- | --- | --- |
  | A 只记档 | 不动那六处 | 注释继续指错行（读者按行号翻会翻到无关内容），但代码行为无影响 |
  | B 逐条改指真实行 | 1254 → 725（或 706） | 六处一改就准；但下次正文重排又会漂移，属于「修一次坏一次」的锚点 |
  | C 改成按节名/引句引用 | 例：`AI_EXPERIENCE.md`《核心规则》「微光者与轮回者为一池制、[回始]不回填」 | 不再漂移；六处文案各改一行，无行为影响 |
  | D 加锚点守卫 | 写一条测试：扫代码注释里的 `AI_EXPERIENCE.md:NNN`，校验该行确实含引号里的关键词 | 一劳永逸（漂移立刻红），但要新增一个扫描工具＋把现有引号关键词补齐才能过 |
- 我的建议：**C**（顺手可做，六处文案），若要根治再加 **D**。等裁定，未动手。

## 四、手操轮回实测观测（2026-09-18，扭曲都市 7 场全生还）

数据来源＝`data/handplay_20260918/trace.jsonl`（511 步，`rng_seed=20260918`，驱动器 `sim/handplay_cycle_20260918.py`）。
本节只记录观测与待裁定问题，**未改任何规则/数值**（遵循第二节「非必要就不修改」）。

### 4.1 局概览（战始HP→战终HP、结局、碎片）

| 场 | 怪（血限/攻力×攻次） | 回合 | 我方HP | 结局（引擎判定） | 碎片 |
| --- | --- | --- | --- | --- | --- |
| 1 | 骨天使 222/3×7 | 5 | 60→48 | 手雷【无力2】清空出手 → **凡庸炸裂**（+【残骸】1/1） | +20 |
| 2 | 奇美拉 228/2×9 | 3 | 50→50 | 自施【变形】两次 → 攻次攻力归0 → **雕塑**（得奇美拉雕塑12/12） | 0 |
| 3 | 奇美拉 228/2×9 | 2 | 52→52 | 普攻+雕塑9发抢在自雕塑前 **命零** | +20 |
| 4 | 奇美拉 228/2×9 | 4 | 54→48 | 残韵【转换】剥掉变形（→我得定型）断自毁线后 **命零** | +20 |
| 5 | 血肉巨囊 252/1×8、肠水母 204/1×12 | 12 | 61→**1** | 巨囊每回合刷新爆裂＝付光法力 → **凡庸**；水母打到7.8% → **救赎·接纳**（员工「溺」） | +41 |
| 6 | 奇美拉、肠水母、奇美拉 | 9 | 53→49 | 两只奇美拉 **自雕塑**（各12/12）；水母剥自愈后打到7.8% → **救赎·接纳**（员工「溺」→b7 阵亡） | 0 |
| 7 | 人头气球×2、眼树×2 | 10 | 69→69 | 气球×2 **命零**；眼树①**救赎·接纳**（员工「盲」，战终 攻次1→2）；眼树②**救赎·终结** | +59 |

合计：普攻 1920 伤、消耗品 545 伤（雕塑 450／电击枪 75／手雷 20）；招架 12 次；碎片收入 160。
七场打完 `player_dead=False`，引擎滚入新轮回（`phase=setup`、`battle=0`、碎片重置 20、精力 3）＝晋级。

### 4.2 道纹／残韵使用率（实测计数，含"存而不用"）

- **发动过的道纹只有 4 次**：透支3（×1，**满蓝白放**，见 4.5）、自愈1（×1）、固执1（×1）、束缚1（×1）。
- **持有却全程 0 次发动**：封印（b3 学习所得）、定型（b4 残韵所得）、滋养（b6 残韵所得）、庇护类未学。
  → 学习/收割的产出有相当比例是"入库不用"；`封印` 的正文是「使一个目标怪物延后X回合再入场」，
  对已进场怪是否合法我当时未验证（怕白付异变），这直接压低它的使用率。
- **残韵只用出 4 次，全是【转换】**：变形→定型 ×1、自愈→滋养 ×2、滋养→自愈 ×1（收割自愈两步法成立 ✓）。
  **【反转】【曲解】全程 0 次**，末了还剩 曲解2／转换1／反转2＝5 枚未花。
  原因：本局怪物池反复出现奇美拉（5 次）与自愈承载怪（3 次），转换边（定型／滋养／滑翔）刚好都用得上；
  反转边（衰败／自残／蒙蔽／无力）与曲解边（无神／退化）没有遇到匹配的承载怪或代价不合算（衰败消耗4X＝自砍攻力）。
- **残韵经济实测**：三相残韵盘每场净 +1 枚（战始消耗1、战终得2）；事件「真拒绝」另送随机 1 枚（`AI_EXPERIENCE.md:494`）。
  7 场下来残韵从未成为瓶颈（末场还余 5），瓶颈反而是"没有合适的边可打"。

### 4.3 怪物自毁线：本次 12 只怪里 7 只不是我打死的

- **凡庸 3 只**（骨天使＝被手雷无力2 削掉出手；血肉巨囊＝每回合刷新爆裂付光法力；员工溺＝我漏调 `resolve_ally_phases`）。
- **雕塑 3 只**（奇美拉：自施变形两次 → 攻次攻力归0）。
- **救赎 3 只**（肠水母×2 接纳、眼树×1 接纳＋×1 终结）。
- 真正"打到命零"的只有 4 只（奇美拉×2、人头气球×2）。
- 根因分三层（可复算），**第三层是本局的主因，也是本仓当前的接线缺口**：
  1. 怪物每回合必须发动一种道纹（有候选时道纹提交是强制的，`engine/combat.py:4353/4508`）→ 它没有"只普攻"这个选项；
  2. 道纹代价与攻力同源（法力＝攻力），而怪物出厂自带遗物【某人的偏爱】每[回始]法力回满
     （`AI_EXPERIENCE.md:278/698/784/1385`、`物品索引.md:261`）→ 它敢一直花，但花光那一回合就打不出伤害，
     于是踩【凡庸】（连续5回合未使敌掉血）；【变形】更狠：付光法力后互换，速度归0 且持续9回合 → 攻次+攻力双0 → 【雕塑】；
  3. **X 一律拉到可负担上限**：`engine/combat.py:5549-5560` 明确「提交方没有给 X 时回退到可负担上限而非报错，
     想要更精细取值的 AI 自行提交 x 即可（见 `sim/duel_common.py`）」；而战术选 X 的
     `sim/monster_targets.py::pick_monster_daowen_x`（2026-09-16 用户令选案 C，**评分返回 None 的档位＝会把自己玩死，直接跳过**）
     **只接进了 `sim/duel_common.py`（死斗）**，`sim/build_learner.py::_resolve_monster_turn`（主怪物驱动）从不提交 `x`。
     本局手操照 build_learner 口径拼装 → 实测证据：奇美拉【必中】异变+45＝5×9、【变形】消耗9法力、
     血肉巨囊【爆裂】消耗8法力＝2×4，全是上限值。
- **因此 4.3 的"7 只自毁"只在 max-X 口径下成立**：若走战术选 X，必中9（异变45→崩解45/50）会被评分跳过，
  变形1 只付1法力（速度2↔法力8 → 攻次8×攻力1＝8伤，不是0），爆裂1 只付2法力（攻力仍6）→ 大概率不雕塑、不凡庸。
  连带影响本局经济：3 个奇美拉雕塑＝36 发×15＝450 伤，占我消耗品总伤 545 的 82%，而 b2/b6 两场 0 碎片也由此而来。

### 4.4 经济与物品

- **消耗品不占出手**（`AI_EXPERIENCE.md:580`）＋残韵不占出手（L626）→ 单回合爆发只受耐久限制：
  b7 R10 我一回合内 8 发雕塑（120）＋普攻 64＝184 伤，把 192 血的眼树②压到 8 → 触发救赎线（≤19.2）→ 我选终结。
  （上一版本文写的"9 发雕塑 135 伤打穿眼树"是错的：第 9 发被引擎以「必须先结算【救赎】」拒掉，seq502。）
- **雕塑是全场最大单件输出**：3 只奇美拉自雕塑 = 36 发 × 15 伤，我共用掉 30 发（450 伤），末场仍余。
  对照：雕塑路径 **0 碎片**（`leave_no_shards`），命零路径 19–21 碎片。
- **b7 逐怪伤害归属（trace 复算；回答"是不是直接用石头把四个怪砸死"）**：

  | 怪（ref） | 普攻 | 石头 | 合计 | 结局 |
  | --- | --- | --- | --- | --- |
  | 人头气球①（enemy:0） | 160 | 60 | 220 | 命零（普攻收尾） |
  | 人头气球②（enemy:2） | 160 | 30 | 190 | 命零（**最后一发是石头**，seq484 `target_died=true`） |
  | 眼树①（enemy:1） | 176 | — | 176 | 打到 ≤10% → 救赎·接纳（得员工「盲」） |
  | 眼树②（enemy:3） | 64 | 120 | 184 | 石头压到 8/192 → 救赎·终结（seq504，按命零给 19 碎片） |
  | 员工「盲」 | 28（打在人头气球上，日志按名字记、未细分 ref） | | | |

  b7 我方总输出 798＝普攻 560（70%）／石头 210（26%）／员工 28（4%）；
  全轮回总输出 2503＝普攻 1920／石头 450／电击枪 75／员工 38／手雷 20。
  → "用石头砸死四个怪"不准确：石头只占 b7 四分之一，但**确实是它收掉了人头气球②、并把眼树②压进救赎线**；
  四只怪没有一只是纯石头打死的（两只气球以普攻为主，两只眼树走救赎离场）。
- **碎片收支**：收入 160（20/0/20/20/41/0/59）；支出＝学习2档10＋雇佣医生10＋修行2档×3＝45＋休整3档25＋工资12＝102。
  精力每场固定回 3、**不累积** → 局外必须花光（我 21 次子行动：休整7／修行6／探索6／学习2）。
- 修行兑换实测顺手：2点=1法限（攻力＝当前法力，故 +1 法限同时 +1 招架减免/击）；1点=6血限。
  法限 4→8 后普攻从 32/回合 涨到 64/回合，招架减免 4→8（能把攻力≤8 的怪整击抹平）。

### 4.5 手操失误（AI 侧，不是规则缺陷，逐条存档防重犯）

1. **b1R1 透支3 满蓝白放**：法力受法限封顶 → 12 点流血换 0 法力还搭 1 出手。透支是回蓝工具不是扩池器。
2. **b2 雇佣医生时碎片归零** → 战终工资门槛（12）**先于**碎片奖励结算 → `pay` 被引擎拒（"碎片不足…请改为提交 refuse"）
   → 只能拒付 → 医生离队＋失信 1 击。教训：派员工前必须留出 ≥12 碎片（`sim/build_learner.py` 早有该预留逻辑）。
3. **b5R3 误判爆裂可被招架削**：爆裂写的是「攻击者**失去等量生命**」，招架只减「受到的伤害」→ 反伤绕过招架，
   我 60→3/64 险死。能挡它的是【固执】（单次失去生命最高为1）与【蒙蔽】（造成伤害无效）。
4. **b7 漏调 `resolve_ally_phases`** → 员工溺连续 5 回合未出手 → **凡庸炸裂**（护卫不占它的出手，但它仍须自己出手）。
5. **两次把消耗品/残韵打到错误的 enemy 下标**：已死怪物仍占 `enemy:0/1/2` 槽位 → 「找不到target_ref」；
   按名字走的 `prepare_attack` 不受影响，按 ref 走的 `consume_item`/`use_resonance` 会静默打空（耐久/残韵会退还）。

### 4.6 待用户裁定的问题（Q1–Q12）

按「非必要就不修改」，以下 12 条只列观测与两种可选方向，等裁定后再动；不裁定的就保持现状。
（用户 2026-09-18 反馈「Q5 以后在报告里没看到」＝上一轮把 4.8 整节插进了本表 Q4 与 Q10 之间，markdown 表被截断；现已重排为 4.6 完整表 → 4.7 → 4.8，见 5.3。）
用户已看过 Q1–Q4：Q4 的落点是 Q10＋Q11（都属怪物 AI/引擎逻辑），Q1–Q3 尚未裁定。

**2026-09-18（夜）裁定全部到位**：Q1／Q2／Q3／Q4／Q7／Q8／Q9＝**B（保持现状，不改）**；
Q5＝**护卫直接算[员工]/[朋友]的一次出手**；Q6＝**A（工资改到碎片奖励之后）**；
Q10＝**「不是自定义 X 值的错，是怪物 AI 太蠢」→ 修 AI（战术选 X 接进驱动）**；
Q11＝**不硬编码顺序，怪物 AI 自己选先攻击还是先道纹**；Q12＝**废案清理干净**。
实装面、探针读数与回归结果见第六节。

| # | 观测 | 若要改的方向A | 方向B（保持现状的理由） |
| --- | --- | --- | --- |
| Q1 | 爆裂反伤＝「失去生命」，绕过招架，只有固执/蒙蔽能挡 | 在招架正文补一句「同时削减『失去生命』类反伤」 | 现状即刻意区分"伤害/失去生命"两条轴，固执正是它的答案 |
| Q2 | 雕塑路径 0 碎片但给 12发×15伤，明显优于命零的 19–21 碎片 → 玩家会主动养雕塑 | 雕塑耐久或伤害下调（如 12→6 发） | 雕塑是"放弃经济换即时战力"的取舍，末场我确实用它打赢了四怪局 |
| Q3 | 消耗品不占出手 → 单回合可连发 9 发雕塑，爆发无上限 | 加"每回合消耗品次数上限" | 耐久本身就是预算；加上限要动 `consume_item` 主路径与全部测试 |
| Q4 | 怪物裁定 AI＋道纹代价 → 12 只怪 7 只自毁（3 凡庸/3 雕塑/3 救赎） | ~~给怪物 AI 加"不自废"约束~~ **已查明不是优先级表的问题**：主因是 X 一律开到可负担上限（见 Q10），次因是怪物阶段硬编码「道纹先于攻击」（见 Q11） | 自毁线本身是玩家可经营的策略面（我这局正是靠它过关） |
| Q5 | 纯护卫型员工必被凡庸炸裂（护卫不占出手，5 回合未出手即死） | 护卫视为出手／待命与护卫中的员工不计凡庸 | 也可视为"员工必须参战"的既定口径，只是需要写进正文提示 |
| Q6 | 工资门槛先于碎片奖励 → 碎片不足时雇佣＝必然拒付＋失信 | 工资改到碎片奖励之后结算，或允许欠薪 | 现状是刻意的经济闸门（`WAGE_CAP=12`＋失信三振），改序会动 `battle_end` 主流程 |
| Q7 | 三相残韵盘在**最后一场**是净亏损（战始消耗、战终兑现） | 末场自动跳过／正文补一句提示 | 玩家可自行 `use=false`（我 b7 就这么做），属可学习成本 |
| Q8 | 事件「真拒绝」送随机残韵 → 叙事型选项（写信/接电话/流血换答案）永远劣选，我 3 次全拒 | 给叙事选项一点机械价值（如信件＝下一轮回开局提示） | 真拒绝奖励是既定裁定（L494），叙事选项本就定位为角色扮演成本 |
| Q9 | 坏死（6回合无法获得回复）＋寄生（我受伤80%转成它回复）叠加 → 自愈/血泵/休整全废，b5 打到 1/64 | 给"无法获得回复"设一个下限或豁免（类比第一杯） | 两件都是水母的既定道纹，叠加本就应当致命；我靠招架+束缚+凡庸线活下来了 |
| Q10 | **战术选 X 只接进死斗驱动**：`pick_monster_daowen_x` 未在 `sim/build_learner.py::_resolve_monster_turn` 使用 → 引擎缺 x 回退上限 → 脚本轮回与手操里怪物一律开满 X 自废/自爆（本局 3 雕塑＋2 凡庸＋崩解45/50 均由此） | 把 `pick_monster_daowen_x` 接进 build_learner（照 `duel_common.py:97-103` 的写法，仅 `x_free` 时提交 x） | 引擎回退上限是 2026-09-16 面板去 X 时刻意的"不跑坏"兜底；但代价是**所有脚本轮回的平衡数据都偏向怪物自毁**，本局即样本 |
| Q11 | **怪物阶段硬编码「道纹先于攻击」**：`_execute_monster_phase` 先结算道纹、再跑攻击循环；而轮回者的两次出手顺序是自由的（`prepare_attack`/`use_daowen` 各自独立）。《怪物准则》第8/11条只规定「2 次出手＝1 攻击＋1 道纹」与预算预留，**没有规定先后**。后果＝怪物为道纹付光法力的那一回合普攻必然 0 伤（攻力＝当前法力）→ 血肉巨囊连刷爆裂 5 回合直接撞【凡庸】 | 把攻击循环挪到道纹结算之前（怪物先打再花）：单回合法力型道纹（爆裂/超频/急速/坏死…）不再自废，本局 2 条凡庸线里至少 1 条消失 | 代价是【必中】的"下X次选择目标无法闪避"从**下一回合**才生效（现在同回合普攻就吃得到）；【变形】仍锁死后续 9 回合（速度0→攻次0），只是把【雕塑】换成【凡庸】。改序要动 `_execute_monster_phase` 主流程＋所有按 prepare 快照提交的驱动（命中数/出手数契约），影响面比 Q10 大 |
| Q12 | `全道纹索引.md:164`（生成器 `sim/gen_daowen_index.py` 的 note_lines）仍写「怪物道纹递增：每实际发动一次该道纹本场 X 累加+2×副本阶级」，而递增机制 2026-09-16 已废止（现行《怪物准则》第9条＝面板不写 X、发动方自选）；`sim/prompt_slim_apply.py` 的旧提示词 blob 同样残留（含已删的局外【领悟】、净化5X 等旧口径） | 删掉生成器里这条 note 并重生成索引（`tests/test_daowen_cost_consistency.py` 守卫逐字节一致）；`prompt_slim_apply.py` 是否一并清理另议 | 视为历史档案不动——但索引是**注入 AI 提示词的派生文档**，读到它的 AI 会按递增预测 X（本轮我只改了家族税那两句，递增这句按「非必要不修改」留着等裁定） |

### 4.7 顺带查实的现行事实（存档）

- 凡庸/崩解/雕塑/癌变四条自毁线里，**凡庸与命零给碎片，雕塑与救赎（接纳）走 `leave_no_shards` 不给碎片**；
  癌变怪不给碎片但永久 +8 休整恢复量（本局未触发，`死者之书.md` 无新增）。
- 救赎触发点＝打到 **≤10% 血限**且该怪无原始道纹；`option=1/接纳` 得到的是**半血员工**
  （肠水母 204→102、眼树 192→96，攻力也减半：12→6、13→7），`option=2/终结` 走正常命零管线给碎片（眼树 19）。
- 员工战终成长：盲 存活 1 场后 `ally_growth: 攻击次数2`（1→2）。
- 招架实测：减免＝结算时当前法力，对"受到的伤害"生效（b7 法限8 时把攻力8 的怪整击抹平）；
  对"失去生命"类（爆裂反伤、透支流血）不生效。
- 怪物为发动道纹付法力＝自削攻力，这是本局多只怪输出归零的直接原因（超频10/急速10/爆裂8/变形9/增殖8/坏死12/定型2X）。

### 4.8 探针：把战术选 X 接上，能救回哪几条自毁线（机制验证，非轮回数据）

造场法照 `tests/test_action_budget.py`（`battle_start` 后替换 `state.enemies` 为池内真怪），
怪定义取自 `parse_monster_pool("副本索引.md")["扭曲都市"]`，对比「引擎缺 x 回退的可负担上限」与
`pick_monster_daowen_x` 的战术取值（探针库 `data/test_probe_xpick*.db`，已被 `.gitignore` 的 `data/test_*` 排除）：

| 怪·道纹 | 缺 x 回退 | 战术选 X | 后果差异 |
| --- | --- | --- | --- |
| 奇美拉·必中 | 9（异变+45 → 崩解45/50） | **1**（异变+5 → 崩解5/50） | **接上就不会把自己推到崩解线** ✓ |
| 奇美拉·变形 | 9（消耗9法力 → 攻次0） | 9（同上） | **接上也救不回来** ✗ 见下 |
| 奇美拉·飞行 | 9（异变+45） | 9（同上） | 单步预演看不见"下一次异变就撞线" ✗ |

- 变形仍取上限的原因（读码结论，未再跑第二次探针）：`pick_monster_daowen_x` 对每个候选档位真实预演，
  **预演失败即 `continue` 跳过，三档全废则回退上限**（`sim/monster_targets.py:169-176`＋`best=max_x` 初值）。
  而各驱动拼命中数用的是 `hits_n = 怪的 attack_power`（`sim/build_learner.py:393-396`），这个覆盖只对上
  变形 X=上限 的互换结果（法力9 → 攻次9）；X=1 时互换后攻次是 8，预演提交被「每个攻击出手必须提交N次命中选择」拒
  → 低档位全被跳过 → 回退 9。**即：变形的战术选 X 事实上不可达，死斗路径同样中招。**
- 飞行取 9 属评分视野问题：`pick_monster_daowen_x` 是**单步预演**（其 docstring 自陈），
  看不见"这一手 45 异变之后，下一手任何异变道纹都会撞崩解线 50"。
- 结论：接线（Q10 方向A）能消掉**异变型自爆**（必中/飞行 的 5X 是它们**自身**的正文代价，不是家族税——
  家族税只额外压在【自愈】身上，已于 5.1 删除），但消不掉**法力型自废**
  （变形/爆裂 这类把攻力/攻次付空的）——后者要么按 X 逐档重算命中数（改 3 处驱动的拼装口径），
  要么给怪物 AI 增一条"发动后攻力或攻次将归 0 则改选/降档"的护栏（属新规则，需裁定）。

## 五、2026-09-18（晚）已实装的两条更正

### 5.1 删除怪物「家族税」（用户令：「删掉怪物莫名其妙的家族税」）

**被删的旧口径**：`combat.py::_resolve_monster_daowen_choice` 里两个硬编码支付点——
「原始怪物道纹每次发动付异变5X」与「怪物侧【封印】付异变X」，与道纹自身代价**并存**；
同时 `apply_daowen_effect` 的 `cost_mutation` 分支带 `caster.entity_type != "怪物"` 限定，
把怪物排除在统一代价总线外（防双付）。净效果＝【自愈】这类自身代价为【冷却X】的道纹，
在怪物侧被额外扣一份异变5X（报告 D5.3 记的"怪物侧代价是双份"）。

**现行口径**：怪物发动道纹只按该道纹**自身 calc 声明的代价**支付，异变代价统一走代价总线
（`combat.py:3426`），怪物与轮回者/[朋友]/[员工]同口径（《怪物准则》第5条「发动代价类道纹时必须照常支付对应代价」）。

| 道纹 | 删除前（怪物侧） | 删除后（怪物侧） |
| --- | --- | --- |
| 自愈X | 冷却X ＋ 异变5X（双份） | **只付冷却X，异变 0 层** ← 唯一实质变化 |
| 狂暴/全力/疯狂/减速/必中/飞行 | 异变5X（硬编码点付，总线跳过怪物） | 异变5X（改由总线付）——**数额与时机都不变** |
| 封印X | 异变X（硬编码点付） | 异变X（总线付）——不变 |
| 原初X 借用门票 | 异变5X（`execute_evolution` 自结算） | 异变5X——**不属家族税，照旧** |

**连带保住的两条口径**（不是新增规则，是把旧行为搬到新结算点）：
1. 付异变达 50 层仍【崩解】命零且**道纹效果中断**——中断点从硬编码支付点移到总线的
   `interrupted_by_cost` 守卫（`combat.py:3450-3457`），只认「异变」这一种代价（流血/衰老致死照旧结算完效果）；
   这条中断现在对**轮回者/[朋友]/[员工]同样成立**（此前只有怪物侧的硬编码支付点会中断，玩家付异变崩解后效果照样落地）；
2. 怪物阶段返回值仍带 `{"collapsed": 道纹名}`（新增 `_monster_collapse_marker`，`combat.py:5539`），
   死者照旧进统一死亡管线（`_apply_numeric_cost_part` → `_on_entity_death`，焦黑发丝/分裂/死者之书都不掉）；
   攻击出手仍被中断（`_execute_monster_phase` 的 `if not monster.is_alive: continue`）。

**必须澄清的一点（回答"删了税怪物是不是就不自爆了"）**：**不是**。
4.3/4.8 的异变型自爆（奇美拉【必中】X=9 → 异变+45 → 崩解45/50）来自必中**自身**的正文代价「异变5X」，
不是家族税；家族税只额外压在【自愈】上。所以删税救回的是自愈承载怪（不再每发动一次叠 5X），
必中/飞行 的自爆仍要靠 Q10（战术选 X 接线）或 Q11（改出手顺序）处理。

**X 上限探测**：`_monster_max_daowen_x`（`combat.py:5501`）去掉家族税特例，改按 calc 声明的异变量封顶，
取值口径与总线一致（总线优先级 流血＞衰老＞疲惫＞异变，故只有前三种都不存在时才真的付异变）。
【自愈】的可用 X 不变：紧约束一直是 `X_LIMITS["冷却"]=7`，家族税算出的 9 从来没生效过。

**改动面（全部已跑回归）**：
- `engine/combat.py`：代价总线分支、中断守卫、删两个硬编码支付点、`_monster_collapse_marker`、X 探测、`YUANCHU_COST_RATE` 注释
- `engine/models.py`：`MUTATION_COLLAPSE_THRESHOLD` 注释（崩解只由道纹/消耗品自身异变代价产生）
- `sim/balance_sim.py`：镜像只对「自身代价为异变5X 的六条」计异变（自愈不再计）＋三处注释
- 正文 `AI_EXPERIENCE.md`：《原始怪物道纹与转化道纹》自愈段、L1057《关键机制口径》各改一句；
  《已删内容》表加两行（家族税、滋养加成休整）防写回
- `全道纹索引.md`：改生成器 `sim/gen_daowen_index.py` 两句后重生成（`tests/test_daowen_cost_consistency.py` 守卫逐字节一致，diff 仅 2 行）
- `sim/kb_regression.py`：C2-03 探针的 gold 锚点改指现行文本（旧锚点"仅首次发动支付异变5X"已不可达）
- 测试改新口径：`tests/test_daowen_wiring.py`（自愈怪物 5→0 层，函数名同改）、
  `tests/test_engine.py`（借用自愈 20→10 层；"持续怪"块改断言 0 层＋冷却2场；"临界怪"崩解用例改用减速2，因自愈已不产生异变）
- ~~**未动**：`sim/prompt_slim_apply.py` 的历史提示词 blob、`sim/prompt_audit.py:205` 的历史审计条目~~ → **2026-09-18 夜用户裁定「整个文件删掉」，两文件已删**（见 6.6）；本行留档只为说明「当时曾判定不动、后被裁定推翻」，不要再去找这两个文件。
- 新增验证探针 `sim/probe_family_tax_removal.py`（**机制验证，非轮回数据**，铁律6），四条读数：
  ① prepare 可负担 X 上限＝必中/减速/飞行 9（异变45）、变形 9（法限9）、自愈 **7**
  （`X_LIMITS["冷却"]=7` 才是紧约束，家族税算出的 9 从来没生效过）；
  ② 发动自愈 → 异变 **0** 层、冷却 7 场、已损生命 175% 回复照常；
  ③ 发动必中9 → 异变仍 **45** 层（崩解 45/50）＝删税救不回异变型自爆；
  ④ 40 层上发动面板写死 X=2 的减速 → 50 层命零、玩家 60/60 无伤、返回值仍带 `collapsed: 减速`。
- 全量回归：`9 failed / 1697 passed / 3 xfailed`，与第二节基线**逐条一致**（9 条全是既有 AI/mock 类）。

### 5.2 更正「滋养加成局外休整」——用户指出得对，是文字错，不是机制错

用户原话：「滋养作为战斗中的 buff，战斗结束就清除，怎么可能加成休整这个局外行动？」→ **查实为真**。

证据链（三段都可复算）：
1. 滋养只带 `duration` → 走 combat 通用状态块挂 `StatusEffect`，`scope` 取默认值 `BATTLE`（`engine/models.py:158`）；
2. [战终]清场只保留 `COST/RUN/PERMANENT` 三种 scope，其余状态一律清除（`engine/api.py:5292-5313`，过滤在 5313 行）；
3. 【休整】是**战前**行动（`engine/api.py:1408` 注册在 pre_battle 表、实现 `_pre_battle_xiuzheng` 在 1437 行，
   ctx `timing="pre_battle_action"`）。
→ 滋养状态与【休整】永不同时在场，`apply_heal` 里那句 `entity.has_status("滋养")` 在休整时恒为 False。

引擎行为一直是对的；错的只是**文字**，共三处，已改：
`AI_EXPERIENCE.md:395`（"覆盖一切来源（…休整…）" → "覆盖战斗内的一切来源（道纹／消耗品／寄生…）"＋一句它不加成局外行动）、
`engine/daowen.py::calculate_ziyang` docstring、`engine/models.py::apply_heal` 注释。
《已删内容》表加一行防写回。**未改任何数值或机制。**

### 5.3 报告排版修复（用户"Q5 以后没看到"的原因）

上一轮把 4.8 探针整节插进了 4.6 的 Q 表**中间**（Q4 行与 Q10 行之间），markdown 表被截断，
Q5–Q10 六行渲染成孤立文本 → 用户看不到。现已重排为 **4.6 完整 Q1–Q12 表 → 4.7 现行事实 → 4.8 探针**，
并新增 Q11（怪物阶段「道纹先于攻击」）、Q12（`全道纹索引.md` 的"怪物道纹递增"陈述已废止）。
4.4 同时更正了一处数字错误（b7 R10 是 8 发石头 120 伤＋普攻 64，不是 9 发 135；第 9 发被救赎门禁拒掉）。

## 六、2026-09-18（夜）用户裁定 Q1–Q12 的落点与实装

用户逐条裁定（原话要点）：「Q1234 都 B」「Q5 直接把护卫也算员工/朋友的一次出手」「Q6 A」
「Q789 都 B」「Q10 不是自定义 X 值的错，是怪物 AI 太蠢」「Q11 不要硬编码，怪物 AI 自己选
先攻击还是先道纹」「Q12 废案清理干净」；另附一条独立指令：**【必中】代价 异变5X → 异变X**。

### 6.1 【必中】降价：异变5X → 异变X（用户的循环论证成立）

用户给出的循环：怪物用当前速度逐次闪避轮回者的攻击（速度掉到 0）→ [回始]法力被出厂遗物
【某人的偏爱】回满 → 对自己发动【变形】把法力换回速度 → 下个[回始]法力又满
（1/10/8 → 1/0/8 → 变形 → 1/8/0 → 1/8/8；攻次＝当前速度、攻力＝当前法力，于是下一回合
8 击 × 8 伤）。硬解只有【必中】，而 5X 的代价把这条硬解压在崩解线上（X=9 即异变45/50，
再动一次就自爆）→ 裁定：**降到 异变X**，让反闪避工具付得起。

改动面（全部已跑回归）：
- `engine/daowen.py::calculate_bizhong`：`cost_mutation=x`（docstring 首行／`summary`／理由注释同改；
  正文红线要求 docstring 首行＝规范文本，索引与一致性测试都解析它）
- 正文 `AI_EXPERIENCE.md`：《原始怪物道纹与转化道纹》必中段、《关键机制口径》各一句
- 生成器 `sim/gen_daowen_index.py` 的原始段 note → 重生成 `全道纹索引.md`（总览表／原始段／必中条目）
- `engine/combat.py:46` 代价注释；`sim/balance_sim.py` 镜像不再写死倍率，改读引擎
  `DaoWenEngine.resolve(...)["cost_mutation"]`（模块级 `register_all()`）
- 测试改新口径：`tests/test_engine.py`（必中3：异变 15→3）、
  `tests/test_xijie_and_bizhong.py`（两处 fixture `cost_formula="5X"→"X"`）

**降价后的可负担上限（探针 `sim/probe_family_tax_removal.py` 实跑，机制验证非轮回数据）**：
必中 `max_x` 从 9（异变45，崩解线封顶）变成 **30**；30 不是崩解线算出来的，是
`CombatEngine._DAOWEN_X_PROBE_CAP = 30` 的试探封顶（崩解线闸要 X≥50 才咬得住 1X 代价）。
必中9 现在只叠 **9 层异变**（原 45 层），同阶段两击各吃一层 → 余数 7。
⚠ **副作用（用户上一轮问的「1X 的上限后果」，实测答案）**：上限是 30 而非 49，
且**没有加任何新上限**（按「非必要不修改」，封顶交给战术选 X，不由引擎写死）；
但引擎缺 X 时的回退是「可负担上限」（`combat.py:5602 if missing: effective_x = max_x`），
所以不选 X 的驱动一发必中就是异变 30/50 —— 这正是 6.4（Q10）必须同时接线的原因。

### 6.2 Q5：护卫算[朋友]/[员工]当回合的一次出手

- 机制点：`engine/combat.py::_tick_mediocrity_counters`（`round_end` 对全体存活双方逐个 tick）。
  新增前置分支：`entity_type in ("朋友","员工") 且 _beifu_left > 0` → `no_action_rounds = 0`
  并**直接返回**（`no_damage_rounds` 冻结不推进）。两条计数都要处理：只清「未出手」救不下纯护卫，
  因为「未使敌掉血」那条同样会在第 5 回合炸裂。**冻结不清零**——护卫层数用尽后从原值接着累计。
- 正文：《特殊事件》凡庸条加一句（护卫中的[朋友]/[员工]视为当回合已出手…）；
  《行动接口口径》`command_ally` 条把「不占盟友出手」改为「护卫**算**盟友当回合的一次出手」。
- 注释同步：`engine/api.py:3920`（施加背负处）、`sim/guard_cmd_full_run.py` 两处旧口径文案。
- 回归钉：`tests/test_guard_command.py::test_guard_counts_as_ally_action_for_mediocrity`
  （真实跑 6 个回合：每回合续护卫＋轮回者出手＋怪物阶段，纯护卫的岩行者全程存活、两条计数恒 0；
  层数用尽后 tick 一次 → 计数变 (1,1)）、`::test_guard_mediocrity_exempt_only_for_friends_and_employees`
  （怪物背着【背负】不享受豁免）。
- 手操战报里的**失误④**（b7 漏调 `resolve_ally_phases` 导致员工溺被凡庸炸裂）在纯护卫场景下不再成立；
  但「员工仍须自己出手才有输出」这条战术常识不变（护卫只免死，不代打）。

### 6.3 Q6＝A：战终先入账击杀[碎片]，再问工资

- 旧序：逃跑处理 → `_compute_pending_wages` → **工资待决 gate（早返回 `completed:False`）** →
  遗物/法术【战终】→ 碎片结算 → `shards += shard_reward + bonus_total`。
  后果：刚打完这场、账上碎片不够时，雇佣＝必然拒付＋失信一击（明明这场怪的奖励够付）。
- 新序：碎片奖励**在 gate 之前**算好并入账，其余顺序一字不动（遗物/法术仍必须在统一清理之前）。
- 重入安全：gate 早返回意味着 `battle_end` 会被调用两次，所以
  ① 碎片循环抽成 `_battle_end_shard_preview()`（纯读取：按 `[战始][血限]` 快照与道纹数算，
  结果按引擎实例缓存，第二次调用不重复构建 ctx）；
  ② 入账用一次性标记 `event_modifiers["battle_end_shards_credited"]` 守住，战终完成时 pop；
  ③ `battle_start` 处兜底清除标记与缓存（防存档读档/战终中断后跨场残留）；
  ④ 原 `shards += shard_reward + bonus_total` 改成只加 `bonus_total`（副本彩头），禁止二次加总。
- 正文：《行动接口口径》`battle_end` 条重排为「击杀[碎片]先入账 → 工资门槛 → 遗物/法术【战终】→
  统一清理 → 副本彩头」，并写明「刚打完这场的击杀碎片已经到账，可以直接用来付工资」。
- 回归钉：`tests/test_employee_economy.py::test_kill_shards_are_credited_before_wage_gate`
  （碎片 0 开局、3 回合工资 8、击杀奖励 12：第一次 `battle_end` 返回 `completed=False` 时
  碎片已＝12 → `pay` 成功 → 余 4；第二次 `battle_end` 完成且仍是 4，标记已清）、
  `::test_shard_credit_flag_is_reset_at_battle_start`。
  既有 15 条员工经济用例全绿（含「碎片不足时 pay 必须被拒绝」——该用例无击杀奖励，不受影响）。

### 6.4 Q10：不是自定义 X 值的错，是怪物 AI 太蠢 → 战术选 X 接进脚本轮回驱动

- 病灶：`sim/build_learner.py::_resolve_monster_turn` 拼 `daowen` 时**从不提交 `x`** →
  引擎回退到可负担上限（`combat.py:5602`）→ 脚本轮回与手操里的怪物一律开满 X
  （本局 3 雕塑＋2 凡庸＋崩解45/50 均由此）。`pick_monster_daowen_x` 此前只接进死斗驱动
  （`sim/duel_common.py:97-103`）。
- 修法：照死斗的写法接进 build_learner，但**必须在整份 `choices` 拼好之后**评分——
  PVE 怪物阶段要求全体 actor 一起提交（`combat.py:6002 set(submitted) != set(expected)` 即拒），
  只交一个会被拒。为此给 `sim/monster_targets.py::pick_monster_daowen_x` 加了可选参数
  `all_choices`（传入完整列表时按整份提交预演、只替换本 actor 的 X；缺省 None＝死斗单 actor
  部分提交，旧调用方零改动）。逐个 actor 原地打补丁，后面的 actor 评分时看到的是前面已定档的真实 X。
- 实测（同一驱动、同一怪，机制验证非轮回数据）：
  | 怪 | prepare 上限 | 接线后实发 | 异变 |
  | --- | --- | --- | --- |
  | 必中 | 30 | **15**（预演评分选中档） | 15/50 |
  | 必中＋减速 | 30 / 9 | 选减速 **X=1** | 5/50 |
  | 变形 | 7（法限） | 按评分 | 0（付法力） |
  | 自愈 | 7（冷却闸） | **X=1** | 0，冷却 1 场 |
  接线前这四种一律开满（必中 30、减速 9＝异变45 当场崩解、自愈 7）。
- 说明：必中 X=15 是 `TacticalAI._score_candidate` 从怪物视角评出来的（候选档＝1／中档／上限），
  与死斗侧同一口径同一函数，本轮**没有调评分**；若嫌 15 层太豪，属另议（改候选档位或评分权重）。

### 6.5 Q11：不硬编码顺序，怪物 AI 自选先攻击还是先道纹（`attack_first`）

- 引擎：`_execute_monster_phase` 的每个 actor 拆成 `_daowen_step()`／`_attack_step()` 两个闭包，
  按提交的 `attack_first` 排序（`true`＝先普攻后道纹；缺省 `false`＝现行「道纹先」，老提交零改动照样合法）。
  道纹步里怪物命零 → 返回 False，后续步骤不再结算；先攻击时怪物在自己的攻击里命零 → 道纹不结算。
- 契约不变量：**出手数与命中数一律按 prepare 快照校验**（`base_attack_actions`／`base_hits_per_attack`），
  与先后顺序无关——这条本来就是 2026-09-17 的口径，改序不需要动它。
- 面板公示：`prepare_monster_phase` 每个 actor 新增 `attack_first_allowed: True` 与 `order_note`
  （「attack_first=true 先普攻再发动道纹；缺省 false=先发动道纹再普攻」），发动方不必猜。
- 静态校验：`_validate_monster_phase_static` 里 `attack_first` 非布尔即拒（零副作用，在龙息/道纹/攻击之前）。
- 正文：《怪物准则》新增第 12 条（顺序由发动方决定＋两种顺序的战术含义＋快照契约与先后无关）；
  《行动接口口径》末尾的指路句改为「第 11~12 条」。
- 回归钉：新文件 `tests/test_monster_phase_order.py`（5 条）——
  缺省路径【必中】压掉首击闪避（`dodge_fail_reason=必中攻击无法闪避`、伤害照吃）、
  `attack_first=true` 时同样提交闪避全部成功且伤害 0、层数留到下回合（跨回合用例：
  存的 1 层＋新攒 1 层 → 下回合前两击都必中）、面板公示、非布尔拒绝且零副作用。

### 6.6 Q12：废案清理（已废止的「怪物道纹递增」＋陈旧锚点）

- 生成器 `sim/gen_daowen_index.py`：删掉原始段的递增 note、通用规则指路句里的「怪物道纹递增」、
  区域专属段的「（道纹递增+N/次）」；改为现行口径（面板不写X、发动方自选；阶级只用于死斗分槽）。
  重生成 `全道纹索引.md`（含 6.1 的必中降价，共 9 行 diff），`tests/test_daowen_cost_consistency.py` 9 条全绿。
- `engine/gamedata.py:16` 注释：`REGION_TIERS` 的现行用途是死斗阶级分槽/进阶封存
  （`api.py:4639`、`api.py:4761`），不再写「递增机制用」。
- `sim/kb_regression.py`：旧 C2-04（gold＝「累加+2×副本阶级」）已不可达 → 删除，
  改立 stale 探针 **S5-07**（递增文本不得以当前规则语境出现，gold＝「面板不写X」「发动方自选」）；
  C3-07 净化的 gold 锚点从「消耗5X」改指现行「净化X：消耗2X」。
  手工核查（本沙箱 `git tag` 为空、跑不了 pre-slim 对照，见第二节）：`AI_EXPERIENCE.md`／`README.md`／
  `全道纹索引.md`／`副本索引.md`／`物品索引.md`／`法术索引.md`／`死者之书.md`／`data/build_knowledge.json`
  里已无递增陈述；只有本报告的 Q12 行与 5.x 存档行引用旧文本，且同行都带「已废止」否定语境
  （`_stale_active_lines` 的 NEG 白名单会放过，不算污染）。
- 探针 `sim/probe_family_tax_removal.py`：按必中降价刷新文案与读数，并新增第 5 段
  （缺省回退开满 X=30 → 异变30/50），把 Q10 的必要性钉在同一份探针里。
- **两个一次性工具已整文件删除（用户裁定 2026-09-18 夜：「整个文件删掉」）**：
  `sim/prompt_slim_apply.py`（2026-08-24 提示词瘦身执行器，含递增／局外【领悟】／净化5X／
  原始纹仅首次付异变5X 等旧口径 blob，有 NOOP 幂等门、永不再执行）与
  `sim/prompt_audit.py`（同期审计盘点器，条目里含「原始怪物纹仅首次支付异变5X」这类已废陈述）。
  产出物照常留档：`data/experiments/prompt_audit_2026-08-24.json`、`audit/prompt_slim_audit_2026-08-24.md`
  （该 md 里「生成器 `sim/prompt_audit.py` 可复跑」一句从此失效，作为 dated 记录不改写）。
  删除前已核查：无任何测试或生产代码引用这两个文件。

### 6.7 Q1／Q2／Q3／Q4／Q7／Q8／Q9＝B（保持现状，一行存档防重提）

| # | 结论（B＝不改） | 一句话理由（用户口径） |
| --- | --- | --- |
| Q1 | 爆裂反伤仍绕过招架 | 「伤害／失去生命」两条轴的区分是刻意的，固执/蒙蔽就是它的答案 |
| Q2 | 雕塑 0 碎片换 12发×15伤 不动 | 放弃经济换即时战力的取舍，末场确实靠它赢了四怪局 |
| Q3 | 消耗品不加每回合次数上限 | 耐久本身就是预算；加上限要动 `consume_item` 主路径与全部测试 |
| Q4 | 不给怪物 AI 加「不自废」约束 | 落点是 Q10＋Q11（已按裁定修 AI，不改规则） |
| Q7 | 三相残韵盘末场净亏损不自动跳过 | 玩家可自行 `use=false`（b7 就这么做），属可学习成本 |
| Q8 | 叙事型选项不给机械价值 | 真拒绝奖励是既定裁定，叙事选项定位为角色扮演成本 |
| Q9 | 坏死＋寄生叠加不设下限/豁免 | 两件都是水母的既定道纹，叠加本就应当致命 |

### 6.8 顺带查实的两处陈旧覆盖（已修）＋一处只存档不动

- **已修**：`sim/build_learner.py` 与 `tests/monster_phase_support.py` 都有一段
  「选了【变形】就把命中数改成 `attack_power`（＝当前法力）」的旧覆盖。命中数契约自 2026-09-17 起
  是 prepare 快照，实测（速限3/法力12 的骨天使自施变形）：按快照提交 3 击**成功**，
  按旧覆盖提交 12 击被引擎拒（`骨天使每个攻击出手必须提交3次命中选择`）——
  法力≠速度的变形怪每次都白烧一次重试额度（`max_attempts = len(actors)+2`），
  还会让第一遍统计的 `hold_counts`（反应法术「末击声明」）与实际提交数错位。两处旧覆盖已删。
- **已修（用户裁定 2026-09-18 夜：只改这句 docstring，不动行为）**：
  `engine/combat.py::_validate_monster_phase_static` 的 docstring 原写「hits 命中数…必须按执行时的
  真实状态校验（如【变形】会改变命中数）」，与实现（`hits_per_action = expected[actor_ref]
  ["base_hits_per_attack"]`，快照）相反——正是它把 sim 驱动带偏成上面那条陈旧覆盖。
  现改为：出手数与命中数一律按 prepare 快照校验、阶段内真实改速度也不动契约；
  真正按执行时状态校验的是目标当前速度／存活性／血影所需生命／本回合已发动集合。

### 6.9 接线后暴露的引擎层缺陷：预演污染 id-keyed 账本（已修；不是规则改动）

Q10 把战术选 X 接进 `build_learner` 后，`tests/test_build_learner.py::test_fixed_seed_is_reproducible`
开始失败（同一 seed＋同一决策 rng，两次 `play()` 结果不同）。定位链（可复算）：

1. `pick_monster_daowen_x` 每只怪每回合做 3 档预演（1／中档／上限），走 `engine/ai_preview.py::preview_sequence`；
2. `preview_sequence` 是**副本执行**：deepcopy `state`/`dice` 后在副本上真实跑动作，但**复用同一个
   `CombatEngine` 对象**——`_monster_daowen_round_used`／`_monster_activated`／`_resonance_rewrites`／
   `_monster_evolved` 这些按 `id(entity)` 建索引的账本仍是真实账本；
3. 副本里的怪是新对象、id 与真实怪不同，预演于是把「本回合已发动逆鳞」写进真实账本的**垃圾键**；
   副本被回收后地址复用，后来的真实怪／新副本继承这条记录 →
   `_validate_monster_daowen_schema` 报「断角羊不能发动道纹【逆鳞】」（它本回合并没发动过）；
4. 是否命中取决于内存分配顺序 → 同一进程内第一次跑与第二次跑不一致（跨进程单跑反而稳定）。
   实测证据：两次跑各 918 次预演，第 645 次（逆鳞 X=30 档）run1 通过、run2 被假拒绝，
   而预演前的可见状态（回合/异变/冷却/本回合已用集合）逐项相同；单跑 X 恒为 1 时两次跑完全一致。

修法（`engine/ai_preview.py::preview_sequence`）：进副本前把 7 个 combat 侧账本
（上述 4 个 ＋ `_sanxiang_consumed`／`_split_clones_spawned`／`_effect_chain_depth`）deepcopy 存好、
给副本一份独立拷贝，`finally` 里原样归还——预演对这些账本的写入一律作废。
账本值会被**原地改写**（`_monster_round_used(monster).add(name)`、`_monster_activated.setdefault(id, set())`），
所以必须 deepcopy 而非浅拷贝。修完该用例立即回绿。

旁证：`sim/win_only_ai.py:71-98` 早就手工存/还这批账本（同一坑的局部绕行，现成冗余但不动）；
`ActionPreview.snapshot()/restore()`（旧回滚式预演）也存了它们，只有副本执行这条路漏了。

**未动（存档等裁定）**：副本里按 id 查 `_resonance_rewrites` 恒 miss（副本实体 id ≠ 真实实体 id），
所以预演**看不到残韵改写**（已被改写成别名的道纹，预演仍按原名评估）。这是既有的保真度缺口，
与本轮裁定无关；修它要给账本做 id 重映射，按「非必要不修改」先记在这里。

### 6.10 全量回归

- 定向：`tests/test_guard_command.py`(18)、`tests/test_monster_phase_order.py`(5)、
  `tests/test_employee_economy.py`(17)、`tests/test_daowen_cost_consistency.py`(9)、
  怪物阶段/预算/代价/闪避/波及/死斗等 9 文件(77＋1 既有失败)全绿。
- 全量：`9 failed / 1706 passed / 3 xfailed`（248s）。9 条与第二节基线**逐条同名**
  （全是既有 AI/mock 类，含 `test_build_learner::test_valid_and_invalid_are_separated`）；
  passed 由 1697 → **1706** ＝ 本轮新增 9 条（`test_monster_phase_order.py` 5、
  `test_guard_command.py` 2、`test_employee_economy.py` 2），无新回归。
- 中途出现过一次真回归：`test_build_learner::test_fixed_seed_is_reproducible`（Q10 接线暴露的
  预演污染 id-keyed 账本，见 6.9），修 `engine/ai_preview.py` 后回绿，全量重跑即为上面这行。
- 文档守卫单跑：`test_ai_experience_policy`／`test_ai_policy`／`test_daowen_cost_consistency`／
  `test_f7_doc_consistency`／`test_rulings_2026_09_16`／`test_ally_commands`／`test_resonance_network`／
  `test_ziyu_ziyang_rework`／`test_ally_daowen_x_free` 共 99 条全绿（正文《已删内容》表新增 5 行后复跑）。

### 6.11 工具沉淀（A+B，用户在本轮末尾选定的路线）

第六节末尾我承认「本轮慢，我这一侧占约三分之一」：一是**诊断靠反复重跑猜层**，二是**改一条规则也要等全量 4–5 分钟**。用户选「先沉淀工具，不碰规则」，两件都已建成、跑通，并各做过一次**反向对照**（故意把问题放回去，确认工具真能抓到，不是空转）。

**A `sim/diag_repro.py`（复现性诊断）** —— 针对 6.9 那类「同 seed 两次跑结果不同」的问题：同一 seed 连跑 N 局（默认 2），只挂钩子、不改引擎、跑完自动还原，逐层比对并打印**最先分歧的那一层**＋首次分歧上下文：

| 层 | 钩子 | 暴露什么 |
| --- | --- | --- |
| L1 | `play()` 结果 | 通关/死亡/回合数（末层，最粗） |
| L2 | `execute_action` | 每次行动的 `kind/x/caster`＋返回的 `success/survivors/hp` |
| L3 | `pick_monster_daowen_x` | 战术选 X 的**入参**（快照/上限/预算）与出参——X 值分歧的源头 |
| L4 | `ActionPreview.preview` | 预演返回的 `ok/expected_success/x/daowen_name/survivors/hp_before→hp_after/cost_snapshot` |
| L5 | `_score_candidate` | 候选评分 `score/expected_value/collapse_risk` |
| L6 | `_validate_monster_daowen_choice`＋`resolve_monster_daowen_choice` | **拒绝理由**与裁决入出参（理由里的 token 会归一化，才能看出「同一对象换个身份被拒」） |
| L7 | `CombatEngine.__init__` 登记＋结算审计 | id 账本污染：`_monster_activated`/`_monster_daowen_round_used`/`_resonance_rewrites` 中「键不在本场名册」的**垃圾键计数** |

32 位实体 token 统一归一成 `TOK<n>`，否则两次 run 的 token 不同会把每层都判成分歧。用法：`PYTHONPATH=. .venv/bin/python sim/diag_repro.py`（默认龙心谷/seed42/decision9；`--region/--seed/--decision/--runs/--quick/--layer L6 --context 2500/--dump`）。
**反向对照（已实跑）**：`git show cc6d5ac^:engine/ai_preview.py > engine/ai_preview.py` 把预演还原到修复前后再跑 → 工具立刻报 **L7 垃圾键 21+21=42**、**L6 五次假拒绝**（`in_round_used=true` 但 `can_use=true` ＝ id 账本污染的指纹，round3 vs round2）并给出首次分歧上下文，L5 评分同步分歧；`git checkout engine/ai_preview.py` 还原后 7 层全绿、垃圾键 0。也就是说，6.9 那个当时靠人肉排查很久才定位的根因，现在一条命令就能指到层。

**B `sim/check_rule_change.py`（改一条规则后的一键自检，约 7 秒）** —— 四步，任何一步不过即非零退出：

1. **索引同步**：重跑 `sim/gen_daowen_index.py`，比对前后哈希。防「引擎改了、`全道纹索引.md` 没重生成」——`kb_regression.py` 在本沙箱跑不了（`git tag` 为空，只能 checkout 提交号跑不了 tag 名），这一步是它的等价替代。
2. **守卫测试子集**：12 个规则测试文件（道纹 X／怪物 X 与选择／怪物阶段顺序／命中／预演快照／凡庸／残韵网络／护卫指令／员工经济／战斗一致性／轮回模拟／AI 一致性）。失败先对照 `KNOWN_BASELINE`（第二节那 9 条既有失败），**只报新增**；某条基线失败若**回绿会提醒删名单**，避免基线越攒越大。
3. **废案扫描**：活语料分两层——**硬层**＝真正注入 AI 的规则文本（`全道纹索引.md`/`副本索引.md`/`道纹.md`/`轮回者道纹.md`/`怪物道纹.md`/`怪物道纹详表.md`/`残韵详表.md`/`怪物手册.md` 等），逐行匹配 16 条已废口径（家族税、怪物道纹递增、残韵三旧型、旧转化规则、攻次攻力加点池、格挡[回终]清空、杀伐2X、必中 5X、旧工资门槛…）并报 `文件:行号`；**带否定语境词的行放过**（删除/废止/反转/已删/不得再写回去/降为…），且整张《已删内容》表跳过——那张表是红线指定的废案档案存放处，不是漏洞。**软层**＝`报告.md`/`机制迁移台账.md` 等工作日志，只警告不阻塞（红线只管正文，工作日志本来就要记沿革；本轮实测软层 20 处、硬层 0 处）。
4. **口径速览**：用引擎 `resolve_monster_daowen` 打本轮涉及的 7 条道纹的**代价与效果摘要**（读引擎、不抄文档，避免文档幻觉）。本轮读数即 6.1 的结论：必中 `异变+1`，其余异变类 `异变+5`，自愈 `冷却1场`。

用法：`PYTHONPATH=. .venv/bin/python sim/check_rule_change.py`（`--full` 跑全量 4–5 分钟、`--skip-tests/--skip-index/--max-hits N`）。
**反向对照（已实跑）**：往 `副本索引.md` 注入一行「怪物道纹递增：每实际发动一次该道纹本场 X 累加+2×副本阶级。」→ 第 3 步立刻报**硬层 3 处命中**并非零退出（注入行已还原，`git status` 无 diff）。

**仍未做（需用户裁定后才动）**：6.x 里反复出现的三项**结构性**改动——① 规则文本与引擎统一到一个数据源（消灭「一条规则改 5–6 处」）；② 拆 `engine/combat.py`（约 6000 行巨石，改动定位与回归面都受它拖累）；③ 引擎 id 账本改对象账本（6.9 的根治方向，现在靠 `ai_preview` deepcopy 隔离＋L7 审计兜住）。这三项都属「非必要不修改」要先报裁定的范围，本轮只沉淀了工具、没动结构。

**追加（同日，用户裁定「从小到大改，先做最小的改动，是哪个？」→ 选 B）**：③ 的**最小切片**（账本清单单一化，含把 `_dodge_counts`/`_dodge_round` 一并纳入隔离）已做完，见 **6.12**；①、②、③-full（id 键→对象键）仍未动。

### 6.12 ③ 的最小切片：账本清单单一化（用户裁定「从小到大改，先做最小的改动」→ 选 B）

**为什么这是最小的那一刀**（三件事的实测改动面）：③-lite＝3 个调用点、约 40 行、**零行为改动**；
③-full（id 键→对象键）＝63 处引用跨 15 文件、engine 内 60 处 `id(`；②（拆 `combat.py`）＝6289 行、
**单个 `CombatEngine` 类**、174 个 def、**68 个文件**引用；①（规则文本与引擎统一数据源）＝一条规则
现在同步 5–6 处、牵动 99 条文档守卫。且 ③-lite 是 ③-full 的前置：先有权威清单，才知道要迁哪几本键。

**改了什么**：

- 新增 `engine/ledger_isolation.py`：权威清单 `COMBAT_LEDGERS`（9 项，每项登记 名字／默认值／是否 id 键／
  是否 `roster_bound`／这本账记什么）＋ `snapshot`／`enter_copy_world`／`restore_world`／`copy_world`
  （上下文管理器）＋ `roster_ids`／`audit_id_ledgers`。
- `engine/ai_preview.py::preview_sequence` 改用 `copy_world(eng)`（**−54 行**手抄清单）。
- `sim/win_only_ai.py::_swap_world` 改用同一个 `copy_world`（**−54 行**；原先同一批字段「存一遍＋还一遍」
  写两次，漏字段不报错、只静默漂移）。`_PLAYOUT_DEPTH` 计数保留不动。
- `sim/diag_repro.py`：L6 拒绝快照与 L7 审计改读权威清单（`ID_KEYED_LEDGERS`／`audit_id_ledgers`）；
  L7 从「只审 3 本」变成「审全部 id 键账本」，并分两个数：`junk_keys`（门禁）／`unbound_keys`（参考）。
- 新增 `tests/test_ledger_isolation.py`（14 条守卫用例）。
- `sim/check_rule_change.py` 的守卫子集加入该文件（12→13 文件），并注明「同 seed 可复现」不进子集
  （那条单跑约 29s，会把 7 秒的自检拖成半分钟），要验复现性跑 `sim/diag_repro.py`。

**裁定项落地（用户选 B）**：`_dodge_counts`（id(目标)→本回合自动闪避次数，每回合每目标上限 2 次）
＋ `_dodge_round`（配套的回合哨兵）纳入隔离。实测依据：整局脚本轮回里 **4789 次预演，自动闪避决策
被调用 0 次、`_dodge_counts` 被写 0 次** → 是**潜在漏洞而非活 bug**，纳入属结构保险。两者
`roster_bound=False`：目标中途离场时计数会滞留到回合末才清，属合法，若进门禁 L7 会误报。

**顺带删掉的死代码（删除，需用户知情；不同意可单独 revert 这一处）**：`engine/ai_preview.py` 的
`snapshot()`／`restore()`／`_restore_entity()`／`_restore_group()` 共 **83 行**——这是 2026-08-19 改成
「副本执行」之前的**回滚式预演**，全仓无任何调用者（连字符串/`getattr` 式间接引用都查过），却带着
**第 4 份手抄账本清单**（7 本，且缺 `_dodge_counts`）。留着它就得要么继续同步第 4 份清单、要么在新守卫
用例里给它开豁免——两条都会让坑复活。它那段「实体必须原地恢复以保 id 稳定」的理由已保留在
`preview_sequence` 的副本执行 docstring 与 `ledger_isolation.py` 的模块 docstring 里，没有丢。

**一处有意的语义收紧**：还原改为**按引用归还**（identity restore，与 `win_only_ai` 原口径一致）；
`ai_preview` 原来是「归还一份 deepcopy」（内容相等但对象已换）。按引用更安全：外部若持有某本账的原对象
（如 `_monster_round_used(monster)` 返回的内层集合），还原后它仍是本体，不会变成脱钩副本。
**实测无差异**：`sim/diag_repro.py --runs 2` 的 7007 actions／261 picks／783 previews／2558 scores
与改前**逐条同数**、`junk_keys=0`；全量回归 `9 failed / 1720 passed / 3 xfailed`（9 条与第二节基线
逐条同名；passed 1706→1720＝新增 14 条守卫用例，无新回归）。

**写实现时自查出的一个真隐患（已修，并钉成用例）**：懒初始化的账本（`_dodge_counts`/`_dodge_round`
只在自动闪避路径里才创建）若属性不存在就用清单里的默认值兜底，`restore_world` 会把那个**共享默认对象**
设成真实账本 → 之后任何写入都污染全项目默认值（下一台引擎一出生就带着上一场的闪避计数）。
现在改成「属性不存在时 deepcopy 一份默认值」，用例
`test_lazy_ledger_is_not_restored_as_the_shared_default_object` 钉住。

**守卫用例怎么防复发（这一刀的真正收益）**：

- `test_every_container_ledger_created_in_combat_is_registered`：**静态**扫 `engine/combat.py` 里
  `self._x = {}`／`set()`／`[]`／`defaultdict(` 的创建点，未登记即失败（豁免表 `EXEMPT_CONTAINERS`
  当前为空：实测 combat.py 创建的容器恰好 5 个，全部已登记）。为什么必须静态——新账本在多数局面里是
  **空的**（`_resonance_rewrites` 整局脚本轮回都没被写过），运行时扫描看不出键类型，抓不到「忘了登记」。
- `test_runtime_id_keyed_containers_are_registered`：跑一局真实脚本轮回后扫 `vars(combat)`，键是 id 形态
  （>10^6，避开 X 值/回合数这类小整数键）而未登记即失败（辅门禁，抓别的模块挂上来的账本）。
- `test_all_three_call_sites_reference_the_single_source`＋`test_hand_copied_lists_are_gone`：三个调用点
  必须引用权威清单，且 `ledger_defaults`／`combat._monster_activated = copy.deepcopy`／`LEDGERS = (`
  这些手抄痕迹不得复活。
- 另有：identity/内容双还原、副本世界必须拿到深拷贝、`_dodge_counts` 跨预演不变、审计只读不改账本、
  门禁与参考分层、api.py 回滚清单 ⊆ 权威清单（见 D7）。
- **反向对照（已实跑）**：把 `_resonance_rewrites` 从清单临时摘掉 → **3 条用例失败**（静态门禁点名该账本）；
  还原后 14 条全绿。值得记一笔：第一版只有运行时扫描时，这个反向对照**没有失败**（因为它当时是空容器），
  门禁形同虚设——静态扫描就是为补这个洞加的。

**仍未做**：①（规则文本与引擎统一数据源）、②（拆 `combat.py`）、③-full（id 键→对象键）。
③-full 现在有了前置：要迁的就是清单里 `id_keyed=True` 的那 5 本。

### 6.13 ③-full：账本键 `id(entity)` → `Entity.runtime_id`（用户裁定「按顺序做 321」，键类型选 A，并先做 C 量化）

**先做 C（量化）再动手**，用户要求「A+先量 C」。同一支探针（键口径无关：先按真实名册把账本键翻回角色、
再用**当前键口径**下副本角色的键去查副本世界的账本）在改前改后各跑一次整局脚本轮回（7 场）：

| 指标 | BEFORE（id 键） | AFTER（runtime_id 键） |
| --- | --- | --- |
| 进入副本世界次数 | 4879 | 4861 |
| **盲副本世界**（看不见该角色的回合内/本场状态） | **2779（57%）** | **0** |
| 看不见的 `_monster_daowen_round_used` 条目 | **2779** | 0（改为看得见 **4363**） |
| 看不见的 `_monster_activated` 条目 | **2503** | 0（看得见 **4315**） |
| 「预演说能用、真打被拒」 | 0 | 0 |
| 校验调用（预演侧／真实侧） | 1566／558 | 1476／560 |
| 固定 seed、battles=7 的单局耗时 | 14.4s | 14.3s |

**C 的结论修正了我的预期**：盲点很大（57%）但**没有**泄漏成「白挑一次出手」（M2＝0），因为候选生成
直接读真实账本、不经预演 → 「预演保真」这条是**保真度欠账，不是活 bug**。

**真正查到活 bug 的是 M3（怪物阶段回滚）**，用确定性构造复现（不是模拟数据）：
`combat.py::_monster_phase_restore` 在怪物阶段出错时把 `state.__dict__` 整体换成快照副本 ——
**实体对象全被换掉**（`runtime_id` 不变、`id` 全变），于是 id 键账本 100% 失配：

| | BEFORE（id 键） | AFTER（runtime_id 键） |
| --- | --- | --- |
| 回滚后账本非空条目／其中对得上在场角色 | 1／**0** | 2／**2** |
| 狂暴的额外出手（回滚前 → 回滚后） | 2 → **1** | 2 → **2** |
| 「本回合已发动」是否仍可见 | 否 | 是（`["狂暴"]`） |
| L7 垃圾键 | 0 | 0 |

即：**怪物阶段一旦回滚，怪物就静默丢掉本场已激活的持续效果**（`_monster_attack_actions` 读
`_monster_activated` 决定狂暴 +1 出手）。手操时一次非法的怪物阶段提交就会走到这条路径；
脚本轮回里 0 次触发（所以全量回归一直绿），这也解释了它为什么能一直藏着。
回归用例：`tests/test_ledger_isolation.py::test_monster_phase_rollback_keeps_ledgers_reachable`
（夹具先断言出手数＝2，避免退化成 1==1 空过）。

**为什么是 runtime_id 而不是实体对象当键**（用户选 A 的依据，都已查证）：
- `Entity` 是 `@dataclass`（默认 `eq=True`）→ **不可哈希**；要当键就得违背 hash/eq 契约
  （`__hash__ = object.__hash__` 但 `__eq__` 按全字段比较）或改 `__eq__` 语义（波及全仓按值比较的用例）。
- 账本持有实体引用＝本仓 2026-08-22 已修过的雷：`api.py`「存 runtime_id 而非实体引用
  （**引用环会炸事务回滚递归**）」、`combat.py::_jiahuo_target`/`_beifu_target` 同款注释。
- runtime_id 是既有约定（`state.personality_traits`、`GameState.entity_by_runtime_id`），
  跨 deepcopy 稳定、可序列化（存档/回放/断点续跑都用得上），且已离场角色不会被账本拽着不回收。
- 撞键风险已排除：`_spawn_fenlie_clones` 用 `Entity(...)` **新建**（克隆拿到全新 runtime_id），
  engine 里没有任何 `runtime_id = ` 重赋值。

**性能核查（排除「字符串键更慢」的猜测，逐层量过）**：
- 同一 seed 单局工作量：seed42 `execute_action` 6378→6480（+1.6%）、previews 4220→4268、
  picks 251→240、耗时 12.8→13.0s；seed7 1015→1017、1.5→1.4s → **每步成本没变**。
- 固定工作量的确定性用例：`test_fixed_seed_is_reproducible` 28.80→28.41s；
  `test_fitness_fixed_mode_is_deterministic` 60.57→**52.76s（更快）**；`test_bone_angel…` 10.86→11.47s。
- 但 `test_random_seed_mode_varies_samples` 32.6→113s（三次重跑 113.9／116.6／111.5，**不是噪声**；
  该用例的种子由 `SpyRandom(5)` 决定，是确定的）。逐局拆开：
  run1 seed 1337671203／扭曲都市：3.2s→3.3s、cleared 0→0（**完全一致**）；
  run2 seed 1588920085／龙心谷：23.2s→67.1s、**cleared 2→4**（同一 seed 打得深得多）；
  run3：seed 由 1167717557／罪孽都市 变成 1644382883／扭曲都市（run2 多打 2 场，共享 rng 被多消耗，
  第三局的种子跟着变），1.9s→44.0s、cleared 0→4。
  → **变慢＝打的局数/回合变多（内容变多），不是每步变贵**。全量套件因此 230s→313s。
- 这也是本轮唯一可观测的**行为变化**：预演变诚实后，脚本 AI 在部分 seed 下活得更久、cleared 2→4。
  属平衡面变化，按用户裁定「平衡数据等所有规则完善了再测」本轮不重标，只记档；
  重测时以本段为对照（尤其 `test_build_learner::test_valid_and_invalid_are_separated` 的通关率信号）。

**改动清单**（键一律 `runtime_id`；`grep` 复查 engine/sim/tests 已无 `id(` 索引账本）：
`engine/combat.py` 20 处（含 4 处类型标注/注释）、`engine/api.py` 回滚快照与还原
（删掉 id→`(kind,index)`→id 的翻译，覆盖面仍 3 项，见 D7）、`engine/ledger_isolation.py`
（`id_keyed`→`entity_keyed`、`roster_ids`→`roster_runtime_ids`/`roster_entities`、
`audit_id_ledgers`→`audit_ledgers`、docstring 记全部实测依据）、`sim/build_learner.py` 3 处、
`sim/{duel_common,produce_real_winners,pick_best_report,handplay_one}.py` 各 1 处、
`sim/diag_repro.py`（清单/审计/文案）、`tests/{test_engine,test_plight_drive,test_twisted_tools_engine,
test_guard_command,test_monster_phase_transaction}.py`、`test_real_playthrough.py`、
`tests/test_ledger_isolation.py`（14→17 条）。

**新守卫**：`test_no_id_keyed_ledger_access_left` 用 **AST** 扫 `engine/`＋`sim/`，凡「同一条语句里
既用了账本、又用了 `id()`」即失败并点名 `文件:行号`。用 AST 而非文本匹配：注释/docstring 里讨论历史
（「当时都按 id(entity) 建索引」）不该报警。实现上必须**剪掉嵌套作用域**——第一版没剪，
`ast.walk` 把整个 `class CombatEngine` 当成一条语句，报了 `api.py:83`、`combat.py:32` 两个假阳性。
**反向对照（已实跑）**：把 `combat.py:5318` 一处改回 `id(monster)` → 本用例失败并点名该行；还原后 17 条全绿。

**验证**：`sim/diag_repro.py --runs 2` → 6990 actions／246 picks／738 previews／2511 scores
两次逐条一致、rejects=0、junk_keys=0（③-lite 时是 7007／261／783／2558，差异即上面那条行为变化）；
全量回归 `9 failed / 1723 passed / 3 xfailed`（9 条与第二节基线**逐条同名**，passed 1720→1723
＝守卫用例 14→17）；`sim/check_rule_change.py` 四步全过。

**③ 到此收口**：③-lite（清单单一化，6.12）＋③-full（键改 runtime_id，本节）都已完成。
剩下 **②拆 `combat.py`**（6289 行、单类、174 def、68 文件引用）与 **①规则文本与引擎统一数据源**
（一条规则现在同步 5–6 处、牵动 99 条文档守卫），按用户「321」的顺序，下一刀是 ②。

### 6.14 ②：拆 `engine/combat.py`（门面 + Mixin 分片；6289 → 2574 行，67 个引用方零改动）

**刀法**：② 的目标是可维护性，不是重写。所以只做**整块搬迁**——`CombatEngine` 变成门面类，
继承四个 Mixin，方法体逐字节搬走，一行逻辑没改。

| 分片 | 原行区间 | 行数 | def | 内容 | 随块搬走的类级常量 |
| --- | --- | --- | --- | --- | --- |
| `combat_parts/monster_life.py`（MonsterLifeMixin） | 2149–2763 | 643 | 23 | 困境/逃跑/进化/癌变/赎罪/雕塑/负债/碎片/怪物数量/许愿/谈判 | PROLIFERATION_THRESHOLD、CANCER_THRESHOLD、DEBT_THRESHOLD、SCULPTURE_DAMAGE、SCULPTURE_SHIELD、REDEMPTION_HP_RATIO、TIER1_REGIONS |
| `combat_parts/daowen_effect.py`（DaowenEffectMixin） | 2764–3826 | 1091 | 1 | `apply_daowen_effect`（**单方法 1019 行**）＋内置法术流程表 | SPELL_FLOWS、BUILTIN_SPELL_DAOWEN |
| `combat_parts/spells.py`（SpellReactionMixin） | 3827–4882 | 1084 | 38 | 法术/DSL 步骤展开/反应提交校验/全局时点法术/自动反应（含 `_dodge_budget_reset`＝③-lite 那两个账本的创建点） | _WIRED_TRIGGERS、_REACTION_SPELL_SLOTS、MAX_SPELL_LOOP_CYCLES、NAMED_DEATH_MECHANICS |
| `combat_parts/monster_phase.py`（MonsterPhaseMixin） | 5176–6169 | 1023 | 19 | 怪物回合两阶段：准备/静态校验/选择解析/快照与回滚/执行 | _DAOWEN_X_PROBE_CAP |
| `combat.py`（门面，保留） | — | 2574 | 92 | 类声明、`__init__`、事件、伤害结算、`resolve_attack`、`round_start/round_end`、代价支付、死亡与克隆、`process_relics`、凡庸 | REGION_EXCLUSIVE_DAOWEN、ORIGINAL_MONSTER_DAOWEN、YUANCHU_COST_RATE、WAVE_NUMERIC_KEYS、MAX_EFFECT_CHAIN_DEPTH、SHAREABLE_NUMERIC_COSTS |

共搬出 3728 行；类声明改为
`class CombatEngine(DaowenEffectMixin, MonsterLifeMixin, SpellReactionMixin, MonsterPhaseMixin)`。
四块的方法名在原单类里本就唯一 → MRO 顺序不影响解析；类级常量随块搬走后仍按 MRO 可见
（`CombatEngine.CANCER_THRESHOLD`、`self._DAOWEN_X_PROBE_CAP` 照旧能用）。

**只做了两处机械修正**（都不是逻辑改动，且都查证过）：
1. 分片住在子包里，相对 import 要多加一个点：`.`→`..`（4 个文件共 52 处，含函数体内的
   `from ..spell_dsl import ...`）。第一版漏了这点，直接 `ModuleNotFoundError: engine.combat_parts.models`。
2. `_flow_step_variants` 是 `@staticmethod`，递归时写的是 `CombatEngine._flow_step_variants(...)`；
   搬进 Mixin 后 `CombatEngine` 在该模块里不存在（反向 import 会成环），改成
   `SpellReactionMixin._flow_step_variants(...)`——同一个函数、同样不经实例，
   且全仓 grep 确认**没有任何外部调用方**（只有自身递归）。同块 4108 行 docstring 里提到的
   「CombatEngine 活引用」是叙述文字，不改。

**零改动面（可核对）**：`import` 战斗引擎的 **67 个文件一个都没动**（`git status` 里没有任何
sim/api/models 的引用方）；`sim/diag_repro.py` 那类 monkeypatch 打在 `CombatEngine` 上，
继承来的方法一样能被替换与还原。

**这一刀真正的成本不在搬代码，在守卫**：全仓有 **15 处**「读 combat.py 源码文本做断言」的地方，
代码搬进分片后它们会**静默失效**——「旧实现不得复活」变成永真、「新实现必须存在」变成永假。
其中一处还是**生产代码**：`engine/validator.py::_MIGRATION_GUARD_PROTECTED_FILES`
（已迁移机制护栏，原先只列 combat.py／combat_hooks.py／api.py）。处理：

- 新增 `tests/source_scan.py`：家族＝`engine/combat.py` ＋ `engine/combat_parts/*.py`（**按目录 glob**，
  以后再切片不用改这里），提供 `COMBAT_SOURCE`／`combat_source()`／`combat_relative_paths()`；
  拼接时每段前带 `# ===== 相对路径 =====`，断言失败能直接定位到是哪个分片。
- 改 14 处测试 ＋ 1 处生产守卫：9 个 `test_*_migration.py`、`test_r01_r38_contract.py`、
  `test_r39_r47_contract.py`、`test_grenade_action_budget.py`（扫描清单补分片）、
  `test_f7_doc_consistency.py`（「旧名」注释豁免原先按文件名 `combat.py` 判定，注释跟着代码走进分片
  就得放宽到 `p.parent.name == "combat_parts"`，否则变假阳性）、
  `test_ledger_isolation.py`（容器创建点静态扫描：`_dodge_counts` 的创建点已随 spells 分片搬走，
  不改这条会直接红）、`engine/validator.py`（护栏清单加 glob，实测已覆盖 7 个文件）。
- **不需要改**的：用 `rglob("*.py")` 扫 engine/ 或全仓的守卫（`test_battle_victory_predicate`、
  `test_relic_choice_removal`、`test_simulate_removal`、`test_f7` 的文件遍历）天然覆盖子包。

**反向对照（三个探针都种在分片文件里，实跑过）**：往 `combat_parts/monster_phase.py` 尾部种
①`MONSTER_ACTIVATE_PRIORITY = 1`、②`self._planted_ledger = {}`、③`if entity.has_status("龙鳞")` →
`test_r11_r17_repository_has_no_legacy_automatic_monster_policy` 失败并点名该常量、
`test_every_container_ledger_created_in_combat_is_registered` 失败并点名 `_planted_ledger`、
`check_migrated_mechanism_guards()` 报 1 条违规且 `file` 正是 `engine/combat_parts/monster_phase.py`；
还原后三项全干净，分片文件与备份 `diff` **逐字节一致**。
探针特意种在分片而不是 combat.py，要证明的正是「搬走的代码没有逃出守卫」。

**验证**：受影响的 18 个守卫文件 **277 passed**（16.4s）；`sim/diag_repro.py --runs 2` →
6990 actions／246 picks／738 previews／2511 scores、rejects=0、junk_keys=0，两次逐条一致
（与 6.13 记录的数字**完全相同**＝行为零变化）；全量回归 `9 failed / 1723 passed / 3 xfailed`
（9 条与第二节基线逐条同名，309s）；`sim/check_rule_change.py` 四步全过（口径速览里
必中 X=1 仍是 异变+1，6.1 的裁定没被搬迁碰坏）；类表面 `dir(CombatEngine)` 195 个成员，
搬前搬后 `diff` **零差异**。

**没做的（按「非必要就不修改」存档，防重提）**：
- `apply_daowen_effect` 仍是 **1019 行的单个方法**。整块搬走能让 combat.py 瘦下来，但**方法内部**再拆
  （按道纹分派成小函数）是另一种风险等级：它有共享局部状态与顺序依赖，不是「搬行」能保证等价的。
  本轮不动；将来要做，先给它补一层「输入→输出」对照快照再拆。
- 门面 `combat.py` 剩 2574 行，仍是大文件（`api.py` 更大）。要继续切，下一刀是伤害/死亡结算块
  （`_apply_hostile_damage_inner`～`_on_entity_death`）与代价支付块（`pay_numeric_cost`～`_bank_lianxin`），
  刀法与本节相同（整块搬＋家族扫描口径已就位，成本比这轮低）。

**② 到此收口**，按用户「321」的顺序，下一刀是 **①规则文本与引擎统一数据源**
（一条规则现在同步 5–6 处、牵动 99 条文档守卫）。

### 6.15 ①（第一刀，只做安全加法）：量化「一条规则改几处」＋修两处发布管线的真 bug＋语料清单单一化

**先量再动**（与 ③ 的 C 同一套纪律）。探针：把【自愈】的 25% 改成 30%（只改引擎结算与 summary，
不动 docstring），跑完全部守卫后**还原**。实测「改一条效果数字要同步几处、谁看守」：

| 位置 | 是否要手改 | 有没有机器守卫 | 探针结果 |
| --- | --- | --- | --- |
| `engine/daowen.py` 结算＋summary | 要（1 处） | 行为用例 | 2 条用例红（`test_daowen_wiring` ×2） |
| 同文件 docstring 首行（＝发布正文的来源） | 要（1 处） | **改前没有** | 全绿（漏改无人知） |
| `全道纹索引.md`（生成物） | 不用（重生成） | 逐字节比对 | **全绿**——因为效果正文取自 docstring，docstring 没改就跟着一起错，逐字节比对反而"通过" |
| `AI_EXPERIENCE.md:391`「恢复[目标]25X%已损生命」 | 要（1 处） | **没有** | 全绿（发布给 AI 的口径与引擎相反，静默） |
| 副本/*.md 的效果数字 | 视规则 | 没有 | — |

顺带量清了三件事：

- **代价数字**：硬层语料里可归属到某条道纹的代价数字 **75 处**，其中 72 处已在
  `test_rule_docs_cost_numbers_match_engine` 范围内，**3 处在范围外**（`README.md:41`【封印】异变X、
  `法术索引.md:57-58`【庇护X】消耗X／【再生X】消耗X）。实测这 3 处**恰好都对**——对的是运气不是机器。
- **效果数字**：硬层语料里只有 **17 处**可归属，但引擎与文档**用不同词汇写同一个量**
  （逆鳞：引擎 summary「每掉1HP积1层」vs 文档「每失去1点生命，获得1层」；勾魂「失去2X点当前法力」
  vs summary 无「点」；活血同类）。严格比对今天会报 5 处，其中 4 处是词汇/归属假阳性、
  1 处是 `build_knowledge.json` 里的判决叙述 → **这条守卫现在不可落地**，得先统一量词。
- **引擎内部**：70 条道纹，docstring 首行效果段的数字 vs `resolve(X=1)` 的 summary 数字，
  **正向 0 处不符**（剥掉沿革注记后）；反向 57 处"不符"是 docstring 写 X 代数（持续X）、
  summary 写 X=1 实数（持续1回合），不是漂移 → 正向可比、反向不可比。

**还查出一个方向冲突（①的真身，本轮没动，需裁定）**：`engine/rule_sync.py`（688 行，
`main.py:339`／`api.py:enable_rule_sync`／`ai_player.py:852` 都在用）是**文档 → 引擎**
（把 AI_EXPERIENCE.md／死者之书.md／物品索引.md／副本索引.md 当事实源，`diff_daowen`＋
`auto_apply_patch` 给引擎打补丁）；而 `全道纹索引.md` 是**引擎 → 文档**（生成物）。
同一条规则两个方向同时存在，这才是「一条规则改 5–6 处」的根。

**本轮做的（都是安全加法，不需要裁定）**：

1. **修真 bug（发布物带沿革）**：`sim/gen_daowen_index.py::REVISION` 只认「以日期开头」的句尾注记，
   而【逼债】写的是「（DM裁定D 2026-08-22：旧"否则失去2X点血限"废止）」→ 抽不出来，
   整段沿革被印进**注入 AI 提示词的索引正文**（正文红线：规则正文只写现行口径）。
   放宽成「句尾括号里含日期即算修订注记」，与书写顺序解耦。索引全文只有第 366 行变化。
2. **修真 bug（注释撒谎＋死代码）**：同文件注释写着「修订注记…渲染成 `> 修订：` 行」，
   但渲染代码从来不存在（`revisions` 建好没人读；索引里 `> 修订：` 行数＝**0**）。
   删掉死变量 `revisions`／`notes`，注释改成实话：注记与旧名一律**剥掉且不发布**，
   沿革留三处——引擎 docstring（代码注释不受限）、`archive/`、`STALE_PHRASES`（机器守卫）。
   删完重生成，产物与删前**逐字节一致**。
3. **语料清单单一化**（③-lite 的同一招）：权威挪到 `engine/document_validation.py`
   （`CORPUS_HARD`／`CORPUS_SOFT`／`CORPUS_GLOBS`／`GENERATED_DOCS` ＋ `corpus_files()`／
   `handwritten_rule_docs()`），`sim/check_rule_change.py` 与代价守卫都改成引用它，不再各抄一份。
   实测硬层文件集与改前**逐条一致**（16 个）；代价守卫覆盖面 **8 → 14 个文档**。
   生成物按「重跑生成器逐字节比对」守卫，不做文本扫描（`build_knowledge.json` 里的数字是叙述不是口径）。
4. **新守卫 ×2**（`tests/test_daowen_cost_consistency.py`，9 → 11 条）：
   - `test_published_rule_numbers_are_backed_by_engine`：docstring 首行效果正文里的每个数字，
     必须在引擎 X=1 的 summary 里出现（先剥沿革注记）。带空豁免表 `DOCSTRING_NUMBER_EXEMPT`
     （要豁免得登记名字＋理由，不许直接放宽断言）。
   - `test_generated_index_rule_text_carries_no_history`：索引里行首为 `X：`／`X/Y：` 的规则正文行
     不得含 `20xx-xx-xx`／DM裁定／已废止／旧版／此前口径／原名【。先断言"认出的正文行数 ≥ 道纹条数"，
     防止行首格式变了之后守卫退化成扫不到任何东西（空转）。
5. **废案清单补 1 条**：逼债旧口径「否则失去2X点血限」（从索引正文剥掉后，"怕改回去"交给机器守卫）；
   实测硬层 **17 条口径无残留、不误报**（`archive/experiment_log.md:216` 与引擎 docstring 仍留着沿革）。

**反向对照（三条都实跑，跑完还原并复验 11 passed）**：
- 【自愈】结算 25%→30%（docstring 不改）→ `test_published_rule_numbers_are_backed_by_engine` **失败并点名**；
  这正是改前静默通过的那一类。
- 往【飞行】docstring 效果正文**中段**塞「（旧版为持续1回合）」（生成器剥不掉的位置）→ 重生成后
  进了索引第 192 行 → `test_generated_index_rule_text_carries_no_history` **失败并点名该行**。
- 往 `README.md` 尾部塞「【封印X】消耗9X」→ `test_rule_docs_cost_numbers_match_engine` **失败**
  （证明 README 现在真的被看守；放宽语料之前这条会静默通过）。

**验证**：`tests/test_daowen_cost_consistency.py` 11 passed；`sim/check_rule_change.py` 四步全过
（索引同步／守卫测试／废案扫描 17 条口径／口径速览）；全量回归 `9 failed / 1725 passed / 3 xfailed`
（9 条与第二节基线逐条同名，passed 1723→1725＝两条新守卫）。

**没做／仍有一份手抄（存档防重提）**：`sim/kb_regression.py` 还有第三份语料清单（`shared_files`）——
它要 `git show <TAG>:` 做前后对比，本沙箱跑不起来（无该 tag），改了没法验证，故本轮不动；
它不影响门禁（不在 `GUARD_TESTS` 里）。

### 6.16 ①-B 的前置：查到「发布正文与实现相反」两条（勾魂／爆裂）＋三条文档过期，全部按实现更正

用户裁定 ① 走 **B（文档从引擎生成，彻底单一真源）**。动手前先量「引擎与文档到底差在哪」，
结果量出的不是重复，是**三层互相矛盾**（实现 ↔ docstring 首行 ↔ 文档）：

- `副本/*.md`＋`AI_EXPERIENCE.md` 里的道纹规则行，与引擎 docstring 首行**逐字相同只有 3 条**（共 33 条）。
- 用「docstring 首行效果段 vs `resolve` summary 效果段」的字符二元组 Jaccard 扫全 70 条道纹：
  低重叠 11 条，人工复核后 **10 条是措辞差异（语义相同，如「每次受到伤害-X」vs「每次受伤-1」）、
  1 条是真的规则矛盾＝【勾魂】**。

**两条「发布正文与实现相反」（已按实现更正，零行为改动）**：

1. **【勾魂】同一条规则存在三个版本**：`副本/乱葬岗.md:20`「[回始]使[目标]失去2X点当前法力，持续∞」（最旧）／
   docstring 首行「使[目标]无法获得[法力]，持续X」（2026-08-30 版）／**实现＝`mana_cost_multiplier: 2`**
   （`engine/combat_parts/daowen_effect.py:761` 真的在消费它），summary 写「法力消耗翻倍」
   （2026-09-09 DM裁定：法力改一池制后「无法获得法力」失去作用对象）。**docstring 正文自己记着这次裁定，
   首行却没跟上**；而《全道纹索引》的效果正文是从首行生成的 → 注入 AI 提示词的索引一直在发布 2026-08-30 的旧口径。
   数字守卫抓不到它：首行一个数字都没有。
2. **【爆裂】时点写反**：`engine/daowen.py` 的首行／`effect`／`summary` 都写「受到伤害**后**，攻击者失去等量生命」，
   而真实分发是 `engine/combat_hooks.py::BaolieHook.on_before_damage`（priority 40；反噬致死时返回
   `suppressed` 抑制这次伤害）＝**受到伤害前**；并且 `engine/combat.py::_tick_baolie`「只递减爆裂，
   持续X按持有者的[敌回终]计数」，首行漏写了这条递减。这次**文档是对的**（`副本/扭曲都市.md:19`
   写「受到伤害前…持续回合数在爆裂持有者的［敌回终］递减」），错的是引擎侧与 `AI_EXPERIENCE.md:334`。

**三条文档过期（以实现/引擎为准，已更正）**：【变形】文档还在写 2026-09-17 重做前的「使自身攻击力与
攻击次数互换」（现行＝[目标]当前速度↔当前法力互换、各自被上限钳制、钳掉的部分凭空消失不返还）；
【超频】文档写「使自身速度+X」（现行＝目标由发动方自由指定，实现一直是给 target 加速）；
【定型】文档写「攻击次数与攻击力无法**增加**」（实现 `_panel_keys` 同含 `attack_boost` 与
`attack_reduction`，增减两个方向都挡＝无法**被改变**）。

**改动清单（全是文本更正，无一处改行为）**：`engine/daowen.py` 4 处（勾魂首行；爆裂首行／effect／summary）、
`AI_EXPERIENCE.md` 2 处、`副本/乱葬岗.md` 1 处、`副本/扭曲都市.md` 3 处、重生成 `全道纹索引.md`
（**全文只有 314／423 两行变化**）。行为未变的证据：`sim/diag_repro.py` 与全量回归的数字与改前一致。

**新守卫**：`test_published_rule_wording_matches_engine`（`tests/test_daowen_cost_consistency.py`，11→12 条）
——docstring 首行效果段与 summary 效果段的措辞重叠度低于 0.22 即报警。阈值本身分不开「换了说法」与
「换了规则」（搏命/透支语义相同也是 0.00），所以靠 `WORDING_EXEMPT`（10 条，每条写明为什么只是措辞差异）
区分；并且**豁免用不上时会反过来要求删掉**（`stale` 断言），防止豁免表腐烂成一张永久通行证。

**废案清单 +6**（勾魂×2、爆裂、变形、超频、定型）：更正完就得让机器守住「怕改回去」。
实测硬层 **23 条口径无残留、无误报**。

**反向对照（两条都实跑，跑完还原并复验 12 passed）**：
- 把【勾魂】docstring 首行改回「无法获得[法力]」→ 新守卫失败并点名 `[勾魂] 重叠 0.00`。
- 把 `AI_EXPERIENCE.md:359` 改回旧口径 → 废案扫描报**硬层 1 处命中**、非零退出，点名文件行号与理由。

**验证**：`sim/check_rule_change.py` 四步全过（守卫测试 163 passed／1 条基线失败）；
全量回归 `9 failed / 1726 passed / 3 xfailed`（9 条与第二节基线逐条同名，passed 1725→1726＝新守卫）。

**①-B 的实测成本（下一步要先搬细节，否则生成一次丢一批口径）**：文档普遍比引擎 docstring **更详细**，
直接「文档从引擎生成」会把现行口径丢掉。实测例子：【裂变】文档有「每次结算的伤害＝原伤害÷X，
依全局整数规则**向上取整**」（取整是项目红线），引擎首行没有；【波及】文档有「发动时X按当前合法目标数
自适应降X」；【逆鳞】文档有「随后清除全部【逆鳞】层数」；【活血】文档有「未满2点的余数在[回终]清空」；
【消灾】把「局外发动消耗×2」写在代价段。另有 3 处文档正文内嵌沿革（点金／逼债／分裂 的 DM裁定注记），
按正文红线要剥掉、交给废案清单看守。
→ **B 的正确顺序：先把文档里多出来的现行细节搬进引擎 docstring（约 10 条），再让文档从引擎生成。**

### 6.17 ①-B 第 0 步：把文档里多出来的现行口径搬进引擎（6 条，逐条对着实现核过）

用户裁定 ①-B 走**整节生成**、并同意现在就剥文档正文里的沿革。整节生成的前提是：引擎 docstring 首行
必须承载「文档那一节现在写的全部现行口径」，否则生成一次就丢一批规则。所以第 0 步是**把细节搬进引擎**，
每一条都先在实现里核对为真才搬（不是把文档抄过去就算）：

| 道纹 | 搬进引擎首行的现行细节 | 实现依据（核对过） |
| --- | --- | --- |
| 分裂 | 复制体继承本体**除【分裂】外**的全部道纹，防止无限套娃 | `combat_parts/damage_death.py::_spawn_fenlie_clones`：`if dw_name == "分裂": continue` |
| 假钞 | 战斗中失去[碎片]时**优先失去[假碎片]** | `combat_parts/cost_payment.py::_shards_of/_lose_shards_of`（docstring 与实现同口径） |
| 裂变 | 每次结算的伤害＝原伤害÷X，**依整数规则向上取整** | `combat.py:686`：`per = math.ceil(damage / xv)` |
| 逆鳞 | 下一次造成伤害时伤害+全部层数，**随后清除全部层数** | `combat.py:627-631`（加成后 `_nilin = 0`）、`daowen_effect.py:130-132`、`combat.py:1026-1027`（状态过期也清零） |
| 活血 | 每个完整回合内每累计失去2点生命；**未满2点的余数在[回终]清空** | `combat.py:1082`（`hp_lost_this_round // 2`）＋`combat.py:784`（回终归零） |
| 赎金 | 若目标没有碎片，则其失去X点**当前**速度 | `daowen_effect.py:649-650`（`_shards_of(target) <= 0` 才走 `_lose_current_speed`） |

重生成《全道纹索引》后，这 6 条的发布正文都变完整了（索引只有这 6 行变化）。
`WORDING_EXEMPT` 相应 +2（裂变、假钞：正文比 summary 详细，summary 是简写）、活血的理由补全，共 12 条。

**顺带量出：措辞重叠度这类守卫在 B 落地后会失去意义**——发布正文一旦故意比 summary 详细，
对称重叠度必然下降（本轮 11→12 条低于阈值，全是"正文更详细/换了说法"）。它的价值只在**过渡期**
（抓勾魂那类"数字对得上、规则却是旧的"）。B 落地后发布正文与 summary 应从同一份数据派生，
届时这条守卫可以退休，不必长期养着 12 条豁免。

**没动、需要进一步核实或裁定的四条（都不改，先记档）**：
1. 【波及】`AI_EXPERIENCE.md:314` 写「发动时X按当前合法目标数**自适应降X**：合法目标不足面板X时按目标数结算」，
   但 `api.py:549-556` 在合法目标不足 `fixed_x` 时把该指令标成 **available=False**（理由串「波及X需要X个目标，
   当前仅N个存活非自身角色」），注释还写「与玩家侧同一规则（2026-08-22 BUG-01 指令侧）」；
   玩家侧 `api.py:2431-2434` 要求 `len(dodge_targets) == wave_count`。**文档说的"降X结算"与代码的"不可发动"
   看起来相反**，但 `wave_count` 的来源还没追到底，所以本轮不改任何一侧。
2. 【庇护】`AI_EXPERIENCE.md:311` 写格挡「保留到被打掉或[战终]」，而 `combat.py::round_end` 的 docstring
   明写「2. 格挡清空」；但 `grep current_shield engine/combat.py` **一处都没有** → 清空动作在别处（或已迁到机制层），
   没核实清楚之前不改文档。
3. 【赌命】`罪孽都市.md:19` 写「从轮回者方开始发放数字」，引擎首行只写「按存活角色投随机数」；
   发号顺序的实现还没定位（`api.py:2665` 起是付费预检），未核实不搬。
4. 【活血】`combat.py:1082` 用 `hp_lost_this_round // 2`（**向下取整**），而 `AI_EXPERIENCE.md:203`
   的整数规则写「所有计算都向上取整」。文档口径「每累计失去2点生命获得回复1、余数清空」本身就是整除语义，
   所以这更像是**整数规则的适用范围问题**（是否只管伤害/公式除法）而不是活 bug。按「非必要就不修改」
   ＋「平衡数据等所有规则完善了再测」，本轮不改行为，只记档等你裁定：整数规则是否要给
   「按满 N 计一次」这类计数型效果开一个明确的例外口径。

**验证**：`tests/test_daowen_cost_consistency.py` 12 passed；`sim/check_rule_change.py` 四步全过
（索引与引擎同源、守卫测试 163 passed／1 条基线失败、硬层 23 条口径无残留）；
全量回归 `9 failed / 1726 passed / 3 xfailed`（9 条与第二节基线逐条同名）。
本轮全是 docstring/索引文本，**零行为改动**。

**①-B 下一步（已裁定，待做）**：建生成器把 `副本/*.md` 的「道纹定义」整节与 `AI_EXPERIENCE.md` 的
道纹口径整节改成生成块（标记包裹），顺序与「起点/终点」这类角色标注需要一份引擎侧的权威数据
（`REGION_EXCLUSIVE_DAOWEN` 是无序集合，而文档现有顺序＝残韵闭环顺序，有意义，不能随便排）；
同时把点金/逼债/分裂 三处文档正文里的 DM裁定注记剥掉、交废案清单看守。

### 6.18 上轮 4 条未核实项的落地（用户裁定 2026-09-19）：活血记待办、庇护与赌命改正文、波及删历史逻辑

上轮我报了 4 条「查了但没核实到位」的项，用户逐条给了裁定。本轮按裁定落地，**前三条零行为改动**，
第四条（波及）是行为改动，单独一片提交。

**① 活血 → 记入待办重做**（承露盏已取代其作用）：见第三节 **D8**（含两者对照表、`//2` 取整的记档）。
本轮不改口径、不动结算；只在 `calculate_huoxue` docstring 补「待重做」注记，并把 `AI_EXPERIENCE.md:350`
那行与引擎首行不一致的简写口径同步了（漏「每个完整回合内」「余数[回终]清空」）。

**② 庇护 → 格挡改为战终清除**（不是回始/回终自动清）：
- 实现本来就对：`engine/combat.py:997-1000` 有 2026-09-10 的裁定注释，战终由 `api.py:5333` 的
  `entity.clear_shield()` 统一清；`AI_EXPERIENCE.md:311` 与 `calculate_bihu` 的 summary 也已写对。
- 过期的是两处**文本**：`engine/daowen.py:99` 引擎首行还写着「持续1」；`engine/combat.py::round_end`
  的 docstring 还列着「2. 格挡清空」。本轮都改成现行口径（首行＝「保留到被打掉或[战终]」；
  docstring 去掉那条并注明为何不清）。
- 查证后**没动** `calculate_bihu` 返回的 `duration: 1`：它对本道纹是惰性的——`daowen_effect.py` 里
  读 `calc["duration"]` 的三个分支都挂在别的字段上（全力/attack_reduction/…），格挡走 `target_shield`
  分支（`:448`）只加点数、不建状态；全仓也没有任何 `has_status("庇护")` 的读取方。删它属行为面改动
  （万一将来接上就少一轮），按「非必要不修改」留着，此处记档防重提。

**③ 赌命 → 真 bug，是「文档与实现打架」，不是发号顺序**：
- 复查结论：发号顺序其实是对的。`combat.py:854-873` 里 `alive = player_side + enemy_side`，轮回者方
  确实排第一；`auto_roll` 不按名缓存，多个赌命持有者各投各的。
- 真 bug 在口径：发布正文（`engine/daowen.py:925` 首行、`副本/罪孽都市.md`、旧 summary）写的是
  「失去 30% **当前生命**」，而实现是 `math.ceil(tgt.blood_limit * 30 / 100)`＝30% **血限**
  （代码注释自称「用户裁定口径：血限30%」）。两者对残血目标差出一个量级。
- 本轮按**实现**口径改文档（因为注释记着这是用户裁定过的口径，且改结算＝改平衡数据，而平衡重跑已被
  推迟）：首行补全「按场上存活角色统计、从轮回者方开始发放数字」＋改成「失去等同30%[血限]的生命」，
  summary 同步，`副本/罪孽都市.md` 那句拗口的「30%[血限]的当前生命」改成同一口径。
  → 30% 的基数到底该按「血限」（现实现）还是「当前生命」（旧文档）：**用户裁定 2026-09-19＝
  等平衡测试时一起调整**（与「平衡数据等所有规则完善了再测」同批）。本轮只把文档对齐到实现、
  没有改结算；重跑平衡时这条要一并定，见第三节 **D11**。
- 顺手消掉一个同源隐患：`:865` 原来把 30 硬编码在结算里，而 `calculate_duming` 已声明
  `duming_hp_pct=30`（两份口径）。改成结算读引擎声明字段，避免「改一处忘一处」。

**④ 波及 → 历史遗留，删掉「降X结算」与「不可发动」**：见 **6.19**（行为改动，单独一片）。

**验证（本片：①②③）**：`tests/test_daowen_cost_consistency.py` 等 77 passed；
`sim/gen_daowen_index.py` 重生成后索引只有庇护/赌命两行变化（同源）；全量回归见片末。

### 6.19 波及：删掉「自适应降X」与「不可发动」门禁，X 上限改成场上当前角色总数（行为改动）

**裁定**（2026-09-19，两条追问的答案）：
- X 上限＝**场上当前角色总数**（含发动者自己），**并且**改 AI 侧让发动方自己按可标记目标数选 X
  （选项 C）；
- 怪物 prepare 里「可标记目标数为 0 → 不给出该道纹」这条**保留**（一个都标记不了，本来就无法发动）。

**为什么这是历史遗留**：旧怪物面板把 X 写死（如【波及3】），solo 场上只有 1 个可标记目标时
任何提交都被拒，于是 2026-08-23 补了「自适应降X」（prepare 快照 `wave_effective_x`＝min(面板X, 候选数)，
结算按它取 mark_count）。2026-09-16 起面板不再写 X、X 由发动方自选，这条补丁就没有存在理由了。

**引擎侧（4 处）**
| 位置 | 改动 |
| --- | --- |
| `api.py::_max_legal_daowen_x` | 波及上限 `min(legal, 存活非自身角色数)` → `min(legal, len(_combat_entity_refs()))`＝场上角色总数 |
| `api.py` 指令列表 | **删除**「固定X波及在目标不足时 `available=False` ＋原因文案」的门禁 |
| `monster_phase.py` prepare | `hard_cap=len(dodge_target_options)` → `len(refs)`；**删除** `wave_effective_x` 的计算与输出字段 |
| `monster_phase.py` resolve／静态校验 | `mark_count` 不再读 prepare 快照，改为**本次发动的 X**（resolve 用 `calc["mark_targets"]`；静态校验按 x_free 读提交的 x，未提交则用 prepare 的上限） |

**AI 侧（裁定 C 的第二半：发动方自己收 X，7 处）**
- `sim/monster_targets.py` 新增 `wave_candidate_count` / `clamp_wave_x` / `apply_wave_submission`
  （把「本次 X」与「恰好 X 个目标提交」写成一对）；`pick_wave_dodge_targets` 不再读 `wave_effective_x`。
- `pick_monster_daowen_x`：波及的候选档位先按可标记目标数收上限，逐档预演时同步改 `dodge_targets`
  ——否则每一档都因「提交数≠X」预演失败，评分会退化成一律回退到上限。
- 5 处驱动（`sim/duel_common.py`、`sim/build_learner.py`、`sim/authentic_asymmetric_duel.py`、
  `sim/generate_duel_report.py`、`engine/ai_player.py` 怪物侧）改用 `apply_wave_submission`；
  前两处还要在评分器定完 `dao["x"]` **之后**再对齐一次目标数（它们原本是「先拼目标、后定 X」，
  旧机制下两者解耦才不会出错）。
- `engine/ai_player.py` 玩家侧占位 AI：X 收到可标记目标数（无候选时跳过该行动，不再提交 X=0）。
- `engine/ai_tactics.py::_params`：`len(candidates) >= x` 才提交 → 改为按 `min(x, 候选数)` 成对提交。

**两个已知后果（裁定时就讲明、这里存档）**
1. 人看到的 X 上限是字面值（含自己）。场上 3 个角色时 schema 给到 X=3，而可标记的只有 2 个
   → 照上限提交会被拒（「必须为3个目标显式提交dodge_targets」）。AI 不会踩，手操需要自己选小。
2. 固定 X 面板在目标不足时无法结算 → 见第三节 **D9**（只影响未实现的巴别塔/永夜庭草案）。

**文档/知识库**
- `AI_EXPERIENCE.md:314` 波及正文的括注换成现行口径；「已删内容」表新增一行（自适应降X ＋ 不可发动门禁）。
- `sim/gen_daowen_index.py` 的 `NOTES["波及"]` 与「7. 自由控X」条目改写 → `全道纹索引.md` 重生成（同源）。
- `sim/kb_regression.py`：C2-07 的 gold 从「自适应降X」改为「场上当前角色总数」并把旧词降为 stale；
  S5-01 的 stale 收进两套旧语义（「不进入 daowen_options」＋「自适应降X」）。
- `monster_phase.py:201-207` 那段把已删机制写成现行的注释一并改正（注释虽不受正文红线约束，
  但描述过期机制就是下一次漂移的源头）。

**测试**（`tests/test_wave_target_limit_and_phase_recovery.py`，6 条重写＋夹具现代化）
- 夹具 `_dw` 增加 `x_free` 参数，熔岩蜥的三条道纹改成不写 X（对齐现行面板）；顺带清掉夹具 docstring 里
  已废止的「X 每发动一次 +2」递增机制说明。
- 新断言：`max_x == 场上角色总数`（solo=2、加友军=3/4）、`"wave_effective_x" not in option`（防机制被写回）、
  `clamp_wave_x` 收到可标记目标数、照字面上限提交被拒、指令侧不再 `available=False`。
- `_legal_daowen` 改用 `apply_wave_submission`（X 与目标数必须成对）。

**验证**：该文件 13 passed；全量回归 `9 failed / 1726 passed / 3 xfailed`，与基线**同名同数**
（已用 `git stash` 在改动前的树上复跑这 9 条确认，非本轮引入）；`sim/check_rule_change.py` 四步全过
（索引同步／守卫测试／废案扫描／口径速览）；守卫测试 21 passed。

### 6.20 波及扩散实验（用户令 2026-09-19）：单向性、1v1 不平分、自我增益被送走

> **后记（同日）**：本节「结论二（1v1 不平分）」「结论三（自我增益被送走）」已被用户裁定的选案 A 改写，
> 落地与前后对比见 **6.21**；本节原文保留为**裁定前**的实测证据，不再代表现行行为。

**用户的问题**：① 怪物用【波及】标记「自己＋轮回者」后，轮回者发动不同道纹会发生什么？
② 反过来，轮回者标记「自己＋怪物」后，怪物发动不同道纹会发生什么？

**工具**：`sim/probe_wave_spread.py`（新增，可复现）；原始矩阵
`data/experiments/wave_spread_2026-09-19.md`（70 条道纹 × 6 场景 ＝ 420 行，另含速度类对照与 S5 平分演示）。
探针要点：走真实行动接口发动（`use_daowen` / `prepare_monster_phase`＋`resolve_monster_phase`）；
按道纹的**自然目标**提交（自用类补自己的 ref，其余补敌方 ref，避免「探针替道纹选目标」污染结论）；
每条道纹一场独立战斗、只装这一条；**怪物发动**的场景把怪物速度设 0（命中数＝当前速度 → 零命中，
普攻不造成伤害），**轮回者发动**的场景怪物不进阶段、速度放开到 6（速度类道纹作用在怪物身上也看得见）。

**先回答问题的前提：「标记自己」做不到**
- 轮回者把自己写进 `dodge_targets` → 整笔被拒：「波及目标必须为当前存活的非自身角色」（一个都挂不上）。
- 怪物侧 prepare 的候选直接排除自己（`dodge_target_options` 过滤 `actor_ref`），候选里只有 `player:0`。
- 即便用引擎原语硬给自己挂上标记，`_wave_targets(caster)` 里 `entity is caster` 也会跳过自己 → 不产生扩散。
- 所以「自己＋对方都带波及效果」只能靠**互标**（各自被对方挂上）构造，探针把它作为①②的合法等价形（P3/M4）。

**结论一：波及是单向的——别人挂在你身上的标记，对你发动道纹毫无影响（0/140 条有差别）**
- P1（怪物→轮回者 单向标记，轮回者发动）与 P0（无标记基线）逐条比对：**全部一致**；
  M2（轮回者→怪物 单向标记，怪物发动）与 M0 同样**全部一致**。
- 机制：标记带 `source`＝挂标记者的名字，`_wave_targets(caster)` 只认 `s.source == caster.name` 的标记
  → 只有「你自己挂出去的标记」会让「你自己发动的道纹」扩散。
- 直接回答用户：怪物用波及标记轮回者之后，轮回者发动任何道纹都**照常**（不扩散、不平分、不改落点）；
  反过来同理。真正会改变行为的是「你自己也挂了标记出去」（互标）。

**结论二：1v1 互标时，数值平分从不触发（0/140 条出现 `wave_spread`）**
- 扩散名单＝`[本次目标（若不等于施法者）] + 自己的波及目标`，1v1 里去重后只剩 1 个 →
  数值类分支的 `len(effective) >= 2` 门槛不成立 → 伤害/治疗/格挡全额留在提交的目标上。
- S5 演示（轮回者＋两只怪、两只都被标记）：杀伐 `target_damage=[2,2]`、再生 `target_heal=[4,4]`、
  庇护 `target_shield=[2,2]`、减速打中两只 → 总量不变、一分为二 ✓ 平分规则本身是好的，只是 1v1 看不到。

**结论三：互标时 13 条自我增益类道纹把自己的状态送给了对方（自己拿不到）**
- 借力、兴奋、加速、急速、活血、滋养、爆裂、龙鳞、背负（整体转移）；
  庇护、全力、变形、逆鳞（**部分**转移：数值/面板部分留自己，状态部分给对方）。
- 同族的 自食/飞行/滑翔/狂暴/必中/固执/贯穿 走 `self_targeted` 白名单，**不受波及影响**
  → 同样是自我增益，两类行为相反。
- 机制、代码位点与三个改法见第三节 **D10（待裁定）**。本轮**未改任何代码**。

**其余（与波及无关的既有门槛，实验里如实记到）**：封印（怪物）＝「目标必须是当前在场的怪物」；
搏命/洞察/消灾/赌命（怪物）＝prepare 未列出（条件门槛）；赌命（轮回者）＝假碎片不足被标记不可发动。

**验证**：探针只读不改规则，全量回归仍为基线 `9 failed / 1726 passed / 3 xfailed`；
本轮新增文件 `sim/probe_wave_spread.py`、`data/experiments/wave_spread_2026-09-19.md`，未动引擎。

### 6.21 D10 选案 A 落地：波及扩散名单保留施法者自己（行为改动）

**裁定**：用户 2026-09-19 选 **A**（照 `daowen_effect` 自己注释的意图修：扩散名单含施法者自己，波及目标额外各得一份）。
对副作用的原话：「要是担心波及当内鬼，直接不用不就行了？」→ 接受自施时数值平分的代价，**不为它加保护**。
同日另一条裁定：`wave_spread` 日志按名单记而不按实际落地记（贯穿那例）＝**先不动，只记档**。

**改了 2 处（`engine/combat_parts/daowen_effect.py`）**
1. `:57-72` 扩散名单 `effective` 的构造：`[本次目标（**若不等于施法者**）]` → `[本次目标（为 None 时用施法者）]`；
   去重同时从 `wt not in effective`（走 `__eq__`）改成**同一性**去重（`Entity` 不可哈希，与下方【缄默】全场名单同一写法）。
2. `:967-980` `self_targeted` 白名单分支：原来 `et = caster` 单点挂状态，改为 `for et in [caster] + wave_targets`
   ——与其余状态类同一口径（自己那一份不丢、波及目标额外一份），消掉「同为自我增益、两类行为相反」的不一致。
   白名单 7 条中 **6 条**受影响（贯穿/飞行/滑翔/狂暴/固执/自食）；**必中不受影响**——它走 `:904` 自己的
   `guaranteed_hits` 分支（`grant_bizhong(caster, …)` 恒挂施法者），白名单那一项对它是空转（保留，勿删）。

**验证（三层）**
1. **探针前后对比**（`sim/probe_wave_spread.py`，70 道纹×6 场景×2 份矩阵）：改后矩阵存
   `data/experiments/wave_spread_2026-09-19_after_A.md`（改前那份保留作证据，勿覆盖）。
   基线 **P0/M0 变化 0 条**、单向 **P1/M2 变化 0 条** → A 只在「双方互标」时改变行为，单向性完好；
   互标 **P3 变化 36 条、M4 变化 34 条**。
2. **新增 5 条守卫**（`tests/test_daowen_wiring.py`，15 passed）：①自我增益自己保留＋对方一份；
   ②自疗总量不变且平分（再生2：8 → 4+4）；③白名单也扩散（贯穿）；④**别人挂的标记不扩散**（单向性守卫）；
   ⑤打敌方目标的平分口径不变（杀伐2 打靶怪甲＋标记靶怪乙 → 各 2，施法者不被拉进名单）。
   **负向对照已做**：`git stash` 把引擎改动退回旧实现后，①②③ 转红，④⑤ 仍绿（它们钉的正是「A 不该改变的行为」）。
3. **全量回归**＝基线 `9 failed / 1726 passed / 3 xfailed`（9 条同名，AI/mock 类，非本轮引入）。

**A 的既定后果（用户已认可；供将来平衡测试一并处理）**
1. **自施数值类开始平分**：再生 8→4+4、庇护格挡 4→2+2、净化 异变-2→-1/-1、增殖 血限+2→+1/+1、
   自食 回2→1+1、超频 加速平分。1v1 里等于把一半治疗/格挡送给敌人。
2. **百分比类效果的「池子」随波及目标数增长**（既有设计，非 A 引入）：自愈总量＝各目标按自身已损生命×百分比
   之和再平分（`:364-368`），自施＋1 标记 → 池子 50→100、双方各回 50；`heal_percent` 同款（`:396-400`）。
3. **敌人也进「回复类」下游判定**：`check_cancer` 遍历 `wave_status_targets`（`:450`，`monster_life.py:227`
   的阈值判定＝累计回复达阈值即癌变，轮回者/同伴**直接命零**、怪物增殖）。A 之后互标自疗会给敌人回血 →
   敌人 `total_healed` 累积 → 可被自己的治疗奶死；同理施法者自己在互标情形下也纳入判定
   （此前互标时反而漏掉自己，A 让它与无标记基线一致）。
4. **`wave_spread` 日志假阳性面变大**：**12 条道纹**现在报「扩散→甲,乙」但对波及目标实际什么都没做
   （疯狂/缄默/必中/坠落/分裂/招魂/搏命/消灾/点金/假钞/透支/尸爆）。原因＝日志按**名单长度**记
   （`:80` 数值分支、`:85` 状态分支 `elif len(effective) >= 2`），A 前自施名单只剩 1 人不触发、A 后变 2 人就记了。
   按裁定**先不动，只记档**（将来若要修：改成「按实际落地记」，发射点就是这两行）。

**文档同步（发布正文＝引擎 docstring 首行，单一真源）**
- `engine/daowen.py:147` 波及首行补两条口径：「所有**由你**挂上波及效果的目标（别人挂在你身上的标记不影响你自己的道纹）」
  ＋「[目标]选自己时你自己也保留一份，波及目标各额外得一份」。单向性是本次实验查实、此前正文没写的。
- 波及 **summary 同步扩写**（怪物 prepare 读的就是 summary，这两条口径直接影响它的战术判断）；
  措辞漂移守卫因此**自然通过**（bigram 重叠回到 0.22 阈值之上），无需登记 `WORDING_EXEMPT`。
- `AI_EXPERIENCE.md:314`、`sim/gen_daowen_index.py` 的 NOTES、`全道纹索引.md`（重生成，468 行）同步。

**未做**：平衡数据重跑（用户令：等所有规则完善后再测，届时与 D11 赌命基数一并定）；`wave_spread` 日志改造。

### 6.22 ①-B 第一刀：副本区域「道纹网络」整节改成引擎生成（＋两个生成器共用的解析层）

**范围**：只有**引擎已实现闭环**的四个区域（扭曲都市／罪孽都市／龙心谷／乱葬岗，各 8 条）。
其余四区（永夜庭／沉沦海／荒疫古城／巴别塔）的道纹尚未接入引擎，正文仍是手写设计稿——引擎里没有事实源，
生成等于凭空造，**不生成**（并有守卫钉住这个范围）。

**1. 共享解析层 `sim/daowen_doc.py`（新建）**：首行格式解析、修订注记剥离、闭环链渲染、`NOTES`（引擎机制注记）、
区域→闭环映射。`sim/gen_daowen_index.py` 改为import 它，330→252 行，**输出逐字节不变**（已 `diff -q` 验证）。
为什么单独一层：这套解析若两个生成器各抄一份，改一条口径就要同步两处——①-B 要消灭的正是这个。
顺带查明：「引擎侧权威顺序数据」**已经存在**＝`ResonanceEngine.CLOSED_LOOPS`（4 个区域闭环的
`(src, 残韵类型, dst)` 三元组），与四区文档的链行逐条一致，起点＝第一条边的 src（与文档「（起点/终点）」标注吻合）
→ 6.17 记的「需要一份引擎侧权威数据」这步不用新建，直接用。

**2. `sim/gen_region_daowen.py`（新建）**：把四区的「道纹网络」整节重写成 HTML 注释标记包裹的生成块
（`环形闭环主轨：` 链行＋`道纹定义：` 编号行＋`> 注：` 行）。格式对齐 `engine/rule_sync.py` 的解析口径
（保留它认的两个小节标签；定义行匹配它的标准行正则；注行缩进＋引用号，不会被误抽成道纹或事件）。
`--check` 只校验（漂移即非零退出），`--print <区域>` 看单区结果。

**3. 生成前先把「文档有、引擎没有」的现行口径搬进引擎**（否则生成＝裁剪式删口径，撞 L1040 红线）。
逐条核过实现，**四条都是引擎漏写、无矛盾**，所以没占用裁定：
- 【定型】首行 +「（增减都挡）」——`daowen_effect.py:537` 的 `panel_locked` 同时挡 `attack_boost`（增）、
  `attack_reduction`（减）与【全力】锁定；
- 【分裂】首行 +「[血限]与生命均为10Y」——`damage_death.py:550 _spawn_fenlie_clones` 的 docstring 就是
  「count 个 clone_hp **血限/生命**」，两者都写；summary 同步（原 summary 也漏了生命）；
- 【超频】首行：自由指定目标这条口径此前**只存在于会被剥掉的日期注记里**（发布正文等于没说），
  改写成现行口径「（目标可自由指定：自己/队友/敌人）」；summary 同步；沿革留在 docstring 正文段；
- 【分裂】的 X/Y 语义与「`use_daowen` 额外传 `y`」接口口径此前**只活在 `副本/乱葬岗.md`**
  （索引与《行动接口口径》都没有）→ 搬进 `NOTES["分裂"]`，生成时以注行发布。
- 【变形】的钳制/蒸发与「不填则自身」已在 `NOTES["变形"]`，生成时以注行发布 → 无损失。

**4. 顺手查出两处发布错误（都已修）**
- `NOTES["点金"]` 写「状态【点金】…保留」，但状态名是**【洗劫】**（`cost_payment.py:99 has_status("洗劫")`、
  `events.py:23 帮派令：[战始]获得【洗劫3】`、`物品索引.md:92`）；改名的是**道纹**（洗劫→点金）。
  这条注记此前已随索引发布出去了 → 按引擎更正（手抄注记的漂移，正是 ①-B 要根治的形态）。
- `AI_EXPERIENCE.md:343`【赌命】写「失去30%当前生命」，引擎是「等同30%[血限]的生命」→ 按 `ca056d3`
  的既有裁定（文档对齐实现）补齐，并把发号顺序（从轮回者方开始）一并对齐。**注意**：D11 记的
  「30% 基数（血限 vs 当前生命）等平衡测试再定」不受影响——这里改的是文档与现行实现不符，不是改基数。

**5. 沿革剥离（用户裁定「现在就剥」）**：点金/逼债/分裂 三处 DM裁定注记与「旧版为…」随整节生成消失。
`STALE_PHRASES` +2（点金「与伤害脱钩」用正则 `点金.{0,24}夺取`，避免误伤注记里保留的【洗劫】夺碎片机制；
分裂「本体血限20%的复制体」）；逼债那条 6.15 已登记过，不重复。

**兼容性：谁在读这些文档（生成格式改动的实测后果）**
- `engine/rule_sync.py::extract_daowen_from_file`（运行时按「道纹定义：」标签＋行正则抽道纹，
  喂 `diff_project_daowen`／`extract_project_rules`）：**生成格式一度让【分裂】抽不出来**——
  规范头是 `分裂X/Y`，而它的标准正则只认 `X`。实测 `dungeon_daowen` 64→63、`in_engine_only=['分裂']`，
  `tests/test_rule_sources.py`（钉 64）立刻红 → 把正则放宽为 `X(?:/Y)?`，抽取回到基线
  （8 区×8＝64，逐区域同名同序）。
- 口径数字守卫（`名字X` 与代价 token 的 24 字归属窗口）：分裂的紧凑行落进窗口后，按「每X倍率」模型
  要求写 `衰老10X`，而分裂的代价是**双参数公式** `衰老X×10Y`（旧文档因为名字与代价相距 >24 字而侥幸躲过）。
  改法＝守卫**跳过双参数道纹**（判据 `"y" in resolve(...)`），另加正向守卫
  `test_dual_param_cost_formula_matches_engine`：钉正文含公式串 `衰老X×10Y`，且引擎在 (1,1)/(3,2) 两点
  算出的 `cost_blood_limit == x*10*y`、`clone_hp == 10*y`、`split_clones == x`。跳过≠不看守。

**守卫（新增 7 条，`tests/test_region_daowen_generated.py`）**：①逐字节重生成；②标记唯一；
③块内 8 条与 `CLOSED_LOOPS` 同序＋起点标注＋成环＋每条残韵边都印出；④定义行正文与引擎首行逐字相同；
⑤块内无沿革（日期水印／DM裁定／废止／旧版／原名）；⑥`rule_sync` 抽取兼容（含 64 与 `diff_project_daowen` 齐平）；
⑦未实现四区不得被生成。
**负向对照三条都做过**：手改数字（抵扣 3X→4X）→ ②④ 红；塞日期水印 → 5 条红；回退 `rule_sync` 的 `/Y` 放宽 →
精确红 2 条（抽取相关）。门禁 `sim/check_rule_change.py` 第 1 步新增 **1b**（区域整节同源检查，漂移即 ✗ 并替你重生成，
已用手改负向验证），守卫子集 +2 文件（`test_region_daowen_generated.py`、`test_rule_sources.py`）。

**验证**：全量回归 `9 failed / 1739 passed / 3 xfailed`（9 条与基线逐条同名；通过数 1731→1739＝新增 8 条用例）；
四步闸门全过（含新 1b）；索引与四区正文均与引擎同源。

**过程教训（记档）**：做负向对照时用 `git checkout <file>` 还原，把**未提交**的生成结果与 `rule_sync` 改动
一起回滚到 HEAD 了（罪孽都市/龙心谷 的生成块消失、`/Y` 放宽消失），害我误判「守卫乱红」。
还原未提交的工作一律用 `/tmp` 副本，不用 `git checkout`——与「沙箱重置别用 `git reset --hard`」是同一条教训的变体。

**下一刀（①-B-2，未做）**：`AI_EXPERIENCE.md` 的三节改成生成块——`### 道纹体系`(305)、
`### 副本专属道纹`(322)、`### 原始怪物道纹与转化道纹`(366)。已查明它是**残缺且漂移**的抄本：
每区只列 5–7 条（其余推给「中间节点见《全道纹索引》」一句），【赌命】那条已按上文更正。
注意 `tests/test_rule_sources.py` 钉着 `common_daowen == 38`（从 AI_EXPERIENCE.md 抽的条数），
改这三节必须同步核这个数。

### 6.23 ①-B 第二刀：规则正文三节改成引擎生成（用户裁定「先继续做 B」→ D12 后指定本刀）

- **范围**＝`AI_EXPERIENCE.md` 的三节：`### 道纹体系`（杀伐闭环 11 条）、`### 副本专属道纹`
  （四个已实现区域 32 条）、`### 原始怪物道纹与转化道纹`（原始 7＋转化 19＋净化 1，含 19 条回溯边）。
  生成器 `sim/gen_experience_daowen.py`（新），渲染层复用 ①-B 第一刀的 `sim/daowen_doc.py`
  （新增 `shafa_block/region_exclusive_block/monster_tree_block/monster_back_block`）。
  事实源＝`engine/gamedata.py` 的归属集合＋`engine/daowen.py` 的 docstring 首行＋
  `ResonanceEngine.CLOSED_LOOPS`＋`daowen_doc.NOTES`。4 个标记块（BEGIN/END GENERATED），
  两段手写散文（副本专属的引子、原始↔转化双向路径说明）由生成器持有——与
  `gen_daowen_index.py` 的 `note_lines` 同一种做法（它们不是引擎事实，是设计说明）。
- **格式不是审美选择，是抽取器倒推的**：`rule_sync.extract_daowen_from_file` 运行时就读这份文件
  （从 `### 道纹体系` 抽到 `### 特殊事件`），只认「标准行 `名X：…`」与「内联行 `名X（含消耗/代价）」，
  且内联正则 `([^（）]+)` **不容忍嵌套括号**。于是：道纹体系用标准行、怪物树用内联行、
  副本专属用项目符号 `- 【名】X：…`（抽取器不认＝刻意，区域专属的事实源是索引与副本正文）。
  实测生成前后：抽出 **38 条、名字集合逐名一致、`diff_project_daowen()` 双向为空**（守卫钉死）。
- **踩到并修掉的陷阱**：【眩晕】【必中】的引擎首行带嵌套括号（`（伤害被格挡吃满…）`／
  `（攻击与道纹通用，共用层数）`），生成进树行后内联正则匹配失败 → 通用道纹会静默从 38 掉到 36，
  而 `test_rule_sources.py` 只报「条数不对」看不出是哪条。两条的机制口径改由 `NOTES` 发布
  （渲染成「注：」行，索引与正文都印），首行只留净口径；`WORDING_EXEMPT` 登记【必中】并写明理由
  （措辞守卫 0.21＜0.22，差的就是那个括号）。守卫 `test_tree_lines_have_no_nested_parens` 把这条钉住。
- **两处「发布正文与实现相反」被生成自动修正**（6.16 查出的同类，这次是真矛盾）：
  | 道纹 | 文档旧口径 | 引擎/实现 | 结论 |
  | --- | --- | --- | --- |
  | 疯狂 | 使[目标]出手次数+X | 所有角色出手次数+X（`daowen_effect.py:968-975` 盖全场存活角色，2026-08-17 全局裁定） | 文档陈旧，生成即修正 |
  | 眩晕 | 失去生命后立刻苏醒 | 首行写「受到伤害后解除」，但 `models.py:560-562` 只在 `remaining>0`（格挡吸收＋固执压帽后实际掉血）才摘状态 | **文档对、引擎首行不精确** → 改引擎首行/effect/summary 三处 |
  眩晕这条是「格挡吃满不掉血则不解除」，所以「受到伤害」是错的口径；已按实现更正，
  机制细节发布在 `NOTES["眩晕"]`（不进首行，理由同上）。
- **文档独有的现行口径搬进引擎侧**（`NOTES` 12→18 条，否则生成＝裁剪式删口径，踩 L1040 红线）：
  封印的【镇魔印】联动 5 句、点金的改制理由、龙鳞的「与加害对称」、自愈的已损生命计价、
  滋养的翻倍口径＋组合满血复活＋癌变翻倍（原正文 395-397 三行散文）。净化那条不手抄，
  由 `UNIMPLEMENTED_REGION_EXCLUSIVE_DAOWEN` 派生。波及的 X 上限三句经查 **`NOTES` 里本来就有**
  （6.19 实装时已搬），本轮误加的重复段已删。搬入时按正文红线剥掉日期水印
  （封印原文的「2026-09-16 起无需学习」）。
- **沿革剥离 2 行**：全力（374「【强化】改名…」）、全速（380「由【迟滞】改版…」）——内容已在
  `NOTES["全力"]/["全速"]` 与首行里，生成后正文只留现行口径；沿革仍在引擎 docstring 正文段、
  `archive/`、`check_rule_change.py::STALE_PHRASES`。超频那行的「2026-09-17 用户令」水印同样剥掉
  （首行已有「目标可自由指定：自己/队友/敌人」）。
- **副本专属节的降级名单废除**：旧正文只给 24 条正文，另 8 条（畸变/加害/逼债/假钞/背负/伤痕/分裂/缄默）
  挂一句「效果正文同样见《全道纹索引》」。现在 32 条全部有正文（＋分裂/缄默/尸爆/招魂的长注），
  那份手维护的 8 条名单随之删除——它本身就是漂移源（名单与区域集合的差集要靠人数出来）。
- **顺带修好一个假事件**：手写散文行「两者组合即满血复活：滋养（×2）＋…」被
  `rule_sync.extract_events_from_file` 的第三个模式当成事件名收进结果（42 条）；改成生成块后它是
  `> 注：` 行，抽取器不认 → 41 条。真事件池不受影响（`events.py::parse_events` 走 `EVENT_NAMES` 白名单，
  从来没收到过它）。守卫 `test_three_sections_contribute_nothing_to_event_extraction` 钉「三节零贡献」
  （不钉条数，别处新增通用事件不该让它红）。
- **守卫与门禁**：`tests/test_experience_daowen_generated.py` 13 条（逐字节同源／render 幂等且能冲掉手改
  的负向对照／标记唯一／三节各自的覆盖与顺序／正文逐字＝引擎首行／NOTES 都真的印出来了／
  内联行无嵌套括号／正文红线无沿革／rule_sync 38 条与双向 diff／区域专属不得混进通用／事件零贡献）。
  门禁 `sim/check_rule_change.py` 加 **1c 步**（重跑生成器比对，与索引、副本区域同款）＋把新测试纳入守卫子集。
- **全量回归**：`9 failed / 1752 passed / 3 xfailed`（7 分 31 秒）。9 条＝既有 AI/mock 基线（第二节逐条列明），
  通过数 1739→1752＝本轮新增 13 条守卫，**零回归**。门禁四步全绿。
- **本刀的已知代价（不隐瞒，两条待裁定见 D13/D14）**：
  1. 正文里 8 处术语方括号随引擎首行原样发布而「消失」（再生`[回复]`、贯穿`[伤害]`、封印`[目标]`、
     狂暴/无力`[回始]`、自食/寄生`[回复]`、滑翔`【飞行】`、活血`[目标]`），另有几处数字旁的空格
     （`4X 点`→`4X点`）与全角括号（增殖`［目标］`保留原样，因为引擎首行就是全角）。
     这是 **D12「只记档不改」的连带后果显形在规则正文里**：引擎首行不统一，生成就把不统一发布出去。
     引擎自身也不一致（衰败的首行有 `[回始]`、狂暴的没有）。→ **D13**
  2. 波及定义行少了旧正文的「（按平分规则）」这个交叉引用（引擎首行只写「数值平分」）。
     口径没变（平分规则在《基础定义》里），少的只是一个指路括号；未补，按需可加进 `NOTES["波及"]`。
  3. 六处代码注释引用 `AI_EXPERIENCE.md:1254`（微光者一池制）在**本刀之前就已失效**：
     HEAD 的 1254 行是 ai_preview 盲区笔记，真实口径在 706/725 行。本刀只修了被自己推移的那一条
     （`test_daowen_cost_consistency.py` 的 `:391`→改成按节名引用，因为三节现在会随引擎漂移）。→ **D14**
