# 报告

> **当前未处理事项只在第三节《设计待办》**（D1–D3 方向已定/暂不实装，D4 待重标，D6 已裁定不改）。
> 第 4.6 节的 **Q1–Q12 已于 2026-09-18（夜）逐条裁定并实装**，落点与读数见第六节；该表保留为观测存档。
> 第五节是同日（晚）两条更正的实装记录。
> 新硬伤按「现状 → 为什么是问题 → 建议做法」并附复现/定位命令的格式，进第三节（需裁定的另开表）。

## 一、上一轮待办的落点（仅作索引，无需再处理）

| 项 | 结论 | 落点 |
| --- | --- | --- |
| P1 35 条道纹 `summary` 与程序不符 | 全量同步（裁定 sync_all） | `全道纹索引.md`（生成器区域专属道纹改 `sorted()`，重生成可复现）、`副本/*.md`、`物品索引.md` |
| P2 三只怪物战术被道纹重做打崩 | 怪物 AI 依据道纹语义自行推导战术 | `AI_EXPERIENCE.md`《怪物准则》第 9/10/11 条、`archive/monster_tactics_2026-09-17.md` |
| P3 高爆手雷对轮回者无效 | 重做为「全场 20 伤害；伤害后生命≥50%[血限]给【无力2】，否则【无力1】」 | `engine/api.py`、`物品索引.md`、`tests/test_grenade_action_budget.py` |
| P4 条件触发式道纹冗余清单 | 逐条核实：仅「加害/龙鳞」确证纯对称重复（裁定保留），4 条论断被推翻，另挖出「缄默是死状态」 | `archive/P4_daowen_redundancy_verification_2026-09-17.md` |
| P5 怪物战术文档覆盖率 7% | plan_b | `archive/monster_tactics_2026-09-17.md` |
| P6 战斗行动接口正文无条文 | 分六批补齐：60/64 有专属四要素条文，余 4 条指路专题正文 | `AI_EXPERIENCE.md`《行动接口口径（全部行动接口的四要素条文）》 |
| 附：缄默无消费方 | 已实装（全场封禁「由[命零]触发的效果」） | `engine/combat.py`、`engine/daowen.py`、`tests/test_jianmo_silence.py` |
| 附：怪物出手预算不校验 / AI 用不了需目标的工具 | 两处接口不可达已修 | `engine/combat.py::prepare_monster_phase`、`engine/ai_tactics.py::try_consumable` |
| D4 滋养口径待裁定 | 用户裁定（2026-09-18）＝「消耗2X。使[目标]受到的恢复量翻倍，持续X」——做成**放大器**，不清零累计、不抬阈值、不豁免癌变 | `engine/daowen.py::calculate_ziyang`、`engine/models.py::apply_heal`（统一回复入口结算）、`tests/test_ziyu_ziyang_rework.py` |
| D5 自愈口径待裁定 | 用户裁定（2026-09-18）＝「代价：冷却X。恢复[目标]25X%已损生命」；ROUND_START 上的自愈机制整体删除 | `engine/daowen.py::calculate_ziyu`、`engine/combat.py`（`heal_missing_percent` 分支＋波及平分＋`_monster_max_daowen_x` 崩解线封顶）、`engine/mechanisms/builtins.py`、`机制迁移台账.md`《已回撤》 |
| 附：癌变能否被绕开 | 用户裁定：不加任何新豁免途径，唯一免疫仍是遗物【第一杯】（刻意的强度限制）；滋养×2 同时把癌变进度×2 是预期张力 | `AI_EXPERIENCE.md:396`、`tests/test_ziyu_ziyang_rework.py` |
| 附：残韵 19 条回边不可达 | 选 A：`use_resonance` 改 target-first（自由选目标→再选其身上的道纹），未给目标才回落施法者本人 | `engine/api.py::_find_resonance_holder`、`tests/test_resonance_target_first.py` |

## 二、环境注意事项（每次新会话必读）

- **用户工作原则（2026-09-18）：非必要不修改。** 用户已多次遇到「改来改去最终改回原版」，
  所以现在任何"顺手改一改""统一一下口径"的冲动都必须先报给用户裁定，不得自行动手；
  评估结论若是不改，也要在本节或三节留一行存档，避免下次会话重新提。
- `.venv` 不进快照：新会话需 `python3 -m venv .venv && .venv/bin/pip install -r requirements-dev.txt`。
- **既有失败基线 9 条（AI/mock 类），不是回归**，逐条为：
  `test_ai_basic_attack_candidate::test_at_one_by_one_daowen_still_wins`、
  `test_ai_tactics::test_ai_can_declare_parry_under_lethal_threat`、
  `test_build_learner::test_valid_and_invalid_are_separated`、
  `test_monster_daowen_x_free::test_heal_x_scales_with_missing_hp`、
  `test_unified_ai::test_ai_player_is_the_combat_and_high_level_entrypoint`、
  `test_win_only_ai`（4 条：`test_win_only_includes_parry_in_real_candidate_path`、
  `test_collapse_line_veto_rejects_mutation_suicide`、`test_collapse_line_veto_allows_safe_cast`、
  `test_survival_tiebreak_prefers_later_death`）。
- 全量回归口径：`9 failed / 1706 passed / 3 xfailed`（约 4–5 分钟；2026-09-18 夜 Q1–Q12 实装后实测。
  1697 → 1706 ＝ 本轮新增 9 条用例：`test_monster_phase_order.py`(5)、`test_guard_command.py`(2)、
  `test_employee_economy.py`(2)；9 条失败仍与上面的基线名单逐条同名）。
- **改一条规则后先跑 `PYTHONPATH=. .venv/bin/python sim/check_rule_change.py`（约 7 秒）**，别每次都等 4–5 分钟全量：它会一次做完「索引同步（重跑生成器比哈希）＋12 文件守卫测试子集（对照基线只报新增失败）＋废案扫描＋引擎口径速览」，`--full` 才跑全量。遇到「同 seed 两次跑不一致」跑 `sim/diag_repro.py`（7 层定位＋账本污染审计），不要再靠反复重跑猜层。两者用法与反向对照见 6.11。
- **新增账本（战斗运行态容器）只改一处**：`engine/ledger_isolation.py::COMBAT_LEDGERS` —— AI 预演隔离、死斗推演隔离、`sim/diag_repro.py` 的 L7 垃圾键审计同源；`tests/test_ledger_isolation.py` 会**静态**扫 `engine/combat.py` 的容器创建点，未登记即失败。详见 6.12。
- 平衡模拟：`.venv/bin/python sim/run_sim.py all 20000`（种子固定 2026，约 1 分钟）；
  工具库只在扭曲都市发放，改道具后另两副本应保持不变，可当镜像未外溢的反证。

## 三、设计待办（未实装，**不是**现行口径；2026-09-18 用户令开此节）

> 本节收纳「方向已定但口径未裁定」与「暂不实装」的设计。正文（`AI_EXPERIENCE.md`）与
> `全道纹索引.md` 按红线只写现行口径，所以未实装设计一律记在这里，不进正文。

### D1 攻伤×当前生命 —— 不做全局公式（用户已裁定），只允许遗物/道纹级覆盖

- 裁定：**不动核心伤害公式**（普攻伤害 = [攻击次数]×[攻击力]，攻击力 = 当前法力）。
- 允许的形态：做成**遗物或道纹**级别的局部覆盖，复用 `engine/models.py::effective_attack_power`
  的状态层机制（例如「攻击力 = 当前生命×k%」），只对该持有者生效。
- 为什么不做全局（四方案结论存档）：a 比例乘区 → 双向死亡螺旋＋血限双扣；b 绝对加成 →
  怪物血限 192~552 会碾平玩家；c 阈值反螺旋 → 残血增伤留翻盘，但改的是全局节奏；
  d 遗物/道纹级覆盖 → 唯一不污染 79 只怪物输出、不与审计口径
  `ceil(血限/6)+2×攻次+2×攻击力` 双计、不要求 `sim/balance_sim.py`（自带 `m.attack_power`
  伤害模型，不跟随核心公式）重标的方案。
- 实装时需一并处理：血限在审计口径里已计价，覆盖式攻击力要避免与「血限→攻力」双计。

### D2 美杜莎之眼 —— 从 `物品索引.md` 草案区移入此处（用户裁定：先移到设计待办，标明原因）

原文（巴别塔草案，移入前逐字保存在此，未实装、引擎零引用）：

> ### 美杜莎之眼
> 选择一名[目标]，你可以消耗等同其10%当前生命的[石块]耐久，使其立刻触发【雕塑】。雕塑对轮回者同样生效。
> > 来源：巴别塔草案

**为什么移出来（现行规则下它的三个前提都已失效）**：

1. 「雕塑对轮回者同样生效」是**冗余句**：2026-09-09 DM裁定后 `combat.py::_can_be_sculptured`
   对任何角色都返 True，`settle_victory_paths` 第二个循环确实在雕塑玩家侧。
2. 它卖的效果**能被免费复现**：属性统一后 攻次=当前速度、攻力=当前法力
   （`AI_EXPERIENCE.md:1235`），把目标两个池子同时打到 0，回终自动雕塑。速度是唯一行动池
   （减速 10X%／疲惫／透支／萎缩溢出都在抽它），法力被普攻抽（攻伤=当前法力）。
3. **代价算不过来**：石块耐久 = ceil(血限×5%)（`combat.py::_sculpture_monster`），
   遗物却要 ceil(目标当前生命×10%) → 对同阶满血目标一次要 **2 块石块**；怪物血限 192~552
   即单次 19~55 点耐久，而每点耐久本来值 15 伤害／20 格挡，等于烧掉 300~800 伤害换一个
   **不给碎片**的移除，且移除后只返还 ceil(血限×5%)（付 2 拿 1）。文案还**没有部分支付条款**：
   耐久不够就直接不能用。只有目标已掉到石块来源怪血限 ~50% 以下才付得起，而那时直接杀更划算。
4. 仅剩的独占价值：①「立刻」，不必等回终、不必自己耗资源把两池打空；②对满血高攻目标也能强推；
   ③死斗里雕塑对手是列名的多路径胜利（`api.py` 回终 note「多路径胜利（雕塑/癌变/还债）已结算」）。

**处置意向（用户原话意旨）**：等实测**轮回者雕塑流**时发现缺哪个机制，就把这件遗物的效果
改成那个缺失的机制——比现在凭空瞎想要靠谱。届时可直接复用的现行【雕塑】口径：
`AI_EXPERIENCE.md:414-415`（任意角色攻次与攻力同时归 0 → 化为雕塑消耗品 X/Y，Y=ceil(血限×5%)，
每点耐久 15 伤害或 20 格挡；不视为击杀、不提供碎片）。

### D3 尸爆 —— 重做待议（用户：现有方案都看不上，先放待办）

现行正文：「X：消耗3X。[命零]对所有敌方[目标]打出自身[血限]的10X%伤害。」

**无论怎么重做都必须一并修掉的硬伤**：

1. **侧别 bug**：`engine/combat.py` 的尸爆结算固定打 `self.state.get_all_enemy_side()`，
   而它恒返 `state.enemies`（永远是玩家视角的敌方）→ **怪物（不死骸骨／尸霸）发动尸爆时炸的是自己那一侧的怪物**。
   修法：侧别改为相对施法者（「你方之外的所有存活角色」）或相对尸体。
2. **母子语义脱节**：【分裂】已于 2026-09-17 重做为即时结算（造 X 个 10Y 血限复制体、代价衰老 X×10Y），
   尸爆作为它的转化产物仍绑在 [命零] 上，母道纹已经没有"死后触发"的身份了。
3. **代价时点怪账**：3X 法力在"人都要死了"的那一刻支付；若改主动引爆则应在发动时预付。
4. 与【缄默】的边界：缄默封「由[命零]触发的效果」，尸爆整条都在封禁范围内
   （`combat.py` 已有 `qianmo_blocked` 分支）；若重做为非[命零]触发，需明确缄默还封不封。

**已被否的方案存档**（勿再提）：P1 点尸引爆／P2 同归于尽 50X%／P3 每尸炸随机敌；
2026-09-18 批次：尸-A 连锁（任一敌方命零→其余敌方全体）／尸-B 延迟引爆（下一[回始]炸，尸体被移出则不炸）／
尸-C 主动自爆（立刻[命零]，打「你方之外」全体）。

### D4 自愈/滋养重做的连带待办 —— 单卡评分与承载怪战术需按新语义重标

- `data/daowen_single_scores_20260914.json`（`protocol.kind = single_daowen_baseline`，runs=6）是**旧语义**下测的：
  自愈当时排 51、pve 0/6 胜（旧版是[回始]自奶→自我癌变定时器，见 D5 事实2）。重做后该分数不可再用于平衡决策。
  重测入口：`.venv/bin/python sim/build_learner.py --audit-all-daowen --audit-runs 6 --audit-output data/daowen_single_scores_<日期>.json`
  （单卡基准不代表构筑内边际贡献；滋养现在自身不回血，单卡跑分必然偏低，读分时要记住它是**放大器**、
  价值来自与治疗来源配对——与自愈2 配对即满血复活）。
- 5 只承载怪（白骨祭坛、肠水母、血肉巨囊、打手头目、背碑人）的战术口径需复核：怪物发动自愈＝
  **冷却X 场＋家族税异变5X**，且必须显式选定[目标]（`prepare_monster_phase` 会给 `target_options`，含它自己）；
  崩解封顶已把家族税计入（`combat.py::_monster_max_daowen_x`），异变层数高时可用 X 会自动收窄到 0（＝摘除该选项）。

### D5 本轮查实的现行事实（存档，防丢失）

1. **滋养是双刃刀（×2 放大器）**：结算点在 `models.py::apply_heal`（统一回复入口），覆盖**战斗内**的一切来源
   （道纹／消耗品／寄生…）；过量部分同样翻倍计入 `total_healed` → 对怪＝癌变进度×2（癌变不给[碎片]、
   局外【休整】永久+8），对轮回者/[朋友]/[员工]＝更快直接[命零]。同名重复施放**不增强倍率**（恒×2），只叠加持续时间。
   **它不加成局外【休整】**（用户 2026-09-18 指出，已查实）：滋养是 `StatusEffect(scope=BATTLE)`、持续X回合，
   [战终]统一清场只保留 COST/RUN/PERMANENT 三种 scope（`api.py:5292-5313`），而【休整】是战前行动
   （`api.py:1408 _pre_battle_xiuzheng`）→ 两者永不同时在场。此前正文与两处代码注释误写「覆盖…休整」，已更正。
2. **旧自愈机制是怪物的自杀定时器，已整体删除**：旧版[回始]自奶走统一 `heal` 动词、过量照原值累计，
   癌变阈值只有 ceil(血限×2) → 承载怪 ceil(20/X) 回合后必然自我癌变（X=3→7、X=5→4、X=9→3），
   叠加异变5X/次与崩解线50，第二次发动还会直接崩解；两条路都永久离场且不给[碎片]。
   新版按**已损生命**计价、代价改冷却，两条自毁路径都消失。旧【自愈】状态即使残留在存档里也不再有任何
   [回始]结算（`tests/test_shuai_bai_migration.py::test_legacy_ziyu_status_is_inert_and_shuaibai_still_first` 钉死）。
3. **怪物「家族税」已删除**（用户令 2026-09-18，实装见第五节 5.1）：现行口径＝怪物发动道纹只按该道纹
   **自身正文代价**支付，与轮回者/[朋友]/[员工]同口径走统一代价总线 → 自愈对怪物只付【冷却X】、不产生异变层数；
   狂暴/全力/疯狂/减速/必中/飞行 的【异变5X】是它们**自身**的代价（不是家族税），照旧支付、照旧受崩解线封顶。
4. **尸爆侧别 bug**（详见 D3）。

### D6 冷却X 改「每回合计时＋跨场继承」—— 已评估，用户裁定**不改**（2026-09-18）

用户提议把【冷却X】从「X 场战斗」改成「每回合-1、归零即可再用、剩余冷却跨场继承」，
评估后以「太麻烦、非必要不修改」否决。存档评估结论，勿再提：

- **现状即已跨场**：递减点在 `engine/api.py` 的 [战终] 统一清理块（只遍历 轮回者/[朋友]/[员工]，
  **怪物不递减**＝一场一次），实例常驻、[战始] 不重置；闸门只有 `models.py::can_use()`；
  `X_LIMITS["冷却"]=7`。
- **共用这个代价类型的一共 5 条**：固执X（冷却X｜单次失血上限1，持续X）、束缚X（冷却2X｜无法行动，持续X）、
  全速X（冷却X｜攻次=速限，持续X）、畸变X（冷却X｜[回终]扣目标 攻力×攻次 的[血限]，持续X）、
  自愈X（冷却X｜恢复25X%已损生命，瞬发）。
- **只换计时单位会正面破红线**：实测战斗长度（`sim/run_sim.py` 300 局/287 场，粗模型仅作量级参考）
  均值 8.6 回合、中位 7、p90 13；而上面 4 条的**冷却≈持续**，按回合计时后 固执1＝每回合重开
  （接近绝对无敌）、束缚1/2＝目标 50% 回合无法行动（永久控制）、全速X/畸变X＝100% 覆盖，
  直接违反 `AI_EXPERIENCE.md:731`「禁止绝对无敌、绝对控制…擅自按回合递减跨战斗冷却」与
  L279《怪物准则》「严禁单场内重复复读发动」。要改就必须同时重标 5 条的冷却倍率
  （建议红线：冷却回合数 ≥ 持续回合数×3，控制类再+1），连带改 4 处正文、
  `test_daowen_cost_consistency.py::SUMMARY_LEAD["冷却"]`（硬钉「冷却N场」）、
  `validator.py:440`／`watchdog.py:149` 措辞、索引与台账、`sim/balance_sim.py` 激活模型。
- 顺带查实的自愈自平衡性质（存档）：按回合计时时，自愈X 的**每回合回复率＝25X%÷X＝恒为已损25%**，
  与 X 无关（X 大＝大爆发＋等更久），所以它本身不需要重标；怪物侧复读会被家族税异变5X/次
  自然限死（自愈1 每回合复读＝10 回合撞崩解线50）。

### D7 `api.py` 的失败事务回滚只覆盖 3/9 本账本 —— 已查证，未改（属另一裁定）

- 位置：`engine/api.py::_snapshot_combat_runtime`／`_restore_combat_runtime`（供**失败事务回滚**，
  不是副本世界隔离）。
- 现状：只快照/回滚 `_monster_activated`、`_resonance_rewrites`、`_sanxiang_consumed` 三项；
  `_monster_daowen_round_used`、`_monster_evolved`、`_split_clones_spawned`、`_effect_chain_depth`、
  `_dodge_counts`、`_dodge_round` 不在内 → 一次失败提交回滚后，这几本账可能保留提交过程中的写入。
- **为何没顺手改**：它的口径与「副本世界隔离」不同——回滚会重建实体对象，所以它把 id 键翻成
  `(kind, index)` 稳定引用、还原时再翻回 id；扩覆盖面＝改回滚行为（可能让「本回合已发动」被回滚掉，
  进而改变怪物能否再发动），属行为改动，按「非必要不修改」先报裁定。
- 已做的防护：`tests/test_ledger_isolation.py::test_api_rollback_ledgers_are_a_subset_of_the_spec`
  钉住「它覆盖的账本名必须在权威清单内」，防两套口径彻底分叉（覆盖范围可以不同，名字不能陌生）。
- 若要处理，建议顺序：先写一条复现用例（构造一次失败提交，断言 `_monster_daowen_round_used`
  是否残留、残留是否造成可观测后果），确认有真后果再决定扩不扩。

## 四、手操轮回实测观测（2026-09-18，扭曲都市 7 场全生还）

数据来源＝`data/handplay_20260918/trace.jsonl`（511 步，`rng_seed=20260918`，驱动器 `sim/handplay_cycle_20260918.py`）。
本节只记录观测与待裁定问题，**未改任何规则/数值**（遵循第二节「非必要就不修改」）。

### 4.1 局概览（战始HP→战终HP、结局、碎片）

| 场 | 怪（血限/攻力×攻次） | 回合 | 我方HP | 结局（引擎判定） | 碎片 |
| --- | --- | --- | --- | --- | --- |
| 1 | 骨天使 222/3×7 | 5 | 60→48 | 手雷【无力2】清空出手 → **凡庸炸裂**（+【残骸】1/1） | +20 |
| 2 | 奇美拉 228/2×9 | 3 | 50→50 | 自施【变形】两次 → 攻次攻力归0 → **雕塑**（得奇美拉雕塑12/12） | 0 |
| 3 | 奇美拉 228/2×9 | 2 | 52→52 | 普攻+雕塑9发抢在自雕塑前 **命零** | +20 |
| 4 | 奇美拉 228/2×9 | 4 | 54→48 | 残韵【转换】剥掉变形（→我得定型）断自毁线后 **命零** | +20 |
| 5 | 血肉巨囊 252/1×8、肠水母 204/1×12 | 12 | 61→**1** | 巨囊每回合刷新爆裂＝付光法力 → **凡庸**；水母打到7.8% → **救赎·接纳**（员工「溺」） | +41 |
| 6 | 奇美拉、肠水母、奇美拉 | 9 | 53→49 | 两只奇美拉 **自雕塑**（各12/12）；水母剥自愈后打到7.8% → **救赎·接纳**（员工「溺」→b7 阵亡） | 0 |
| 7 | 人头气球×2、眼树×2 | 10 | 69→69 | 气球×2 **命零**；眼树①**救赎·接纳**（员工「盲」，战终 攻次1→2）；眼树②**救赎·终结** | +59 |

合计：普攻 1920 伤、消耗品 545 伤（雕塑 450／电击枪 75／手雷 20）；招架 12 次；碎片收入 160。
七场打完 `player_dead=False`，引擎滚入新轮回（`phase=setup`、`battle=0`、碎片重置 20、精力 3）＝晋级。

### 4.2 道纹／残韵使用率（实测计数，含"存而不用"）

- **发动过的道纹只有 4 次**：透支3（×1，**满蓝白放**，见 4.5）、自愈1（×1）、固执1（×1）、束缚1（×1）。
- **持有却全程 0 次发动**：封印（b3 学习所得）、定型（b4 残韵所得）、滋养（b6 残韵所得）、庇护类未学。
  → 学习/收割的产出有相当比例是"入库不用"；`封印` 的正文是「使一个目标怪物延后X回合再入场」，
  对已进场怪是否合法我当时未验证（怕白付异变），这直接压低它的使用率。
- **残韵只用出 4 次，全是【转换】**：变形→定型 ×1、自愈→滋养 ×2、滋养→自愈 ×1（收割自愈两步法成立 ✓）。
  **【反转】【曲解】全程 0 次**，末了还剩 曲解2／转换1／反转2＝5 枚未花。
  原因：本局怪物池反复出现奇美拉（5 次）与自愈承载怪（3 次），转换边（定型／滋养／滑翔）刚好都用得上；
  反转边（衰败／自残／蒙蔽／无力）与曲解边（无神／退化）没有遇到匹配的承载怪或代价不合算（衰败消耗4X＝自砍攻力）。
- **残韵经济实测**：三相残韵盘每场净 +1 枚（战始消耗1、战终得2）；事件「真拒绝」另送随机 1 枚（`AI_EXPERIENCE.md:494`）。
  7 场下来残韵从未成为瓶颈（末场还余 5），瓶颈反而是"没有合适的边可打"。

### 4.3 怪物自毁线：本次 12 只怪里 7 只不是我打死的

- **凡庸 3 只**（骨天使＝被手雷无力2 削掉出手；血肉巨囊＝每回合刷新爆裂付光法力；员工溺＝我漏调 `resolve_ally_phases`）。
- **雕塑 3 只**（奇美拉：自施变形两次 → 攻次攻力归0）。
- **救赎 3 只**（肠水母×2 接纳、眼树×1 接纳＋×1 终结）。
- 真正"打到命零"的只有 4 只（奇美拉×2、人头气球×2）。
- 根因分三层（可复算），**第三层是本局的主因，也是本仓当前的接线缺口**：
  1. 怪物每回合必须发动一种道纹（有候选时道纹提交是强制的，`engine/combat.py:4353/4508`）→ 它没有"只普攻"这个选项；
  2. 道纹代价与攻力同源（法力＝攻力），而怪物出厂自带遗物【某人的偏爱】每[回始]法力回满
     （`AI_EXPERIENCE.md:278/698/784/1385`、`物品索引.md:261`）→ 它敢一直花，但花光那一回合就打不出伤害，
     于是踩【凡庸】（连续5回合未使敌掉血）；【变形】更狠：付光法力后互换，速度归0 且持续9回合 → 攻次+攻力双0 → 【雕塑】；
  3. **X 一律拉到可负担上限**：`engine/combat.py:5549-5560` 明确「提交方没有给 X 时回退到可负担上限而非报错，
     想要更精细取值的 AI 自行提交 x 即可（见 `sim/duel_common.py`）」；而战术选 X 的
     `sim/monster_targets.py::pick_monster_daowen_x`（2026-09-16 用户令选案 C，**评分返回 None 的档位＝会把自己玩死，直接跳过**）
     **只接进了 `sim/duel_common.py`（死斗）**，`sim/build_learner.py::_resolve_monster_turn`（主怪物驱动）从不提交 `x`。
     本局手操照 build_learner 口径拼装 → 实测证据：奇美拉【必中】异变+45＝5×9、【变形】消耗9法力、
     血肉巨囊【爆裂】消耗8法力＝2×4，全是上限值。
- **因此 4.3 的"7 只自毁"只在 max-X 口径下成立**：若走战术选 X，必中9（异变45→崩解45/50）会被评分跳过，
  变形1 只付1法力（速度2↔法力8 → 攻次8×攻力1＝8伤，不是0），爆裂1 只付2法力（攻力仍6）→ 大概率不雕塑、不凡庸。
  连带影响本局经济：3 个奇美拉雕塑＝36 发×15＝450 伤，占我消耗品总伤 545 的 82%，而 b2/b6 两场 0 碎片也由此而来。

### 4.4 经济与物品

- **消耗品不占出手**（`AI_EXPERIENCE.md:580`）＋残韵不占出手（L626）→ 单回合爆发只受耐久限制：
  b7 R10 我一回合内 8 发雕塑（120）＋普攻 64＝184 伤，把 192 血的眼树②压到 8 → 触发救赎线（≤19.2）→ 我选终结。
  （上一版本文写的"9 发雕塑 135 伤打穿眼树"是错的：第 9 发被引擎以「必须先结算【救赎】」拒掉，seq502。）
- **雕塑是全场最大单件输出**：3 只奇美拉自雕塑 = 36 发 × 15 伤，我共用掉 30 发（450 伤），末场仍余。
  对照：雕塑路径 **0 碎片**（`leave_no_shards`），命零路径 19–21 碎片。
- **b7 逐怪伤害归属（trace 复算；回答"是不是直接用石头把四个怪砸死"）**：

  | 怪（ref） | 普攻 | 石头 | 合计 | 结局 |
  | --- | --- | --- | --- | --- |
  | 人头气球①（enemy:0） | 160 | 60 | 220 | 命零（普攻收尾） |
  | 人头气球②（enemy:2） | 160 | 30 | 190 | 命零（**最后一发是石头**，seq484 `target_died=true`） |
  | 眼树①（enemy:1） | 176 | — | 176 | 打到 ≤10% → 救赎·接纳（得员工「盲」） |
  | 眼树②（enemy:3） | 64 | 120 | 184 | 石头压到 8/192 → 救赎·终结（seq504，按命零给 19 碎片） |
  | 员工「盲」 | 28（打在人头气球上，日志按名字记、未细分 ref） | | | |

  b7 我方总输出 798＝普攻 560（70%）／石头 210（26%）／员工 28（4%）；
  全轮回总输出 2503＝普攻 1920／石头 450／电击枪 75／员工 38／手雷 20。
  → "用石头砸死四个怪"不准确：石头只占 b7 四分之一，但**确实是它收掉了人头气球②、并把眼树②压进救赎线**；
  四只怪没有一只是纯石头打死的（两只气球以普攻为主，两只眼树走救赎离场）。
- **碎片收支**：收入 160（20/0/20/20/41/0/59）；支出＝学习2档10＋雇佣医生10＋修行2档×3＝45＋休整3档25＋工资12＝102。
  精力每场固定回 3、**不累积** → 局外必须花光（我 21 次子行动：休整7／修行6／探索6／学习2）。
- 修行兑换实测顺手：2点=1法限（攻力＝当前法力，故 +1 法限同时 +1 招架减免/击）；1点=6血限。
  法限 4→8 后普攻从 32/回合 涨到 64/回合，招架减免 4→8（能把攻力≤8 的怪整击抹平）。

### 4.5 手操失误（AI 侧，不是规则缺陷，逐条存档防重犯）

1. **b1R1 透支3 满蓝白放**：法力受法限封顶 → 12 点流血换 0 法力还搭 1 出手。透支是回蓝工具不是扩池器。
2. **b2 雇佣医生时碎片归零** → 战终工资门槛（12）**先于**碎片奖励结算 → `pay` 被引擎拒（"碎片不足…请改为提交 refuse"）
   → 只能拒付 → 医生离队＋失信 1 击。教训：派员工前必须留出 ≥12 碎片（`sim/build_learner.py` 早有该预留逻辑）。
3. **b5R3 误判爆裂可被招架削**：爆裂写的是「攻击者**失去等量生命**」，招架只减「受到的伤害」→ 反伤绕过招架，
   我 60→3/64 险死。能挡它的是【固执】（单次失去生命最高为1）与【蒙蔽】（造成伤害无效）。
4. **b7 漏调 `resolve_ally_phases`** → 员工溺连续 5 回合未出手 → **凡庸炸裂**（护卫不占它的出手，但它仍须自己出手）。
5. **两次把消耗品/残韵打到错误的 enemy 下标**：已死怪物仍占 `enemy:0/1/2` 槽位 → 「找不到target_ref」；
   按名字走的 `prepare_attack` 不受影响，按 ref 走的 `consume_item`/`use_resonance` 会静默打空（耐久/残韵会退还）。

### 4.6 待用户裁定的问题（Q1–Q12）

按「非必要就不修改」，以下 12 条只列观测与两种可选方向，等裁定后再动；不裁定的就保持现状。
（用户 2026-09-18 反馈「Q5 以后在报告里没看到」＝上一轮把 4.8 整节插进了本表 Q4 与 Q10 之间，markdown 表被截断；现已重排为 4.6 完整表 → 4.7 → 4.8，见 5.3。）
用户已看过 Q1–Q4：Q4 的落点是 Q10＋Q11（都属怪物 AI/引擎逻辑），Q1–Q3 尚未裁定。

**2026-09-18（夜）裁定全部到位**：Q1／Q2／Q3／Q4／Q7／Q8／Q9＝**B（保持现状，不改）**；
Q5＝**护卫直接算[员工]/[朋友]的一次出手**；Q6＝**A（工资改到碎片奖励之后）**；
Q10＝**「不是自定义 X 值的错，是怪物 AI 太蠢」→ 修 AI（战术选 X 接进驱动）**；
Q11＝**不硬编码顺序，怪物 AI 自己选先攻击还是先道纹**；Q12＝**废案清理干净**。
实装面、探针读数与回归结果见第六节。

| # | 观测 | 若要改的方向A | 方向B（保持现状的理由） |
| --- | --- | --- | --- |
| Q1 | 爆裂反伤＝「失去生命」，绕过招架，只有固执/蒙蔽能挡 | 在招架正文补一句「同时削减『失去生命』类反伤」 | 现状即刻意区分"伤害/失去生命"两条轴，固执正是它的答案 |
| Q2 | 雕塑路径 0 碎片但给 12发×15伤，明显优于命零的 19–21 碎片 → 玩家会主动养雕塑 | 雕塑耐久或伤害下调（如 12→6 发） | 雕塑是"放弃经济换即时战力"的取舍，末场我确实用它打赢了四怪局 |
| Q3 | 消耗品不占出手 → 单回合可连发 9 发雕塑，爆发无上限 | 加"每回合消耗品次数上限" | 耐久本身就是预算；加上限要动 `consume_item` 主路径与全部测试 |
| Q4 | 怪物裁定 AI＋道纹代价 → 12 只怪 7 只自毁（3 凡庸/3 雕塑/3 救赎） | ~~给怪物 AI 加"不自废"约束~~ **已查明不是优先级表的问题**：主因是 X 一律开到可负担上限（见 Q10），次因是怪物阶段硬编码「道纹先于攻击」（见 Q11） | 自毁线本身是玩家可经营的策略面（我这局正是靠它过关） |
| Q5 | 纯护卫型员工必被凡庸炸裂（护卫不占出手，5 回合未出手即死） | 护卫视为出手／待命与护卫中的员工不计凡庸 | 也可视为"员工必须参战"的既定口径，只是需要写进正文提示 |
| Q6 | 工资门槛先于碎片奖励 → 碎片不足时雇佣＝必然拒付＋失信 | 工资改到碎片奖励之后结算，或允许欠薪 | 现状是刻意的经济闸门（`WAGE_CAP=12`＋失信三振），改序会动 `battle_end` 主流程 |
| Q7 | 三相残韵盘在**最后一场**是净亏损（战始消耗、战终兑现） | 末场自动跳过／正文补一句提示 | 玩家可自行 `use=false`（我 b7 就这么做），属可学习成本 |
| Q8 | 事件「真拒绝」送随机残韵 → 叙事型选项（写信/接电话/流血换答案）永远劣选，我 3 次全拒 | 给叙事选项一点机械价值（如信件＝下一轮回开局提示） | 真拒绝奖励是既定裁定（L494），叙事选项本就定位为角色扮演成本 |
| Q9 | 坏死（6回合无法获得回复）＋寄生（我受伤80%转成它回复）叠加 → 自愈/血泵/休整全废，b5 打到 1/64 | 给"无法获得回复"设一个下限或豁免（类比第一杯） | 两件都是水母的既定道纹，叠加本就应当致命；我靠招架+束缚+凡庸线活下来了 |
| Q10 | **战术选 X 只接进死斗驱动**：`pick_monster_daowen_x` 未在 `sim/build_learner.py::_resolve_monster_turn` 使用 → 引擎缺 x 回退上限 → 脚本轮回与手操里怪物一律开满 X 自废/自爆（本局 3 雕塑＋2 凡庸＋崩解45/50 均由此） | 把 `pick_monster_daowen_x` 接进 build_learner（照 `duel_common.py:97-103` 的写法，仅 `x_free` 时提交 x） | 引擎回退上限是 2026-09-16 面板去 X 时刻意的"不跑坏"兜底；但代价是**所有脚本轮回的平衡数据都偏向怪物自毁**，本局即样本 |
| Q11 | **怪物阶段硬编码「道纹先于攻击」**：`_execute_monster_phase` 先结算道纹、再跑攻击循环；而轮回者的两次出手顺序是自由的（`prepare_attack`/`use_daowen` 各自独立）。《怪物准则》第8/11条只规定「2 次出手＝1 攻击＋1 道纹」与预算预留，**没有规定先后**。后果＝怪物为道纹付光法力的那一回合普攻必然 0 伤（攻力＝当前法力）→ 血肉巨囊连刷爆裂 5 回合直接撞【凡庸】 | 把攻击循环挪到道纹结算之前（怪物先打再花）：单回合法力型道纹（爆裂/超频/急速/坏死…）不再自废，本局 2 条凡庸线里至少 1 条消失 | 代价是【必中】的"下X次选择目标无法闪避"从**下一回合**才生效（现在同回合普攻就吃得到）；【变形】仍锁死后续 9 回合（速度0→攻次0），只是把【雕塑】换成【凡庸】。改序要动 `_execute_monster_phase` 主流程＋所有按 prepare 快照提交的驱动（命中数/出手数契约），影响面比 Q10 大 |
| Q12 | `全道纹索引.md:164`（生成器 `sim/gen_daowen_index.py` 的 note_lines）仍写「怪物道纹递增：每实际发动一次该道纹本场 X 累加+2×副本阶级」，而递增机制 2026-09-16 已废止（现行《怪物准则》第9条＝面板不写 X、发动方自选）；`sim/prompt_slim_apply.py` 的旧提示词 blob 同样残留（含已删的局外【领悟】、净化5X 等旧口径） | 删掉生成器里这条 note 并重生成索引（`tests/test_daowen_cost_consistency.py` 守卫逐字节一致）；`prompt_slim_apply.py` 是否一并清理另议 | 视为历史档案不动——但索引是**注入 AI 提示词的派生文档**，读到它的 AI 会按递增预测 X（本轮我只改了家族税那两句，递增这句按「非必要不修改」留着等裁定） |

### 4.7 顺带查实的现行事实（存档）

- 凡庸/崩解/雕塑/癌变四条自毁线里，**凡庸与命零给碎片，雕塑与救赎（接纳）走 `leave_no_shards` 不给碎片**；
  癌变怪不给碎片但永久 +8 休整恢复量（本局未触发，`死者之书.md` 无新增）。
- 救赎触发点＝打到 **≤10% 血限**且该怪无原始道纹；`option=1/接纳` 得到的是**半血员工**
  （肠水母 204→102、眼树 192→96，攻力也减半：12→6、13→7），`option=2/终结` 走正常命零管线给碎片（眼树 19）。
- 员工战终成长：盲 存活 1 场后 `ally_growth: 攻击次数2`（1→2）。
- 招架实测：减免＝结算时当前法力，对"受到的伤害"生效（b7 法限8 时把攻力8 的怪整击抹平）；
  对"失去生命"类（爆裂反伤、透支流血）不生效。
- 怪物为发动道纹付法力＝自削攻力，这是本局多只怪输出归零的直接原因（超频10/急速10/爆裂8/变形9/增殖8/坏死12/定型2X）。

### 4.8 探针：把战术选 X 接上，能救回哪几条自毁线（机制验证，非轮回数据）

造场法照 `tests/test_action_budget.py`（`battle_start` 后替换 `state.enemies` 为池内真怪），
怪定义取自 `parse_monster_pool("副本索引.md")["扭曲都市"]`，对比「引擎缺 x 回退的可负担上限」与
`pick_monster_daowen_x` 的战术取值（探针库 `data/test_probe_xpick*.db`，已被 `.gitignore` 的 `data/test_*` 排除）：

| 怪·道纹 | 缺 x 回退 | 战术选 X | 后果差异 |
| --- | --- | --- | --- |
| 奇美拉·必中 | 9（异变+45 → 崩解45/50） | **1**（异变+5 → 崩解5/50） | **接上就不会把自己推到崩解线** ✓ |
| 奇美拉·变形 | 9（消耗9法力 → 攻次0） | 9（同上） | **接上也救不回来** ✗ 见下 |
| 奇美拉·飞行 | 9（异变+45） | 9（同上） | 单步预演看不见"下一次异变就撞线" ✗ |

- 变形仍取上限的原因（读码结论，未再跑第二次探针）：`pick_monster_daowen_x` 对每个候选档位真实预演，
  **预演失败即 `continue` 跳过，三档全废则回退上限**（`sim/monster_targets.py:169-176`＋`best=max_x` 初值）。
  而各驱动拼命中数用的是 `hits_n = 怪的 attack_power`（`sim/build_learner.py:393-396`），这个覆盖只对上
  变形 X=上限 的互换结果（法力9 → 攻次9）；X=1 时互换后攻次是 8，预演提交被「每个攻击出手必须提交N次命中选择」拒
  → 低档位全被跳过 → 回退 9。**即：变形的战术选 X 事实上不可达，死斗路径同样中招。**
- 飞行取 9 属评分视野问题：`pick_monster_daowen_x` 是**单步预演**（其 docstring 自陈），
  看不见"这一手 45 异变之后，下一手任何异变道纹都会撞崩解线 50"。
- 结论：接线（Q10 方向A）能消掉**异变型自爆**（必中/飞行 的 5X 是它们**自身**的正文代价，不是家族税——
  家族税只额外压在【自愈】身上，已于 5.1 删除），但消不掉**法力型自废**
  （变形/爆裂 这类把攻力/攻次付空的）——后者要么按 X 逐档重算命中数（改 3 处驱动的拼装口径），
  要么给怪物 AI 增一条"发动后攻力或攻次将归 0 则改选/降档"的护栏（属新规则，需裁定）。

## 五、2026-09-18（晚）已实装的两条更正

### 5.1 删除怪物「家族税」（用户令：「删掉怪物莫名其妙的家族税」）

**被删的旧口径**：`combat.py::_resolve_monster_daowen_choice` 里两个硬编码支付点——
「原始怪物道纹每次发动付异变5X」与「怪物侧【封印】付异变X」，与道纹自身代价**并存**；
同时 `apply_daowen_effect` 的 `cost_mutation` 分支带 `caster.entity_type != "怪物"` 限定，
把怪物排除在统一代价总线外（防双付）。净效果＝【自愈】这类自身代价为【冷却X】的道纹，
在怪物侧被额外扣一份异变5X（报告 D5.3 记的"怪物侧代价是双份"）。

**现行口径**：怪物发动道纹只按该道纹**自身 calc 声明的代价**支付，异变代价统一走代价总线
（`combat.py:3426`），怪物与轮回者/[朋友]/[员工]同口径（《怪物准则》第5条「发动代价类道纹时必须照常支付对应代价」）。

| 道纹 | 删除前（怪物侧） | 删除后（怪物侧） |
| --- | --- | --- |
| 自愈X | 冷却X ＋ 异变5X（双份） | **只付冷却X，异变 0 层** ← 唯一实质变化 |
| 狂暴/全力/疯狂/减速/必中/飞行 | 异变5X（硬编码点付，总线跳过怪物） | 异变5X（改由总线付）——**数额与时机都不变** |
| 封印X | 异变X（硬编码点付） | 异变X（总线付）——不变 |
| 原初X 借用门票 | 异变5X（`execute_evolution` 自结算） | 异变5X——**不属家族税，照旧** |

**连带保住的两条口径**（不是新增规则，是把旧行为搬到新结算点）：
1. 付异变达 50 层仍【崩解】命零且**道纹效果中断**——中断点从硬编码支付点移到总线的
   `interrupted_by_cost` 守卫（`combat.py:3450-3457`），只认「异变」这一种代价（流血/衰老致死照旧结算完效果）；
   这条中断现在对**轮回者/[朋友]/[员工]同样成立**（此前只有怪物侧的硬编码支付点会中断，玩家付异变崩解后效果照样落地）；
2. 怪物阶段返回值仍带 `{"collapsed": 道纹名}`（新增 `_monster_collapse_marker`，`combat.py:5539`），
   死者照旧进统一死亡管线（`_apply_numeric_cost_part` → `_on_entity_death`，焦黑发丝/分裂/死者之书都不掉）；
   攻击出手仍被中断（`_execute_monster_phase` 的 `if not monster.is_alive: continue`）。

**必须澄清的一点（回答"删了税怪物是不是就不自爆了"）**：**不是**。
4.3/4.8 的异变型自爆（奇美拉【必中】X=9 → 异变+45 → 崩解45/50）来自必中**自身**的正文代价「异变5X」，
不是家族税；家族税只额外压在【自愈】上。所以删税救回的是自愈承载怪（不再每发动一次叠 5X），
必中/飞行 的自爆仍要靠 Q10（战术选 X 接线）或 Q11（改出手顺序）处理。

**X 上限探测**：`_monster_max_daowen_x`（`combat.py:5501`）去掉家族税特例，改按 calc 声明的异变量封顶，
取值口径与总线一致（总线优先级 流血＞衰老＞疲惫＞异变，故只有前三种都不存在时才真的付异变）。
【自愈】的可用 X 不变：紧约束一直是 `X_LIMITS["冷却"]=7`，家族税算出的 9 从来没生效过。

**改动面（全部已跑回归）**：
- `engine/combat.py`：代价总线分支、中断守卫、删两个硬编码支付点、`_monster_collapse_marker`、X 探测、`YUANCHU_COST_RATE` 注释
- `engine/models.py`：`MUTATION_COLLAPSE_THRESHOLD` 注释（崩解只由道纹/消耗品自身异变代价产生）
- `sim/balance_sim.py`：镜像只对「自身代价为异变5X 的六条」计异变（自愈不再计）＋三处注释
- 正文 `AI_EXPERIENCE.md`：《原始怪物道纹与转化道纹》自愈段、L1057《关键机制口径》各改一句；
  《已删内容》表加两行（家族税、滋养加成休整）防写回
- `全道纹索引.md`：改生成器 `sim/gen_daowen_index.py` 两句后重生成（`tests/test_daowen_cost_consistency.py` 守卫逐字节一致，diff 仅 2 行）
- `sim/kb_regression.py`：C2-03 探针的 gold 锚点改指现行文本（旧锚点"仅首次发动支付异变5X"已不可达）
- 测试改新口径：`tests/test_daowen_wiring.py`（自愈怪物 5→0 层，函数名同改）、
  `tests/test_engine.py`（借用自愈 20→10 层；"持续怪"块改断言 0 层＋冷却2场；"临界怪"崩解用例改用减速2，因自愈已不产生异变）
- ~~**未动**：`sim/prompt_slim_apply.py` 的历史提示词 blob、`sim/prompt_audit.py:205` 的历史审计条目~~ → **2026-09-18 夜用户裁定「整个文件删掉」，两文件已删**（见 6.6）；本行留档只为说明「当时曾判定不动、后被裁定推翻」，不要再去找这两个文件。
- 新增验证探针 `sim/probe_family_tax_removal.py`（**机制验证，非轮回数据**，铁律6），四条读数：
  ① prepare 可负担 X 上限＝必中/减速/飞行 9（异变45）、变形 9（法限9）、自愈 **7**
  （`X_LIMITS["冷却"]=7` 才是紧约束，家族税算出的 9 从来没生效过）；
  ② 发动自愈 → 异变 **0** 层、冷却 7 场、已损生命 175% 回复照常；
  ③ 发动必中9 → 异变仍 **45** 层（崩解 45/50）＝删税救不回异变型自爆；
  ④ 40 层上发动面板写死 X=2 的减速 → 50 层命零、玩家 60/60 无伤、返回值仍带 `collapsed: 减速`。
- 全量回归：`9 failed / 1697 passed / 3 xfailed`，与第二节基线**逐条一致**（9 条全是既有 AI/mock 类）。

### 5.2 更正「滋养加成局外休整」——用户指出得对，是文字错，不是机制错

用户原话：「滋养作为战斗中的 buff，战斗结束就清除，怎么可能加成休整这个局外行动？」→ **查实为真**。

证据链（三段都可复算）：
1. 滋养只带 `duration` → 走 combat 通用状态块挂 `StatusEffect`，`scope` 取默认值 `BATTLE`（`engine/models.py:158`）；
2. [战终]清场只保留 `COST/RUN/PERMANENT` 三种 scope，其余状态一律清除（`engine/api.py:5292-5313`，过滤在 5313 行）；
3. 【休整】是**战前**行动（`engine/api.py:1408` 注册在 pre_battle 表、实现 `_pre_battle_xiuzheng` 在 1437 行，
   ctx `timing="pre_battle_action"`）。
→ 滋养状态与【休整】永不同时在场，`apply_heal` 里那句 `entity.has_status("滋养")` 在休整时恒为 False。

引擎行为一直是对的；错的只是**文字**，共三处，已改：
`AI_EXPERIENCE.md:395`（"覆盖一切来源（…休整…）" → "覆盖战斗内的一切来源（道纹／消耗品／寄生…）"＋一句它不加成局外行动）、
`engine/daowen.py::calculate_ziyang` docstring、`engine/models.py::apply_heal` 注释。
《已删内容》表加一行防写回。**未改任何数值或机制。**

### 5.3 报告排版修复（用户"Q5 以后没看到"的原因）

上一轮把 4.8 探针整节插进了 4.6 的 Q 表**中间**（Q4 行与 Q10 行之间），markdown 表被截断，
Q5–Q10 六行渲染成孤立文本 → 用户看不到。现已重排为 **4.6 完整 Q1–Q12 表 → 4.7 现行事实 → 4.8 探针**，
并新增 Q11（怪物阶段「道纹先于攻击」）、Q12（`全道纹索引.md` 的"怪物道纹递增"陈述已废止）。
4.4 同时更正了一处数字错误（b7 R10 是 8 发石头 120 伤＋普攻 64，不是 9 发 135；第 9 发被救赎门禁拒掉）。

## 六、2026-09-18（夜）用户裁定 Q1–Q12 的落点与实装

用户逐条裁定（原话要点）：「Q1234 都 B」「Q5 直接把护卫也算员工/朋友的一次出手」「Q6 A」
「Q789 都 B」「Q10 不是自定义 X 值的错，是怪物 AI 太蠢」「Q11 不要硬编码，怪物 AI 自己选
先攻击还是先道纹」「Q12 废案清理干净」；另附一条独立指令：**【必中】代价 异变5X → 异变X**。

### 6.1 【必中】降价：异变5X → 异变X（用户的循环论证成立）

用户给出的循环：怪物用当前速度逐次闪避轮回者的攻击（速度掉到 0）→ [回始]法力被出厂遗物
【某人的偏爱】回满 → 对自己发动【变形】把法力换回速度 → 下个[回始]法力又满
（1/10/8 → 1/0/8 → 变形 → 1/8/0 → 1/8/8；攻次＝当前速度、攻力＝当前法力，于是下一回合
8 击 × 8 伤）。硬解只有【必中】，而 5X 的代价把这条硬解压在崩解线上（X=9 即异变45/50，
再动一次就自爆）→ 裁定：**降到 异变X**，让反闪避工具付得起。

改动面（全部已跑回归）：
- `engine/daowen.py::calculate_bizhong`：`cost_mutation=x`（docstring 首行／`summary`／理由注释同改；
  正文红线要求 docstring 首行＝规范文本，索引与一致性测试都解析它）
- 正文 `AI_EXPERIENCE.md`：《原始怪物道纹与转化道纹》必中段、《关键机制口径》各一句
- 生成器 `sim/gen_daowen_index.py` 的原始段 note → 重生成 `全道纹索引.md`（总览表／原始段／必中条目）
- `engine/combat.py:46` 代价注释；`sim/balance_sim.py` 镜像不再写死倍率，改读引擎
  `DaoWenEngine.resolve(...)["cost_mutation"]`（模块级 `register_all()`）
- 测试改新口径：`tests/test_engine.py`（必中3：异变 15→3）、
  `tests/test_xijie_and_bizhong.py`（两处 fixture `cost_formula="5X"→"X"`）

**降价后的可负担上限（探针 `sim/probe_family_tax_removal.py` 实跑，机制验证非轮回数据）**：
必中 `max_x` 从 9（异变45，崩解线封顶）变成 **30**；30 不是崩解线算出来的，是
`CombatEngine._DAOWEN_X_PROBE_CAP = 30` 的试探封顶（崩解线闸要 X≥50 才咬得住 1X 代价）。
必中9 现在只叠 **9 层异变**（原 45 层），同阶段两击各吃一层 → 余数 7。
⚠ **副作用（用户上一轮问的「1X 的上限后果」，实测答案）**：上限是 30 而非 49，
且**没有加任何新上限**（按「非必要不修改」，封顶交给战术选 X，不由引擎写死）；
但引擎缺 X 时的回退是「可负担上限」（`combat.py:5602 if missing: effective_x = max_x`），
所以不选 X 的驱动一发必中就是异变 30/50 —— 这正是 6.4（Q10）必须同时接线的原因。

### 6.2 Q5：护卫算[朋友]/[员工]当回合的一次出手

- 机制点：`engine/combat.py::_tick_mediocrity_counters`（`round_end` 对全体存活双方逐个 tick）。
  新增前置分支：`entity_type in ("朋友","员工") 且 _beifu_left > 0` → `no_action_rounds = 0`
  并**直接返回**（`no_damage_rounds` 冻结不推进）。两条计数都要处理：只清「未出手」救不下纯护卫，
  因为「未使敌掉血」那条同样会在第 5 回合炸裂。**冻结不清零**——护卫层数用尽后从原值接着累计。
- 正文：《特殊事件》凡庸条加一句（护卫中的[朋友]/[员工]视为当回合已出手…）；
  《行动接口口径》`command_ally` 条把「不占盟友出手」改为「护卫**算**盟友当回合的一次出手」。
- 注释同步：`engine/api.py:3920`（施加背负处）、`sim/guard_cmd_full_run.py` 两处旧口径文案。
- 回归钉：`tests/test_guard_command.py::test_guard_counts_as_ally_action_for_mediocrity`
  （真实跑 6 个回合：每回合续护卫＋轮回者出手＋怪物阶段，纯护卫的岩行者全程存活、两条计数恒 0；
  层数用尽后 tick 一次 → 计数变 (1,1)）、`::test_guard_mediocrity_exempt_only_for_friends_and_employees`
  （怪物背着【背负】不享受豁免）。
- 手操战报里的**失误④**（b7 漏调 `resolve_ally_phases` 导致员工溺被凡庸炸裂）在纯护卫场景下不再成立；
  但「员工仍须自己出手才有输出」这条战术常识不变（护卫只免死，不代打）。

### 6.3 Q6＝A：战终先入账击杀[碎片]，再问工资

- 旧序：逃跑处理 → `_compute_pending_wages` → **工资待决 gate（早返回 `completed:False`）** →
  遗物/法术【战终】→ 碎片结算 → `shards += shard_reward + bonus_total`。
  后果：刚打完这场、账上碎片不够时，雇佣＝必然拒付＋失信一击（明明这场怪的奖励够付）。
- 新序：碎片奖励**在 gate 之前**算好并入账，其余顺序一字不动（遗物/法术仍必须在统一清理之前）。
- 重入安全：gate 早返回意味着 `battle_end` 会被调用两次，所以
  ① 碎片循环抽成 `_battle_end_shard_preview()`（纯读取：按 `[战始][血限]` 快照与道纹数算，
  结果按引擎实例缓存，第二次调用不重复构建 ctx）；
  ② 入账用一次性标记 `event_modifiers["battle_end_shards_credited"]` 守住，战终完成时 pop；
  ③ `battle_start` 处兜底清除标记与缓存（防存档读档/战终中断后跨场残留）；
  ④ 原 `shards += shard_reward + bonus_total` 改成只加 `bonus_total`（副本彩头），禁止二次加总。
- 正文：《行动接口口径》`battle_end` 条重排为「击杀[碎片]先入账 → 工资门槛 → 遗物/法术【战终】→
  统一清理 → 副本彩头」，并写明「刚打完这场的击杀碎片已经到账，可以直接用来付工资」。
- 回归钉：`tests/test_employee_economy.py::test_kill_shards_are_credited_before_wage_gate`
  （碎片 0 开局、3 回合工资 8、击杀奖励 12：第一次 `battle_end` 返回 `completed=False` 时
  碎片已＝12 → `pay` 成功 → 余 4；第二次 `battle_end` 完成且仍是 4，标记已清）、
  `::test_shard_credit_flag_is_reset_at_battle_start`。
  既有 15 条员工经济用例全绿（含「碎片不足时 pay 必须被拒绝」——该用例无击杀奖励，不受影响）。

### 6.4 Q10：不是自定义 X 值的错，是怪物 AI 太蠢 → 战术选 X 接进脚本轮回驱动

- 病灶：`sim/build_learner.py::_resolve_monster_turn` 拼 `daowen` 时**从不提交 `x`** →
  引擎回退到可负担上限（`combat.py:5602`）→ 脚本轮回与手操里的怪物一律开满 X
  （本局 3 雕塑＋2 凡庸＋崩解45/50 均由此）。`pick_monster_daowen_x` 此前只接进死斗驱动
  （`sim/duel_common.py:97-103`）。
- 修法：照死斗的写法接进 build_learner，但**必须在整份 `choices` 拼好之后**评分——
  PVE 怪物阶段要求全体 actor 一起提交（`combat.py:6002 set(submitted) != set(expected)` 即拒），
  只交一个会被拒。为此给 `sim/monster_targets.py::pick_monster_daowen_x` 加了可选参数
  `all_choices`（传入完整列表时按整份提交预演、只替换本 actor 的 X；缺省 None＝死斗单 actor
  部分提交，旧调用方零改动）。逐个 actor 原地打补丁，后面的 actor 评分时看到的是前面已定档的真实 X。
- 实测（同一驱动、同一怪，机制验证非轮回数据）：
  | 怪 | prepare 上限 | 接线后实发 | 异变 |
  | --- | --- | --- | --- |
  | 必中 | 30 | **15**（预演评分选中档） | 15/50 |
  | 必中＋减速 | 30 / 9 | 选减速 **X=1** | 5/50 |
  | 变形 | 7（法限） | 按评分 | 0（付法力） |
  | 自愈 | 7（冷却闸） | **X=1** | 0，冷却 1 场 |
  接线前这四种一律开满（必中 30、减速 9＝异变45 当场崩解、自愈 7）。
- 说明：必中 X=15 是 `TacticalAI._score_candidate` 从怪物视角评出来的（候选档＝1／中档／上限），
  与死斗侧同一口径同一函数，本轮**没有调评分**；若嫌 15 层太豪，属另议（改候选档位或评分权重）。

### 6.5 Q11：不硬编码顺序，怪物 AI 自选先攻击还是先道纹（`attack_first`）

- 引擎：`_execute_monster_phase` 的每个 actor 拆成 `_daowen_step()`／`_attack_step()` 两个闭包，
  按提交的 `attack_first` 排序（`true`＝先普攻后道纹；缺省 `false`＝现行「道纹先」，老提交零改动照样合法）。
  道纹步里怪物命零 → 返回 False，后续步骤不再结算；先攻击时怪物在自己的攻击里命零 → 道纹不结算。
- 契约不变量：**出手数与命中数一律按 prepare 快照校验**（`base_attack_actions`／`base_hits_per_attack`），
  与先后顺序无关——这条本来就是 2026-09-17 的口径，改序不需要动它。
- 面板公示：`prepare_monster_phase` 每个 actor 新增 `attack_first_allowed: True` 与 `order_note`
  （「attack_first=true 先普攻再发动道纹；缺省 false=先发动道纹再普攻」），发动方不必猜。
- 静态校验：`_validate_monster_phase_static` 里 `attack_first` 非布尔即拒（零副作用，在龙息/道纹/攻击之前）。
- 正文：《怪物准则》新增第 12 条（顺序由发动方决定＋两种顺序的战术含义＋快照契约与先后无关）；
  《行动接口口径》末尾的指路句改为「第 11~12 条」。
- 回归钉：新文件 `tests/test_monster_phase_order.py`（5 条）——
  缺省路径【必中】压掉首击闪避（`dodge_fail_reason=必中攻击无法闪避`、伤害照吃）、
  `attack_first=true` 时同样提交闪避全部成功且伤害 0、层数留到下回合（跨回合用例：
  存的 1 层＋新攒 1 层 → 下回合前两击都必中）、面板公示、非布尔拒绝且零副作用。

### 6.6 Q12：废案清理（已废止的「怪物道纹递增」＋陈旧锚点）

- 生成器 `sim/gen_daowen_index.py`：删掉原始段的递增 note、通用规则指路句里的「怪物道纹递增」、
  区域专属段的「（道纹递增+N/次）」；改为现行口径（面板不写X、发动方自选；阶级只用于死斗分槽）。
  重生成 `全道纹索引.md`（含 6.1 的必中降价，共 9 行 diff），`tests/test_daowen_cost_consistency.py` 9 条全绿。
- `engine/gamedata.py:16` 注释：`REGION_TIERS` 的现行用途是死斗阶级分槽/进阶封存
  （`api.py:4639`、`api.py:4761`），不再写「递增机制用」。
- `sim/kb_regression.py`：旧 C2-04（gold＝「累加+2×副本阶级」）已不可达 → 删除，
  改立 stale 探针 **S5-07**（递增文本不得以当前规则语境出现，gold＝「面板不写X」「发动方自选」）；
  C3-07 净化的 gold 锚点从「消耗5X」改指现行「净化X：消耗2X」。
  手工核查（本沙箱 `git tag` 为空、跑不了 pre-slim 对照，见第二节）：`AI_EXPERIENCE.md`／`README.md`／
  `全道纹索引.md`／`副本索引.md`／`物品索引.md`／`法术索引.md`／`死者之书.md`／`data/build_knowledge.json`
  里已无递增陈述；只有本报告的 Q12 行与 5.x 存档行引用旧文本，且同行都带「已废止」否定语境
  （`_stale_active_lines` 的 NEG 白名单会放过，不算污染）。
- 探针 `sim/probe_family_tax_removal.py`：按必中降价刷新文案与读数，并新增第 5 段
  （缺省回退开满 X=30 → 异变30/50），把 Q10 的必要性钉在同一份探针里。
- **两个一次性工具已整文件删除（用户裁定 2026-09-18 夜：「整个文件删掉」）**：
  `sim/prompt_slim_apply.py`（2026-08-24 提示词瘦身执行器，含递增／局外【领悟】／净化5X／
  原始纹仅首次付异变5X 等旧口径 blob，有 NOOP 幂等门、永不再执行）与
  `sim/prompt_audit.py`（同期审计盘点器，条目里含「原始怪物纹仅首次支付异变5X」这类已废陈述）。
  产出物照常留档：`data/experiments/prompt_audit_2026-08-24.json`、`audit/prompt_slim_audit_2026-08-24.md`
  （该 md 里「生成器 `sim/prompt_audit.py` 可复跑」一句从此失效，作为 dated 记录不改写）。
  删除前已核查：无任何测试或生产代码引用这两个文件。

### 6.7 Q1／Q2／Q3／Q4／Q7／Q8／Q9＝B（保持现状，一行存档防重提）

| # | 结论（B＝不改） | 一句话理由（用户口径） |
| --- | --- | --- |
| Q1 | 爆裂反伤仍绕过招架 | 「伤害／失去生命」两条轴的区分是刻意的，固执/蒙蔽就是它的答案 |
| Q2 | 雕塑 0 碎片换 12发×15伤 不动 | 放弃经济换即时战力的取舍，末场确实靠它赢了四怪局 |
| Q3 | 消耗品不加每回合次数上限 | 耐久本身就是预算；加上限要动 `consume_item` 主路径与全部测试 |
| Q4 | 不给怪物 AI 加「不自废」约束 | 落点是 Q10＋Q11（已按裁定修 AI，不改规则） |
| Q7 | 三相残韵盘末场净亏损不自动跳过 | 玩家可自行 `use=false`（b7 就这么做），属可学习成本 |
| Q8 | 叙事型选项不给机械价值 | 真拒绝奖励是既定裁定，叙事选项定位为角色扮演成本 |
| Q9 | 坏死＋寄生叠加不设下限/豁免 | 两件都是水母的既定道纹，叠加本就应当致命 |

### 6.8 顺带查实的两处陈旧覆盖（已修）＋一处只存档不动

- **已修**：`sim/build_learner.py` 与 `tests/monster_phase_support.py` 都有一段
  「选了【变形】就把命中数改成 `attack_power`（＝当前法力）」的旧覆盖。命中数契约自 2026-09-17 起
  是 prepare 快照，实测（速限3/法力12 的骨天使自施变形）：按快照提交 3 击**成功**，
  按旧覆盖提交 12 击被引擎拒（`骨天使每个攻击出手必须提交3次命中选择`）——
  法力≠速度的变形怪每次都白烧一次重试额度（`max_attempts = len(actors)+2`），
  还会让第一遍统计的 `hold_counts`（反应法术「末击声明」）与实际提交数错位。两处旧覆盖已删。
- **已修（用户裁定 2026-09-18 夜：只改这句 docstring，不动行为）**：
  `engine/combat.py::_validate_monster_phase_static` 的 docstring 原写「hits 命中数…必须按执行时的
  真实状态校验（如【变形】会改变命中数）」，与实现（`hits_per_action = expected[actor_ref]
  ["base_hits_per_attack"]`，快照）相反——正是它把 sim 驱动带偏成上面那条陈旧覆盖。
  现改为：出手数与命中数一律按 prepare 快照校验、阶段内真实改速度也不动契约；
  真正按执行时状态校验的是目标当前速度／存活性／血影所需生命／本回合已发动集合。

### 6.9 接线后暴露的引擎层缺陷：预演污染 id-keyed 账本（已修；不是规则改动）

Q10 把战术选 X 接进 `build_learner` 后，`tests/test_build_learner.py::test_fixed_seed_is_reproducible`
开始失败（同一 seed＋同一决策 rng，两次 `play()` 结果不同）。定位链（可复算）：

1. `pick_monster_daowen_x` 每只怪每回合做 3 档预演（1／中档／上限），走 `engine/ai_preview.py::preview_sequence`；
2. `preview_sequence` 是**副本执行**：deepcopy `state`/`dice` 后在副本上真实跑动作，但**复用同一个
   `CombatEngine` 对象**——`_monster_daowen_round_used`／`_monster_activated`／`_resonance_rewrites`／
   `_monster_evolved` 这些按 `id(entity)` 建索引的账本仍是真实账本；
3. 副本里的怪是新对象、id 与真实怪不同，预演于是把「本回合已发动逆鳞」写进真实账本的**垃圾键**；
   副本被回收后地址复用，后来的真实怪／新副本继承这条记录 →
   `_validate_monster_daowen_schema` 报「断角羊不能发动道纹【逆鳞】」（它本回合并没发动过）；
4. 是否命中取决于内存分配顺序 → 同一进程内第一次跑与第二次跑不一致（跨进程单跑反而稳定）。
   实测证据：两次跑各 918 次预演，第 645 次（逆鳞 X=30 档）run1 通过、run2 被假拒绝，
   而预演前的可见状态（回合/异变/冷却/本回合已用集合）逐项相同；单跑 X 恒为 1 时两次跑完全一致。

修法（`engine/ai_preview.py::preview_sequence`）：进副本前把 7 个 combat 侧账本
（上述 4 个 ＋ `_sanxiang_consumed`／`_split_clones_spawned`／`_effect_chain_depth`）deepcopy 存好、
给副本一份独立拷贝，`finally` 里原样归还——预演对这些账本的写入一律作废。
账本值会被**原地改写**（`_monster_round_used(monster).add(name)`、`_monster_activated.setdefault(id, set())`），
所以必须 deepcopy 而非浅拷贝。修完该用例立即回绿。

旁证：`sim/win_only_ai.py:71-98` 早就手工存/还这批账本（同一坑的局部绕行，现成冗余但不动）；
`ActionPreview.snapshot()/restore()`（旧回滚式预演）也存了它们，只有副本执行这条路漏了。

**未动（存档等裁定）**：副本里按 id 查 `_resonance_rewrites` 恒 miss（副本实体 id ≠ 真实实体 id），
所以预演**看不到残韵改写**（已被改写成别名的道纹，预演仍按原名评估）。这是既有的保真度缺口，
与本轮裁定无关；修它要给账本做 id 重映射，按「非必要不修改」先记在这里。

### 6.10 全量回归

- 定向：`tests/test_guard_command.py`(18)、`tests/test_monster_phase_order.py`(5)、
  `tests/test_employee_economy.py`(17)、`tests/test_daowen_cost_consistency.py`(9)、
  怪物阶段/预算/代价/闪避/波及/死斗等 9 文件(77＋1 既有失败)全绿。
- 全量：`9 failed / 1706 passed / 3 xfailed`（248s）。9 条与第二节基线**逐条同名**
  （全是既有 AI/mock 类，含 `test_build_learner::test_valid_and_invalid_are_separated`）；
  passed 由 1697 → **1706** ＝ 本轮新增 9 条（`test_monster_phase_order.py` 5、
  `test_guard_command.py` 2、`test_employee_economy.py` 2），无新回归。
- 中途出现过一次真回归：`test_build_learner::test_fixed_seed_is_reproducible`（Q10 接线暴露的
  预演污染 id-keyed 账本，见 6.9），修 `engine/ai_preview.py` 后回绿，全量重跑即为上面这行。
- 文档守卫单跑：`test_ai_experience_policy`／`test_ai_policy`／`test_daowen_cost_consistency`／
  `test_f7_doc_consistency`／`test_rulings_2026_09_16`／`test_ally_commands`／`test_resonance_network`／
  `test_ziyu_ziyang_rework`／`test_ally_daowen_x_free` 共 99 条全绿（正文《已删内容》表新增 5 行后复跑）。

### 6.11 工具沉淀（A+B，用户在本轮末尾选定的路线）

第六节末尾我承认「本轮慢，我这一侧占约三分之一」：一是**诊断靠反复重跑猜层**，二是**改一条规则也要等全量 4–5 分钟**。用户选「先沉淀工具，不碰规则」，两件都已建成、跑通，并各做过一次**反向对照**（故意把问题放回去，确认工具真能抓到，不是空转）。

**A `sim/diag_repro.py`（复现性诊断）** —— 针对 6.9 那类「同 seed 两次跑结果不同」的问题：同一 seed 连跑 N 局（默认 2），只挂钩子、不改引擎、跑完自动还原，逐层比对并打印**最先分歧的那一层**＋首次分歧上下文：

| 层 | 钩子 | 暴露什么 |
| --- | --- | --- |
| L1 | `play()` 结果 | 通关/死亡/回合数（末层，最粗） |
| L2 | `execute_action` | 每次行动的 `kind/x/caster`＋返回的 `success/survivors/hp` |
| L3 | `pick_monster_daowen_x` | 战术选 X 的**入参**（快照/上限/预算）与出参——X 值分歧的源头 |
| L4 | `ActionPreview.preview` | 预演返回的 `ok/expected_success/x/daowen_name/survivors/hp_before→hp_after/cost_snapshot` |
| L5 | `_score_candidate` | 候选评分 `score/expected_value/collapse_risk` |
| L6 | `_validate_monster_daowen_choice`＋`resolve_monster_daowen_choice` | **拒绝理由**与裁决入出参（理由里的 token 会归一化，才能看出「同一对象换个身份被拒」） |
| L7 | `CombatEngine.__init__` 登记＋结算审计 | id 账本污染：`_monster_activated`/`_monster_daowen_round_used`/`_resonance_rewrites` 中「键不在本场名册」的**垃圾键计数** |

32 位实体 token 统一归一成 `TOK<n>`，否则两次 run 的 token 不同会把每层都判成分歧。用法：`PYTHONPATH=. .venv/bin/python sim/diag_repro.py`（默认龙心谷/seed42/decision9；`--region/--seed/--decision/--runs/--quick/--layer L6 --context 2500/--dump`）。
**反向对照（已实跑）**：`git show cc6d5ac^:engine/ai_preview.py > engine/ai_preview.py` 把预演还原到修复前后再跑 → 工具立刻报 **L7 垃圾键 21+21=42**、**L6 五次假拒绝**（`in_round_used=true` 但 `can_use=true` ＝ id 账本污染的指纹，round3 vs round2）并给出首次分歧上下文，L5 评分同步分歧；`git checkout engine/ai_preview.py` 还原后 7 层全绿、垃圾键 0。也就是说，6.9 那个当时靠人肉排查很久才定位的根因，现在一条命令就能指到层。

**B `sim/check_rule_change.py`（改一条规则后的一键自检，约 7 秒）** —— 四步，任何一步不过即非零退出：

1. **索引同步**：重跑 `sim/gen_daowen_index.py`，比对前后哈希。防「引擎改了、`全道纹索引.md` 没重生成」——`kb_regression.py` 在本沙箱跑不了（`git tag` 为空，只能 checkout 提交号跑不了 tag 名），这一步是它的等价替代。
2. **守卫测试子集**：12 个规则测试文件（道纹 X／怪物 X 与选择／怪物阶段顺序／命中／预演快照／凡庸／残韵网络／护卫指令／员工经济／战斗一致性／轮回模拟／AI 一致性）。失败先对照 `KNOWN_BASELINE`（第二节那 9 条既有失败），**只报新增**；某条基线失败若**回绿会提醒删名单**，避免基线越攒越大。
3. **废案扫描**：活语料分两层——**硬层**＝真正注入 AI 的规则文本（`全道纹索引.md`/`副本索引.md`/`道纹.md`/`轮回者道纹.md`/`怪物道纹.md`/`怪物道纹详表.md`/`残韵详表.md`/`怪物手册.md` 等），逐行匹配 16 条已废口径（家族税、怪物道纹递增、残韵三旧型、旧转化规则、攻次攻力加点池、格挡[回终]清空、杀伐2X、必中 5X、旧工资门槛…）并报 `文件:行号`；**带否定语境词的行放过**（删除/废止/反转/已删/不得再写回去/降为…），且整张《已删内容》表跳过——那张表是红线指定的废案档案存放处，不是漏洞。**软层**＝`报告.md`/`机制迁移台账.md` 等工作日志，只警告不阻塞（红线只管正文，工作日志本来就要记沿革；本轮实测软层 20 处、硬层 0 处）。
4. **口径速览**：用引擎 `resolve_monster_daowen` 打本轮涉及的 7 条道纹的**代价与效果摘要**（读引擎、不抄文档，避免文档幻觉）。本轮读数即 6.1 的结论：必中 `异变+1`，其余异变类 `异变+5`，自愈 `冷却1场`。

用法：`PYTHONPATH=. .venv/bin/python sim/check_rule_change.py`（`--full` 跑全量 4–5 分钟、`--skip-tests/--skip-index/--max-hits N`）。
**反向对照（已实跑）**：往 `副本索引.md` 注入一行「怪物道纹递增：每实际发动一次该道纹本场 X 累加+2×副本阶级。」→ 第 3 步立刻报**硬层 3 处命中**并非零退出（注入行已还原，`git status` 无 diff）。

**仍未做（需用户裁定后才动）**：6.x 里反复出现的三项**结构性**改动——① 规则文本与引擎统一到一个数据源（消灭「一条规则改 5–6 处」）；② 拆 `engine/combat.py`（约 6000 行巨石，改动定位与回归面都受它拖累）；③ 引擎 id 账本改对象账本（6.9 的根治方向，现在靠 `ai_preview` deepcopy 隔离＋L7 审计兜住）。这三项都属「非必要不修改」要先报裁定的范围，本轮只沉淀了工具、没动结构。

**追加（同日，用户裁定「从小到大改，先做最小的改动，是哪个？」→ 选 B）**：③ 的**最小切片**（账本清单单一化，含把 `_dodge_counts`/`_dodge_round` 一并纳入隔离）已做完，见 **6.12**；①、②、③-full（id 键→对象键）仍未动。

### 6.12 ③ 的最小切片：账本清单单一化（用户裁定「从小到大改，先做最小的改动」→ 选 B）

**为什么这是最小的那一刀**（三件事的实测改动面）：③-lite＝3 个调用点、约 40 行、**零行为改动**；
③-full（id 键→对象键）＝63 处引用跨 15 文件、engine 内 60 处 `id(`；②（拆 `combat.py`）＝6289 行、
**单个 `CombatEngine` 类**、174 个 def、**68 个文件**引用；①（规则文本与引擎统一数据源）＝一条规则
现在同步 5–6 处、牵动 99 条文档守卫。且 ③-lite 是 ③-full 的前置：先有权威清单，才知道要迁哪几本键。

**改了什么**：

- 新增 `engine/ledger_isolation.py`：权威清单 `COMBAT_LEDGERS`（9 项，每项登记 名字／默认值／是否 id 键／
  是否 `roster_bound`／这本账记什么）＋ `snapshot`／`enter_copy_world`／`restore_world`／`copy_world`
  （上下文管理器）＋ `roster_ids`／`audit_id_ledgers`。
- `engine/ai_preview.py::preview_sequence` 改用 `copy_world(eng)`（**−54 行**手抄清单）。
- `sim/win_only_ai.py::_swap_world` 改用同一个 `copy_world`（**−54 行**；原先同一批字段「存一遍＋还一遍」
  写两次，漏字段不报错、只静默漂移）。`_PLAYOUT_DEPTH` 计数保留不动。
- `sim/diag_repro.py`：L6 拒绝快照与 L7 审计改读权威清单（`ID_KEYED_LEDGERS`／`audit_id_ledgers`）；
  L7 从「只审 3 本」变成「审全部 id 键账本」，并分两个数：`junk_keys`（门禁）／`unbound_keys`（参考）。
- 新增 `tests/test_ledger_isolation.py`（14 条守卫用例）。
- `sim/check_rule_change.py` 的守卫子集加入该文件（12→13 文件），并注明「同 seed 可复现」不进子集
  （那条单跑约 29s，会把 7 秒的自检拖成半分钟），要验复现性跑 `sim/diag_repro.py`。

**裁定项落地（用户选 B）**：`_dodge_counts`（id(目标)→本回合自动闪避次数，每回合每目标上限 2 次）
＋ `_dodge_round`（配套的回合哨兵）纳入隔离。实测依据：整局脚本轮回里 **4789 次预演，自动闪避决策
被调用 0 次、`_dodge_counts` 被写 0 次** → 是**潜在漏洞而非活 bug**，纳入属结构保险。两者
`roster_bound=False`：目标中途离场时计数会滞留到回合末才清，属合法，若进门禁 L7 会误报。

**顺带删掉的死代码（删除，需用户知情；不同意可单独 revert 这一处）**：`engine/ai_preview.py` 的
`snapshot()`／`restore()`／`_restore_entity()`／`_restore_group()` 共 **83 行**——这是 2026-08-19 改成
「副本执行」之前的**回滚式预演**，全仓无任何调用者（连字符串/`getattr` 式间接引用都查过），却带着
**第 4 份手抄账本清单**（7 本，且缺 `_dodge_counts`）。留着它就得要么继续同步第 4 份清单、要么在新守卫
用例里给它开豁免——两条都会让坑复活。它那段「实体必须原地恢复以保 id 稳定」的理由已保留在
`preview_sequence` 的副本执行 docstring 与 `ledger_isolation.py` 的模块 docstring 里，没有丢。

**一处有意的语义收紧**：还原改为**按引用归还**（identity restore，与 `win_only_ai` 原口径一致）；
`ai_preview` 原来是「归还一份 deepcopy」（内容相等但对象已换）。按引用更安全：外部若持有某本账的原对象
（如 `_monster_round_used(monster)` 返回的内层集合），还原后它仍是本体，不会变成脱钩副本。
**实测无差异**：`sim/diag_repro.py --runs 2` 的 7007 actions／261 picks／783 previews／2558 scores
与改前**逐条同数**、`junk_keys=0`；全量回归 `9 failed / 1720 passed / 3 xfailed`（9 条与第二节基线
逐条同名；passed 1706→1720＝新增 14 条守卫用例，无新回归）。

**写实现时自查出的一个真隐患（已修，并钉成用例）**：懒初始化的账本（`_dodge_counts`/`_dodge_round`
只在自动闪避路径里才创建）若属性不存在就用清单里的默认值兜底，`restore_world` 会把那个**共享默认对象**
设成真实账本 → 之后任何写入都污染全项目默认值（下一台引擎一出生就带着上一场的闪避计数）。
现在改成「属性不存在时 deepcopy 一份默认值」，用例
`test_lazy_ledger_is_not_restored_as_the_shared_default_object` 钉住。

**守卫用例怎么防复发（这一刀的真正收益）**：

- `test_every_container_ledger_created_in_combat_is_registered`：**静态**扫 `engine/combat.py` 里
  `self._x = {}`／`set()`／`[]`／`defaultdict(` 的创建点，未登记即失败（豁免表 `EXEMPT_CONTAINERS`
  当前为空：实测 combat.py 创建的容器恰好 5 个，全部已登记）。为什么必须静态——新账本在多数局面里是
  **空的**（`_resonance_rewrites` 整局脚本轮回都没被写过），运行时扫描看不出键类型，抓不到「忘了登记」。
- `test_runtime_id_keyed_containers_are_registered`：跑一局真实脚本轮回后扫 `vars(combat)`，键是 id 形态
  （>10^6，避开 X 值/回合数这类小整数键）而未登记即失败（辅门禁，抓别的模块挂上来的账本）。
- `test_all_three_call_sites_reference_the_single_source`＋`test_hand_copied_lists_are_gone`：三个调用点
  必须引用权威清单，且 `ledger_defaults`／`combat._monster_activated = copy.deepcopy`／`LEDGERS = (`
  这些手抄痕迹不得复活。
- 另有：identity/内容双还原、副本世界必须拿到深拷贝、`_dodge_counts` 跨预演不变、审计只读不改账本、
  门禁与参考分层、api.py 回滚清单 ⊆ 权威清单（见 D7）。
- **反向对照（已实跑）**：把 `_resonance_rewrites` 从清单临时摘掉 → **3 条用例失败**（静态门禁点名该账本）；
  还原后 14 条全绿。值得记一笔：第一版只有运行时扫描时，这个反向对照**没有失败**（因为它当时是空容器），
  门禁形同虚设——静态扫描就是为补这个洞加的。

**仍未做**：①（规则文本与引擎统一数据源）、②（拆 `combat.py`）、③-full（id 键→对象键）。
③-full 现在有了前置：要迁的就是清单里 `id_keyed=True` 的那 5 本。
