# ACTIVE_MINIMAL_DIFF（2026-08-24 提示词瘦身·仅列语义变化）

> 执行基线快照：git tag `pre-slim-2026-08-24`。本文档只列**实际发生语义变化**的条目；
> 纯措辞压缩（同义缩写）与纯迁移（原文逐字入 archive）不在此列。
> 字节级保全核验：08-21 变更节、残韵闭环策略节、08-22 变更+批三~十九、D-01 原文、CF-1 原文、
> 报告历史——全部与 `git show pre-slim-2026-08-24:` 切片逐字一致。

## 一、合并（语义并集，无信息删除）

| 新位置 | 原规则 → 新规则 |
|---|---|
| AI_EXPERIENCE 开发规则 #2 | R02「不能把代码现状直接当成游戏规则」+ R11「直接修改核心状态必须谨慎」+ R19「对特殊数值规则不要擅自推理」+ R22「最终总原则」 → **一条「不能把代码现状当成游戏规则，也不得自造规则」**：现状≠规则、核心字段直改≠自动 Bug、规则未明确列两种解释等裁定、不确定先报告 |
| 开发规则 #3 | R03「禁止为统一强改特殊规则」+ R07「特殊机制允许绕过统一入口」+ R08「死亡规则」 → **一条**：特殊机制可绕过统一流程 + 死亡五分类判断 + 绕过三要件 + 不统一≠错误 |
| 开发规则 #8 | R10「一个效果只能有一个正式执行入口」+ R20「长期维护优先保护四件事」 → **一条**：单入口 + 因果链四件事 |
| 开发规则 #12 | R15「不要扩大修改范围」+ R16「禁止看到大文件就主动拆分」 → **一条**：最小必要修改，长度不构成重构理由 |
| 协作规范 9 与推演铁律 6 | 「凡用脚本得到的轮回数据一律视为虚假数据」三处声明（C09 / 测试含义 / I06） → 推演铁律 6 为唯一正本，C09 与测试含义改指针 |
| KB-L00+L01 | 加点最优 6/8/11 + 血牛负收益 → 一条正反合并 lesson |
| KB-L19/L21/L26/L27/L28 | 复测置信/冻结定位/存量承载/迁移门/KO → 一条「库成熟期判据」 |
| KB-L22/L23/L24 | 回注不深挖=赌博/深挖稳定器/75-25最优 → 一条「精英回注机制定案」 |

## 二、按 CF 裁决改写（语义变化，均有 DM 明令）

| 项 | 原规则 → 新规则 |
|---|---|
| CF-1 推送条款 | 「推送到当前会话分支 `arena/01a01970-linji-disiyuzhou`」→ 「现行口径=《实验灾备协议》；旧版原文见 archive/hypotheses/cf1_old_push_clause.md」 |
| CF-2 波及目标数 | 生产库中旧句「合法目标不足 X 即过滤」→ 移出至 archive/hypotheses/wave_x_filter_deprecated.md；现行规则=第十四批裁定①降 X（README:467/引擎/测试已是事实源，库内不重复抄定义） |
| CF-3 批次流水 | AI_EXPERIENCE 内含批三~十九流水 → 整体逐字迁 archive/experiment_log.md（附批次×数据×commit 对照表）；库内仅存「实验与变更档案」指针节 |
| CF-4 报告口径 | 「只留最新」×「按日期分节」并存 → 统一为「当前有效状态在库内，历史进 archive」：文档管理规则重写为 6 条；报告.md 历史部分迁 archive/report_history_2026-08-24.md，现文件仅存禁区清单+待办 |
| 文档分工 | 新增 archive/ 档案面登记（experiment_log / hypotheses / tactics_handplay / report_history） |

## 三、KB-L03 降级（DM §四 指令）

- 原：「贯穿是 PvE 纯死位……其真实价值仅在 PvP 反擂主（擂主持庇护格挡）」（陷阱·确定性知识）
- 新：「【未证实假设·等死斗专项重验】贯穿 PvE 纯死位仍成立（同种子消融 200 局逐位相同）；但'反擂主专武'推断在十九批死斗台未获证实（1/8≈池均值 10.7%）——证据不足，暂不作构筑/定价依据」（kind: 假设）

## 四、压缩时的事实处置（两处主动备案，未静默引入）

1. **风险阈值数字**：原风险分层表写「HP≤10%」，而 `engine/ai_preview.py` 实现口径为 15% 血限——文档与代码存在出入。**未擅自改写为 15%**：新文只写「HP 极低仍降……具体数值以 ai_preview.py 实现为准」，此出入列为待 DM 裁定项（见末节）。
2. **死斗限时**：压缩工程约束时曾补入「超时判擂主卫冕」（学习器侧墙钟守护语义），因该口径属 sim 驱动侧而非正文规则，**已撤除**，保持正文与 08-21 变更节既有口径。

## 五、OVER_SPEC 压缩迁出（语义保全于 archive/tactics_handplay.md）

手操经验 31 → 13 条；迁出 13 条战术全文至战术库（爆裂护城河/退化降维锁/避风铃叠甲链/逆鳞反打/逼债清算破盾斩/分级承伤/无神转守/0×0 面板/残血死斗优先级/折速血契/残韵小转大/低X逼闪/爆裂忌满杀）。库内合并为一行「组合矩阵」索引 + 六条最高频 DM 裁定。

## 六、契约红线执行记录（边界§五）

- 场景示例内部压缩**已触发契约守卫并停止**：`tests/test_ai_policy.py::test_six_scenarios_contain_exact_mechanic_details` 逐字锚定场景 3 残韵句与场景 6 三轮循环句——该项压缩撤销、场景 1–6 全部原文保留（AI_EXPERIENCE 行数 492→525 即此恢复所致）。
- 其余契约外壳（工程准则 1~12 编号/12 marker/交付 7 段、协作规范九小节标题、行文十诫、推演铁律、职责划分、原子流水线、H1/映射句、README 两个锚点标题）逐字未动。

## 七、压缩量化

| 项 | 前 | 后 |
|---|---|---|
| AI_EXPERIENCE.md 行数 | 923 | **525**（场景恢复前 492） |
| AI_EXPERIENCE 子条目（盘点口径） | 234 | 172（±2 为两期盘点口径微调） |
| KB lessons | 32 | 25 |
| 文档级条目 | 9 | 14（archive 新增 5 份档案） |
| 总条目 | 275 | **211** |
| F 类代码强制条的篇幅 | 31 条全文 | 压缩为单行/指针/原则句（约 -65% 篇幅），原则全部保留 |

## 八、待 DM 裁定项（瘦身中发现的新出入，按边界未自行处置）

1. 风险阈值文档值（10%）与代码值（15%）不一致——改文档、改代码、或维持"以代码为准"指针，请裁定。
