# 第四宇宙战斗系统：机制系统化与可扩展性审计报告（2026-08-19）

> 审计目标：回答"当前架构距离『少量稳定、通用、字面意义明确的基础规则 + 机制自然组合』还差什么"，
> 以及"第四宇宙是否适合采用《杀戮尖塔》式的 Effect / Trigger / Condition / Target 思路"。
> 本次**只审计、不重构**。所有结论均有文件与行号证据。

## 0. 结论先行（不绕弯）

**距离目标还很远，但地基是对的，不需要重写。**

- **远在哪里**：今天任何新机制都必须改核心代码 3～6 处（管线 if、记账字段、回始/回终循环、序列化、测试），
  "新增机制 → 只描述【什么时候/对谁/条件/做什么】"目前**做不到**。用户举的三个例子，没有一个能只靠声明实现。
- **对在哪里**：上一轮收尾已经把最贵的三样东西铺好了——统一的**因果上下文**（`EffectContext`）、
  **事件词汇表**（`CombatEventType` 14 种 + `TriggerTiming` 11 个时点）、**统一写入入口**
  （伤害/回复/代价/血限/死亡/离场）。这三样正是"事件/时机 + 条件 + 效果"架构的全部地基。
  缺的只有**语法层**：让机制以数据声明而不是 if 语句存在。
- **适合 StS 思路**：适合，但要选对 StS 的哪一半——选**遗物/Power 的响应式监听模式**（`atBattleStart`、
  `onAttacked`、`atEndOfTurn` 这类"触发点 + 条件 + 效果"声明），**不是**它的 Action 队列过程式引擎。
  原因与最小方案见第 7 节。

**一句话判断**：当前系统 = "词汇表已经建好，但没有语法"。上一轮收尾做的是"把补丁打得规范"；
本报告要论证的是下一步——"让补丁不再需要打"。

---

## 1. 审计方法与基线

- 实测基线：**886 个测试全部通过**（`python -m pytest tests/ -q`，35 秒）。
  上一份审计报告的 850 通过 / 1 红（`test_r46_*`）已在此期间修复，当前是干净基线。
- 审计范围：`engine/combat.py`（4257 行）、`engine/api.py`（4829 行）、`engine/combat_hooks.py`（492 行）、
  `engine/combat_events.py`（68 行）、`engine/effect_context.py`（123 行）、`engine/daowen.py`（1326 行）、
  `engine/models.py`（1162 行）、`engine/events.py`（763 行）、`engine/handlers/`、`sim/`、`tests/`。
- 方法：不靠文件名猜测，用 grep 实测每个机制的"状态定义点 / 触发点 / 效果点 / 记账点 / 重置点 / 测试点"分布。
- 注：上一份《战斗系统架构收尾审计报告》已按用户要求清理（其 P0/P1 修复均已落地，当前 886 测试全绿）；本报告引用的相关结论已并入正文。

---

## 2. 现状地图：一个机制到底"住在哪里"

现在一个战斗机制（状态、遗物、道纹被动）横跨 **2～5 个文件**。机制代码一共分布在 **11 条渠道**里：

| # | 渠道 | 位置 | 内容形态 |
|---|------|------|----------|
| 1 | 伤害管线 7 段 | `combat.py::_apply_hostile_damage_inner`（594–671 行） | 每段内部按机制名硬编码 if：乘区(龙族血脉)→加减区(加害/龙鳞)→重定向(嫁祸/背负)→伤害前(爆裂)→濒死保护(撤退/负岳碑/断尾求生)→落地(格挡/固执/眩晕)→伤害后(逆鳞/伤痕/切割/寄生/负岳索/龙族血脉斩杀/劫掠/救赎) |
| 2 | 攻击结算 | `combat.py::resolve_attack`（1170 行起） | 必中/血影/闪避/回锋刀目标校验/逆鳞/蒙蔽/坠落/贯穿/震岳龙躯/裂变/兴奋，共 11 个机制的 if 块平铺在一个函数里 |
| 3 | 回始 | `combat.py::round_start`（1386 行起） | 自愈/衰败/勾魂/洞察/狂暴/畸变标记 + 逼债/清算/赌命账本循环 + 守夜灯(敌回始) + 遗物选择(回锋刀/血契/余火印…) + 各计数器归零 |
| 4 | 回终 | `combat.py::round_end`（1569 行起） | 畸变/凡庸(带优先序与中断裁定)/血族血脉/赤族诅咒/活血/皮衣/手术·强制移植 + 格挡法力清空 + `tick_status_effects` 到期副作用(强化/弱化/僵化回滚、逆鳞清零、嫁祸清零、飞行/滑翔、变形还原) |
| 5 | 遗物触发框架 | `combat.py::process_relics`（3443 行起） | 每遗物每时点一个 if 块；而 焦黑发丝在死亡管线、皮衣在回终、避风铃在闪避、血影在攻击、不朽之躯散布在所有面板写入点、龙血瓶在 `apply_heal` 里 |
| 6 | 道纹效果执行 | `combat.py::apply_daowen_effect`（2406–3045，约 640 行） | 主体是"calc key → 效果"表驱动（这很好），但夹杂 蒙蔽/坏死/逆鳞/坠落/六种煞气/血债多段/自愈除外/赌命除外/不朽之躯/定型 等十来个特殊 case |
| 7 | 怪物道纹结算 | `combat.py::_resolve_monster_daowen_choice`（3781 行起） | 按名字分支：冲击的 dodge_targets 矩阵、赌命、消灾、原始道纹异变支付、冷却、回锋刀目标白名单校验 |
| 8 | 动作层 | `engine/api.py`（4829 行） | 血食直写 `current_hp=0; is_alive=False`（`_action_blood_feast`，4097 行起，直写在 4113 行）、买路财直写 `take_damage`（2761 行）、消耗品按名字 if 链（2908 行起）等 |
| 9 | Hook 类 | `engine/combat_hooks.py` | 9 个 Hook 类，每个类=一个机制；其中 `AfterDamageEffectsHook` 一个类塞了 5 个机制 |
| 10 | 局外事件 | `engine/events.py` | "按事件名 + 选项文本"的巨型 if 链（本报告只标注，不在战斗改造范围内） |
| 11 | **第二实现** | `sim/balance_sim.py`（311 行 `exclusive_round_start` 等） | 模拟器**独立重写**了一份战斗结算——这是全仓库最危险的一份重复，规则改一边必然漂移 |

数字证据（实测）：

```
has_status(...) 硬编码条件       73 处（combat.py 47 + api.py 4 + hooks/models 22）
side_has(...)   遗物条件         33 处
is_alive = False 直写            10 处
current_hp = ... 直写            20 处
add_status(...) 状态授予点       26 处
hasattr/getattr 私有记账字段     89 处命中（_jiahuo_left/_beifu_left/_nilin/_bizhai/_qingsuan/
                                _dongcha_pending/_shouyedeng_granted/_bianxing_original/
                                _transplanted_daowen/_pending_split_clones/_resonance_rewrites…共 ~12 种）
CombatEvent 的游戏逻辑订阅者      0 个（见 2.1）
道纹公式函数（calculate_*）      73 个
```

### 2.1 关键发现：事件总线是"只写不读"的

`CombatEvent` / `CombatEventType`（`combat_events.py`，14 种事件）已经存在，`CombatEngine._emit`（combat.py:59）
与 `GameState.emit_combat_event`（models.py:837）在伤害、回复、血限、死亡等关键点**都有发事件**。
但全仓库实测：**没有任何游戏逻辑订阅这些事件**。`event_stream()` 无消费方，`battle_report.py` 也不读它，
`api.py:3641` 只负责清空列表。

机制要回答"本回合发生过什么"时，不走总线，而是各自维护私有账本：
`_hp_loss_events / _heal_events / _speed_change_events / _blood_limit_events` 四个事件列表 +
`hp_lost_this_round / damage_dealt_this_round / actions_used_this_round / blood_oath_used_this_round /
mana_inflicted_this_round / no_action_rounds / no_damage_rounds` 等近十个计数器字段。
唯一的"读事件"机制是**皮衣**（combat.py:1751 读 `_hp_loss_events`）——它证明了这个需求存在，也证明了总线该承担它却没承担。

### 2.2 典型案例解剖：机制"活血"横跨几个文件

以用户自己规则书里的"本回合掉过血就回复"为例：

1. 记账：`hp_lost_this_round` 字段定义在 `models.py:237`，累加点有 3 处（`Entity.take_damage`、`_raw_hp_loss`、`BaolieHook` 直写——后者带一段注释解释为什么必须手写，因为 Hook 拿不到 combat）；
2. 结算：`combat.py::round_end`（1759 行，活血 if 块）里的专用逻辑；
3. 重置：`combat.py::round_start`（1404 行附近）归零 + 私有事件列表清空；
4. 测试：`tests/test_huoli_global.py` 单独一份。

**5 处核心代码 + 1 份测试**。而它的规则原文只有一句话。这就是要消灭的维护模式。

---

## 3. 逐题回答（审计五问）

### 3.1 哪些代码在重复实现同一种基础行为？

**重复 #1：扣血 + 记账，六条路径。**
`Entity.take_damage`（格挡/固执/眩晕）、`_raw_hp_loss`（绕过格挡）、`BaolieHook`（直写 `attacker.current_hp`）、
`pay_numeric_cost→_pay_bleed_cost`（走 take_damage）、`round_end` 赤族诅咒（直接 `take_damage(20, "代价")`，
注释明确说"不能走 pay_numeric_cost 否则改规则"）、`_action_buy_road`（直接 `take_damage`）。
六条路径各自维护 hp_lost_this_round / _hp_loss_events / ctx / 死亡判定的不同子集——上一份审计的 P0 问题全部由此产生。

**重复 #2：死亡，三种口径。**
`_check_hp_zero_death`（统一管线）vs 10 处直写 `is_alive=False` vs `depart_battle`（离场≠命零）vs 崩解（`add_mutation` 内部翻标记）。
上一份审计已证明"同是死亡，来源不同结果不同"（焦黑发丝不触发、招魂捞不到尸体）。

**重复 #3：受伤时改伤害数值，同一形状写了三遍。**
`DragonBloodlineMultiplierHook`（×2）、`JiahaiHook`（+X）、`LonglinHook`（−X 且 max(0) 截断）。
三者结构完全同构：`触发=incoming damage；条件=has 某物；效果=amount 算术`。差别只在数值和截断方式。
今天再加一个"受到伤害时伤害+3"的新状态 = 新写一个 Hook 类 + 注册 + 测试。

**重复 #4：受伤后做某事，散布在三个渠道。**
`AfterDamageEffectsHook`（逆鳞叠层/伤痕扣血限/切割/寄生吸血/负岳索回复/龙族血脉斩杀——**一个类五个机制，且每个内部还手写 lethal 判定次序**）、
`resolve_attack`（兴奋+速度）、`round_end`（活血/皮衣按累计值结算）。同一个"after damage"时机，三个家。

**重复 #5：回始/回终逐状态结算，十个同构 if 块。**
自愈/衰败/勾魂/洞察/畸变/凡庸/血族血脉/赤族诅咒/活血/皮衣，全部是
`for entity in all: if entity.has_status("X"): 做一件事`——唯独效果体和记账字段不同。

**重复 #6：状态 + 隐藏计数器模式，~10 次重造轮子。**
嫁祸(`_jiahuo_left`)/背负(`_beifu_left`)/逆鳞(`_nilin`)/洞察(`_dongcha_pending`)/逼债(`_bizhai`)/清算(`_qingsuan`)/
守夜灯(`_shouyedeng_granted`)/变形(`_bianxing_original`)/移植(`_transplanted_daowen`)/分裂(`_pending_split_clones`)。
每种都有自己的一套"何时挂、何时减、何时清"逻辑，写在不同文件的缝隙里。

**重复 #7（最危险）：`sim/balance_sim.py` 是战斗结算的独立第二实现。**
它有自己的 `exclusive_round_start`（311 行）等函数。引擎与模拟器对同一规则必然漂移——
穷举胜率报告的结论建立在第二实现上，两边数值不一致时无法知道哪边是"真"。

### 3.2 哪些 Hook 是为了弥补"没有通用机制系统"而产生的？

**整个 `CombatHook` 体系就是。** 它不是通用机制系统，而是"把 if 从 combat.py 搬进 Hook 类"的补丁容器。三条证据：

1. **一个类塞五个机制**：`AfterDamageEffectsHook`（combat_hooks.py:252）把逆鳞/伤痕/切割/寄生/负岳索/龙族血脉斩杀
   串在一个方法里，还不得不在方法内部手写"先翻 is_alive 再通知死亡"的次序注释——因为它没有独立监听点可用。
2. **系统已分叉出两条实现渠道**：`BifenglingHook.on_dodge` 与 `ShouyedengHook.on_round_start`
   各自带"⚠️ 双实现登记"注释——引擎真实分发走 `_note_dodge` / `_grant_shouyedeng`，Hook 版本只有单元测试调用，
   "迁移前禁止接线，否则 +3 两次"。同一个机制两份代码，这本身就是架构信号。
3. **`explicit_dispatch` 标记的存在就是补丁**：它存在的唯一理由是防止"通用遍历 + 显式分发"两条路径把同一机制触发两次
   （combat_hooks.py:255–258 的注释写得明明白白）。一个机制系统如果需要"绕开自己"，说明分发模型错了。

另外，`_hp_loss_events / _heal_events / _speed_change_events / _blood_limit_events` 这四个私有事件列表
也是"没有可查询总线"的补丁——机制只能自己记账自己读。

### 3.3 哪些特殊入口可以被统一基础效果取代？

**可以被取代（迁移收益高、风险低）**：

| 类别 | 现有入口 | 可统一为 |
|------|----------|----------|
| 伤害数值修正 | `DragonBloodline/Jiahai/Longlin` 三个 Hook | `when=伤害调整, condition, effect=amount算术` |
| 伤害后反应 | `AfterDamageEffectsHook` 5 个 if + 兴奋 + 活血 + 皮衣 | `when=伤害落地, effect=基础动词` |
| 回始/回终计时器 | `round_start/round_end` 的 10 个 per-status 块 | `when=回始/回终, condition, effect` |
| 遗物触发 | `process_relics` 十几个 if 块 + 散落四处的遗物 | 遗物=机制声明表的条目 |
| 死亡反应 | `_on_entity_death` 内焦黑发丝/招魂/分裂 | `when=命零, condition, effect` |

**不可被取代，必须保留为"命名动词"（这是系统真正的不可约部分）**：

- 伤害**重定向**（嫁祸/背负）——改变管线目标，不是数值效果；
- 濒死**保护/代偿**（撤退/负岳碑/断尾求生）——拦下伤害并产生副支付；
- 伤害前**反噬压制**（爆裂）——攻击者在命中前死亡、本次伤害作废；
- **斩杀**（龙族血脉对怪物直接命零）；
- **多段拆分**（裂变）；
- **生成实体**（分裂复制体、赶尸铃行尸）；
- **批量优先结算**（凡庸：非轮回者优先 + 战场清空即中断其余结算）；
- **多路径胜利**（救赎/癌变/还债）；
- **决策协议耦合**（闪避/必中/血影/回锋刀反击目标选择、负岳碑付账选择——这些与 prepare/resolve 两阶段 AI 协议绑定）。

注意：这些"动词"今天的问题是**没有名字**——它们以 if 块的形式内联在管线里。目标不是消灭它们，
是给它们注册名（`redirect / mitigate / reflect / execute / split / spawn / batch`），让新机制能**引用**它们。

### 3.4 哪些机制目前必须写专用代码？

两类：
1. 上表的"动词"清单（约 9 个），它们是控制流，永远需要代码——但只需要写**一次**动词本体，
   之后新机制通过声明引用；
2. 与两阶段显式决策协议绑定的机制（回锋刀、负岳碑、血契等可选遗物）——这些的复杂度在
   "AI 要提交什么参数"而不在效果本身，属于 API 协议问题，不应塞进效果系统。

此外**每个道纹的数值公式**（`daowen.py` 的 73 个 `calculate_*`）也永远是一对一的代码——
规则原文就是"消耗X造成2X伤害"这种字面公式，这不该统一（统一公式 = 丢失字面性）。该统一的是公式**产物的应用**。

### 3.5 三个新机制，今天分别要改哪里？（实测推演）

**例1：「每当任意角色因为失血死亡时，使所有敌人获得1点疯狂」**
今天：① 若"疯狂"是新状态，定义状态效果语义；② `_on_entity_death` 加 if 块遍历敌人加状态；③ 补 ctx；④ 补测试。
→ 动核心 2～3 处。
理想：`when=ENTITY_DIED, target=所有敌人, effect=gain_status(疯狂,1)` 一条声明 + 1 个测试。

**例2：「本回合每受到一次伤害，下次回复量+2」**
今天：① Entity 加计数器字段（含序列化 `_serialize_entity_full` 同步）；② 伤害管线加累加点；
③ `apply_heal` 加消费点；④ `round_start` 加归零；⑤ 补测试。
→ 动核心 4～5 处，且"伤害管线累加点"有多个正确位置（走 `_record_hp_loss_event` 还是 `take_damage` 还是管线末尾？），又是新的裁定坑。
理想：`when=DAMAGE_APPLIED(自身), effect=counter+1` + `when=HEAL_APPLIED, condition=counter>0, effect=回复+2×counter并清零`。

**例3：「如果一个角色在本回合既受到过伤害又获得过回复，则回合结束时……」**
今天：① 两个计数器（或读两个私有事件列表）；② `round_end` 加 if 块；③ `round_start` 归零；④ 测试。
→ 动核心 3 处。
理想：`when=ROUND_END, condition=events_this_round(DAMAGE)且events_this_round(HEAL), effect=…`。

三个例子的共同点：**所需的事实数据（谁、何时、受/回多少）在事件流里已经全部存在**（伤害有 DAMAGE_APPLIED、
回复有 HEAL_APPLIED、死亡有 ENTITY_DIED、回合边界有 ROUND_START/ROUND_END）。缺的只是"有人按声明去读它"。

---

## 4. 正式回答 A–H

### A. 当前架构距离这种设计还差什么

1. **事件总线只写不读**（最大缺口）。`CombatEvent` 有词汇表、有发出点、零订阅者。机制系统化 = 让订阅成为合法入口。
2. **没有"机制声明"这个第一公民**。机制今天的存在形态是"分散的 if 块 + 私有字段 + 重置代码"，没有
   `{when, target, condition, effect, priority, state}` 这种可注册、可排序、可枚举、可审计的单元。
3. **效果没有动词库**。同是"使目标失去生命"，有六条手写路径；同是"回复"，四处手写。
   应该只有 `apply_heal` 一个动词，其余全是"读声明、调动词"。
4. **条件不是可组合对象**。`has_status("X")` 出现 73 次、`side_has` 33 次，每次都内联在管线里，
   无法表达"本回合既受过伤害又获得过回复"这种组合条件。
5. **私有记账没有统一的生命周期**。~12 种 `_xxx` 字段 + 近 10 个计数器，各自在 `round_start`/`round_end`
   的缝隙里归零，新增机制 = 新增字段 + 新增归零点，且序列化必须同步（漏一次就是存档 bug）。
6. **优先级只存在于 Hook 层**。管线内联 if 块和回始/回终循环的顺序是"代码位置隐式顺序"，
   无法被声明层控制；要数据化必须先把每个内联块的相对顺序显式固化（像上一轮对 Hook priority 做的那样）。
7. **存在第二实现**（`sim/`）。任何"表驱动"改造若不同步 sim，会加剧漂移；这是迁移时必须列入计划的风险点。

### B. 哪些现有系统应该保留（不要动）

1. **`EffectContext` 与 ctx 穿透**（effect_context.py）——"为什么发生"的唯一载体，因果链已落地，保留并继续补齐（血限等缺口）。
2. **六个统一写入入口**：`_apply_hostile_damage` / `apply_heal` / `pay_numeric_cost` / `_apply_blood_limit_change` /
   `_check_hp_zero_death` / `_on_entity_death` + `depart_battle`。它们就是未来的"动词实现体"，一个都不能拆散。
3. **伤害管线的七段次序与 Hook priority 数值**（combat_hooks.py 头部注释已声明"顺序即规则"）——这是 DM 裁定，
   加害先于龙鳞、爆裂先于格挡……任何改造都只能"照抄这段顺序"，不能重排。
4. **`DaoWenEngine` 的 calc 公式层**（73 个 `calculate_*` 及其标准化 calc dict）——字面公式就该一对一的写；
   改造的是 calc dict 的**应用侧**（`apply_daowen_effect` 的效果段），不是公式侧。
5. **`StatusEffect` 状态系统与 `side_has` 语义**——它们是条件语言的词汇，是资产不是负担。
6. **`TriggerTiming`（11 时点）与 `CombatEventType`（14 事件）枚举**——字面时点词汇表已经建好，直接当机制的 `when` 用。
7. **prepare/resolve 两阶段决策协议**（怪物回合、闪避提交、遗物选择）——AI/API 契约，与效果系统分层。
8. **安全网**：`watchdog.py` / `validator.py` / `rule_sync.py` / `dm_rulings.py` + 886 个测试——它们是"每个迁移阶段必须全绿"的客观标尺。

### C. 哪些系统应该逐渐降级为底层基础设施

1. **`CombatHook` / `CombatHookManager`**：从"机制登记处"降级为"管线内部的监听分发器"。
   机制以后写在声明表里，管线各段只负责"按 priority 调监听器"。Hook 类作为迁移过渡继续存在，逐个被声明条目替代后删除。
2. **`CombatEvent` 总线**：从"只写审计日志"升格为"机制分发的骨干"。`_emit` 调用点就是触发点；
   各 `_record_*_event` 私有列表退役，由总线按回合窗口查询取代。
3. **四个私有事件列表 + 近 10 个计数器字段**：降级为"回合窗口事件查询"的缓存实现，不再由各机制单独维护。
   字段本身为序列化兼容可以保留，但写入点收敛到总线。
4. **`round_start` / `round_end`**：降级为"宣布时点 + 基础记账重置"（法力、格挡、tick 等框架动作），
   机制化内容全部迁移为回始/回终监听器。
5. **`process_relics`**：降级为"遗物声明表的执行器"，if 块逐个迁出，最终它只遍历表。

### D. 哪些系统属于历史遗留的特殊分支

1. **`BaolieHook` 直写 `attacker.current_hp`**（combat_hooks.py:109–127）——Hook 拿不到 combat 的产物，
   靠注释维持正确性（还带 hp_lost_this_round 手工同步）。这是"通道不统一"的化石。
2. **避风铃 / 守夜灯的"双实现登记"**（combat_hooks.py:90–105、120–137）——同一机制两份代码，
   Hook 版只有测试在调。迁移前禁止接线（会重复触发），但最终应合并为一份（需 DM 确认合并方向）。
3. **10 处直写 `is_alive = False`、20 处直写 `current_hp =`**——统一入口落地前的遗留，
   上一份审计已列出其中 3 处是已证实的 bug 源（血食/买路财/崩解）。
4. **`_apply_hostile_damage` 的 `legacy_ctx` 兼容层**（含 `context_warning` 字段）——迁移期垫片，
   全部调用点补上 ctx 后可整体删除。
5. **`AfterDamageEffectsHook` / `LethalMitigationHook` / `DamageRedirectionHook` 三个"巨 Hook"**——
   每个都包着多个机制 + 内部手写的次序/致死逻辑，是"机制没地方住"时代的合租房。
6. **`sim/balance_sim.py` 的独立结算**——最大的历史叉路。理想终点是模拟器驱动真引擎或消费同一张声明表；
   在此之前任何引擎侧机制改造都要评估对 sim 的漂移。
7. **`events.py` 的"事件名+选项文本"巨型 if 链**——与战斗同构的问题，但属局外事件域，本次不纳入改造范围，只登记在案。
8. **`api.py` 中按名字分发的消耗品 if 链**（`_consume_named_event_item` 等）——与 process_relics 同病，迁移优先级低于战斗核心。

### E. 最值得改造的 3～5 个核心抽象

按"收益/风险比"排序（全部是**新增层**，不拆现有层）：

**E1. 效果动词表（Verb Registry）——最高价值。**
把已存在的统一入口登记为命名动词：`damage / heal / hp_loss / blood_limit / speed / mana / shield /
status / cost / mutation / spawn / depart / execute / redirect / split`。
每个动词 = 一个 `(spec, ctx) → detail` 函数，实现体就是现有的统一入口（零新逻辑）。
价值：消灭重复 #1/#3/#4/#5 的"手写算术"，`apply_daowen_effect` 的效果段、Hook 的效果体、
round 循环的效果体全部改为调动词。**这是唯一一个"做了立刻减少代码行数"的抽象。**

**E2. 机制声明表（Mechanism Registry）——核心语法。**
`{name, when(事件/时点), target(选择器), condition(条件), effect(动词调用), priority, state(机制私有计数)}`。
机制从"if 块"变成"可枚举的表条目"，`process_relics` / round 循环 / 死亡管线 / Hook 类逐个迁入。
附带获得三样免费能力：机制清单可导出审计、优先级集中声明、新机制注册点唯一。

**E3. 事件总线升格为分发骨干（Trigger Bus）——承载 E2 的运行层。**
在 `_emit` / `emit_combat_event` 处加订阅分发（**订阅者初始为空，行为零变化**），
支持按 `(event_type, 谓词)` 订阅、按 priority 排序、按回合窗口查询。
顺序安全的关键设计：分发**在 `_emit` 调用点同步执行**——今天机制代码的位置就是未来的触发点，
迁移 = 把 if 块从代码位置搬到同位置的监听器，顺序天然不变；嵌套触发沿用现有 `_effect_chain_depth` 保险丝。

**E4. 条件组合子（Condition combinators）——让规则字面可写。**
一小撮命名函数：`has_status / side_has / is_alive / entity_type / hp_at_least /
events_this_round(事件, 主体=自己|敌方|任意) / first_time(entity, key) / not_ / any_ / all_`。
用户的例 3 直接写成 `all_(events_this_round(DAMAGE_APPLIED), events_this_round(HEAL_APPLIED))`。
价值：把 73 处 `has_status` 从"散布的 if"变成"可复用、可组合、可测试"的词汇。

**E5. 回合窗口记账标准化（Round Scope）——消灭私有字段乱象。**
一个回合作用域对象统一管理"本回合计数/事件窗口/重置"，机制声明通过它读写自己的计数器，
`round_start/round_end` 只保留一次 reset 循环。旧字段逐步收敛，序列化兼容期内保留字段但写入点统一。

> 注意：E1–E5 加在一起约 400～600 行新代码，是"表 + 注册器 + 组合子"级别的最小系统，
> **不是**通用规则引擎（没有 DSL、没有脚本、没有反射动态加载）。这一点在第 7 节再强调。

### F. 改造后，新增一个普通机制需要修改哪些地方

以"每当任意角色因失血死亡时，使所有敌人获得 1 点疯狂"为例（假定 疯狂 状态与 gain_status 动词已存在）：

| 步骤 | 内容 | 文件 |
|------|------|------|
| 1 | 一条声明条目（when/target/condition/effect/priority） | 机制声明表（唯一必改点） |
| 2 | 一个测试 | tests/ |
| 3 | 若"疯狂"是新状态语义 | 状态动词表加一行（一次性的动词级工作） |
| 4 | 若涉及新控制流（如"转移伤害"） | 动词表注册新动词 + 管线加一个调用点（不可约成本，但也是一次性的） |

**核心代码 `combat.py` 在 3、4 之外零修改。** 与今天的 3～6 处修改对比，维护面从"核心文件"转移到"声明表"。
而声明表的条目互相独立、可静态检查（`validator.py` 可新增"声明完整性检查"：每个 when 的事件类型必须存在、
每个 effect 的动词必须注册、priority 冲突报警），这正是把"维护规则"变成"校验器"。

### G. 最小可行迁移方案（分阶段，每阶段全绿才进下一阶段）

**Phase 0 — 冻结基线 + 建立"机制免入区"（0 行为变化）**
- 以当前 886 全绿 + 上一份审计报告为基线；
- 用 `validator.py` 增加一条**静态护栏**：`combat.py` 的管线函数（`_apply_hostile_damage_inner`、
  `resolve_attack`、`round_start`、`round_end`、`_on_entity_death`、`process_relics`）禁止新增
  `has_status("新名字")` 分支——新增机制必须走声明表；护栏本身带豁免清单，防止误伤迁移中的老代码。
- 这是"先立规，后迁移"的关键一步，成本极低、收益是永久挡住模式回潮。

**Phase 1 — 总线升格但订阅者为空（0 行为变化）**
- `_emit` / `emit_combat_event` 加订阅分发代码；初始零订阅者；全量测试必须与原基线逐字节同结果。

**Phase 2 — 垂直切片验证（1 个机制，证明模式）**
- 选"加害"（`JiahaiHook`）这一个纯数值机制迁移为声明条目；删除 Hook 类；全绿。
- 这步验证三件事：声明能表达它、顺序不变（priority 10→同值）、ctx 不断链。
- 模板定型后，后续机制迁移就是机械劳动。

**Phase 3 — 按家族批量迁移（每个家族单独一个 PR/一次提交）**
- 顺序建议：伤害数值修正（龙鳞/龙族血脉）→ 伤害后反应（逆鳞/伤痕/切割/寄生/负岳索/兴奋）→
  回始/回终计时器（自愈/衰败/勾魂/洞察/畸变/活血/皮衣/血族血脉/赤族诅咒）→
  遗物触发（process_relics 全部）→ 死亡反应（焦黑发丝/招魂/分裂）。
- 每迁一个，删除旧 if 块与旧 Hook 类，并新增"迁移前后行为等价"的黄金测试。

**Phase 4 — 动词表接管道纹效果段（大块但低风险）**
- `apply_daowen_effect` 的 640 行中，效果应用段改为调动词表；calc 公式层不动；特殊 case（蒙蔽/坏死/
  煞气/自愈除外…）保留为"效果前/后修正器"并在声明中登记，**不顺手"简化"任何一条现有规则**。

**Phase 5 — 硬动词（最后做，或不做）**
- 重定向/濒死保护/凡庸/裂变/分裂等控制流机制：把管线各段改为"开放监听点"，逐个迁入。
- 凡庸、多路径胜利这类裁定密集的机制**允许永远留在原位**，只在管线里留一个明确的"专属段"标记。

**Phase 6 — sim 对齐（可选但建议排期）**
- 评估 `balance_sim.py` 改为消费声明表或直接驱动真引擎；在此之前，引擎侧新机制必须同步登记 sim 侧差异。

每阶段验收标准：**全量 pytest 绿 + 黄金场景战报逐字节 diff 为 0**。

### H. 绝对不要动的东西（防炸清单）

1. **伤害管线七段次序与 Hook priority 数值**——上一轮已把它们固化为 DM 裁定，动一个数 = 改规则（文档里有加害/龙鳞的具体反例）。
2. **`Entity.take_damage` 的格挡/固执/眩晕语义**，以及"代价不被格挡、不被固执压帽"的例外——这是字面规则本体。
3. **`pay_numeric_cost` 的血誓戒/烙痕钉/血契副触发**、`apply_heal` 的龙血瓶溢出存储、不朽之躯的钳制点、
   蒙蔽/坏死/定型/不朽之躯的阻断规则——这些横切块每条都对应一条 DM 裁定，迁移只能平移不能"顺手改对"。
4. **道纹 calc dict 的键与数值**（73 个公式、X 上限表、冷却语义）——公式是规则的镜像，任何"统一公式"都禁止。
5. **骰子调用点与调用次序**——`dice.auto_roll` 的消耗顺序是确定性随机序列的一部分，重排事件 = 改变所有下游随机结果。
6. **序列化格式**（`_serialize_entity_full` 等）——兼容期内禁止改字段名/类型；新计数器走通用 state dict。
7. **prepare/resolve 两阶段协议与 AI 提交参数契约**（闪避/血影/回锋刀目标/负岳碑付账）——AI 与 API 消费者依赖它。
8. **凡庸的"非轮回者优先 + 战场清空即中断"、多路径胜利（救赎/癌变/还债）的结算次序**——裁定密度最高的区域，最后迁移或永久保留。
9. **避风铃/守夜灯的双实现**——在 DM 裁定合并方向之前，不要以"重构"名义顺手合并（那是行为变更，不是架构变更）。
10. **`sim/balance_sim.py` 与引擎的同频改造**——禁止在同一提交里同时改两边；先引擎、后 sim、分开验证。

---

## 5. 特别问题：第四宇宙适合 StS 式 Effect / Trigger / Condition / Target 吗？

### 5.1 结论：非常适合——但要选对 StS 的哪一半

《杀戮尖塔》其实有两个层次的架构：

- **上层：Power / Relic 的声明式反应**（"`onAttacked`、`atBattleStart`、`atEndOfTurn`"）——**响应式监听**；
- **下层：AbstractAction 队列**（DamageAction、ApplyPowerAction 入队、依序弹出执行）——**过程式编排**。

第四宇宙需要的是**上层，不是下层**。理由：

1. **规则原文本身就是"时点 + 条件 + 效果"句式**。用户的例子"每当场上有一个怪物死亡时，你的速度+2"
   （焦黑发丝原文）就是一条 Power 声明。第四宇宙的机制绝大多数是**被动反应**，不是主动编排——
   StS 的 Action 队列解决的是"一张牌打出 15 个动作按序执行"，第四宇宙没有这个需求。
2. **词汇表已经建好了 90%**：`TriggerTiming`（战始/战终/回始/回终/敌回始/敌回终/目标选定/受到伤害前/
   受到伤害后/失去生命后/失去生命前/常驻）+ `CombatEventType`（14 种）+ `EffectContext.mechanic/subtype/tags`。
   这不是需要发明的新东西，是把**已有的字面词汇**从"枚举"升级为"分发点"。
3. **顺序敏感性比 StS 更重，这恰恰支持监听模式而非队列模式**。加害先于龙鳞、爆裂先于格挡——这些顺序
   在"监听器按 priority 排序"下是显式声明；在 if 堆叠下是隐式位置。StS 的 Power 也有同样的顺序需求
   （Barricade/Orichalcum 顺序争议），它靠硬编码列表解决；我们已有 priority 机制，比它还清晰。
4. **一个关键差异要注意**：StS 的 Power 是"挂在角色上随角色移动"的对象，天然解决"状态+私有计数器"的归宿问题
   （`_jiahuo_left` 这类字段在 StS 里就是 Power 的实例属性）。E2 的机制声明表 + 每实体 state dict 就是
   对这个模式的轻量复刻——**这是比事件总线本身更重要的借鉴定式**：机制的私有状态不该长在 Entity 上，
   应该长在机制自己的实例里。

### 5.2 最小版本（约 400 行，不是通用规则引擎）

基于现有代码给出最小设计（本次不实现，仅设计；Phase 1/2 验证后再落地）：

**① 分发点 = 现有 `_emit` 调用点（零新触发点）**

```python
# engine/mechanisms.py（新文件，约 400 行）
class TriggerBus:
    """订阅式分发。只在现有 _emit / emit_combat_event 处调用，初始零订阅者。"""
    def __init__(self):
        self._listeners: dict[CombatEventType, list[Mechanism]] = defaultdict(list)
    def register(self, m: "Mechanism") -> None:
        self._listeners[m.when].append(m)
        self._listeners[m.when].sort(key=lambda x: x.priority)   # 数字小先执行，与 Hook 同义
    def dispatch(self, event: CombatEvent, combat) -> None:
        # 同步执行（保持现有"内联代码位置"的时序语义）；
        # 监听器产生的新事件进入下一层 dispatch，沿用 combat._effect_chain_depth 保险丝。
        for m in list(self._listeners[event.event_type]):
            if not self._listeners[event.event_type]: ...  # 迁移期保护：机制可能在途中注销
            if m.condition(event, combat):
                m.effect(event, combat, m)
```

**② 机制声明 = 字面规则的直接翻译**

```python
@dataclass
class Mechanism:
    name: str                 # 字面名（"焦黑发丝"）
    when: CombatEventType     # 【什么时候】
    target: Callable          # 【对谁】选择器，返回实体列表
    condition: Callable       # 【满足什么条件】组合子
    effect: Callable          # 【做什么】动词调用
    priority: int = 100
    state: dict = field(default_factory=dict)   # 机制私有计数器（StS Power 模式），按 entity.id 分片

# 用户例1：
Mechanism(
    name="疯狂扩散",
    when=CombatEventType.ENTITY_DIED,
    target=lambda ev, c: [e for e in c.state.get_all_enemy_side() if e.is_alive],
    condition=lambda ev, c: True,
    effect=lambda ev, c, m: [VERBS["status"](e, {"name": "疯狂", "value": 1, "source": m.name}, ev.ctx) for e in ...],
)
# 用户例2（"每受一次伤害，下次回复+2"）拆成两条：
#   A: when=DAMAGE_APPLIED, target=[自己], condition=is_alive, effect=state["hits"]+=1
#   B: when=HEAL_APPLIED,   target=[自己], condition=state["hits"]>0,
#      effect=heal(actual_heal + 2*state["hits"] 后再 apply_heal，随后清零)
# 用户例3：
#   when=ROUND_END, target=[本实体], 
#   condition=all_(events_this_round(DAMAGE_APPLIED), events_this_round(HEAL_APPLIED)), effect=...
```

**③ 动词表 = 现有统一入口的注册表（实现体零新逻辑）**

```python
VERBS = {
    "damage":       combat._apply_hostile_damage,      # 含全部管线语义
    "heal":         state.apply_heal,                  # 含龙血瓶溢出语义
    "hp_loss":      combat._raw_hp_loss,
    "blood_limit":  combat._apply_blood_limit_change,  # 含命零判定
    "cost":         combat.pay_numeric_cost,           # 含血誓戒/烙痕钉/血契语义
    "status":       add_status_with_ctx,               # 薄封装：add_status + ctx 记账
    "speed":        combat._gain_speed / _lose_current_speed,
    ...
}
# 硬动词同样注册：redirect/mitigate/reflect/execute/split/spawn —— 
# 实现体就是今天管线里那几段代码，原样搬入，顺序不动。
```

**④ 条件组合子（20 行级别）**：`has_status / side_has / is_alive / entity_type / events_this_round / any_ / all_ / not_`。

**⑤ 回合窗口**：`TriggerBus.events_this_round(entity, event_type)` 直接查询已发事件列表（取代 `_hp_loss_events` 等私有列表），
`round_start/round_end` 各保留一次窗口清空。

**不做什么（刻意的边界）**：
- 不做 DSL/脚本/JSON 机制配置——机制声明就是 Python 数据，`validator.py` 负责静态检查；
- 不做通用"规则引擎"（前向链/冲突消解）——触发顺序全部由 priority 显式声明，不做自动推导；
- 不做 Action 队列——事件同步分发、嵌套靠现有保险丝收敛；
- 不迁 events.py 的局外事件域、不迁道纹公式层。

**为什么这个版本"小且够用"**：它不发明任何新语义——when 复用 `CombatEventType`，
effect 复用七个统一入口，顺序复用 priority，窗口复用现有事件列表。它只做一件事：
**把"机制写在哪里"从核心文件搬到声明表**。

### 5.3 预期的直接收益

- 新增普通机制：核心代码零修改（第 F 节流程）；
- 机制全景可审计：`validator.py` 输出"声明表 vs 残留 if 块"漂移清单，把"维护规则"变成"校验失败"；
- 文档/代码同源：声明条目含字面名与 `when`，`rule_sync.py` 可以把 README 规则与声明表逐条对账；
- sim 侧未来可消费同一张表，消除第二实现漂移。

---

## 6. 最后直说

**当前代码距离"新增机制 → 组合已有基础规则"还很远，这是事实**：任何新机制今天都要动 3～6 处核心代码，
且每个接触点都暗藏裁定坑（哪个记账点算数、死亡通知次序、序列化同步）。

但**不需要也不应该重写**。上一轮收尾已经把"词汇表"（ctx、事件类型、时点枚举）和"动词实现体"
（七个统一入口）铺好了——本报告 E 节的五个抽象全部是**新增一个声明层**，不是替换结算逻辑。
风险最高的部分（管线顺序、横切裁定、骰子次序）都在 H 节冻结清单里。

路线就是：**Phase 0 立护栏 → Phase 1 空总线 → Phase 2 一个机制证明模式 → 按家族平移**。
每一步以"886 测试全绿 + 战报逐字节一致"为验收。如果连"加害"都迁不动，
就说明本报告对顺序语义的理解有误——那是唯一一个真正便宜的实验，也是唯一的正确起点。
