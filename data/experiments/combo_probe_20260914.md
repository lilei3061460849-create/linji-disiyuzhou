# 构筑完整 PVE 实引擎探针记录（2026-09-14）

## 判定口径

- 可复跑探针：`sim/probe_combo_full.py`。
- 引擎：仓库 `engine.api.GameEngine`。
- 战斗：`battle_start` → `round_start` → `TacticalAI` 实际出手 → `resolve_ally_phases` → `_resolve_monster_turn` → `round_end` → `battle_end`。
- 局外：真实调用 `choose_discovered_relic`、`setup_choose_initial_daowen`、`setup_choose_resonance`、`setup_choose_region`、`pre_battle_action`。
- 七场：每场必须成功调用 `battle_end`；未完成第7场不记为通关。
- 角色名：`新角色探针`，未使用顾砚舟。
- 默认属性：血7、速8、法10；属性变体另行抽查。
- 目标遗物不在开局候选时，该seed记为“遗物缺席”，不伪造选择。
- 目标道纹的扭曲都市专属路径使用真实战斗动作 `use_resonance`：
  - `畸变 --曲解--> 超频`；
  - `超频 --反转--> 搏命`。
- 【血炼周天】不是 `SPELL_REGISTRY` 中的普通法术；探针按真实接口两步提交 `pre_battle_action(sub=custom_spell)`，先取得待裁定中断，再以 `dm_approved=true` 重提交并确认学会。

## 代表性完整七场尝试

| 构筑 | seed / 副本 | 开局遗物候选 → 实选 | 初始道纹候选 → 实选 | 目标获得 | 实际清除 | 失败场次与引擎结果 |
|---|---|---|---|---|---:|---|
| 承露盏 + 血炼周天 | 1 / 龙心谷 | 同魂笔、承露盏、买路财 → 承露盏 | 血债、再生、束缚 → 再生 | 局外真实学会血炼周天 | 0/7 | 第1场；怪物阶段后 `player_dead`，事件流为 `entity_died(cause=cancer)`，烙痕祭司在场 |
| 承露盏 + 血炼周天 | 12 / 扭曲都市 | 守夜灯、三相残韵盘、承露盏 → 承露盏 | 封印、波及、庇护 → 庇护 | 局外真实学会血炼周天 | 1/7 | 第2场；`resolve_monster_phase` 返回 `player_dead`，在场怪物为千手蜈蚣 |
| 回锋刀 + 超频 | 15 / 扭曲都市 | 回锋刀、血誓戒、承露盏 → 回锋刀 | 杀伐、固执、波及 → 杀伐 | 第1场真实执行 `曲解·畸变→超频`，引擎返回 `granted_daowen=超频` | 0/7 | 第1场；`resolve_monster_phase` 返回 `player_dead`，在场怪物为千手蜈蚣 |
| 避风铃 + 超频 | 78 / 扭曲都市 | 回锋刀、买路财、避风铃 → 避风铃 | 封印、血债、透支 → 封印 | 第1场真实执行 `曲解·畸变→超频`，引擎返回 `granted_daowen=超频` | 4/7 | 第5场；初始人头气球及回合增援孢子母体未能在玩家死亡前清除，`resolve_monster_phase` 返回 `player_dead` |
| 回锋刀 + 搏命 | 4 / 扭曲都市 | 回锋刀、血契、买路财 → 回锋刀 | 增殖、贯穿、庇护 → 庇护 | 第1场真实执行 `反转·超频→搏命`，引擎返回 `granted_daowen=搏命` | 0/7 | 第1场；`resolve_monster_phase` 返回 `player_dead`，在场怪物为骨天使 |
| 避风铃 + 搏命 | 8 / 扭曲都市 | 回锋刀、避风铃、无所求 → 避风铃 | 庇护、血债、杀伐 → 庇护 | 第1场真实执行 `反转·超频→搏命`，引擎返回 `granted_daowen=搏命` | 0/7 | 第1场；`resolve_monster_phase` 返回 `player_dead`，在场怪物为骨天使 |

## seed 1—100 扫描汇总

遗物缺席的seed跳过；其余seed均真实选择目标遗物并进入完整PVE循环。下表的 `max` 是本次探针在该组合和副本下的最大实际清除场数，不是规则上限。

| 构筑 | 遗物 | 副本 | 目标遗物被选seed数 | 目标道纹真实残韵获得数 | max 实际清除 |
|---|---|---|---:|---:|---:|
| 承露盏 + 血炼周天 | 承露盏 | 罪孽都市 | 34 | — | 0 |
| 承露盏 + 血炼周天 | 承露盏 | 扭曲都市 | 34 | — | 1 |
| 承露盏 + 血炼周天 | 承露盏 | 龙心谷 | 34 | — | 1 |
| 回锋刀 + 超频 | 回锋刀 | 扭曲都市 | 25 | 6 | 5 |
| 避风铃 + 超频 | 避风铃 | 扭曲都市 | 23 | 7 | 5 |
| 回锋刀 + 搏命 | 回锋刀 | 扭曲都市 | 25 | 7 | 5 |
| 避风铃 + 搏命 | 避风铃 | 扭曲都市 | 23 | 11 | 5 |

其余副本中，目标超频/搏命没有完成对应的扭曲都市残韵获得路径；扭曲都市是本次速度类构筑的实际测试副本。

## 结论

本次真实引擎结果中：

1. 【承露盏】＋【血炼周天】：0/7 或最多1/7，没有完整七场 PVE 通关。
2. 速度遗物＋【超频】：目标道纹可以通过真实残韵动作获得，但代表性完整流程最高4/7；扫描最高5/7，没有7/7。
3. 速度遗物＋【搏命】：目标道纹可以通过真实残韵动作获得，但代表性完整流程0/7；扫描最高5/7，没有7/7。
4. 两组均未达到“完整7场PVE成功”门槛，因此本轮不启动死斗测试，也不把局部道纹数学测试当作通关结论。
