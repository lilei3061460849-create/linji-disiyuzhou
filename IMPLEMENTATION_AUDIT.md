# 实现情况审计：代码 vs README 规则书

> **⚠ 归档说明（2026-08-04 分支合并时补记）**
> 本审计针对的是 143017e「精简引擎」谱系（arena/019fc75b 分支）。
> 2026-08-04 多分支合并后，主干采用 main（PR#2 谱系）的引擎实现：
> 文中「修复记录」提到的 engine/content.py、engine/spells.py、tests/test_fixes.py
> 已由 main 谱系的 engine/gamedata.py + engine/api.py 法术管线取代，未随合并保留；
> 其完整内容仍可通过本合并提交的第二父提交（f6939df）在 git 历史中查阅。
> 本文保留作为该谱系的审计记录；2026-08-04 合并时的最新缺口核对结论见合并 PR 描述与对话记录。

> **状态更新（2026-08-03 第二轮）**
> 本文档下半部分列出的 P0 缺口已修复，详见文末「修复记录」。
> 「怪物×3规则」经确认为 README 未及删除的废案，已从正文与代码中彻底移除。


审计对象：`engine/`（7128 行 Python）对照 `README.md`（68KB 规则正文）
审计日期：2026-08-03

---

## 总览

| 层次 | 状态 |
|------|------|
| 数学层（道纹公式、伤害/回复/格挡计算） | ✅ 基本完整 |
| 框架层（状态机、行动分发、中断、DM裁定库、校验器） | ✅ 完整 |
| **内容层（遗物/法术/事件/怪物/副本数据）** | ❌ **几乎全空** |
| **流程层（第8场死斗、特殊事件自动触发）** | ❌ **未实现** |

一句话结论：**引擎的"计算器"和"骨架"做完了，"内容数据库"和"后半段流程"没做。**
所有 63 个道纹的公式都能算，但游戏里没有任何一件遗物、一个法术、一个事件、一只怪物是有数据的 —— 这些全靠调用方（AI/DM）手工传参。

---

## 一、✅ 已实现（可用）

### 1. 道纹系统 —— 完整度最高的部分
- **63 个道纹的数值公式全部实现**（`engine/daowen.py`），逐一比对 README 无遗漏、无多余：
  - 杀伐闭环 7 个：杀伐/再生/庇护/固执/血债/冲击/慈悲
  - 锐利闭环 7 个：锐利/增殖/束缚/透支/贯穿/封印/缓慢
  - 原始怪物道纹 7 个 + 转化道纹 19 个
  - 三个一阶副本专属道纹各 8 个（扭曲都市/罪孽都市/龙心谷）
- `DaoWenEngine.resolve()` 统一分发，`ceil()` 落实了「整数规则：所有计算向上取整」
- 自由控 X 规则：X 由调用方传入并校验 ≥1

### 2. 残韵系统
- `ResonanceEngine` 实现了转换/反转/曲解三条路径的闭环查表（`find_transformation`）
- 落实了残韵作用规则第 2、3 条：作用于自己拥有的道纹 → 永久替换；作用于他人道纹 → 施法者获得
- 残韵库存校验 + 消耗（`_action_use_resonance`）

### 3. 战斗计算核心
- 伤害/格挡/闪避/回复/血限增减的实体级结算（`models.Entity`）
- **格挡[敌回终]清空**、**法力[敌回终]清空**、持续X回合递减（`combat.round_end`）
- **[回始]法力补满至法限**
- **怪物非专属道纹 ×3 规则**：`is_monster_triple()` 正确排除了原始/转化道纹与副本专属道纹
- 怪物出手次数 = 回合数÷3 向上取整（`battle_flow.get_monster_actions`）
- 闪避消耗 1 点当前速度、必中无法闪避
- 代价伤害不被格挡吸收（`take_damage(..., "代价")`）

### 4. 随机数机制（完全符合规则）
- `DiceEngine` 池系统：程序只给范围（如 `1~15`），由玩家报数，程序按池内顺序结算
- 引擎自身不生成随机数 —— 符合「随机数规则」
- `EventPool` 支持「已触发事件移出池」的池收缩逻辑

### 5. 中断 + DM 裁定机制（设计亮点）
- 急中生智 / 进化 / 逃跑与追击 / 员工叛变 / 死之传承 / 未见场景 六类 `InterruptType`
- SQLite 先例库 `DMRulingsDB`，带 FTS 全文搜索、`find_similar()` 先例匹配、`match_count` 计数
- 落实「未见场景裁定落库法则」的技术载体
- 怪物困境检查 `check_monster_difficulty()`：6 类信号，≥2 条即触发进化/逃跑裁定

### 6. 规则校验器（README 没要求，属于超额实现）
- `RuleValidator` 22 条内置检查：HP/法力/速度越界、未持有道纹、冷却中道纹、飞行选目标、闪避速度扣除、属性点总和、碎片非负、精力非负……
- 违规入库 + 例外白名单 + 人工裁决接口

### 7. 局外系统（6 个通用行动全部有档位与碎片扣费）
- 领悟 / 休整(8·24·48) / 修行(1~6点) / 学习 / 共鸣 / 探索 —— 档位与碎片消耗数值与 README 完全一致
- 3 点精力池、共鸣额外扣 1 精力、[战终]精力恢复至 3
- 三个副本专属行动的壳：维修 / 雇佣 / 炼心

### 8. 其他
- 开局 25 属性点分配（1点=6血限=1速限=2法限）、血限仅开局可得
- [战终]碎片奖励公式：`血限×2% + 道纹数×5`（与 README 一致）
- 存档/读档 JSON、行动历史、规则文件热同步 `RuleSync`
- AI 接入层：Gemini / Groq / OpenRouter / DeepSeek 四个后端

---

## 二、❌ 未实现（缺口）

### 🔴 P0：会直接崩溃的 BUG

`execute_action()` 分发表里注册了两个**根本不存在的方法**，调用即抛异常：

| 行动 | README 中的地位 | 现状 |
|------|----------------|------|
| `use_spell` | 法术是核心系统（「道纹是词汇，法术是语句」） | `AttributeError: '_action_use_spell'` |
| `consume_item` | 消耗品(X/Y)有完整规则定义 | `AttributeError: '_action_consume_item'` |

`engine/README.md` 的「行动类型一览」把这两个列为可用行动，属于**文档撒谎**。

### 🔴 P0：法术系统整体缺失

README 定义了 9 个可学法术（先发制人 / 临界泄压 / 生生不息 / 后发制人 / 以牙还牙 / 借力打力 / 不死不休 / 千刀万剐 / 咎由自取），**代码中一个都搜不到**。

同时缺失的配套规则：
- **积木规则**（法术必须由已解锁道纹原版效果组合）—— 无校验
- **循环规则**（终点结算可触发自身启动条件，法力充足时自动循环）—— 「不死不休」「千刀万剐」全靠这条，未实现
- **中断规则**（法力耗尽则流程中断）—— 未实现
- **法术阶级规则**（X 种道纹 = X 阶，最多 X 个自定义条件）—— `Spell.rank` 字段存在但从不计算、从不校验
- **自创法术** + [战终]修订 —— `_pre_battle_xuexi` 只扣碎片，返回一句"请告知引擎学习的具体内容"，不落库

`Spell` 类在全代码库中**从未被实例化过一次**。

### 🔴 P0：内容数据库全部为空

| 内容 | README 数量 | 代码中 |
|------|------------|--------|
| 遗物 | 12 件（血誓戒/买路财/同魂笔/回锋刀/折速法印/三相残韵盘/鲜血契约/避风铃/守夜灯/钱袋/卖身契/无所求/忘忧香） | **0 件**，`Relic()` 从未实例化，`relics_pool` 永远是空 list（`_pre_battle_gongming` 里硬编码 fallback `pool_size=12`） |
| 通用事件 | 10 个（无名冢/遗忘书屋/祭坛/过路商人/猩红暴雨/无名碑林/回音长廊/回忆当铺/手术/无魂泥潭） | **0 个** |
| 怪物面板 | 一阶三副本各 8 只 | **0 只**，`千手蜈蚣` 只在 demo 和测试里手搓 |
| 副本 | 一阶 3 + 二阶 3 + 三/四/五阶 | 仅 3 个一阶副本**有专属道纹**，无怪物池、无区域背景、无事件池 |
| 法器 | 通关奖励 | 仅 `artifacts: list[dict]` 空字段 |
| 消耗品 | 绝息淤泥、龙心等 | `Consumable` 类从未实例化 |

`RuleSync` 有从 README 正则提取遗物/事件/怪物的能力，但提取结果**没有任何一条接进 GameState**，纯粹是报告工具。

### 🟠 P1：代价系统只做了一半

`daowen.py` 产出 5 种代价字段，`api.py` 只消费 3 种：

| 代价 | 计算 | 实际扣除 |
|------|------|---------|
| 流血 `cost_hp` | ✅ | ✅ |
| 衰老 `cost_blood_limit` | ✅ | ✅ |
| 疲惫 `cost_speed` | ✅ | ✅ |
| **异变 `cost_mutation`（7 个道纹用到）** | ✅ | ❌ **完全不扣**，「异变达 50 层变怪物」从未实现 |
| **碎片 `cost_shards`** | ✅ | ❌ 不扣 |
| **枯竭（失去法限）** | ❌ 无字段 | ❌ |
| **萎缩（失去速限）** | ❌ 无字段 | ❌ |
| **失忆（永久失去X种道纹）** | ❌ | ❌ |
| **唯一（本轮回限一次）** | ❌ | ❌ |
| **冷却X** | `duration` 字段有 | ❌ `cooldown_remaining` **从来没有被赋过值，也没有在[战终]递减**。固执/束缚/迟滞的冷却代价形同虚设 |

→ 直接违反 README「代价原则：任何收益必须有代价，且代价必须真实生效」。

### 🟠 P1：状态效果只认 11 个

`add_status()` 会给任意道纹挂 buff，但战斗流程里真正**读取并生效**的只有 11 个状态：
`自愈 / 狂暴 / 畸变 / 退化 / 贯穿 / 蒙蔽 / 束缚 / 定型 / 坏死 / 固执 / 兴奋`

其余 50+ 个道纹（飞行、坠落、寄生、衰败、无神、加速、眩晕、无力、缓慢、增殖、封印……）挂上状态后**没有任何一处代码消费它**，等于挂了个装饰。
例如：
- 【飞行】不会真的阻止非飞行角色选定目标（validator 里有个 `_check_flying_targeting`，但只在事后告警，不阻断）
- 【寄生】的伤害转化回复没有接入 `take_damage`
- 【眩晕】【束缚】不会真的跳过出手

### 🟠 P1：第 8 场·最终死斗 = 空壳

- `GamePhase.DEAD_DUEL` 枚举存在
- `_get_dead_duel_actions()` 只 return 一句 `"note": "无法逃跑，只能战斗到一方倒下"`
- **先手顺序（速限→法限→血限→当前生命）未实现**
- **残韵任意时刻插队未实现**
- 「最终的冠冕」封存机制：只有 `sealed_candidate` 空字段，无触发逻辑
- 第 7 场打完不会自动进入第 8 场

### 🟠 P1：8 个"全程自动触发"特殊事件，7 个没有触发器

README 明确「不参与事件池，不消耗精力，满足条件后**自动触发**」：

| 事件 | 状态 |
|------|------|
| 急中生智 | 🟡 半实现：只能手动 `declare_wit`，「同种解法第二次失效」未实现 |
| 逃跑与追击 | 🟡 半实现：只能手动声明，「逃跑方须消耗出手发动急中生智、追击方逐个破解」流程未实现 |
| 进化 | 🟡 半实现：困境检查有，但需手动 `declare_evolution`；已有特性库（脱囊/腐蚀）未入库 |
| **死之传承** | ❌ 仅枚举名，轮回者[命零]时不触发，遗言不入 `death_book_wisdom` |
| **员工叛变** | ❌ 仅枚举名，[战终]不做「员工攻击总值 ≥ 轮回者当前生命 + 朋友攻击总值」检查，镇压/让利/急中生智三选项不存在 |
| **撤退** | ❌ 完全没有。朋友/员工受致死攻击时不会自动撤退 |
| **最终的冠冕** | ❌ 完全没有 |
| **初拥之夜** | ❌ 完全没有。9 个血族特性（血族血脉/不朽之躯/鲜血之翼/血族尖牙/真理眼/寒冰法力/血影/血食/封存血脉）一个都没有 |

### 🟡 P2：战斗流程的缺口

- **出怪机制未实现**：`battle_start` 只是 `current_battle += 1` 然后返回"请抽取怪物"。
  「数量=战斗场数，一阶副本-2，最低1，允许重复抽选」这条公式**代码里没有**（只写在 `AI_EXPERIENCE.md` 里）
- **战斗背景**：README 要求[战始]选择一种战斗背景，代码无此概念
- **怪物白板开局**（[战始]第一回合仅具备基础攻击/生命，道纹须主动发动后才生效）：`apply_monster_passive` 反而在回始就直接给庇护上格挡，**违反此规则**
- **[目标]消耗 1 点速度转移目标**（基础定义里的规则）未实现
- `apply_monster_passive` 里庇护写死 `4*3=12`，忽略了怪物面板上的实际 X 值
- 队友（朋友/员工/临时朋友）：有 list 字段，但没有任何加入、行动、结算逻辑；雇佣行动只返回一句说明文字
- 休整的恢复量返回一句"请通过 heal_to_entity 行动分配" —— **`heal_to_entity` 这个行动不存在**

### 🟡 P2：龙心谷专属机制
- `LongJiXin` 数据类写好了（代价类型 + 耐久抵扣）
- 但「龙性」资源、8 个龙族特性（龙族血脉/龙威/龙族利爪/龙息/震岳龙躯/吞骸龙胃/断尾求生/烬翼）**全部未实现**，代码里搜不到"龙性"二字

### 🟡 P2：其他小缺口
- 「消耗品同名叠加合并」`Consumable.merge()` 写了但无人调用
- 「持续X同名效果合并」✅ 已实现（`add_status` 内调用 `merge_with`，数值与回合数相加）
- 「共计X」（豁免该次发动的全部消耗与代价）未实现
- 「声明」机制（不可更改、指定时点结算正误）未实现
- 黑名单 `blacklist_level` 字段存在，无逻辑

---

## 三、修复优先级建议

| 优先级 | 事项 | 理由 |
|--------|------|------|
| **P0-1** | 补 `_action_use_spell` / `_action_consume_item` 或从分发表和文档中删除 | 当前调用直接崩，且 engine/README 在骗人 |
| **P0-2** | 建 `content/` 数据模块：12 遗物 + 10 事件 + 9 法术 + 一阶 24 怪物面板 | 没有内容，引擎就只是个计算器，跑不了一局完整游戏 |
| **P0-3** | 冷却/异变/枯竭/萎缩/失忆/唯一 六种代价真实扣除 | 违反 README 最硬的一条「代价必须真实生效」 |
| **P1-1** | 出怪公式 + 战斗背景 + 怪物白板开局 | [战始]流程目前是空的 |
| **P1-2** | 员工叛变 / 死之传承 / 撤退 三个自动触发器 | 规则写明"自动"，现在连手动都没有 |
| **P1-3** | 状态效果消费层：飞行/眩晕/束缚/寄生/无力 等至少覆盖到怪物常用道纹 | 挂了 buff 不生效 = 静默错误，比崩溃更难查 |
| **P2** | 第 8 场死斗 + 最终的冠冕 + 初拥之夜 | 一阶还没跑通，可以后置 |

---

## 四、附：文档一致性问题

1. `engine/README.md` 的「行动类型一览」列出 19 个行动，其中 `use_spell`、`consume_item` **实际会抛异常**。
2. `AI_EXPERIENCE.md` 里的「出怪公式（已确认）」是引擎行为的事实来源，但**该公式没有写进代码**，压测脚本各自实现了一遍。
3. `AI_EXPERIENCE.md` 自称「局外维修/雇佣/炼心的详细效果待实现」—— 属实，三者都只返回说明文字。
4. 根目录有个空文件 `ALL`（1 字节），疑似误提交。

---

## 五、修复记录（2026-08-03 第二轮）

### 0. 废案清除：怪物×3规则
经确认为 README 未及删除的废案，已彻底移除：
- `README.md`：删除「怪物特性」第4条，后续条目 5-8 重编号为 4-7
- `AI_EXPERIENCE.md`：删除 3 处遗留待办与"已修复"记录
- `engine/combat.py`：删除 `is_monster_triple()` 与 `MONSTER_OWN_DAOWEN`
  （`REGION_EXCLUSIVE_DAOWEN` 保留，改作副本道纹归属校验用途）
- `engine/api.py`：`_execute_daowen_effect` 移除全部 multiplier 分支
- `engine/battle_flow.py`：庇护由写死的 `4*3=12` 改为按面板 X 值计算 `4X`

### ✅ P0-1 两个崩溃行动已修复
- 新增 `_action_use_spell`：完整实现积木/中断/循环/自由控X规则
- 新增 `_action_consume_item`：使用不消耗出手，X 归零后自动移除

### ✅ P0-2 内容库已建立（新增 `engine/content.py`）
| 内容 | 数量 | 来源 |
|------|------|------|
| 遗物池 | 13 件 | README 正文逐字抄录 |
| 通用事件 | 10 个 | 含选项、代价、收益结构化 |
| 怪物面板 | 36 只（3 副本 ×12） | 正则提取自正文，含攻击次数/攻击力/血限/道纹X值 |
| 消耗品 | 绝息淤泥 | 事件产出 |

新增 `engine/spells.py`：9 个可学法术全部可执行，支持自创法术。

配套接入：
- `battle_start` 实现出怪公式（数量=场数，一阶-2，最低1，允许重复）
- `共鸣` 接入真实遗物池（发现/自选15碎片），修正为消耗 2 点精力
- `探索` 接入真实事件池（已遇到的事件自动移出）
- `学习` 从"返回一句说明"改为真正授予法术/转化道纹，并拒绝原始怪物道纹
- `DiceEngine.resolve_pool` 新增 `keep` 参数支持重复抽选

### ✅ P0-3 代价系统已补全
| 代价 | 修复前 | 现在 |
|------|--------|------|
| 冷却 | 从不赋值、从不递减 | 发动即锁定，[战终]递减，到期解锁 |
| 异变 | 完全不扣 | 累积到 `Entity.mutation_stacks`，达50层自动怪物化 |
| 唯一 | 无 | `Entity.used_unique` 记录，本轮回不可再用 |
| 枯竭/萎缩 | 无 | 已实现扣除逻辑（当前无道纹产出，前向兼容） |
| 碎片 | 不扣 | 已接入 |
| **多段伤害** | **血债等完全不造成伤害** | 逐段独立结算，格挡逐段吸收 |

### 测试
新增 `tests/test_fixes.py`：14 项回归测试全部通过。
原有 `tests/test_engine.py` 7 项、4 个 demo 均无回归。

### 遗留问题（未在本轮处理）
1. **README 内部矛盾**：遗物池标称「共12件」，实际列出 13 件（含忘忧香）。
   代码按 13 件实现，需正文确认应删哪件或改为 13。
2. 状态效果消费层：飞行/眩晕/束缚/寄生等仍未在战斗流程中真实生效。
3. 第 8 场死斗、最终的冠冕、初拥之夜、员工叛变、死之传承、撤退 仍未实现。
4. 遗物效果目前为文本 + tags，尚未接入自动触发（如血誓戒的[回始]联动）。
