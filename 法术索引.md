# 法术索引

本文件是**法术的唯一完整说明处**：法术设计原则、三大法则（积木/循环/中断）、
法术阶级规则、内置可学法术清单、反应型法术的战斗流程示例，以及自创法术
（DSL）的完整标准语法与已知限制，全部收录于此，不再分散在 README、
《死者之书》与仓库根目录的散落文档里。

- **可学法术清单的引擎事实源仍是 `死者之书.md`**：`engine/rule_sync.py`
  的 `extract_spells_from_file()` 只读取《死者之书》的「## 可学法术」章节
  来注册法术数据（供 `RuleSync` 规则同步与自动巡检使用），不读取本文件。
  本文件下方的《内置可学法术》表是《死者之书》该章节的**可读性镜像**，
  两者数值必须一致；若冲突，以《死者之书》「## 可学法术」章节的原始
  条目为准（并应视为文档不同步，需要同步修正）。
- 自创法术的语法规则、解析器实现与真实引擎实测证据，本文件是唯一事实源。

## 目录

- [一、法术设计原则](#一法术设计原则)
- [二、三大法则与法术阶级规则](#二三大法则与法术阶级规则)
- [三、内置可学法术](#三内置可学法术)
- [四、反应型法术战斗流程示例](#四反应型法术战斗流程示例)
- [五、自创法术标准语法](#五自创法术标准语法)
- [六、"未接线"是什么意思](#六未接线是什么意思)

---

## 一、法术设计原则

法术设计必须赋予后手角色与反应型法术在先手伤害爆发前建立防御或打出交错
交锋的能力。

法术必须完全由已有道纹组成，必须通过局外【学习】行动方可掌握并开启，
不允许凭空创造效果。所有法术必须遵守积木法则、循环法则、中断法则（见
下节）。

道纹只有两字，不是技能，而是概念，是世界的最小规则单元，相当于语言中的
词语。**法术相当于语句，由多个已有道纹组合而成**（该表述原文见
[README](README.md) 「【三、道纹与残韵设计原则】」）。

自创法术须完全由创建时已拥有的道纹按三大法则组装，[战终]可以进行修订。

## 二、三大法则与法术阶级规则

**自由控X规则**：施法者在发动道纹或法术时，可根据当前剩余法力与战术需求，
自由指定/控制公式中的代数 X 数值（1 ≤ X ≤ 当前可用法力/代价上限），无需
强制一次性清空全部法力。

**积木规则**：法术不得凭空创造新机制，必须完全由已解锁道纹的原版效果
组合而成。每一步结算必须严格遵守对应道纹的代数公式（如【庇护X】消耗X
法力获得2X格挡，或【再生X】消耗X法力恢复3X生命）。

**循环规则**：允许法术生效流程的终点结算触发该法术自身的启动条件，从而
在法力充足时自动循环。

**中断规则**：法术一旦法力耗尽或中间流程失效则生效流程中断。

**法术阶级规则**：法术使用X种不同道纹即为X阶（最高9阶）。X阶法术最多
拥有X种自定义条件。每个可以独立判断真假的要求均计为一种条件；使用"且"
"或"等连接多个要求时分别计数。目标可选、资源充足、角色存活以及法力耗尽
或中间流程失效时中断，属于所有法术共享的全局规则，不计入条件数量。

## 三、内置可学法术

以下表格是《死者之书》「## 可学法术」章节的可读性镜像（详见文首说明）。

| 法术 | 所需道纹 | 触发条件 | 生效流程 |
|---|---|---|---|
| 先发制人 | 杀伐 | 受到伤害前 | 受到伤害前→发动杀伐 X |
| 生生不息 | 再生 | 失去生命后 | 失去生命后→发动再生 X |
| 后发制人 | 庇护 | 受到伤害前 | 受到伤害前→发动庇护 X |
| 以牙还牙 | 杀伐，再生 | 失去生命后 | 失去生命后→发动再生 X→发动杀伐 Y |
| 借力打力 | 杀伐，庇护 | 受到伤害前 | 受到伤害前→发动庇护 X→发动杀伐 Y |
| 不死不休 | 血债 | 失去生命后 | 失去 X 点生命→发动血债 X→付出代价→失去 X 点生命（循环） |
| 千刀万剐 | 血债，再生 | 失去生命后 | 失去生命后→发动再生 X→发动血债 X→付出代价→失去生命后（循环） |
| 咎由自取 | 坠落、杀伐、血债 | 目标发动道纹前 | [目标]发动道纹前→若其处于飞行，发动坠落X，否则跳过→发动杀伐Y→若未造成伤害，发动血债Z，否则跳过 |

完整条目原文（含"咎由自取"的世界观简介"金石定型"）见
[死者之书 · 可学法术](死者之书.md#可学法术)。

## 四、反应型法术战斗流程示例

以下示例摘自战斗推演格式正文（完整战斗结构与七步原子流水线见
[README](README.md#战斗整体结构) 「六、战斗推演格式」），演示反应型法术
在真实结算中的完整数值流转。

### 场景：防御型反应法术（【后发制人】受到伤害前获得格挡）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐10】（消耗 10 法力，原始伤害 20）
  [预先响应] 【受到伤害前】B 触发法术【后发制人】（循环1）：
    └ B 对自身发动【庇护5】（消耗 5 法力）→ 获得 10 点格挡（格挡 0→10，B 法力 30→25）
  [目标反应] B 选择不闪避
  [数值落地] 杀伐造成 20 点伤害：10 点被格挡吸收（格挡归 0），穿透 10 点伤害
    └ B 实际失去 10 点生命（生命 60→50，A 法力 20→10）
```

### 场景：交错反击型法术（【以牙还牙】受伤后先回血再反击）
```markdown
出手1（A）：
  [动作声明] A 对 B 发动【杀伐5】（消耗 5 法力，伤害 10）
  [目标反应] B 选择不闪避
  [数值落地] B 受到 10 点伤害（生命 60→50，A 法力 20→15）
  [后置响应] 【失去生命后】B 触发法术【以牙还牙】：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）→ [回复] 6 点生命（生命 50→56，B 法力 30→28）
    └ 步骤2：B 对 A 发动【杀伐4】（消耗 4 法力，B 法力 28→24）
        └ A 选择不闪避 → A 受到 8 点伤害（生命 60→52）
```

### 场景：循环自伤法术（【千刀万剐】失去生命后自驱动循环至法力耗尽中断）
> 初始：A 生命 60/60；B 生命 35/60、法力 6/60（设定每轮循环取 X=2）。A 造成 1 点伤害使 B 生命 35→34 触发法术。

```markdown
出手1（A 造成 1 点伤害，B 生命 35→34）：

  【失去生命后】B 触发循环法术【千刀万剐】（第 1 轮循环，X=2）：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）
    │   └ B [回复] 6 点生命（生命 34→40，B 法力 6→4）
    ├ 步骤2：B 对 A 发动【血债2】（支付代价【流血2】）
    │   ├ B 支付流血 2 代价（B 生命 40→38）
    │   ├ A 选择不闪避
    │   └ A 受到 2 次 1 点伤害（共 2 点伤害，A 生命 60→58）
    └ 步骤3：B 因支付【流血2】再次失去生命 → 判定法力充足（剩余 4），触发第 2 轮循环。

  【失去生命后】B 触发循环法术【千刀万剐】（第 2 轮循环，X=2）：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）
    │   └ B [回复] 6 点生命（生命 38→44，B 法力 4→2）
    ├ 步骤2：B 对 A 发动【血债2】（支付代价【流血2】）
    │   ├ B 支付流血 2 代价（B 生命 44→42）
    │   ├ A 选择不闪避
    │   └ A 受到 2 次 1 点伤害（共 2 点伤害，A 生命 58→56）
    └ 步骤3：B 再次失去生命 → 判定法力充足（剩余 2），触发第 3 轮循环。

  【失去生命后】B 触发循环法术【千刀万剐】（第 3 轮循环，X=2）：
    ├ 步骤1：B 对自身发动【再生2】（消耗 2 法力）
    │   └ B [回复] 6 点生命（生命 42→48，B 法力 2→0）
    ├ 步骤2：B 对 A 发动【血债2】（支付代价【流血2】）
    │   ├ B 支付流血 2 代价（B 生命 48→46）
    │   ├ A 选择不闪避
    │   └ A 受到 2 次 1 点伤害（共 2 点伤害，A 生命 56→54）
    └ 步骤3：B 再次失去生命 → 【中断检查】：B 当前法力为 0，无法支付【再生2】的 2 点法力，循环自然中断。

[最终本轮出手结算结果]：
  - A 生命：60 → 54（累计承受 6 点伤害）
  - B 生命：35 → 46（净回血 11 点）
  - B 法力：6 → 0（3 轮循环消耗完毕）
```

（更多示例——基础攻防、闪避交互、残韵插队逆转——见
[README](README.md#典型实战推演场景示例) 「六、战斗推演格式 § 典型实战推演场景示例」。）

---

## 五、自创法术标准语法

本节是【学习→自创法术】提交 `trigger_condition`（触发条件）与
`effect_flow`（效果流程）两个字段时必须遵守的语法规范。解析器实现见
`engine/spell_dsl.py`；提交时若写法不符合本规范，【学习】行动会直接
返回失败并附带具体错误原因，不会出现"学会了但因为文本对不上而永远
不会触发"的情况。

### 5.1 触发条件（trigger_condition）

不要求逐字匹配某个固定字符串，只要求包含可判定核心关键词（可以带
"我方/自身/对方/时/的"等修饰词，解析器会自动剥离）。当前支持并且
**已经过真实引擎实测**的写法：

| 规范时机 | 常见写法示例 | 引擎执行状态 |
|---|---|---|
| 受到伤害前 | "受到伤害前"、"我方受到伤害前"、"受到攻击前" | ✅已接线，会真实触发 |
| 失去生命后 | "失去生命后"、"掉血后"、"失去生命后（循环）" | ✅已接线，会真实触发 |
| 目标发动道纹前 | "目标发动道纹前"、"对方发动道纹前" | ✅已接线，会真实触发 |
| 受到伤害后 | "受到伤害后"、"受到攻击后" | ⚠️能通过语法校验、可学会，但战斗管线暂无"受到伤害后"这个独立结算点，不会被触发（等价于占位） |
| 失去生命前 | "失去生命前"、"生命减少前" | ⚠️同上，暂不会被触发 |
| 战始 | "战始"、"战斗开始时"、"开局时" | ⚠️同上，暂不会被触发 |
| 战终 | "战终"、"战斗结束时" | ⚠️同上，暂不会被触发 |
| 回始 | "回始"、"回合开始时"、"每回合开始时" | ⚠️同上，暂不会被触发 |
| 回终 | "回终"、"回合结束时" | ⚠️同上，暂不会被触发 |
| 敌回始 | "敌回始"、"敌方回合开始时" | ⚠️同上，暂不会被触发 |
| 敌回终 | "敌回终"、"敌方回合结束时" | ⚠️同上，暂不会被触发 |

"⚠️"的六种时机目前只解析、不执行，属于诚实标注的已知限制（原因见下方
《六、"未接线"是什么意思》），学习时会返回 `wired: false` 与 `warning`
字段明确提示，不会静默学会成摆设。

#### 5.1.1 完整识别规则（供精确排查歧义写法用）

解析流程分两步：**①剥离修饰词** → **②按关键词组合优先级匹配**。

**第①步：剥离修饰词**（这些词出现在原文里会被直接删掉，不影响判定）：

```
我方 / 自身 / 对方 / 己方 / 自己的 / 时 / 的 / （循环） / (循环)
```

**第②步：按下表顺序依次尝试匹配**（顺序即优先级——排在前面的规则优先
命中，这是为了避免"敌回始/敌回终"被更泛化的"回始/回终"关键词提前吃掉）：

| 优先级 | 规范时机 | 命中条件（剥离修饰词后的文本需同时满足每一组关键词中的至少一个） | 排斥条件 |
|---|---|---|---|
| 1 | 敌回始 | 含"敌" **且** 含（"回合开始"或"回始"） | 无 |
| 2 | 敌回终 | 含"敌" **且** 含（"回合结束"或"回终"） | 无 |
| 3 | 回始 | 含"回合开始"或"回始"或"每回合开始" | 原文**不能**含"敌"字 |
| 4 | 回终 | 含"回合结束"或"回终"或"每回合结束" | 原文**不能**含"敌"字 |
| 5 | 战始 | 含"战斗开始"或"战始"或"开局" | 无 |
| 6 | 战终 | 含"战斗结束"或"战终"或"结局" | 无 |
| 7 | 目标发动道纹前 | 含（"发动道纹"或"使用道纹"或"出招"或"发动法术"） **且** 含"前" | 无 |
| 8 | 受到伤害前 | 含（"受到伤害"或"受伤"或"承伤"或"受到攻击"） **且** 含"前" | 无 |
| 9 | 受到伤害后 | 含（"受到伤害"或"受伤"或"承伤"或"受到攻击"） **且** 含"后" | 无 |
| 10 | 失去生命前 | 含（"失去生命"或"损失生命"或"掉血"或"扣血"或"生命减少"） **且** 含"前" | 无 |
| 11 | 失去生命后 | 含（"失去生命"或"损失生命"或"掉血"或"扣血"或"生命减少"） **且** 含"后" | 无 |

**关键行为说明（均已用 `parse_trigger()` 实测确认）**：

- 判定所用的"排斥条件（如敌回始的'敌'字）"检查的是**剥离修饰词前的原文**，
  "含'前'/含'后'"等关键词组也是在**剥离修饰词后的文本**里查找——两步分工不同，
  写复杂句子时以此为准。
- "受伤"类关键词只需要**同时出现"受伤/受到伤害"类词根 + '前'或'后'其中一个字**
  即可命中，不要求这两部分紧邻或顺序固定。例如"受到伤害前后"这种同时含
  "前""后"的病态写法，会按规则表**从上到下第一个匹配到的规则**命中——
  这里会先匹配"受到伤害前"（因为它在表里排在"受到伤害后"之前），而不是报错，
  **建议不要这样写**，容易产生歧义（引擎会静默按前者理解，不会报错提醒你）。
- 缺少"前/后"关键词的裸写法（例如只写"受到伤害"、"受伤"、"生命减少"，
  不写明前还是后）**会被直接拒绝**，报错"无法识别触发条件"，因为无法判断
  是"前"结算点还是"后"结算点。
- 完全不认识的词汇（如"乱写一通"）、空字符串、纯空白，都会被直接拒绝。
- "敌方受到伤害前"这种写法里虽然含"敌"字，但因为"受到伤害前"系列规则本身
  没有排斥"敌"字的限制，所以正常识别为"受到伤害前"，不会被误判成敌回合系列
  （敌回合系列的"敌"字排斥/包含判定只针对"回合开始/回合结束"这组关键词生效）。

#### 5.1.2 完整实测矩阵（42种同义写法逐一真实解析验证）

以下全部写法均已用 `python3 -c "from engine.spell_dsl import parse_trigger; print(parse_trigger('...'))"`
真实跑过，结果与上表规则完全一致，未发现规则表之外的隐藏行为（更完整的
战斗层实测数据见 `data/trigger_condition_syntax_probe_report.md`）：

```
受到伤害前 / 我方受到伤害前 / 自身受到伤害前 / 受到攻击前 / 受到伤害的前  → 受到伤害前
受到伤害后 / 受到攻击后                                                    → 受到伤害后
失去生命前 / 生命减少前 / 损失生命前 / 掉血前 / 扣血前                       → 失去生命前
失去生命后 / 生命减少后 / 损失生命后 / 掉血后 / 扣血后
  / 失去生命后（循环） / 失去生命后(循环)                                    → 失去生命后
目标发动道纹前 / 对方发动道纹前 / 使用道纹前 / 出招前 / 对方发动法术前         → 目标发动道纹前
战斗开始时 / 战始 / 开局时 / 开局 / 我方开局时                                → 战始
战斗结束时 / 战终 / 结局时 / 结局                                            → 战终
回合开始时 / 回始 / 每回合开始时 / 每回合开始 / 我方回合开始时 / 对方回合开始时 → 回始
回合结束时 / 回终 / 每回合结束时                                             → 回终
敌方回合开始时 / 敌回始 / 敌方回始 / 敌方发动道纹前(!)                        → 敌回始 / 目标发动道纹前(见下)
敌方回合结束时 / 敌回终 / 敌方回终                                           → 敌回终
战斗开始后 / 回合开始前                                                     → 战始 / 回始（"前后"对战始/回始无区分意义，均按核心词命中）

——以下为拒绝案例——
（空字符串）/ （纯空白）        → 拒绝："触发条件不能为空"
受到伤害 / 受伤 / 生命减少      → 拒绝："无法识别触发条件"（缺"前/后"，无法判断结算点）
乱写一通                        → 拒绝："无法识别触发条件"
```

> 注："敌方发动道纹前"是个刻意设计的歧义样例：它同时含"敌"字与
> "发动道纹...前"，但规则表里"目标发动道纹前"排在"敌回始/敌回终"
> 判定组之后、且"目标发动道纹前"本身不检查"敌"字排斥条件，同时
> "敌回始/敌回终"要求的是"回合开始/回合结束"关键词而不是"发动道纹"，
> 所以实测结果是命中"目标发动道纹前"（因为它满足"发动道纹"+"前"这组
> 关键词，而不满足"敌回始"要求的"回合开始"关键词）。如果你想表达
> "敌方即将发动道纹前"的反应型法术，直接写"目标发动道纹前"或
> "对方发动道纹前"最清晰、无歧义。

### 5.2 效果流程（effect_flow）

#### 5.2.1 基本动作步骤

格式：`发动<道纹名>X于<目标身份>`，多个步骤用`→`连接。

**目标身份必须显式声明**，不再靠道纹类型自动猜测，可选：

- `于自身` —— 施法者/受击者自己
- `于攻击者` —— 触发本次反应的对方角色
- `于目标` —— 效果同"于攻击者"（在"目标发动道纹前"触发下，语义是"即将
  发动道纹的那个人"）
- `于施法者` —— 效果同"于自身"
- `于任意目标` —— 结算时由发动方从当前局面的合法目标里现场指定
  （合法性判定与"发动道纹"选择目标同一套规则：飞行状态下互相不可选）

示例：`发动杀伐X于攻击者→发动再生X于自身`

#### 5.2.2 循环

在效果流程末尾加"循环"或"循环直到法力耗尽"标记：

```
发动血债X于攻击者→循环
```

循环语义与内置法术【千刀万剐】【不死不休】完全一致：只要法力足够、
流程未被打断，理论上可以一直循环下去，中止条件只有两个——法力耗尽，
或流程中间某一步失效（比如目标已经死亡/离场）。工程上设置了一个
1万次循环的安全阀（不是游戏规则，是防止异常输入卡死程序的保险丝，
正常法力池永远不可能触达这个次数）。

#### 5.2.3 条件分支（若...则...否则...）

**具体格式**：

```
若<条件表达式> 则 <动作1>；<动作2> 否则 <动作3>
```

- 关键词`若`、`则`、`否则`前后都要有空格分隔与其它成分（见下方真实示例）。
- "否则"分支可省略。条件为假且没有"否则"时，本次分支不做任何事。
- 条件分支和普通动作步骤可以用`→`混合连接，可以放在效果流程的任意位置。

混合连接示例：

```
发动庇护X于自身→若目标 拥有 飞行 则 发动坠落X于目标→发动杀伐X于攻击者→循环
```

**真实可用的完整条件分支法术示例**（已在真实引擎逐回合结算中实测，
证据见 `data/custom_spell_trigger_probe_report.md` §三）：

```json
{
  "name": "临机应变",
  "required_daowen": ["杀伐", "再生"],
  "trigger_condition": "受到伤害前",
  "effect_flow": "若自身 生命 小于 100 则 发动再生X于自身 否则 发动杀伐X于攻击者"
}
```

含义：受到伤害前，如果自己当前生命小于100，就对自己发动【再生X】回血；
否则对攻击者发动【杀伐X】反击。真实战斗实测：玩家生命=50（<100）时走
回血分支（hp 50→55）；玩家生命=200（≥100）时走反击分支（敌方hp
222→214）。两条分支都被真实验证过，不是纸面语法。

#### 5.2.4 条件表达式语法

`<主语> <字段> <比较词> <数值>` 或 `<主语> 拥有/没有 <状态名>`

**主语**：自身/自己、攻击者/敌方/对方、目标、施法者

**数值字段**：生命(当前生命)、血限、法力(当前法力)、法限、速度(当前速度)、
速限、护盾(格挡)；此外`<道纹名>层数`可以读取该道纹当前的X值/层数
（例如"自身 杀伐层数 大于 2"）

**比较词**：大于、小于、大于等于、小于等于、等于、不等于

**逻辑组合**：且、或、非，支持括号嵌套改变优先级

示例：

```
自身 生命 小于 50
(自身 生命 小于 50 或 自身 法力 小于 5) 且 目标 护盾 等于 0
目标 拥有 飞行
非 目标 拥有 飞行
```

### 5.3 完整示例

```json
{
  "name": "以杀养伤",
  "required_daowen": ["杀伐", "再生"],
  "trigger_condition": "受到伤害前",
  "effect_flow": "若自身 生命 小于 50 则 发动再生X于自身 否则 发动杀伐X于攻击者"
}
```

---

## 六、"未接线"是什么意思

**通俗解释**：自创法术分两步——第一步是【学习】时把你写的文字"翻译"成
引擎能理解的结构（判断触发时机、拆解每一步动作、校验道纹是否存在等），
第二步是战斗真正打起来时，引擎要不要在某个时间点"停下来问一句：这个
法术要不要发动？"。

"未接线"说的是**第二步还没做**：这个触发时机的文字写法完全正确、能顺利
通过语法检查、也能成功学会，**但引擎在战斗流程走到那个时间点时，压根不会
停下来问"要不要发动"这一句**——不是法术坏了，也不是学错了，而是那个时间
点本身，工程上还没有开一个"决策窗口"给它。

打个比方：学习法术就像把一份说明书交给系统备案，"接线"就是系统真的在
对应的时间点装了一个"提醒按钮"。目前只有 3 个时间点装了这个按钮
（受到伤害前 / 失去生命后 / 目标发动道纹前），另外 8 个时间点
（战始/战终/回始/回终/敌回始/敌回终/受到伤害后/失去生命前）暂时还没装。
你写的法术在那些时间点是"合法但沉默"的——引擎会诚实地告诉你（学习成功
时返回 `wired: false` 加一句具体警告），不会让你误以为它已经在悄悄生效。

**为什么已接线的3种和未接线的6~8种待遇不一样**：

"受到伤害前/失去生命后/目标发动道纹前"这三种时机在原有引擎里本来就有
一套完整的"停下来问一遍要不要发动反应型法术"的机制（供【后发制人】
【以牙还牙】【咎由自取】等内置法术使用），自创法术的语法扩展是直接
挂到这套既有机制上的，风险可控、已有大量既存测试兜底。

"战始/战终/回始/回终/敌回始/敌回终"这六种在原有引擎里**没有**对应的
"停下来问一遍"决策通道——回合推进（`round_start`/`round_end`/
`battle_start`/`battle_end`）目前是引擎单方面按固定逻辑结算，没有开放
"请提交本回合要不要发动某个法术"这一步。要把这六种时机接通，需要在
这四个阶段动作里新增一个显式的法术选择提交参数（类似遗物结算已有的
`relic_choices`），并且"敌回始/敌回终"在死斗（有明确回合归属方）与
普通战斗（怪物没有独立的"回合开始/结束"动作边界）下语义不完全相同，
需要谨慎设计避免影响【守夜灯】【爆裂】等已经绑定在这两个时机上的
既有机制。这是下一步可以继续扩展的方向，目前诚实标注为"未接线"，
不会伪装成已经能用。

**已接线三种时机的真实触发验证结果**：均已在真实 `GameEngine` 逐回合
结算中跑通并核实资源变化数字，完整证据见
`data/custom_spell_trigger_probe_report.md`（四点改进的完整实测报告）与
`data/trigger_condition_syntax_probe_report.md`（触发条件同义写法穷举
实测报告）。
