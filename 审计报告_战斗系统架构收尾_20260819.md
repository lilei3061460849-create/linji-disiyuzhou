# 第四宇宙战斗系统架构收尾审计报告（2026-08-19）

审计范围：`engine/effect_context.py`、`engine/combat_events.py`、`engine/combat_hooks.py`、
`engine/combat.py`、`engine/api.py`、`engine/handlers/`、`engine/models.py`、`engine/events.py`、
`engine/daowen.py`、`tests/`。

审计前基线：`pytest` = **850 passed / 1 failed**（`tests/test_r39_r47_contract.py::test_r46_normal_event_relic_battle_start_matrix`，
本次修改之前即为红，见 P0-6）。

---

## 0. 现有职责关系（实测调用关系，非文件名猜测）

```
GameEngine(api.py)
  └─ CombatEngine(combat.py)                     战斗结算唯一持有者
       ├─ state: GameState(models.py)            面板/实体/胜负判定
       ├─ dice: DiceEngine
       └─ hook_manager: CombatHookManager(combat_hooks.py)

伤害唯一入口   CombatEngine._apply_hostile_damage(target, amount, damage_type, source, ctx)
  1) hook_manager.apply_multiplier_adjust   （龙族血脉翻倍）
  2) CombatEngine._incoming_adjust → hook_manager.apply_incoming_adjust （加害 → 龙鳞，顺序相关）
  3) hook_manager.apply_redirection         （嫁祸 / 背负；递归重入自身）
  4) hook_manager.apply_before_damage       （爆裂反噬）
  5) hook_manager.apply_mitigation          （撤退 / 负岳碑 / 断尾求生）
  6) Entity.take_damage                     （格挡 / 固执 / 眩晕打断 / is_alive）
  7) hook_manager.apply_after_damage_pipeline（逆鳞 / 伤痕 / 切割 / 寄生 / 负岳索 / 龙族血脉斩杀 / 命零通知）

生命直接损失   CombatEngine._raw_hp_loss(entity, amount, ctx)
代价           CombatEngine.pay_numeric_cost → _apply_numeric_cost_part → _pay_bleed_cost
回复           GameState.apply_heal(entity, amount, ctx)
速度           CombatEngine._lose_current_speed / _gain_speed（内部 _record_speed_change_event）
死亡通知       CombatEngine._on_entity_death(entity, ctx)   —— 幂等（_death_triggers_emitted）
离场           CombatEngine._remove_from_combat → Entity.depart_battle
```

* `EffectContext`（effect_context.py）：**为什么发生 / 来自哪里**。已在 damage / heal / hp_loss /
  speed / cost / death / leave 六条链路上落地，`event_id` + `parent_event_id` 形成链。
* `CombatEvent`（combat_events.py）：**发生了什么**。审计发现该系统在引擎内 **完全未被使用**（见 P2-1）。
* `CombatHook` / `CombatHookManager`：生命周期监听器，**无优先级**，顺序=字面注册顺序（见 P1-3）。

---

## P0：可能直接导致战斗结果错误

### P0-1 血限压迫致 0 生命却不判定命零（两处）
* 文件/函数：`engine/combat.py::apply_daowen_effect`
  * 第 2447 行 `blood_limit_penalty`（逼债·发动瞬间分支）
  * 第 2576 行 `瓦解`（乱葬岗二阶专属）
* 问题：只做 `target.current_hp = min(current_hp, blood_limit)`，**没有任何命零判定**。
* 风险：目标可以停留在 `current_hp == 0 and is_alive is True` 的非法状态——
  仍进入 `get_all_enemy_side()`、仍能出手、战斗无法结束、后续任意 1 点伤害才补刀。
  同一份文件里的另外 3 处血限压迫（回始逼债 1343、回终畸变 1405、blood_limit_reduction 2425）
  **都做了命零判定**，因此这两处属于遗漏而不是规则。
* 建议：统一走一个“血限变化 → 生命封顶 → 统一命零判定”入口。

### P0-2 崩解（异变≥50）死亡不走统一死亡管线（三处）
* `engine/combat.py::_resolve_monster_daowen_choice`（第 3679 行）：
  `paid = monster.add_mutation(...)`，`if paid["collapsed"]: return {...}` —— **直接 return，
  从不调用 `_on_entity_death`**。`Entity.add_mutation` 内部已经把 `is_alive` 置 False。
* `engine/api.py::_action_consume_item`（第 2863 行）：同样只写 `last_death_cause`，不通知死亡。
* `engine/events.py`（第 303 行，纸人冥婚）：`player.add_mutation(3)` 完全不检查 `collapsed`。
* 风险：崩解死者不产生 `_death_ctx`、不触发【焦黑发丝】、不进 `state.dead_monsters`
  （→【招魂】捞不到这具尸体）、不触发【分裂】复制体。同样是死亡，来源不同结果就不同。

### P0-3 【血食】直接宣布死亡，绕过统一死亡管线
* `engine/api.py::_action_blood_feast`（第 4086-4087 行）：`chizu.current_hp = 0; chizu.is_alive = False`。
* 风险：与 P0-2 相同——赤族之死不产生死亡上下文、不触发任何[命零]监听。

### P0-4 【赤族诅咒】回终流血不经过代价管线且死亡无上下文
* `engine/combat.py::round_end`（第 1463 行）：`entity.take_damage(20, "代价")`。
* 风险：不产生 `hp_loss` 事件；`_on_entity_death(entity)` 无 ctx，死因追不到“赤族诅咒”。
  （注：不改为 `pay_numeric_cost`，因为那会额外触发血誓戒/烙痕钉/血契，属于改规则。）

### P0-5 【买路财】补差额扣生命绕过所有记账
* `engine/api.py::_action_buy_road`（第 2759 行）：`player.take_damage(life_cost, "代价")`。
* 风险：不产生 hp_loss 上下文。**注意**：此处有前置校验 `player.current_hp <= life_cost → 报错`，
  所以不会致死，属于可追溯性缺口而非致命 Bug。
* 建议：只补记账，**不要**改走 `pay_numeric_cost`（会额外触发血誓戒/烙痕钉，属于改规则）。

### P0-6 【既有红灯】`test_r46_normal_event_relic_battle_start_matrix`
* 断言 `player.current_hp == 95`，实测 94。
* 归因：`process_relics` 后玩家 90 → `_pay_bleed_cost(player, 1)` → 89 → 负岳索回复 5 → 94。
  测试期望值少算了那 1 点流血代价。
* **本次不修改**：修实现会改流血代价或负岳索的数值，修期望值违反“不得为了变绿改期望”。
  需要 DM 裁定到底是“负岳索回复量应为 6”还是“测试期望应为 94”。

---

## P1：可能导致重复触发 / 因果链断裂

### P1-1 `_on_entity_death` 有 9 处不传 ctx —— 死亡因果链断裂
`engine/combat.py` 第 486 / 797 / 813 / 1343 / 1405 / 1465 / 1731 / 2425 / 3955 行。
这些死亡全部退化成 `source="legacy_death", source_type="legacy", tags={"legacy_context"}`，
`parent_event_id = None`。“杀伐 → 伤害 → 血限下降 → 命零”这条链在最后一跳断掉。

### P1-2 血限变化完全没有 EffectContext / 事件
* `engine/combat.py::_battle_delta` 是血限变化的实际入口，**没有 ctx 参数**。
* 全部 blood_limit 变更点（combat.py 1338 / 1399 / 2414 / 2440 / 2447 / 2576，
  combat_hooks.py 207【伤痕】/ 219【切割】）都无法追溯来源。
* 唯一例外是【切割】：它在 `AfterDamageEffectsHook` 里**手工另建**了一个 `qiege_ctx`，
  说明约定已经存在，只是没有下沉成公共入口，【伤痕】就没有对应上下文。
* 风险：任务书要求的三层链
  `伤害 → 血限下降 → 血限下降触发某效果` 中间那一跳目前不可追溯。

### P1-3 Hook 执行顺序完全依赖字面注册顺序
* `CombatHookManager.__init__` 里 `self._hooks = [...]` 是一个字面量列表，
  `apply_incoming_adjust` 顺序执行。
* 实测顺序 **是规则的一部分**：
  `JiahaiHook`（加害 +X）→ `LonglinHook`（龙鳞 -X，`max(0, ...)` 下限截断）。
  一旦交换，`10 -龙鳞8 +加害2` = 4，而现行 `10 +加害2 -龙鳞8` = 4；
  但当龙鳞 ≥ 伤害时（例如 伤害 6 / 加害 2 / 龙鳞 8）：
  现行 `max(0, 8-8)=0`，交换后 `max(0,6-8)=0 → +2 = 2`。**结果不同**。
* 建议：**保留现有顺序**，只把它显式化为 `priority`，禁止改变任何相对次序。

### P1-4 三个 Hook 被实例化两次，且存在从未使用的第二条分发路径
* `CombatHookManager.__init__` 既把 `DamageRedirectionHook / LethalMitigationHook /
  AfterDamageEffectsHook` 放进 `self._hooks`，又 `self.redirection_hook = DamageRedirectionHook()`
  等**另建了三个实例**。
* `apply_after_damage()`（遍历 `_hooks` 的通用分发）与
  `apply_after_damage_pipeline()`（显式分发）会命中**同一个 `AfterDamageEffectsHook` 逻辑**。
  目前 `combat.py` 只调用后者，所以尚未真实双触发，但这是一个上膛的枪：
  任何人接线 `apply_after_damage` 就会让 伤痕/切割/寄生/负岳索/龙族血脉斩杀 **各触发两次**。

### P1-5 【爆裂】反噬：直接改 HP + 直接宣布死亡
* `engine/combat_hooks.py::BaolieHook.on_before_damage`（第 67-71 行）：
  `attacker.current_hp = max(0, ...)`、`attacker.is_alive = False`。
* `engine/combat.py::_apply_hostile_damage`（第 485-486 行）用 `_on_entity_death(source)` **无 ctx** 兜底。
* 风险：反噬造成的失血既不进 `_hp_loss_events`，也不计入 `hp_lost_this_round`；死亡无来源。
* 说明：**不修 `hp_lost_this_round`**——【活血】按本回合失血回复，补上会改战斗数值，需 DM 裁定。

### P1-6 【避风铃】/【守夜灯】存在“旧逻辑 + Hook”双实现
* 避风铃：`combat.py::_note_dodge`（+3 格挡，引擎实际走这条）
  与 `combat_hooks.py::BifenglingHook.on_dodge`（+3 格挡，**引擎从不调用**）。
* 守夜灯：`combat.py::_grant_shouyedeng`（含封印/存活/轮回者/不朽之躯校验）
  与 `combat_hooks.py::ShouyedengHook.on_round_start`（**引擎从不调用**，且缺少上述校验）。
* 现状：`combat.py` 中 **没有任何 `apply_dodge()` / `apply_round_start()` 调用点**
  （只有 `tests/test_canonical_combat_strictness.py` 和 `tests/test_combat_hooks_architecture.py` 直接调）。
  所以**当前没有真实双触发**；守夜灯还有 `_shouyedeng_granted` 幂等标记兜底，避风铃没有。
* 建议：本次**不合并实现**（两者校验条件不同，合并=改规则），改为
  ① 在两处代码显式标注唯一分发方；② 补一条“经引擎真实路径闪避一次只加 3 格挡”的回归测试钉死。

---

## P2：架构不一致

### P2-1 `CombatEvent` / `CombatEventType` / `event_stream` 是死代码
* `CombatEngine.__init__` 建了 `self.event_stream: list[CombatEvent] = []`，
  全项目**没有任何一处向它写入**；`CombatEventType` 只在 `tests/test_combat_hooks_architecture.py`
  里被构造过一次做序列化测试。
* 后果：任务书要求的“CombatEvent 负责发生了什么”这一半架构事实上不存在，
  `EffectContext` 被迫同时承担“发生了什么”和“为什么发生”。
* 建议：**不要删除任何一方**。在已有的统一收口处（伤害落地 / 回复 / 血限变化 / 命零）
  纯增量地发事件，事件 `data.ctx` 挂 `EffectContext.to_dict()`，二者职责就自然分离。

### P2-2 不存在任何递归 / 链深保护
* 全项目 grep `recursion / depth / visited / chain_limit` → **0 命中**。
* 现有唯一递归点：`_apply_hostile_damage` 因【嫁祸】/【背负】重定向而自调用。
  当前靠 `_jiahuo_left / _beifu_left` 计数递减保证收敛，但没有硬保险。
* 建议：加一个宽松（远高于任何合法链长）的链深保险丝，只作为防御，不参与规则。

### P2-3 `_pay_bleed_cost` / `take_damage` 直连点
`engine/api.py:2759`（买路财）、`engine/combat.py:1463`（赤族诅咒）直接调 `Entity.take_damage`，
绕过 `_pay_bleed_cost` 的上下文记账。见 P0-4 / P0-5。

### P2-4 `engine/handlers/duel.py::_settle_duel`（第 82-83 行）直接 `player.current_hp = 0; is_alive = False`
* 这是**死斗结算**（战斗已结束、走 `last_death_cause="duel"` 与死者之书流程），
  不是战斗内命零。接进 `_on_entity_death` 会在战斗已结束后触发【分裂】等[命零]效果。
* 结论：**本次不改**，仅登记。

### P2-5 局外事件直接改血限/生命（`engine/events.py` 423/427/505/685/706）
战前事件里的 `blood_limit += / current_hp +=` 属于局外永久成长，
不在战斗因果链内，接 Context 收益极低、回归风险高。**本次不改**。

---

## P3：代码清理

* `CombatHook` Protocol 声明了 `on_multiplier_adjust / on_incoming_adjust / on_before_damage /
  on_after_damage / on_dodge / on_round_start` 六个时点，但 `CombatHookManager` 另有
  `apply_redirection / apply_mitigation / apply_after_damage_pipeline` 三个**不在 Protocol 里**的
  专用分发口，协议与实现不同构。
* `engine/api.py` 4801 行（本次任务明令禁止拆分，仅登记）。
* `engine/combat.py` 4029 行，`apply_daowen_effect` 单函数 620 行（仅登记）。

---

# 第四~五阶段：实际修改（最小范围）

原则：**任何改动都不得改变现有战斗数值与触发条件**。
差分验证见文末「回归证据」。

## 1. 新增统一入口（`engine/combat.py`）

| 新增 | 作用 |
| --- | --- |
| `CombatEngine._check_hp_zero_death(entity, ctx)` | **唯一命零判定入口**。生命≤0 → 置 `is_alive=False` → `_on_entity_death(ctx)`。对已离场（`is_departed`）与已通知过（`_death_triggers_emitted`）的实体幂等返回 False。 |
| `CombatEngine._record_blood_limit_event(...)` | 记录一次血限变化的 `EffectContext`（`mechanic="blood_limit_change"`）到 `entity._blood_limit_events`，并发 `BLOOD_LIMIT_CHANGED` 事件。 |
| `CombatEngine._apply_blood_limit_change(...)` | 血限变化统一入口：`_battle_delta` → 记录来源 → 生命封顶 → 统一命零判定。`clamp_hp` / `lethal` 两个开关用于原样保留个别效果的特殊次序。 |
| `CombatEngine._collapse_context(entity, parent)` | 【崩解】专用死亡上下文，供所有 `add_mutation` 调用点复用。 |
| `CombatEngine._emit(...)` / `event_stream` 属性 | 事件流写入口；事实源移到 `GameState.combat_events`，`combat.event_stream` 变为别名视图。 |
| `CombatEngine.MAX_EFFECT_CHAIN_DEPTH = 64` + `_effect_chain_depth` | 效果链保险丝（防御，不参与规则）。 |

## 2. P0 修复

| # | 文件 · 函数 | 修改 |
| --- | --- | --- |
| P0-1 | `combat.py::apply_daowen_effect`（`blood_limit_penalty` / `瓦解`） | 改走 `_apply_blood_limit_change(... lethal=True)`。**行为修正**：血限压到 0 生命时现在会判定命零，不再留下 `hp==0 and is_alive==True` 的非法状态（与同函数内 `blood_limit_reduction`、回始逼债、回终畸变的既有行为对齐）。 |
| P0-2a | `combat.py::_resolve_monster_daowen_choice` | 原始怪物道纹支付异变触发【崩解】时，此前**直接 return**；现补 `_on_entity_death(..., ctx=_collapse_context(...))`。 |
| P0-2b | `api.py::_action_consume_item` | 「获得异变X」触发崩解时补统一死亡通知。 |
| P0-2c | `events.py`（纸人冥婚） | `add_mutation(3)` 补 `collapsed` 判定与统一死亡通知。 |
| P0-3 | `api.py::_action_blood_feast` | `chizu.is_alive = False` → `_check_hp_zero_death(chizu, ctx=血食上下文)`。 |
| P0-4 | `combat.py::round_end`（赤族诅咒） | 补 `hp_loss` 上下文 + `_check_hp_zero_death`。**刻意不改走 `pay_numeric_cost`**（会额外触发血誓戒/烙痕钉/血契＝改规则）。 |
| P0-5 | `api.py::_action_buy_road` | 补 `_record_hp_loss_event` + `_check_hp_zero_death`；数值与触发条件完全不动。 |

## 3. P1 修复

| # | 文件 · 函数 | 修改 |
| --- | --- | --- |
| P1-1 | `combat.py` 全部 `_on_entity_death` 调用点 | 9 处补 ctx（衰老代价 / 异变崩解 / 回始逼债 / 回终畸变 / 赤族诅咒 / 原初X崩解 / 血限压迫 / 凡庸 / 爆裂反噬）。`_on_entity_death` 现在也会在调用方给出 `mechanic="death"` 上下文时沿用其 `subtype`，死因（`collapse` / `mediocrity` / `self_destruct` / `baolie_reflect` …）不再被统一抹成 `hp_zero`。 |
| P1-2 | `combat.py` + `combat_hooks.py` 全部血限变更点 | 改走 `_apply_blood_limit_change`：伤痕 / 切割 / 回始逼债 / 回终畸变 / blood_limit_reduction / blood_limit_increase / blood_limit_penalty / 瓦解 / 衰老代价。【伤痕】此前完全没有上下文，现与【切割】同构。`apply_daowen_effect` 新增 `daowen_ctx` 作为本次结算的链根。 |
| P1-3 | `combat_hooks.py::CombatHook` 及各 Hook | 新增 `priority` 类属性（10/20/…/90），`CombatHookManager` 按 priority 稳定排序。**数值即为重构前字面注册顺序的如实固化，相对次序零变化**；docstring 明确写出「加害必须先于龙鳞」的顺序敏感性与反例。`register_hook` 也按 priority 插入。 |
| P1-4 | `combat_hooks.py::CombatHookManager.__init__` | 三个显式分发 Hook 不再各 new 两份，注册表与 `redirection_hook/mitigation_hook/after_damage_hook` 复用同一实例；三者标记 `explicit_dispatch = True`，全部通用遍历（含 `apply_after_damage`）跳过它们，**从结构上杜绝双触发**。 |
| P1-5 | `combat.py::_apply_hostile_damage_inner` | 爆裂反噬补 `hp_loss` 上下文 + 带 ctx 的死亡通知。**未改** `hp_lost_this_round`（见下方「未修复」）。 |
| P1-6 | `combat_hooks.py::BifenglingHook / ShouyedengHook` | 不合并实现（两侧校验条件不同，合并＝改规则）。改为在两个 Hook 的 docstring 里明确登记「引擎唯一分发方是 combat.py 的 `_note_dodge` / `_grant_shouyedeng`，禁止接线 `apply_dodge()` / `apply_round_start()`」，并新增回归测试钉死「经引擎真实路径闪避一次只 +3 格挡」。 |

## 4. P2 修复

| # | 修改 |
| --- | --- |
| P2-1 | `CombatEvent` 从死代码变成真实事件流：`GameState.combat_events` 为事实源，`GameState.emit_combat_event()` 为写入口。已在四个统一收口处发事件——`DAMAGE_APPLIED`（`_apply_hostile_damage`）、`HEAL_APPLIED`（`GameState.apply_heal`）、`BLOOD_LIMIT_CHANGED`（新枚举值）、`ENTITY_DIED`（`_on_entity_death`）。每条事件的 `ctx` 挂 `EffectContext.to_dict()` 快照 —— **CombatEvent 说“发生了什么”，EffectContext 说“为什么、来自哪”，互不替代**。事件流 `battle_start` 按场重置，长模拟不会无界增长；`_blood_limit_events` 与 `_hp_loss_events` 同样按回合清空。 |
| P2-2 | 加入 `MAX_EFFECT_CHAIN_DEPTH = 64` 保险丝。取值远高于任何合法链长（嫁祸/背负重定向每跳递减计数、本身收敛），因此不改变任何现有结果，只把真死循环变成可诊断异常。 |
| P2-4 | `handlers/duel.py::_settle_duel` **不改**，只加注释说明「死斗败者是战斗已结束后的结算落幕，接进战斗死亡管线会在战斗结束后再触发一轮[命零]效果」。 |
| — | `models.py::Entity.take_damage` / `add_mutation` 加注释：模型层只翻标记，命零通知必须由调用方交给 `_check_hp_zero_death` / `_on_entity_death`。 |

## 5. 明确**不做**的事

* 不拆 `engine/api.py`（本次任务明令禁止）。
* 不拆 `engine/combat.py` / `apply_daowen_effect`。
* 不合并 避风铃 / 守夜灯 的双实现（校验条件不同）。
* 不把【爆裂】反噬失血计入 `hp_lost_this_round`（会改【活血】回复量）。
* 不把【买路财】/【赤族诅咒】改走 `pay_numeric_cost`（会额外触发血誓戒/烙痕钉/血契）。
* 不改 `engine/events.py` 局外事件的血限/生命直改。
* 不改 P0-6 那条既有红灯测试的期望值，也不为了让它变绿改实现。

## 6. 回归证据

* `pytest`：**875 passed, 1 failed**（失败项 = 审计前就红的 P0-6，报错内容逐字一致）。新增 25 条测试（`tests/test_effect_chain_audit.py`），未删除/未修改任何既有测试。
* 差分模拟（改动前后逐字节比对输出）：
  * `sim/run_sim.py`（300 次 7 场通关模拟）→ **完全一致**。
  * `sim/balance_sim.py` × {kill, sculpt, proliferate, debt}（各 48 怪 × 50 次 ≈ 9600 场）→ **完全一致**。
  * `sim/combo_loop_audit.py` / `dominance_test.py` / `duel_pvp.py` / `audit_monsters.py` / `audit_dungeons.py` → 除墙钟耗时外**完全一致**。

---

# 第二次验收（攻击自己的修改）——2026-08-19

方法：把 712681f（收尾修复之前）checkout 到 `git worktree`，用同一份 64 场景探针
对**旧引擎 / 新引擎**跑语义差分，再逐项定向攻击新入口。

## 二次验收发现的问题

### P1-A（已修复）：尸爆被我误改，清零了施法者当前生命
* 位置：`combat.py::apply_daowen_effect` 的 `尸爆` 分支。
* 上一轮我把 `caster.is_alive = False` 改成了 `caster.current_hp = 0; _check_hp_zero_death(...)`。
* 差分证据：`death_shibao` 场景 旧 `M.current_hp=70` → 新 `M.current_hp=0`。
* 判定：**这是我引入的规则变化**。尸爆正文是「自毁式[命零]」，从未规定清零当前生命；
  且旧代码本来就已经调用 `_on_entity_death(ctx=...)`，属于已统一状态，我的"清理"纯属多余。
* 处理：**已回滚**为 `caster.is_alive = False` + `_on_entity_death(ctx=...)`，并加注释说明
  为什么此处刻意不用 `_check_hp_zero_death`（它会抹掉 current_hp）。新增回归测试
  `test_j_shibao_self_destruct_keeps_current_hp_unchanged` 钉死。

### P2-A（已修复）：`ENTITY_DIED` 事件的 `actor_name` 恒为空
* 位置：`models.py::GameState.emit_combat_event`。
* 根因：`EffectContext.to_dict()` 会把实体降级成名字字符串；`AfterDamageEffectsHook`
  用 `normalize_context(detail["ctx"])` 还原出来的 ctx 里 `actor` 已经是 `str`，
  而 `emit_combat_event` 只做 `getattr(actor, "name", "")` → 取不到，落成空串。
* 影响：只影响我这轮新加的观测字段，`_death_ctx["actor"]` 本身正确，因果链未断，无规则影响。
* 处理：**已修复**（`_name()` 兼容 str）。新增 `test_j_entity_died_event_carries_actor_name_from_dict_context`。

### P2-B（只报告，不改）：爆裂反噬致死时 `_death_ctx.source` 记的是根因而非直接死因
* 现象：`死亡 ctx` 为 `source="杀伐"、subtype="hp_zero"、parent=反噬 hp_loss 事件(subtype=baolie_reflect)`。
* 因果链本身完整可追（death → reflect hp_loss → damage），但denormalized 的 `source`
  记的是链根（杀伐）而不是直接机制（爆裂）。
* 这与普通伤害致死的既有约定（`source` = 链根）是**一致的**，所以不算 Bug。
* 附带发现：`_apply_hostile_damage_inner` 中那段"显式构造 爆裂 死亡 ctx"的 fallback 是
  **死代码**——`suppressed=True` 蕴含 `reflected>0`，`reflect_ctx` 必不为 None。
* 结论：**需要人工裁定**要不要把这种情况的 `source` 改标为「爆裂」。本次不动。

### P3-A（只报告）：伤痕 / 切割 的"血限致死"路径结构上不可达
* 【伤痕】血限有 `max(1, ...)` 下限 → 永远压不到 0。
* 【切割】扣血限量 == 本次实际伤害量，`bl - a >= hp - a` 恒成立 → 永远不会把 HP 压到 0。
* 因此 `AfterDamageEffectsHook` 里为这两者准备的 `lethal_ctx` 改写是**防御性死代码**。
* 真正能"伤害/道纹 → 血限归零 → 命零"的只有 瓦解 / 逼债 / 畸变 / blood_limit_reduction。
* 不删（删了以后规则一改就是坑），仅登记。

### P3-B（只报告）：`sim/balance_sim.py:162` 直接 `add_mutation` 不处理 collapsed
* 纯模拟脚本自建的简化战斗循环，不经过 `CombatEngine`，不影响游戏引擎。仅登记。

---

## 十项验收结论

| # | 项目 | 结论 |
| --- | --- | --- |
| 1 | `add_mutation()` 是否全部覆盖 | **是**。引擎内 5 处：代价异变(combat 973)、原初X(1927)、怪物原始道纹(3896)、消耗品(api 2873)、纸人冥婚(events 303) —— 全部接统一死亡通知且带 `_collapse_context`。第 6 处 `combat 2604` 是【净化】的**负**异变，`add_mutation(-x)`（x≥1），活体 `mutation_count` 恒 <50，结构上不可能 collapsed，已实测验证（`purify_negative`）。第 7 处在 `sim/` 脚本内，见 P3-B |
| 2 | `is_alive=False` 是否全部审计 | **是**。engine 内共 **9** 处实写：**A 正常战斗内死亡 1 处**（`_check_hp_zero_death` 唯一入口）；**Hook 内为保持结算次序的临时置位 4 处**（爆裂/伤痕/切割/龙血斩杀，均在同一调用内交回统一入口）；**B 战斗外结算 1 处**（`duel.py` 死斗败者，已注明原因）；**C 模型层原语 3 处**（`depart_battle` 离场、`take_damage`、`add_mutation`，均已加注释指明调用方必须收口）。另有 1 处 `尸爆`（自毁式命零，走 `_on_entity_death` 带完整 ctx）。**无遗漏** |
| 3 | 血限统一入口是否安全 | **是**。11 个血限场景（不影响HP／封顶／降至0／致死／增加／不朽拦截／含hp_reduction／逼债发动／逼债回始／畸变回终／瓦解）逐项差分，除**两处刻意的 P0-1 修复**外与旧代码逐字节一致 |
| 4 | 死亡统一入口是否安全 | **是**。7 种边界（HP>0 / ==0 / <0 / is_alive已False未通知 / is_departed / 重复调用 / None）行为全部正确：不重复、不漏、不误触发；ctx 的 source/source_type/mechanic/subtype/parent_event_id 全部正确透传，无 ctx 时才退化到 legacy |
| 5 | Hook priority 是否保持旧顺序 | **是**。与 712681f 字面注册列表逐项比对 `IDENTICAL: True`；管线阶段顺序实测 `multiplier → incoming(加害→龙鳞) → redirection → before_damage(爆裂) → mitigation → take_damage → after_damage`，旧新完全一致，重定向重入模式也一致 |
| 6 | 是否存在双触发 | **否**。注册表 9 个 Hook / 9 个唯一 id；三个显式分发 Hook 与 `redirection_hook`/`mitigation_hook`/`after_damage_hook` 是同一对象；`apply_after_damage()` 通用遍历实测跳过 `AfterDamageEffectsHook`（血限未被动）。伤痕/切割/寄生/负岳索/龙血斩杀逐项实测各只执行一次 |
| 7 | EffectContext 是否完整 | **是**。链1 伤害→死亡、链2 伤害→血限变化、链2c 道纹→血限→死亡（三层 parent 连续）、链3 伤害→爆裂反噬→死亡、链4 伤害→嫁祸→新伤害（带 `redirected` tag 且 parent 指向原伤害）全部验证通过 |
| 8 | CombatEvent 是否正确 | **是**（修掉 P2-A 后）。每效果一条事件；`event.ctx` 与 `detail["ctx"]` 是同一对象；parent 不丢；`battle_start` 重置已验证；单场峰值 18 条、6 场实测不累积。**重定向只产生 1 条 `DAMAGE_APPLIED`**（重定向在落地前 return，语义正确）；**裂变3 产生 3 条**（三段独立伤害，语义正确，非重复） |
| 9 | depth 保险是否真正生效 | **是**。在 `_apply_hostile_damage` 入口 +1、`finally` −1，只包住"伤害重入"这一条真实递归路径。正常嫁祸×5 / 背负×3 / 爆裂×3 后 depth 均归零；构造 A↔B 互相嫁祸（各 50 次）确实触发 `RecursionError` 且 depth 回滚为 0。**安全边界核算**：`_jiahuo_left` 是**覆盖赋值**不是累加，嫁祸X 消耗 15X 法力，法限上限 = 25点×2 = 50（守夜灯上限约 75）→ X ≤ 5，双向互嫁祸链长上限约 10 « 64，保险丝不会误伤合法链 |
| 10 | 是否存在新的规则差异 | **只有 3 类，全部已知**：① P0-1 血限压迫致死（上一轮已裁定的 Bug 修复，2 个场景）；② 本轮 DM 裁定的爆裂反噬计入失血（4 个场景仅 `hp_lost_this_round` 字段变化）；③ 由②派生的活血触发（1 个场景）。其余 57/64 场景逐字节一致 |

## 人工裁定执行结果

| 裁定 | 执行 |
| --- | --- |
| **1. R46 期望值 95 → 94** | 已改 `tests/test_r39_r47_contract.py:426`。逐步复算已验证：`process_relics` 后 90 → `_pay_bleed_cost(1)` → 89（同时触发烙痕钉对敌 −10，测试自身也断言了这一点）→ 负岳索等量回复 +5 → **94**。原期望 95 等价于"流血代价不扣血"，与同一测试的"烙痕钉已触发"断言自相矛盾。**94 是实现的自然结果，不是实现错误** |
| **2. 爆裂反噬计入 `hp_lost_this_round`** | 已改 `combat_hooks.py::BaolieHook.on_before_damage`，在扣 HP 处 `attacker.hp_lost_this_round += reflect_amt`（与 `Entity.take_damage` / `_raw_hp_loss` 同口径）。**未改**爆裂伤害数值、触发条件或任何其他效果。【活血】实现本身一行未动——它本来就只读 `hp_lost_this_round`，不判断来源 |

## 二次验收最终测试

* `pytest`：**886 passed / 0 failed**
* 新增 10 条测试（本轮）：活血 6 条（爆裂反噬计入失血 / 活血按反噬失血触发 / 普通伤害同结果 /
  护盾全吸收时不触发 / 无爆裂时不触发 / 失血<2 阈值不变）+ 二次验收回归 4 条
  （尸爆不清零生命 / ENTITY_DIED actor_name / 重定向单事件 / 净化不误杀）
* 语义差分：64 场景 → 57 完全一致，7 处差异全部为已裁定项
* 模拟差分：`run_sim.py`、`balance_sim.py`×4（约 38400 场）、`combo_loop_audit`、
  `dominance_test`、`duel_pvp`、`audit_monsters`、`audit_dungeons`
  → 游戏结果**全部与基线一致**（仅墙钟耗时不同）
