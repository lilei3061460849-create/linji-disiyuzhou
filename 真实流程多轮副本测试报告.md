# 真实流程多轮副本测试报告（2026-08-19）

> 方法：完整 `GameEngine` + `execute_action` 公共 API 驱动真实游戏流程，**不构造理想状态**、
> 不调用底层 `CombatEngine` 直接结算、不走测试旁路（仅用测试基建里的两阶段怪物 API 封装做决策提交）。
> 复现脚本：`test_real_playthrough.py`（运行 `python test_real_playthrough.py`，默认 3 个种子 × 多条完整轮回）。
> 本轮**未修改任何生产代码**（`git diff -- engine/ main.py` 为空）；1106 pytest 基线全绿。

## 一、执行规模

| 项 | 值 |
|---|---|
| 种子（完整轮回） | 3（10 / 6 / 7，均含 罪孽都市 开局 + 雇佣 + 杀伐/血债初始道纹） |
| 实际战斗场次 | **17 场**（每场 1~6 回合，共 60+ 回合） |
| 跨进程存档/读档 | 3 次（每条轮回 1 次：战 2 后存档 → 新 `GameEngine` 实例读档 → 继续 3~4 场） |
| 事件/中断处理 | 救赎·接纳 ×3+、战终工资 ×3、死亡遗言审核 ×3（用 `submit_ruling` 真实路径） |

## 二、真实流程覆盖清单

| 用户要求覆盖项 | 实际覆盖 | 佐证 |
|---|---|---|
| 正常战斗 | ✅ | 17 场全部走 `battle_start → round_start → 玩家行动 → 怪物阶段 → round_end → battle_end` |
| 多回合战斗 | ✅ | 单场最长 6 回合，跨回合状态（法力回填/冷却/速度消耗）连续 |
| 多实体战斗 | ✅ | 战 5/11/16/17：2 敌（杀手+狙击手、狂徒+狙击手、毒枭+窃贼）；战 6：**3 敌（打手头目+窃贼+看门犬）** |
| 朋友/员工/怪物同时存在 | ✅ | 玩家 + 雇佣员工 Tank(168血 2×4) + 救赎朋友 RedFriend(126/99血 2×N，逐场成长) 同场对怪 |
| 道纹与遗物获取、使用、封印 | ✅ | 初始道纹发现（杀伐/血债/束缚/贯穿）+ `use_daowen` 多段使用；遗物 无所求/三相残韵盘 获取并持有 |
| 伤害、治疗、死亡、事件链 | ✅ | 杀伐伤害、怪物命中、玩家死亡（3 次）、救赎事件（HP≤10% 战中触发）、死亡遗言中断 |
| 回始、战始、回终 | ✅ | 全部经公共 API 真实结算；回始法力回填、回终清格挡等 |
| 战斗结束与下一场战斗 | ✅ | 战终工资结算（pay_employee_wage）→ 下一场 `battle_start` 连续推进 |
| 存档、退出、重新创建引擎、读档后继续 | ✅ | `save_game` → 新引擎 `load_game` → 相位/子阶段/遗物/员工/朋友完整恢复 → 继续战斗 |
| AI 正常决策路径 | ✅ | 经 `execute_action` 的确定性策略（与 `sim/build_learner` 同接口层）；两阶段怪物 API `prepare_monster_phase` → `resolve_monster_phase` |

## 三、发现的问题

### 问题 1（✅ 真实生产 bug，迁移无关，已复现）

**`sim/handplay_dungeon_with_winner.py` 怪物 AI 决策路径 NameError**

- **最小复现**：任意怪物持有道纹选项（如 扭曲都市 第 1 场 奇美拉，seed 42）时调用
  `sim/alt_path_test.resolve_monster_turn(e, [])`：
  ```
  File "sim/handplay_dungeon_with_winner.py", line 129, in _resolve_monster_turn_hand
      option = _pick_monster_daowen(engine, actor)
  NameError: name 'engine' is not defined
  ```
- **根因**：`_resolve_monster_turn_hand(e, log)` 的参数名为 `e`，但第 129/181 行
  调用 `_pick_monster_daowen(engine, actor)` 使用了未定义名字 `engine`
  （静态证明：`engine` 既非参数、非局部、也非模块全局）。
- **影响**：`sim/alt_path_test.py`（被 `sim/alt_path_full_run.py`、
  `sim/guard_cmd_full_run.py` 等真实流程脚本引用）的怪物决策路径在**任何带道纹的怪物**
  出现时崩溃。现有 pytest 未捕获：测试走的是 `sim/build_learner._resolve_monster_turn`
  （参数名正确为 `engine`，无此 bug）。
- **分类**：生产代码（sim 决策层）既有 bug，与机制迁移无关，不阻塞引擎核心。

### 问题 2（✅ 引擎行为正确，AI 需适配）

- **现象**：我方 AI 预提交闪避后，怪物阶段结算报 `Linji速度不足，不能选择闪避`
  （combat.py:3795/3813）。
- **判定**：**引擎正确**。怪物 减速/束缚 类道纹会在阶段中扣减我方速度，使预提交的闪避
  在结算时非法；引擎在结算前逐 actor 校验并整体回滚，防作弊语义成立。
- **结论**：AI 闪避预算须跨整个怪物阶段共享且保守（本脚本已改为带重试兜底，无伤引擎）。

### 问题 3（⚠️ 真实流程机制确认，非 bug）

- 战终工资待决阻塞 `battle_end` → 需逐员工 `pay_employee_wage(pay/refuse)` ✅ 符合设计；
- 救赎（HP≤10%）在**战斗中**触发并阻塞其它行动 → 需 `resolve_redemption(接纳/无视)` ✅ 符合设计；
- 玩家命零 → `death_inheritance` 中断 → 需 `submit_ruling` 审核遗言 ✅ 符合设计；
- 3 条轮回的 3 场"失败"均为玩家在 2~3 敌高威胁战斗中阵亡（平衡问题，非引擎缺陷）。

## 四、覆盖结论

**引擎核心与 13 个已迁移机制在真实完整流程（含跨进程读档续战）下未发现行为差异。**

本轮在真实流程中发现的唯一问题是 **sim 决策层脚本的既有 NameError**（问题 1），
它恰好落在用户要求的"AI 正常决策路径"上，属于此前自动化测试未覆盖的盲区：
现有测试用的怪物 AI 封装（build_learner）参数名正确，掩盖了 alt_path 封装同名 bug。

## 五、处理建议（本轮未改任何代码，等待统一处理）

1. 修复 `sim/handplay_dungeon_with_winner.py` 第 129/181 行：`engine` → `e`（或改函数签名）；
2. 给该封装补一条"怪物带道纹选项"的最小回归（当前 `tests/` 无覆盖）；
3. 其余两条为 AI 策略适配建议（共享闪避预算），引擎无需改动。
